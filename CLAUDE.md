# Financial Modeler Pro, Claude Code Project Brief
**Last updated: 2026-08-12.**

**Module status (REFM platform), one line each. Full narrative + commits live in [CLAUDE-REFM.md](CLAUDE-REFM.md), NOT here:**
- **Module 1 (Setup / Costs / Financing)**: LOCKED at M2.0 Pass 58 (base). **Asset strategy changes are a MODEL operation (2026-08-11), not a field write** (`lib/state/strategySwitch.ts`, pure): the Strategy dropdown used to write one field, and since the engine gates on `asset.strategy` in every resolver, the old strategy's assumptions stopped feeding the model while the new strategy's never started. Measured: EVERY change dropped the asset's revenue to zero (a Sell asset switched to Operate took project revenue 14,055.0m -> 11,233.0m with no warning) and Sell <-> Sell + Manage was a model no-op. Two causes: `asset.revenue` is partitioned (.sell/.operate/.lease) but an asset carries only the one it was built with, and SUB-UNITS are not partitioned at all (one `category` each), so an Operate resolver counted ZERO keys and even a seeded config would compute nothing. Leaving Sell + Manage additionally HARD DELETED the companion asset with its sub-units, cost lines and cost overrides. Now: the outgoing strategy's sub-units, opex and companion are PARKED in `Asset.retainedByStrategy` and the incoming set is restored, or seeded from the outgoing one (names, counts and areas carry; the RATE does not, because a sale price is not an ADR); Support sub-units belong to no strategy and never move. Retention sits OFF TO ONE SIDE deliberately: `state.subUnits` / `costLines` / `costOverrides` still hold ONLY the active strategy's rows, so none of the ~40 engine and UI readers had to learn to filter. The dropdown now previews via a DRY RUN of the same pure function the store commits (so the dialog cannot promise something else), then a `strategyReview` banner persists across navigation until dismissed, because the empty assumptions live on other tabs. **Both the dialog and the banner are GATED on `assetHasStrategyAssumptions` (2026-08-15)**: an asset is created as `Sell`, so the user's first pick from the dropdown was technically a change and fired a four-item review on an asset with nothing on it. `needsReview` cannot answer this itself (it means "active but empty", which on a new asset is everything), so the gate is a separate pure predicate read from the state BEFORE the switch, shared by the store and the dropdown so they cannot disagree. The switch still RUNS when suppressed (Sell + Manage still seeds its companion). Detail in CLAUDE-REFM.md 2026-08-15. `verify-strategy-switch` **54 passed + 1 PRE-EXISTING FAILURE** ("the fixture exercises Sell + Manage (a companion exists to lose)", the fixture builds no companion so the Sell + Manage clauses never run; measured 2026-08-12 and confirmed pre-existing by stashing unrelated changes. The long-recorded 82 was stale). Teeth proven by three sabotages (4, 12 and 7 failures). **Not browser-verified.** Funding Methods 2/3 gap-sized; conditional IDC + iterative fixed-point solver. Financing sub-tabs: Inputs / Schedules / Funding Gap / Cash Sweep. Dividends are ONE project-level after-debt policy (`Project.dividendPolicy` + `dividendStartYear`) with a terminal 100% payout in the engine.
- **COST ALLOCATION, SILENT MONEY LOSS, FIXED 2026-08-15 (predates all of the above; read this before touching `resolveAllocationFactor` / `resolveDriverFactor` in `src/core/calculations/index.ts`)**: every share basis was written `return total > 0 ? mine / total : 0`, which returns 0 for **EVERY** asset when the denominator is zero. The shares then sum to **0 instead of 1**, so a project-level lump sum on that basis was allocated to NOBODY and **left the model with no warning anywhere**, no banner, no check failure, no zero-looking cell to notice. **Measured**: one asset, a phase with no land, a `fixed` cost line of 1,000,000 moved total capex by **0.000m** on `land_share`, against the full 1,000,000 on `bua_share` or `per_asset`. Reachable on any phase whose basis quantity is genuinely zero (no parcels entered yet, an operational asset with no new build, areas not filled in), so it had been losing money quietly for as long as those bases have existed. Now every share path falls back to a shared `equalPhaseShare` equal split (SIX call sites: bua / gfa / land on the basis path, bua / land / value_share on the driver path), which is the convention `computeAssetLandSqm` and the `category` basis already used; companions are excluded and the factors are proven to sum to exactly 1. Only the `fixed` method consults it (`FIXED_METHODS_NEEDING_ALLOC`), since every `rate_per_X` method already returns an asset-scoped contribution. **The fix can move existing models, upward only, wherever a cost was being lost.** Pinned by `verify-new-project-defaults` section E; full narrative in [CLAUDE-REFM.md](CLAUDE-REFM.md) 2026-08-15b.
- **Module 2 (Revenue + CoS + Schedules + Escrow)**: LOCKED at Pass 9N.
- **Module 3 (Operating Expenses)**: LOCKED at Pass 5d.
- **Module 4 (Financial Statements)**: DONE. Schedules / P&L / CF / BS. **Fund-fee statement position (2026-08-05):** the five fund fees total into a **Total Fund Management Fee** line and **EBITDA is struck AFTER it** (one EBITDA row, already net of fees; `ebitdaBeforeFundFeesPerPeriod` holds the pre-fee measure and is what the PHASE-level P&L uses, since fees are project-level with no phase allocation; `ebitdaAfterFundFeesPerPeriod` is a retained alias). Cash flow carries a visible **Fund Management and Other Expenses** row inside operating activities above Cash Flow from Operations. Both come from the shared `m4Reports.ts` builders, so screen + PDF + Excel move together. **Fund Fee Basis (2026-08-05, pushed 2026-08-09):** a shared `buildFundFeeBasisRows` builder states what each fee is charged ON (base, rate, basis amount, fee charged) beside the statement, rendered by `FundFeeBasisTable` on the consolidated P&L and the M5 fee income section and emitted as a Fund Fee Basis block on the Excel P&L tab (meta columns B/C, so the period axis at column F does not shift), because a fee reading zero previously gave no way to tell whether the RATE was zero or the BASE was empty. Consequence: ICR/DSCR read `ebitdaPerPeriod` so they are now after-fee on a fund project. BS balances by construction; Direct CF == Indirect CF every period. Phase-filtered P&L stops at EBITDA; phase CF shows Operations+Investing; BS is consolidated-only. P&L/CF/BS render from shared pure builders (`lib/reports/m4Reports.ts`) that the on-screen tabs AND the PDF both consume.
- **Module 5 (Returns & RE Metrics)**: DONE. Sponsor-IRR view (FCFF / FCFE / Distributed Equity + terminal value + RE metrics + per-asset + Case Comparison). RE Metrics tab carries editable Lender Covenants (DSCR / ICR / Debt Yield + LTV at peak debt = debt / GDV, since LTV at exit is a trivial 0%); pure display layer over the snapshot, no engine change. **Per-partner returns (2026-07-09):** each equity holder shows FCFE-based + DDM-based IRR/MOIC/equity-multiple driven by ONE time-weighted (dollar-years) shareholding (manual override = agreed share), each stream = agreedShare x the exact consolidated stream so Sigma partners == consolidated per period; total returns byte-identical (horizontal side-by-side tables + green/amber reconciliation chip). Equity partners can be **linked to M1 Parties** (equity-role party -> stores `partyId` + snapshots the name; engine never reads partyId).
- **Module 6 (Scenario Analysis)**: LIVE. A surface over the case engine (no engine rebuild): case list, multi-case assumptions grid (model-aware curated levers + config-inert gating, exhaustively field-audited on the live project), comparison matrix, and a Year-on-Year Impact tab (per-period divergence per case; debt/equity split deduped to one block; debt drawdown is principal, excludes IDC). Presentation in `lib/cases/assumptionGrid.ts` + `lib/reports/caseYoYReport.ts`; engine `applyOverrides`/`snapshot-diff` untouched. Was "Reports"; swapped with Module 7.
- **Module 7, ADDING A SLIDE TEMPLATE IS A TWO-STEP CONTRACT (2026-08-15)**: a deck is saved per project, so an existing deck only gains new slides through `upgradeDeckLayout`, which offers exactly the ids registered in `TEMPLATES_BY_VERSION` ABOVE the deck's own `schemaVersion`. You must **bump `DECK_SCHEMA_VERSION` AND register the new id under that number**. Doing only the first silently lifts every deck's version and adds nothing; doing only the second never fires. Nothing enforced this, so a template added to the library and left unregistered reached freshly seeded decks and was **invisible to every existing one, permanently and silently**. That is what happened to the three fund slides (shipped 2026-08-13, complete and correctly gated, on a fully populated model): a real 36-slide deck exported with zero fund content, and the only route to them was a rebuild that discards arrangement, edits, text boxes and AI narrative. Now `TEMPLATES_BY_VERSION` is COMPLETE (every `SLIDE_TEMPLATES` id under exactly one key, v1 originals included and inert) and **`verify-deck-template-registry` fails on any unregistered template and on a newest-key that does not equal `DECK_SCHEMA_VERSION`**. Detail in [CLAUDE-REFM.md](CLAUDE-REFM.md) 2026-08-15c.
- **Module 7 (IC Presentation Builder)**: LIVE. **Free-form AI drafting + fund sections (2026-08-13, `verify-ic-freeform` 72 / `verify-ic-fund-sections` 84, teeth proven by eight sabotages, NOT browser-verified).** (A) The AI panel no longer offers only six fixed narrative fields: select ANY block and give a free instruction ("write this up", "explain this IRR"). Targeting resolves from the SELECTED OBJECT, deliberately not from `findNarrativeTargets`, which is template-driven and returns nothing for a blank slide. **Grounding is unchanged**: same `collectGrounding`, same whitelisted model facts, no new source, and the verifier MEASURES that by capturing the real request rather than reading the source. `buildGroundedRequest` puts `GROUNDING_RULES` in the system prompt and repeats the figure reminder after the task, so a user instruction sits INSIDE rules it cannot reach around, which is why a user-authored task is safe to send at all. **The refusal contract is stricter than the standing rules**: rule 4 permits a partial answer, which is wrong for an arbitrary instruction because a half-grounded paragraph hides which half was real, so free-form is ALL OR NOTHING via a machine-detectable sentinel (`CANNOT_ANSWER_FROM_MODEL:`) and the review offers NO Apply on a refusal. A refusal returns 200 with an empty draft and KEEPS its counted generation (the call was made; refunding it would make refusing free and answering expensive). **An empty block on a blank slide is described as empty in the prompt**, with its own branch, because a model handed a silent empty string invents a subject. Metered through the SAME `ic_narrative` feature, cap and toggle: a free instruction is a different way to ask, not a second allowance. Draft identity in the review moved from the field key to an explicit `id` (the field key for a fixed draft, the object id for a free-form one), since a block-targeted draft has no field. (B) The fund layer reaches the deck: three toggle-gated slides (Fund Terms and Fees, Fund Distribution Waterfall, Fund Returns and Fee Income) covering all five fund sections, fed by a new `fund` block on `ICReportModel` that reads ENTIRELY from the shared `fundReports.ts` / `m4Reports.ts` builders, following the `schedules.fcff` precedent. Toggle off is byte-identical, proven by seeding absent-vs-populated-but-disabled decks and comparing in full. Detail in [docs/FUND_LAYER_GUIDELINE.md](docs/FUND_LAYER_GUIDELINE.md) section 8b and [CLAUDE-AI.md](CLAUDE-AI.md). A PowerPoint-style slide editor (navigator / 16:9 canvas / properties) that replaced the old form-plus-fixed-report UI. **Slides hold BINDING KEYS, never copied numbers**, so the deck follows Modules 1-6 with no sync step and an unresolved binding renders a visible amber UNLINKED state rather than a stale figure. Direct manipulation (drag / resize / snap / inline edit / undo), 18 institutional templates that auto-omit on `available(model)`, full year-by-year schedules paginated across slides, a live ToC, and native PPTX + PDF export from ONE shared resolved-export contract so a file figure cannot drift from the screen. AI drafting on the narrative blocks (see CLAUDE-AI.md). Free text boxes, presentation versions (migration 207) and a save choice on EVERY save (update the version on screen in place, or save as a new version) rather than a silent overwrite. **Report versions are AUTO-NAMED and the last one opens with the project (2026-08-03):** nothing is typed (`{Project}_Presentation_v1.3_08032026`, named server-side, reusing the model version rollover helper as a PURE function), every save keeps a version (an unlinked save creates the first one), and `getDeck` returns the last saved version plus which version it is, so the editor names what is open and the versions modal is for browsing and opening the others. Report versioning stays entirely separate from model/project versioning: its own table, routes, client and UI. Migration 199. **Full narrative + remaining work (Insert menu, image upload, group / align, PNG export) in [CLAUDE-REFM.md](CLAUDE-REFM.md).**
- **Cases (scenarios)**: Management base + override cases; the active case drives the WHOLE model + all exports (not just the comparison); topbar switcher + Returns Case Comparison + the Module 6 page; per-input "≠ Management" override badge; scenario edits in the change_log; viewing a case never starts an edit session. Engine `lib/cases/applyOverrides.ts` (value-only overrides, base never mutated; per-element grammar via `snapshot-diff.PER_ELEMENT_ARRAYS`).
- **Platform infra**: project-switch state-leak fixed; session-based versioning + per-version change_log. **View/edit lock**: a project opens read-only (no version churn) until you click Edit, which presents an **Edit choice** (edit this version in place / edit a different version / create a new version) plus a mid-session **Save as new version** (`startEditInPlace` / `saveAsNewVersion` in `lib/persistence/module1-sync.ts`); schema-tolerant reads; paginated version history. **PDF full-project report** (`lib/pdf/generateProjectPdf.ts` on pdf-lib) mirrors every module tab via shared `lib/reports/` builders (m4/opex/capex/financing/cos), with per-module part picker + number-scale + **version picker** in `ExportModal` (file named after the chosen version). **PDF EXPORT REVIEW, three passes (2026-08-12, commits `2f546659` / `3cea19dd` / `76ed9e50`):** the equivalent of the workbook review, over BOTH PDFs at all three display scales. Pass 1 (arithmetic + signs): the shared cash-sweep waterfall ADDED interest and principal instead of subtracting them (both series are stored already-negative and the builder negated each twice, overstating every row below "Cash Available" by 2x interest + 2x principal, and breaking the tie to closing cash that the last row claims); the PDF FCFE build-up started from FCFF and re-added the terminal value; "new debt" had three figures under one name; per-asset economics reported ZERO cost and 100% margin on every asset (a sign-convention mismatch in the returns engine, ships on screen too); the summary P&L printed deductions positive beside a negative fund fee row so the column could not be added; and stock rows were summed across periods. Pass 2 (readability + scale): column geometry is now RESOLVED PER DOCUMENT from the widest cell it will draw, because a full-unit export ellipsised **3,867 numeric cells** and thousands 43 (millions stays 104pp; thousands and full go 104 -> 140pp, so wider numbers cost pages, never digits); three label-truncation bugs, two of them the SAME off-by-padding mistake (measure the text, then hand that width to a `drawCell` that subtracts 4pt a side); per-unit rates stay unscaled but now name their unit; and a value that rounds to zero at the displayed precision prints the en-dash instead of "(0.0)" (126 + 13 cells, fixed in the shared `formatAccounting` so screen and PDF move together). Pass 3 (consistency + missing content): one name per metric with its basis stated, gross-vs-net named on a fund project, a Model Integrity Checks table in BOTH documents, DSCR flagged as a covenant reading with a Lender Covenants table, the model version on the cover and every footer, derived Module 5 tab numbers, and Timeline / Land & Area / Fund Inputs / Sensitivity (entitlement-gated) added. **Two shared builders came out of it**, `lib/reports/streamReports.ts` (the FCFF/FCFE/dividend row lists, previously FOUR hand-maintained copies, which is why one drifted) and `lib/reports/checksReport.ts` (the three integrity identities + a RELATIVE, PEAK-anchored tolerance, now shared by both PDFs AND the workbook Checks tab). New verifiers `verify-report-arithmetic` **78**, `verify-report-readability` **55**, `verify-report-consistency` **59**, each with teeth proven by sabotage; toggle-off byte-identical at all three scales. **PASS 4, PRESENTATION (2026-08-13, six items over BOTH PDFs AND the workbook, `verify-report-presentation` 92):** every item was a figure the model computes correctly and a reader cannot interpret. (1) The fund waterfall printed Hurdle Paid's LIFETIME total (5,668.5m) directly under a Total Hurdle Owed row carrying no total and closing at 4,834.0m, so the column read as more paid than was ever owed; a shared `fundWaterfallTotalsNote` now names which rows are flows and which are balances and quotes both figures FROM THE MODEL. (2) **"Total" meant two things in one document**: the P&L used it for a lifetime sum while the balance sheet used it for the CLOSING balance (TOTAL ASSETS printed 2,512.1m, the at-exit figure) and the cash flow statement used it for both at once. The heading is now DERIVED from the rows via a new `M4Row.totalIsBalance` + `resolveTotalColumnKind`: `Closing` on the balance sheet, `Total / Closing` on cash flow and schedules, `Total` unchanged on the P&L. The classifier is shared with the summary PDF's hand-built rows (which had a parallel copy), proven by a sabotage that now breaks all three surfaces where it used to break only two. NOTE `drawPeriodTable` had hardcoded the literal `'Total'` and ignored `table.columns[1]`, so the builders alone changed nothing; and `resolveMetrics` now measures the HEADING as column content, since it is no longer a fixed 5-character word. (3) **A structural zero now says so**: two live assets reported 0 sqm BUA and zero cost with a 100% margin, both correct (an existing operational asset with 3,682.1m of historical pre-capex and no new build, and a companion whose area and cost sit on its parent). New shared `lib/reports/assetNotes.ts` marks the cell `- [a]` and footnotes the reason, on the composition table, the assets table, per-asset economics (cost, margin AND yield) and the workbook's Returns and Land & Area tabs. Detection is on the asset's DECLARED shape (`isCompanion`, `status === 'operational'` + historical spend), never on the zero itself, or a genuinely empty asset would be explained away; a real figure is never suppressed, and `takeFootnotes()` prints only the markers a table actually raised. (4) The three capital bases sat INSIDE the fee basis table with Timing / Rate (and in Excel, Base) empty and their amount in the column that holds a fee charged, so a quantity of capital read as a fee; they are now their own captioned block above it, tagged `capital base`. **Worth recording: the reported symptom was exact for the WORKBOOK only.** Both PDFs already placed the amount under `Basis charged on` (grid columns are per-table and content-sized, so nothing could shift it); the workbook has one `Total` column serving both the basis and the charge rows, which is where the ambiguity actually lived. (5) Distributed Equity IRR and MOIC appeared three times in a ten-page summary, twice as a BYTE-IDENTICAL card set (`headlineReturnCards` called on both the exec summary and the returns page); the returns page now points at them instead, and a shared `fundHeadlineRestatementNote` captions the fund page as the gross-vs-net split of the same metric rather than a third statement of it. (6) All of it confirmed in the FULL report and the workbook, not only the summary. Teeth proven by nine sabotages (8 / 5 / 19 / 2 / 2 / 1 / 1 / 1 / 4 failures); toggle-off re-proved, and the non-fund fixes are asserted to still render with the fund OFF so half a fix cannot pass. **NOT browser-verified.** **Excel MODEL export** (`lib/excel/buildModelWorkbook.ts`): a HARDCODED platform snapshot (every computed cell is the platform value written as a constant, editing does NOT recalculate, re-export after changing inputs), now a FULL module-for-module mirror of every module (Modules 1-6). 16 tabs in MODULE ORDER (Cover, **Summary**, Inputs, Timeline, Land & Area, Capex, Financing, Revenue, Opex, Schedules, P&L, Cash Flow, Balance Sheet, Returns, **Scenarios**, Checks); each output tab is a section-for-section mirror built from the SAME shared `lib/reports/` builders + snapshots the on-screen tabs use. **Cover + Summary + print setup (2026-07-14 cosmetic pass):** the Cover is a clean, module-grouped **Table of Contents** (numbered, hyperlinked, Module 1-6 + Reference bands + legend; shared front-matter primitives `frontMatterCanvas`/`Banner`/`Band`); a new **Summary** tab (2nd, `always`-part) is a one-page executive summary (key facts, a headline-metric tile wall, two side-by-side highlight tables Development economics / Returns & leverage, all in millions, computed from the same returns engine as the Returns tab). `applyPrintSetup` gives every sheet a print-ready A4 layout (fit-to-width, landscape wide tables repeating header rows 1:4, centred portrait one-page Cover/Summary, page-numbered footer). Inputs holds EVERY input grouped by domain band in module order (Project / Phases / Land / Assets / Sub-units / Returns / Capex, then FINANCING / REVENUE / OPEX INPUTS). **Scenarios tab = Module 6 mirror (2026-07-14):** built from the SAME shared `caseComparisonReport` + `caseYoYReport` that feed the on-screen Module 6 and the PDF: 1. Cases & Assumptions (every case + differing-lever grid), 2. Scenario Comparison (headline KPI matrix, delta vs base), 3. Year-on-Year Impact (each changed input + the per-period outputs it drives, base + each scenario delta). `ExportModal` threads the `caseComparison` bundle into `generateModelWorkbookBuffer` and shows the case picker for Excel too, **defaulting the statement tabs to the Management/base case while the Scenarios tab always compares ALL cases** (the user can pick another case for the statements). Degrades to a "no scenarios" note when no cases. Scenarios is an `outputs`-part sheet (hidden under an outputs-off filter). ONE standard navy palette (navy tabs + totals, deep-navy section bands, pale-navy subtotals, navy-pale inputs, red only for genuine check failures); the verifier locks the palette + tab order. **Export improvements (2026-07-28):** (1) **per-tab sub-TOC**, a nav strip at the top of every data tab listing that tab`s sections as internal hyperlinks plus a link straight to the basis-of-calc block and a "Back to Cover" link (reuses the `setSectionSink`/`sectionReg` + `{text,hyperlink}` mechanism the Cover ToC uses). Placement is structure-aware: rows are RESERVED below the frozen header on gridded tabs via a new `insertRowsAt` helper in `styles.ts` (ExcelJS `spliceRows` shifts values/styles but NOT merged ranges, which corrupts the file, so it unmerges, splices, re-merges shifted and rewrites same-sheet hyperlink targets), and appended at the bottom on the designed one-page canvases (Summary / Checks) whose merges span the insert point. ORDER is load-bearing: `applyTabGuides` -> `applyTabSubToc` (both re-base `sectionReg`) -> Cover/Guide last, so every deep link still lands on its section. (2) **palette LOCKED** in `ARGB` + a new exported `ALLOWED_FILLS`: the off-palette `#0C2340` and the `#1B3A6B` FILLS are gone (`#1B3A6B` survives as a text tone only, by decision), the sanctioned darker band is `#0D2E5A`, the sub-TOC strip is `#DDE7F3`, pass green is `#2E7D52`, and the FAST input fill `#E2EAF4` stays deliberately distinct; the verifier now asserts every fill is in `ALLOWED_FILLS`, the three forbidden fills are absent, and every section band is navy or sectionDark. (3) **basis-of-calc DEEPENED** (it already existed on all 15 data tabs since 2026-07-26; the gap was depth + burial): `TAB_GUIDES` is now `{kind:inputs|logic|feeds}` rendered as a labelled Inputs / Calculation / Feeds mini-table describing the real mechanics per tab, shared with the Guide tab. (4) **Timeline rebuilt** from 2 rows into a dated phase schedule (construction / operations windows + project envelope) plus a cell-based Gantt over the period grid, driven by the canonical `computePhaseTimeline` / `computeProjectTimeline`; ExcelJS 4.4 exposes NO chart API (`worksheet.addChart` is undefined) so a native chart is not possible and coloured cells are the fallback. Timeline columns B/C/D reclaimed from 3-char spacers to readable widths. Verifier `verify-excel-export` 187 -> **304**. Verifier `verify-excel-export` 159/159; two-way Sensitivity grid (Returns) is the one pending unit. **Auto-updating platform walkthrough guide** (`lib/guide/`, Topbar "Guide" button): in-app view + PDF/Markdown download, structure derived from the MODULES + MODULE_TABS registries.

- **Public read-only page API (2026-08-10)**: `GET /api/public/pages/[slug]` serves selected CMS pages to **PaceMakers Business Consultants**, so the partner site renders FMP page content without a database connection or a shared deploy. Returns `{version:1, page:{slug,title,meta_title,meta_description,og_image_url,status,updated_at}, sections:[{section_type,content,styles,display_order}]}`, sections filtered to `visible=true` and sorted by `display_order`; no internal columns (row id, is_system, the visible flag, created_at) are in the response. **The public slug is NOT the internal slug**: `SLUG_WHITELIST` in the route file maps `modeling-hub`->`modeling`, `refm`->`modeling-real-estate`, `training-hub`->`training`, because the three names in the spec matched no live `cms_pages` row and a literal whitelist would have 404ed on all of them; the mapping also means a CMS rename cannot break the partner. Anything not in that constant 404s, and so does a draft, so unpublished work cannot be probed. Auth is `x-api-key` against **`FMP_PUBLIC_API_KEY`** (constant-time compare; **fails CLOSED**, an unset key refuses every request). Rate limit 60/min/IP, in-memory and therefore PER INSTANCE on serverless, so the effective ceiling is a multiple of that. Success carries `Cache-Control: public, max-age=60, s-maxage=60, stale-while-revalidate=300`; errors are `no-store`. Rejections write `public_api_unauthorized` to **`public_api_audit` (migration 212)**, a NEW table because `admin_audit_log.admin_id` is NOT NULL (proven by a live insert probe) and an unauthenticated caller has no admin to attribute; the audit write is best effort and never turns a 401 into a 500, so the route works before the migration is applied. Note the section `content` jsonb is passed through VERBATIM, including any nested `visible:false` items the FMP renderer would hide, so a consumer must respect those itself. Verifier `scripts/verify-public-pages-api.ts` **43** (calls the real handler against the live DB). **Admin > API Keys (2026-08-11):** `/admin/api-keys` (System group) shows the key masked with a reveal toggle and a copy button, the endpoint URL, the whitelisted slugs and what the key grants, so the value never has to be dug out of an env file. `SLUG_WHITELIST` moved to `src/shared/api/publicPagesConfig.ts` so the screen and the endpoint read ONE constant. The rule that makes it an API keys screen rather than an arbitrary environment reader: **the client never names an environment variable**, it sends a registry id that the route resolves against a literal constant, so `SUPABASE_SERVICE_ROLE_KEY` and friends are unreachable. GET carries a character count and no part of the value; POST returns the value and writes `admin_audit_log` action `api_key_revealed`, and COPY goes through that same POST so it cannot bypass the record. `requireAdmin` try/catches `getServerSession`, which THROWS without a request scope and would otherwise turn a missing session into a 500. Verifier `verify-admin-api-keys` **55**, teeth proven by five sabotages.

- **Entitlements & Pricing (LIVE, 2026-06-22)**: admin-controlled plan/feature system on `features_registry` + `plan_permissions` + `entitlement_plans` + `user_permissions` (migs 158-165). **Resolver** `src/shared/entitlements/resolveOverrides.resolveEffectiveFeatures` (plan coverage + per-user overrides, override-wins, expiry-ignored) is REUSED everywhere; the live REFM gate (`resolveUserGate` + `gate.ts`, reads the live `users` row not the JWT) enforces module/export/scenarios/save+versioning/branding/sensitivity, the project cap (active = non-archived, mig 161 `refm_projects.archived`), and trial expiry. Admin never blocked; unknown plan = access-preserving safety net. **Admin (one consolidated Users tab + Plan Builder)**: `/admin/users` (+ `/admin/users/[id]` = the merged former "User Access" panel: resolved entitlements, grant/revoke overrides, plan assign, trial) and `/admin/plans` (features + limits + price + Most-Popular/badge + non-module visibility + Trial **length (days)** + coupons). **ONE shared plan-setting path** `setUserPlan`. **Plans reconciled** to trial/solo/pro/firm (mig 160). **Pricing single source of truth = entitlement_plans** (prices mig 162, popular/badge mig 163, feature visibility mig 164, trial_days mig 165). **UNIFIED + path-based (2026-06-25):** ONE shared `app/pricing/PricingPageBody.tsx` (server-loads via `loadPricingCatalog`) renders the polished `PricingExplorer` (platform picker -> per-platform `LivePlanCards` + comparison) in BOTH contexts. The `PricingExplorer` island is SESSION-AWARE: logged-out keeps the `/register` handoff, logged-in runs in-app checkout/trial + resume (no separate marketing-vs-app design or URL). **Clean per-platform URL `/pricing/<segment>`** (`app/pricing/[platform]/page.tsx`): segment SOURCE-DERIVED via `platformPricingSegment`/`platformSlugForSegment` in `src/hubs/modeling/config/platforms.ts` (shortName lowercased, REFM->refm, slug fallback; unknown -> picker). Bare `/pricing` = picker. Dashboard "Get access" + live-platform marketing CTA both link the SAME `/pricing/<segment>`; old `?platform=`/`/modeling/pricing` redirect (next.config). hero/FAQ stay Page Builder; footer legal links publish-status driven. `platform_pricing` + `/admin/pricing` retired. **Payments LIVE (Paddle adapter implemented, sandbox):** provider-agnostic `src/shared/payments/` (adapters paddle[implemented]/paypro[stub], registry, config, signature); checkout route -> active provider adapter -> Paddle.js overlay (`paddleBrowser.ts`, client token only); webhook `/api/payments/webhook/[provider]` verifies HMAC + reuses `setUserPlan` (idempotent). `paddleEnv.paddleEnvMismatch` guards sandbox/live token mismatch; `/admin/payments` badge = client_token+webhook_secret (Paddle Billing has no api_secret). Verifiers: `verify-entitlement-gate` 92, `verify-payments-structure` 79, `verify-subscription-management` 204, `verify-trial-days` 7, `verify-pricing-display` 23, `verify-user-overrides` 13, `verify-footer-legal-links` 12, `verify-plan-builder-modules` 16, `verify-pricing-application-ready` **48 passed + 1 PRE-EXISTING FAILURE** ("live platform card is a clickable button that selects in place", a `PricingExplorer` markup assertion; measured 2026-08-09 and confirmed pre-existing by stashing unrelated changes and re-running. The long-recorded 49 was stale. NOT yet diagnosed), `verify-feature-description` 41. See [CLAUDE-ROUTES.md](CLAUDE-ROUTES.md) + [CLAUDE-DB.md](CLAUDE-DB.md) for the per-route/migration detail.

- **Subscription Management & Billing (2026-06-29, sandbox-first)**: full self-service + admin billing on top of the single plan path. **Diagnostic webhook logging** (`[payments-webhook]` arrived/verified/rejected). **In-dashboard billing tab** (modeling dashboard in-page `activeView`; `BillingView` + `SubscriptionPanel`): per-platform subscriptions (mig 177 `user_platform_subscriptions`, one section per live platform, source-driven), view/cancel-at-period-end/invoices (in-app iframe viewer)/update-card (Paddle hosted), upgrade/downgrade with the target plan's full feature list + a Paddle **preview** differential (PATCH /subscriptions/{id}/preview) + monthly<->annual interval change, and the **timing rule** (upgrade immediate / downgrade deferred to next cycle via mig 178 scheduled cols + the `apply-scheduled-changes` cron, daily in vercel.json). **Single source of truth** (mig 179): `setUserPlan` keeps the per-platform row (store B) in sync on EVERY plan write so the gate (store A) + billing panel + admin views never diverge: a MANUAL plan writes the full row (source manual, dates, amount); every other caller (trial shortcut, self-serve trial, approval queue, the webhook's setUserPlan call) does a PARTIAL `syncPlatformSubscriptionFields` UPDATE-only of `plan_key`+`status` that PRESERVES webhook-owned metadata (source, paddle ids, Paddle dates) and is a no-op when no row exists (no fabricated `paddle`-defaulted row). All trial paths now also apply the SAME live-Paddle guard the plan route uses (shared `PADDLE_BILLED_BLOCK_MESSAGE` + `isUserLivePaddle`, 409) so a Paddle-billed user is never silently moved to trial; the `/admin/users` status dropdown converges store B status manual-only (never clobbers Paddle status). gate honors `expires_at` (additive only; mirrors trial). (2026-06-30: convergence-at-source fix, closes the admin-Trial-vs-billing-Firm divergence from any path; gate access logic unchanged.) **Manual (offline/bank) plans**: admin assigns plan+start+expiry+amount in `/admin/users/[id]`; live-Paddle users are BLOCKED from a silent change. **Admin Revenue** (`/admin/revenue`, mig 180 `payment_transactions` ledger): total + Paddle(reconcilable)/manual split, by month/year/custom + by plan, DB-aggregated. **Convert-to-manual** (safe): period-end default (cancel Paddle at period end + scheduled conversion applied by the subscription.canceled webhook / cron) or immediate (warned). All Paddle calls server-side; key never client-side. Resolver/gate core + admin bypass + none/unknown UNCHANGED. Verifiers: `verify-subscription-management` 204, `verify-entitlement-gate` 92. **Migs 176-180 each were applied to prod as shipped (176-180 done).** Founder TODO: set the server Paddle API key in Admin>Payments (scopes subscription.read+write, transaction.read) to light up the Paddle-side flows. Per-route/migration detail in [CLAUDE-ROUTES.md](CLAUDE-ROUTES.md) + [CLAUDE-DB.md](CLAUDE-DB.md).

- **Post-expiry access (read-only grace + lapse, 2026-06-30)**: a plan that expires no longer drops to instant lockout. The gate now computes a THREE-STATE lapse model purely from the plan's expiry date (the "lapse anchor"): **active** (`now < expiry`, full access), **grace** (`expiry <= now < expiry + 1 calendar month`, READ-ONLY: can log in + view projects, but edit / save / export / create / archive denied + a renew banner), **lapsed** (`now >= expiry + 1 month`, NO platform access like `none`, account still logs in, data NEVER deleted). Anchor priority (`resolveLapseAnchorMs` in `gate.ts`, shared by the live gate AND the admin list): trial -> `users.trial_ends_at`; manual -> `user_platform_subscriptions.expires_at`; canceled/expired Paddle -> `current_period_end` (active renewing -> no anchor). Pure helpers `computeLapseState` + `addCalendarMonths` + `writeBlockReason` (`READ_ONLY_GRACE` | `LAPSED`) in `gate.ts`; `computeGate` gains `lapseState`/`readOnly` (grace KEEPS the feature map so projects stay VIEWable; lapsed = denied like none). **Server-side read-only enforcement** at every write choke point: `POST /api/refm/projects` (create), `.../[id]/duplicate`, `.../[id]/versions` (save = the edit choke point), and the `/api/export/{pdf,excel}` routes via shared `src/shared/entitlements/exportGuard.assertExportAllowed` (the live export is client-side, so `ExportModal` read-only gate is the genuine point + the routes are defense-in-depth). `/refm` layout redirects LAPSED (not grace) to `/choose-plan` via `isNoPlanLockedOut(plan, isAdmin, lapseState)`; dashboard treats lapsed like no-plan. UI: grace renew banner + workspace read-only lock + suppressed Edit/Save/Export/New in `RealEstatePlatform`. **Admin user list** (`/admin/users`): new **Access** (auto active/grace/expired-lapsed by date) + **Expires** columns, computed server-side in `/api/admin/users` with the SAME shared helpers (no divergence from the live gate, correct between cron runs). **Admin bypass + none/unknown distinction UNCHANGED; no migration (reuses migs 177/179 columns); no data ever deleted.** Backward-compat: the legacy `trialExpired`/`planExpired` booleans (no `lapseState`) still map a full expiry to `lapsed`. Verifier `verify-entitlement-gate` 92 (adds grace/lapsed/read-only/anchor/admin-bypass-survives-grace cases).

- **Subscription email lifecycle (2026-07-01, Brevo, branded from financialmodelerpro.com)**: full lifecycle emails via the existing `src/shared/email` stack (`baseLayoutBranded` + `sendEmail`, sender `FROM.noreply`; `sendEmail` gained additive `attachments`, `FROM.support` added). Nine branded templates in `templates/subscription.ts`; dispatch + cron scan in `subscriptionEmails.ts` (self-contained, NEVER throws to caller). **Transactional** (wired at the trigger): welcome/active Paddle (webhook `activated` only, with the invoice PDF fetched SERVER-SIDE via `getInvoicePdfUrl` and attached + a `/dashboard#billing` link), welcome/active manual (admin plan route + convert-to-manual immediate, start/expiry, team-managed no-invoice, skips none/trial), canceled confirmation (self-service cancel route, "access until [period end]"), trial started (self-serve `startTrialForUser` + admin shortcut + approval queue). **Time-based** (daily cron `GET /api/cron/subscription-reminders`, CRON_SECRET, 10:00 UTC, added to vercel.json) at 1 week + 1 day before the date: trial ending (`trial_ends_at`), auto-renewal charge notice ("you'll be charged [amount], cancel before then") for auto-renewing Paddle only, ending-plan expiry ("access ends, renew") for manual + canceled Paddle. **Grace** emails: grace started (on first run after expiry) + grace ending (1wk/1d before grace end = expiry + 1 calendar month), reusing the gate's `computeLapseState`/anchor helpers (NO gate change). Auto-renew vs ending are DISTINCT (classified via a server-side `getSubscription()`; charge notice suppressed on uncertainty so a canceled sub never gets "you'll be charged"). **Idempotent + deduped** via `subscription_email_log` (mig 181, APPLIED 2026-07-01): claim-then-send keyed on (user, platform, type, threshold, anchor_day), so no double-sends and the cron is safe to re-run; anchor_day lets a renewal reminder re-fire for a new period. All Paddle calls server-side. Verifier `verify-subscription-emails` 45 (renders templates + asserts the charge-vs-ends distinction + every trigger wiring + dedupe + cron). Resolver/gate/enforcement UNCHANGED (`verify-entitlement-gate` 92).
- **Billing display fixes + manual invoices (2026-07-01)**: (1) subscription email footer standardized to the FMP company line "A PaceMakers Business Consultants Platform" (matching the pricing credibility line) via a `baseLayoutBranded` overrides param + a `subLayout` wrapper, replacing the Training Hub tagline. (2) **Paddle-wins source fix**: `storeUserPlatformSubscription` now sets `source:'paddle'` (+ clears stale manual `expires_at`/`amount_minor`/`note`) on activation, and `loadUserPaddleContext` treats a row as manual ONLY when there is no `paddle_subscription_id`, so a Paddle sub created over a prior manual record shows the Paddle subscription (not the stale "Managed by your team"). Billing-panel resolution only, NOT the gate. (3) **Combined invoices**: `/api/payments/invoices` merges Paddle invoices + manual receipts into one normalized `{id,source,...}` list; the panel renders it (+ viewer) in both the manual and Paddle branches, routing View by source. (4) **Manual invoices** (mig 182 `manual_invoices` + private `invoices` bucket): admin manual-assign / convert-to-manual-immediate with an amount generates an FMP + PaceMakers branded receipt PDF (`src/shared/payments/manualInvoice.ts`, pdf-lib) with the legal SELLER block (PaceMakers Business Consultants LLP, LLP Reg No 0200688, Gulberg III Lahore address, FBR NTN 6899301 for identification only, NO tax line), bill-to (name/email/company), and the plan + coverage period line item; stores it privately, emails it (PDF attached), and lists it in billing; served via ownership-checked `GET /api/payments/manual-invoice/[id]` (short-lived signed URL). All Paddle calls server-side; resolver/gate/enforcement UNCHANGED. Verifier `verify-subscription-emails` 65 (renders a real receipt PDF). Mig 182 APPLIED 2026-07-01.
- **Email/invoice testing fixes (2026-07-02)**: (1) transactional email dedupe is now PER-EVENT not per-type-per-day (`subscriptionEmails.ts`): `welcome_manual` keys on the assignment `started_at`, `manual_invoice` on `started_at:amount` (claimed BEFORE the receipt is created, so a double-click makes no second receipt while a genuine new assignment does), `canceled` on the period-end, mirroring `plan_changed`. `issueManualInvoice` routes through the dedupe dispatch, RETURNS a result, and `dispatch` logs every outcome ([sub-email] sent id= / skipped / FAILED via console.error) with the Brevo message id, so failures are visible not swallowed. Manual receipt still gated on amount>0 with an admin-UI note ("No amount: no receipt"). New `sendPlanEndedEmail`/`planEndedEmail`: removing a manual plan (admin sets none for a user who had a real plan) sends the manual user a cancellation confirmation (the Paddle-only cancel route never fires for them). (2) Full invoice history survives source flips: Paddle history reads from the DURABLE `payment_transactions` ledger (`listPaddleLedgerInvoices`) not the live API alone; the invoices route merges manual + ledger Paddle + live enrichment (deduped by txn id); the Paddle PDF route authorizes from the ledger (`userOwnsPaddleTransaction`) + builds cfg without a live sub, 404s gracefully when a PDF is unavailable. (3) Immediate plan changes attach the Paddle proration invoice (shared `fetchPaddleInvoiceAttachment`, newest txn -> getInvoicePdfUrl -> base64); scheduled downgrades have none. Verifier `verify-subscription-emails` 113. Resolver/gate/enforcement UNCHANGED.
- **Convert/links/plan-change fixes (2026-07-02)**: (B) **convert-to-manual** no longer re-cancels an already-canceled Paddle sub (the `subscription_update_when_canceled` / "paid through n/a" failure that left users on Trial after Firm): the route reads the LIVE Paddle state and, when `canceled`/no-active-period, assigns the manual plan DIRECTLY via `setUserPlan(source:'manual')` (single converge path; active subs keep period-end/immediate); a `when_canceled` race recovers instead of 502. The webhook baseline-drop now CLEARS the dead Paddle ids (`config.clearPaddleSubscriptionIds`) so a canceled row can't masquerade as live (which mis-rendered billing and blocked manual-assign recovery). Gate inputs/convergence untouched. (C) renew/plan links are now **per-platform** `/pricing/<segment>` (source-driven `platformPricingSegment`, real-estate->refm): email `pricingUrl(platform)` at all call sites, the REFM grace banner, and the dashboard grace banner; pickers stay bare `/pricing`. (A) new **plan-change confirmation email** (`planChangedEmail` + `sendPlanChangedEmail`) on upgrade/interval (immediate) + scheduled downgrade (next cycle), triggered at the change-plan route only (one change = one email), deduped via `subscription_email_log` (marker encodes plan+interval, keyed by timing). Verifiers `verify-subscription-management` 210, `verify-subscription-emails` 90, `verify-entitlement-gate` 92.
- **Email reliability + durable billing history (2026-07-02c)**: (1) **Welcome (Paddle) + trial-started emails now dedupe PER-EVENT** (`welcome_paddle` keyed on the subscription/transaction id, `trial_started` on the trial end date), matching the welcome_manual/canceled pattern. They were on `threshold:'once'` + `anchor_day: today`, so a second purchase/trial the SAME day was swallowed as a duplicate (the "was receiving, now not" on repeated testing). (2) **Assigning a user to `trial` via the admin plan route now sends the trial-started email** (`sendManualPlanWelcomeEmail` skips trial by design and nothing else fired). (3) **Invoice-attachment fetch made fast/best-effort** so it can't stall the send: welcome uses ONE attempt (no sleep, keeps the webhook under Paddle's timeout so a stalled send never leaves the dedupe marker claimed + drops the email), plan-change uses 2 short attempts; the email always sends with the billing link even when the PDF is not yet generated. (4) **Durable Paddle billing history**: the combined `/api/payments/invoices` (manual + Paddle ledger + live) and the panel (renders it in BOTH the manual + Paddle branches) were already correct, but the Paddle `payment_transactions` ledger was only populated by the webhook's `transaction.completed` (unreliable in sandbox), so Paddle history vanished after convert-to-manual ("manual only"). Now the ledger SELF-HEALS: the invoices route backfills settled live Paddle transactions on read (idempotent on txn id), and convert-to-manual captures the Paddle invoice history to the ledger BEFORE the sub goes away, so both sources persist and show together. Founder note: if the `[payments-webhook]` logs show events are not delivered in sandbox, the welcome email + ledger still depend on a live read to populate (self-heal on billing-panel view / convert); verify webhook delivery separately. Resolver/gate/enforcement UNCHANGED. verify-subscription-emails 113, verify-subscription-management 210, verify-entitlement-gate 92.
- **Plan-change invoice + admin cancel visibility (2026-07-02)**: (1) **Immediate plan-change / conversion invoice now attaches reliably.** Root cause was timing + silent swallow: right after a `prorated_immediately` change the proration transaction exists but Paddle has not yet generated its invoice PDF, so `getInvoicePdfUrl` returned `invoice_not_available` and `fetchPaddleInvoiceAttachment`'s bare `catch` dropped it with no log. Fix: `changeSubscriptionPlan`/`shapeSummary` now surface `immediate_transaction.id` (passed as the exact proration txn, subscription-newest-txn as fallback); `fetchPaddleInvoiceAttachment` gained short retry-with-backoff (invoice generation lag) + VISIBLE `[sub-email] invoice-attach ...` logging on every failure reason; `sendPlanChangedEmail` logs `plan_changed invoice NOT attached` when an immediate change sends without one. Welcome-paddle routed through the same shared helper. Conversion emails already attach the manual receipt PDF (`issueManualInvoice`); no Paddle charge applies on convert (Paddle canceled). (2) **Admin now shows Canceling / Canceled** (mig 183 `user_platform_subscriptions.scheduled_cancel_at`, a dedicated column since the `subscription.updated` webhook re-converges store-B `status`). Persisted on self-service cancel (`markSubscriptionCanceling`) + the webhook's parsed `scheduled_change` cancel; cleared on (re)activation. `/api/admin/users` derives `cancelState` (`canceling`/`canceled`) + `cancelAt` from the durable marker (own schema-tolerant query) and supports `?cancel=canceling|canceled` for retention outreach; the list shows a Canceling/Canceled badge + the end date, the detail panel surfaces the live `scheduledCancelAt`. **List/detail convergence (2026-07-02b):** the list reads the DURABLE marker while detail + the user billing panel read LIVE Paddle, so a cancel made before mig 183 (or Paddle-side) left the list showing plain "active". Both live-read routes (`GET /api/payments/subscription` + the admin detail `GET /api/admin/entitlements/user`) now SELF-HEAL the durable marker via `markSubscriptionCanceling(scheduledCancelAt ?? null)` (reusing the live call already made, null clears on resume), so the durable single source converges and the list badge matches the detail. Resolver/gate/enforcement UNCHANGED. Verifiers `verify-subscription-emails` 113, `verify-subscription-management` 210, `verify-entitlement-gate` 92. Mig 183 APPLIED 2026-07-02.
- **Discounts / coupons (Model 1: Paddle owns the discount, 2026-07-06)**: the platform REFERENCES, displays, and passes Paddle discounts; it does not compute them (a discount affects PRICE only, never entitlements/gate). **Schema (mig 184)**: `coupon_codes` gains `paddle_discount_id` (the `dsc_...` the code references; NULL = display-only, will not reduce the charge), `kind` (`public` auto-apply / `private` code), `display_label`, `starts_at`. **Resolver** `src/shared/payments/coupons.ts`: `resolveCouponForCheckout` (code -> Paddle discount id, validity + platform scoped, clear rejection reason on every failure) + `loadActivePublicPromo` (newest active public promo, ONLY when it has a `paddle_discount_id`, so a displayed promo always really applies). **Checkout** (`POST /api/payments/checkout`): accepts `coupon_code`; a typed code resolves server-side (invalid -> `{status:'error'}` rejection, no overlay), else the active public promo auto-applies; the resolved `discountId` flows through `CheckoutRequest`/`CheckoutResult` -> the Paddle adapter's `open_overlay` -> `paddleBrowser.openPaddleCheckout` -> `Paddle.Checkout.open({ discountId })`, where Paddle validates + applies it (a rejected discount surfaces via the existing `checkout.error` handler). A discount id is client-safe; the API key stays server-side. A 100%-off Paddle discount yields a zero charge while Paddle still creates the subscription and the webhook grants the plan (entitlements unaffected). **Checkout coupon field** (`app/pricing/CouponInput.tsx`, controlled): logged-in only (logged-out hands off to `/register`); "Apply" gives immediate feedback via `POST /api/payments/coupon/resolve` (no charge/state); the code is held in `PricingExplorer` and passed to checkout. **Marketing/pricing display**: `loadPricingCatalog` returns the active public `promo`; `LivePlanCards` shows a promo banner + the discounted (struck-through) price for percentage promos; `PricingPageBody` threads it through (the Paddle discount id stays server-side, only label/percent reach the client). **Admin** (`/admin/plans` CouponManager): each entry has the Paddle discount id (with a clear "must exist in Paddle, else it only displays" warning), kind, display label, active toggle, start/expiry; a **Not wired** badge flags rows with no `paddle_discount_id`. **Manual plans**: unchanged, the discount is just the amount the admin enters and the manual receipt shows it (no Paddle). Resolver/gate/enforcement UNCHANGED (`verify-entitlement-gate` 92, `verify-pricing-display` 23, `verify-pricing-application-ready` 48 + 1 pre-existing failure, see the Entitlements bullet). Mig 184 APPLIED 2026-07-06.
- **Discounts auto-linked to Paddle (rework, 2026-07-06b)**: Paddle is now the SINGLE SOURCE OF TRUTH for discounts, replacing the manual re-entry of mig 184. The platform reads the LIVE Paddle discount list via the API (`listDiscounts` in `paddleApi.ts` -> `GET /discounts`, server-side, 60s in-memory cache in `coupons.ts`), so a discount created once in Paddle appears in the platform automatically with its real percentage/code/expiry/redemption-limit/status (Paddle enforces those at checkout; the platform never duplicates them). **Minimal local storage**: only the admin's CHOICE of which Paddle discount is the featured PUBLIC promo (discount id + optional label) in `cms_content` (`section='payments'`, `key='public_promo:{platform}'`); NO discount data stored. The dead `/api/admin/pricing/coupons` + `/api/pricing/validate-coupon` routes were REMOVED (2026-07-07); the `coupon_codes` table (mig 076/184) is now orphaned but RETAINED (data, non-destructive). **`coupons.ts` rewritten, same public signatures** (so checkout/`coupon/resolve`/`pricingCatalog` are unchanged): `resolveCouponForCheckout` matches the typed code against the live Paddle list; `loadActivePublicPromo` returns the featured Paddle discount (only when still active/enabled). **Public auto-apply fixed**: the featured promo now applies by `discountId` with NO code (the old bug: the local row often had no `dsc_` id so nothing was passed and Paddle showed its code field). **Admin** `/admin/plans` CouponManager is now a live Paddle discount viewer + "Feature as public promo" picker (via new `GET/POST /api/admin/payments/discounts`), no re-entry; shows code/amount/expiry/uses/checkout-enabled/status. **Banner site-wide**: new server `PromoBanner` (`src/hubs/main/components/pricing/PromoBanner.tsx`) on the main marketing home page (renders nothing when no promo); the pricing page keeps its in-`LivePlanCards` banner. Private codes still typed at checkout -> matched to a Paddle discount -> applied by Paddle. Resolver/gate/enforcement UNCHANGED; a 100%-off Paddle discount still grants the plan at zero charge. No migration.
- **Discount normalization fix, an unlimited discount is not a used-up one (2026-08-09)**: a live 25% pre-launch promo showed in the admin discount list but never reached the public pricing page and was never passed to Paddle at checkout, so customers were charged full price with nothing rejected and nothing logged. **Cause was ONE normalization line**: `shapeDiscount` (`paddleApi.ts`) read Paddle's nullable numerics as `Number.isFinite(Number(x)) ? Number(x) : null`, and `Number(null)` is `0`, which is finite. So `usage_limit: null`, meaning UNLIMITED, normalized to `usageLimit: 0`, and `isLiveDiscount` then read `timesUsed >= usageLimit` (0 >= 0) as fully redeemed and filtered the discount out. **The two surfaces disagreed silently because only one of them filters**: `loadAdminDiscountView` returns the Paddle list RAW (so admin kept showing it), while the pricing page and checkout both go through `loadActivePublicPromo`, which requires `isLiveDiscount`; a typed code via `resolveCouponForCheckout` would have reported it expired. **Fix**: a `nullableNumber` helper preserves absence as null instead of collapsing it to zero, applied to `usage_limit` and `times_used`. `isLiveDiscount` needed NO change (it already guarded `!= null`), so the bug was never in the predicate: normalization destroyed the null before the predicate saw it. `shapeDiscount` + `isLiveDiscount` are now EXPORTED so the behaviour is pinned rather than trusted from a comment. **Verified live after the fix**: the featured promo resolves (25% off), `restrict_to` is empty so it covers every plan price id, and Paddle applies it by discount id **despite `enabled_for_checkout: false`** (a `pricing-preview` call returned 1,500 -> 1,125 on pro monthly), which confirms the standing assumption that the flag only governs the customer-typed code field. Confirmed end to end by a real checkout. `verify-payments-structure` 65 -> **79** (14 checks: the pin "no usage limit + zero redemptions is LIVE", a code-less checkout-disabled discount still live, and the exhaustion clause still biting so the fix is not just a deleted check; **proven to have teeth, 7 fail against the previous normalization**). Resolver/gate/enforcement UNCHANGED. No migration.
- **Launch countdown banner (2026-08-09)**: a wide (~800px, scaling down), centred, dismissible launch banner announcing the Modeling Hub launch, shown to visitors on the marketing home, both Modeling Hub pages and both Real Estate platform pages. Reads `training_settings.modeling_hub_launch_date`, the SAME key `/api/cron/auto-launch-check` reads, so the countdown a visitor sees and the moment the hub flips are one date rather than two that can drift; nothing is hardcoded. That key had **no admin editor** (the `/api/admin/modeling-coming-soon` route existed but had ZERO callers), so this wires that existing route to a new "Modeling Hub - Public Launch Date" card on `/admin/modules`; the per-surface signin/register dates are a separate thing and are untouched, so the public announcement can be staged apart from when the auth pages open. **Setting the date does NOT schedule an automatic launch**: the cron also requires `modeling_hub_auto_launch`, which is `'false'` and has no UI writing it. **THREE STATES (2026-08-09c)**: `countdown` while the date is in the future, `launched` once it passes (its own editable headline/supporting line + a call to action, and it STAYS until retired rather than vanishing), `hidden` when the admin switches it off or there is no usable date. Only the OFF choice is stored (`modeling_hub_launch_banner_enabled`, **absent means ON** so existing installs are unaffected, checked FIRST so it overrides both date-derived states); countdown vs launched is derived from the date so the two can never disagree with the clock. **The off switch exists to prevent collateral damage**: the only previous way to retire the banner was clearing the launch date, which is the same key the auto-launch cron reads. At zero the banner **switches in place** to the launched message (both copies are sent to the client) instead of disappearing under a visitor. The CTA destination defaults to the platform's own page derived from the config slug (`${APP_URL}/modeling/<slug>`, absolute because the banner also renders on the apex) and is admin-overridable; no destination + no label means no button rather than a dead link. **Session dismissal is keyed on date AND mode**, so dismissing the countdown does not swallow the launch announcement later the same session. **Sizing**: ~800px wide (`min(800px, 100vw - 32px)`), height follows content capped to the viewport with internal scroll; it was square at 400px until 2026-08-09c. `CountdownTimer` caps its own digit grid at `min(440px, 100%)`, so the digits centre in the wide card rather than stretching. Placement is an **ALLOWLIST of five EXACT paths**, not the blocklist `PromoPopup` uses, because this banner covers content: exact matching keeps it off deeper routes, and `'/'` serves both the apex home and the app subdomain root (which rewrites to `/modeling` with the browser path unchanged). Dismissal is **sessionStorage keyed on the launch date**: gone for the session, back next session, and a rescheduled launch is a new announcement. Square on every screen via `min(400px, 100vw - 32px, 100vh - 32px)`. Brand navy gradient + gold accent. Mounted ONCE in `app/layout.tsx` beside `PromoBanner`; pure rules in `launchCountdown.ts`, server read in `LaunchCountdownBanner.tsx`, path/dismissal/tick in `LaunchCountdownPopup.tsx`. **No migration** (`training_settings` is a free-form key/value store, `key text primary key` with no constraint on the key set, so every launch setting is just another row). **Editable copy + platform-aware (2026-08-09b)**: headline and supporting line are admin-editable (`modeling_hub_launch_headline` / `_subline`), and `modeling_hub_launch_platform` stores the platform SLUG only, with the NAME resolved from the platform config at render (`getPlatform(slug).name`, else the first LIVE platform), so no product name is hardcoded in the banner and a rename carries through. Either field may contain a `{platform}` token, mirroring the `{trialDays}` precedent; blank restores the default; an unresolved platform never leaks a literal token to a visitor. **Admin is now ONE dedicated `LaunchBannerCard`** (date + copy + platform + live preview) instead of a second `LaunchStatusCard`, and it carries a **BANNER SHOWING / NOT SHOWING status computed by the same pure resolver the banner uses**, from the SAVED (not draft) date. That status is the fix for the reported "banner does not appear": the date had never been written (`reason: not_set`), and with three cards on the page carrying a launch date and none stating which drove the banner, a save on the wrong card (where "Save" is disabled when the value is unchanged) looked identical to success. `LaunchStatusCard` is back to its pre-banner shape (the two props added earlier were reverted rather than left dead). `verify-launch-banner` **141**. **Not browser-verified.** Note `/modeling-hub` and `/modeling-hub/real-estate` are `revalidate = 60`, so a date change can lag up to a minute there; the other three are `revalidate = 0`.
- **Subscription-email gap closure (2026-07-06)**: fixes the six gaps from the email-system audit; resolver/gate/enforcement UNCHANGED. **P1 (1) Period-end convert-to-manual is no longer silent**: `applyScheduledManualConversion` (webhook `subscription.canceled` + the cron backstop) now sends the manual welcome + issues the branded receipt (when an amount is owed), matching the immediate path; startedAt is anchored to `conv.effectiveAt` so the welcome/receipt dedupe token is STABLE across the webhook/cron race (one email, one receipt). **(2) Welcome/receipt self-heal**: new `selfHealWelcomePaddleEmail` sends the welcome+invoice for a live paid Paddle sub whose `activated` webhook never delivered. It first checks for an existing `welcome_paddle` marker on the STABLE `evt:{subId}` threshold (IGNORING anchor_day), so already-welcomed subs (incl. all existing prod rows) are skipped, no mass re-send. Wired at the billing-panel live read (`GET /api/payments/subscription`, active non-canceled only) AND daily in the reminder cron Pass B (stored row, no extra live call). **P2 (3) Dunning**: adapter now maps `subscription.past_due`/`transaction.payment_failed` -> new neutral `payment_failed`; webhook sends `paymentFailedEmail` (update-card), NO plan change (Paddle retries, access continues), deduped PER BILLING PERIOD (`evt:{subId}` + period-end anchor so the several retry events collapse to one). Retain coexistence: safe alongside Paddle Retain; to hand dunning fully to Retain, drop the `sendPaymentFailedEmail` call in the webhook. **(4) Renewal receipt**: adapter splits `transaction.completed` by `origin` (`subscription_recurring` -> new neutral `renewed`, else `activated`), so a renewal sends the new `renewalReceiptEmail` (not the welcome copy); the `renewed` webhook branch records the ledger txn + sends the receipt with the invoice attached, and does NOT call setUserPlan (plan unchanged). **P3 (5) Double-submit guard**: admin plan route's fallback `started_at` is now DATE-ONLY (`slice(0,10)`), so a rapid double-submit resolves to one dedupe token -> one welcome + one receipt. **(6) Paddle-dashboard cancel**: the webhook `updated` branch now sends the cancellation confirmation when it carries a scheduled cancel-at-period-end, deduped on the SAME `evt:{accessUntil}` key the in-app cancel route uses (no double for in-app cancels). New parsed-event fields `billingPeriodEnd` (+ `renewed`/`payment_failed` types); webhook also resolves the user by stored `paddle_subscription_id` as a fallback for thin renewal/dunning events. All sends go through the existing `[sub-email]` dedupe dispatch (logged sent/skipped/FAILED). Two new templates (`renewalReceiptEmail`, `paymentFailedEmail`), FMP/PaceMakers branding + per-platform `/pricing` links. Verifiers `verify-subscription-emails` 113, `verify-subscription-management` 210, `verify-entitlement-gate` 92. No migration.
- **Account area cleanup (2026-07-01)**: the grace renew banner now ALSO renders on the modeling **dashboard** (`app/modeling/dashboard/page.tsx`, `data-testid="dashboard-grace-banner"`, gated on `ent.lapseState === 'grace'` from `useEntitlements`), so a grace user sees read-only-until-[date] + Renew on login, not only inside the workspace; lapsed still routes to `/choose-plan`. **Settings** (`app/settings/page.tsx`): the redundant Subscription card (Plan/Status/Projects/Upgrade) was REMOVED, it read the legacy `users.subscription_plan` off the NextAuth session (default `'free'`, a dead source separate from the entitlement gate, the cause of the stale "Plan: Free" display, and the ONLY user-facing reader of it), so the dashboard Billing tab is now the single subscription surface. Profile card gained editable **Company + Job Title (required) + Phone + City (optional)** and **profile-image upload/replace** (initials fallback). New authenticated `POST /api/user/avatar` -> `avatars` storage bucket (2 MB, jpg/png/webp) + `users.avatar_url`, shown in Settings + dashboard header/sidebar; `/api/user/profile` GET/PATCH extended (schema-tolerant, `action=profile`). No migration (avatar_url mig 002, company/job_title 172, phone/city 027 already exist). Resolver/gate/enforcement UNCHANGED; `verify-entitlement-gate` still 92.

- **AI foundation (COMPLETE 2026-08-01, units 0 to 9)**: a platform-agnostic AI engine, not a one-off feature. One call path (`runAi`, the only reader of `ANTHROPIC_API_KEY`), a feature registry with admin toggles, server-side metering with DB-driven per-plan caps and refund-on-failure, a grounding abstraction that supplies labelled FACTS and audits every figure in the output, an admin control panel, and the first feature on it: **REFM Module 7 IC narrative generation** (six fields, grounded in the computed model, editable draft, never auto-saved). Migrations 203-206 all APPLIED. Features register DISABLED and are switched on in `/admin/ai-features`. **Full detail, invariants and verifier map in [CLAUDE-AI.md](CLAUDE-AI.md); plan and standing rules in [docs/AI_FOUNDATION_GUIDELINE.md](docs/AI_FOUNDATION_GUIDELINE.md).**

Verifier reality + outstanding ops (migrations pending on prod) live in [CLAUDE-REFM.md](CLAUDE-REFM.md). Full suite green via `npx tsx scripts/verify-*.ts`.

**M2 lock conventions** (apply to M3+ unless overridden):
- The reference Excel at repo root is the verification benchmark, not a behavioural spec. Every reference-specific behaviour stays configurable; never hard-code currency, locale, escrow, or DSO defaults into engine paths.
- Engine storage is project-axis-indexed (`arr[0]` = first active project year). Only the UI window changes per asset / strategy.
- Vintage matrices (cohort year × cash year, cohort year × recognition year, capex year × CoS year) are the canonical mechanic for both recognition and cash distribution.
- PIT recognition handover = LAST construction year (`phaseStart + cp − 1 − projectStart`), NOT first operations year. Verifier A2-1..A2-5 pin this.
- See CLAUDE-REFM.md for the full Pass 7-9 narrative; older pass detail is archived to CLAUDE-FEATURES.md.

> **See also:**
> - [CLAUDE-MODELING-HUB.md](CLAUDE-MODELING-HUB.md), Modeling Hub wrapper, platform catalog, P-Sync admin conventions
> - [CLAUDE-REFM.md](CLAUDE-REFM.md), Real Estate Financial Modeling (REFM), platform status (Modules 1-7) + conventions + Excel/PDF export
> - [CLAUDE-AI.md](CLAUDE-AI.md), AI foundation (client, registry, metering, grounding, admin panel, IC narrative). Load when touching anything AI
> - [docs/FUND_LAYER_GUIDELINE.md](docs/FUND_LAYER_GUIDELINE.md), Fund layer (fund fees, hurdle, carry, gross vs net returns): scope, the linear-fee-base decision, the 7-step sequence, standing rules and known gaps. **Load it when touching anything fund; it is the source of truth and is kept current.** State as of 2026-08-11 (see section 8 of the guideline for the presentation corrections and the toggle-now-ON state): **Steps 1 to 7 LIVE, the fund layer is FEATURE COMPLETE for v1**, migs 208/209/210/211 APPLIED (Steps 4, 6, 6b and 7 needed none). **The ONLY remaining work is browser verification**, which Step 7 explicitly did not close. **Step 7 (end-to-end verify, 2026-08-10):** `scripts/verify-fund-e2e.ts` **86 checks, 0 failures**, run against the REAL project **FMP RE HUB** (3 phases, 8 assets, 14 periods) read from its saved version snapshot and NEVER written back; falls back to the fixture without DB creds and says so. **CORRECTED 2026-08-10:** an earlier version of this bullet said `refm_fund_terms` had ZERO rows. Wrong, and the error is worth remembering: the probe selected a column named `enabled`, the column is `fund_enabled`, the query errored and the empty fallback was reported without checking the error. **FMP RE HUB is CONFIGURED but DISABLED**, carrying a fully populated row written 2026-08-05 (structure fee 0.50%, management 0.25%, custody 0.50%, debt arranging 0.50%, other expenses 3m pa, 8% hurdle, 20% carry, named Fund Manager, 3-row fee matrix) with `fund_enabled` **false**. An empty export is therefore the CORRECT tested behaviour (it is exactly Step 1's fully-populated-but-disabled case), not a defect; flipping only that flag yields 403.844m of fees and 57 fund rows (P&L 17, Cash Flow 4, Returns 36). **SUPERSEDED 2026-08-11: the toggle IS now on.** `refm_fund_terms.fund_enabled` is true and the latest saved version (labelled 1.0, 2026-08-10) carries `project.fundTerms.enabled: true`, so FMP RE HUB exports a full fund workbook: 359.941m of fees, 5,651.850m distributed, ZERO performance fee (the 8% hurdle is never cleared, 1,477.007m unpaid at exit). Both earlier sentences in this paragraph, that the toggle had never been on and that every saved version predates fund terms, are now false and are kept only so the correction is legible. NOTE the project snapshot is edited in place by the user, so a fingerprint captured against it is NOT a stable baseline: use `buildExcelSampleState` for any before/after identity proof. Proves: toggle-off byte-identical across engine + screen builders + Excel + BOTH PDFs (terms fully populated but disabled, and separately at zero rates); the cash chain as an EXACT identity `CFO_on = CFO_off - fee + taxShield` (worst residue 2.4e-7 on 2,040m; **taxPaid is stored NEGATIVE so the shield ADDS**); **the freeze proven decisively**, the structure fee charging on the FEE-FREE fund size 5,466.799280m to 0.000e+0 while the with-fees figure is 5,626.859262m; waterfall conservation and hurdle roll-forward every period; post-fee IRR/MOIC recomputed independently to 1e-9; changing the hurdle leaving FCFF/FCFE/gross dividends byte-identical; and row order + totals agreeing across all four surfaces. **`ebitdaBeforeFundFeesPerPeriod` is NOT the standalone EBITDA** (5,021.992m vs 5,053.947m fund-off, a 31.956m gap): the fee raises funding, which capitalises +35.212m of IDC into Sell-asset inventory, which releases +31.956m to cost of sales. Not a circularity breach (fees are exactly 1,047.144m either way); compare two RUNS, not two fields. Two non-defects pinned so they are not re-reported: the BS residue is 360.8 units on 7,034m (5.1e-8) and IDENTICAL fund off vs on (pre-existing solver convergence), and waterfall `unallocated` is -9.5e-7 on 4,437m (2.2e-16, one machine epsilon). **The real project does not clear an 8% hurdle** (gross DDM IRR 4.38%), so the carry path only runs in the verifier's second cleared-hurdle case; keep such a case in any future edit or the section passes vacuously. **Step 6b (PDF exports + shared presentation builder, 2026-08-10):** the full PDF already had the fee lines / total / EBITDA-after / cash flow row free from the shared `m4Reports` builders but NO waterfall, gross-vs-net, fee income or basis block (the word "hurdle" appeared nowhere in the document); the **summary PDF had nothing and was actively misleading**, printing an `ebitdaPerPeriod` already net of fees with no fee line, so revenue less CoS less opex missed the printed EBITDA by exactly the fee (-35.98m vs -56.76m, 20.78m unexplained). Fixed by extracting `lib/reports/fundReports.ts` as the ONE definition of fund PRESENTATION (row order, labels, which rows are balances, gross-vs-net, earner grid, headline cards); the M5 screen, Excel, the full PDF and the summary PDF ALL render from it and the Step 6 inline Excel rows were DELETED rather than left as a copy. Formatting stays per-surface (each passes its own money/pct/mult); STRUCTURE is what must not drift. **Measurement note worth reusing:** pdf-lib embeds Inter as a CID font, so PDF text is hex GLYPH IDS and a naive grep silently returns nothing (a first diagnosis pass did exactly that and was worthless); `scripts/pdfTextExtract.ts` maps glyph ids back through the embedded font. The check that earns its place is `verify-fund-pdf` section 2: every surface must IMPORT the builder and re-declare NO row label, so two hand-maintained copies cannot pass. Toggle-off proven across all three exports at once by an 8,660-line fingerprint (Excel cells + fill + numFmt + `pageSetup`, plus both PDFs' decoded text), identical at HEAD and after. `verify-fund-pdf` **49**, teeth proven by three sabotages. Also repointed two `verify-fund-fee-income` checks from grepping the M5 component source to asserting the builder output (107 -> 108) and widened a hole in the `verify-fund-excel` toggle-off regex that did not match `Fund Management and Other Expenses`.   **Fee bases corrected to THREE DISTINCT bases (2026-08-10):** total equity 2,632.733m (cash + in kind + existing), debt facility 2,834.066m (existing + draws + capitalised IDC), fund size 5,466.799m = the sum, so a reader can add two and get the third. Management + custody charge on TOTAL EQUITY, structure on FUND SIZE, arranging on the DEBT FACILITY. **The arranging fee is NOT on the facility limit**: that resolves from an LTV cap to 4,273.822m vs 2,834.066m raised, a 1,439.756m gap, so it charged a ceiling never drawn; pinned that facilityLimitOverride and a stated principal do NOT move the fee. `totalEquity`/`debtFacility` resolve INDEPENDENTLY of fundSize (a fundSizeOverride zeroes its components) and the reconciliation display suppresses itself under that override. Interim step the same day, superseded: the two annual fees charged on OPENING NAV; the engine was correct (`fee_t == openingNAV_t x rate`, max single-period NAV 3,953.2m = 0.72x fund size, so NAV never exceeded capital) but the base did not match the model (which sizes a fund by TOTAL period debt+equity funding) and the DISPLAYED basis summed a balance over 14 years (41,752.1m vs 5,466.8m fund size, ~7x, reading as a fault). Both now charge on `fund_size`, the same frozen constant the structure fee uses: all three capital fees share ONE base, the annual fees are now charged in period 0 too (fund size is known at the start where opening NAV was zero), and the basis shows the per-period constant + period count ("0.50% of 5,466.8m, 14 periods"). `opening_nav` stays a legal linear base kind, unused. **Export fix (same day, found by a user export not a verifier):** `Project.fundTerms` only reached the store when the Fund Terms tab MOUNTED, so opening a project and exporting produced a file with no fund content and no warning; `attachToProject` now loads it before hydrate (and before the autosave subscriber, or it causes version churn), filling only an ABSENT value. ExportModal now checks the SELECTED version and warns plainly when it carries no fund terms. The stale "terms do not yet flow into the model" notice and module header (true at Step 2, false since Step 3) are rewritten. Known: the full PDF truncates the inline fee labels in its narrow label column (hence the columnar basis block), and the Excel Summary tab can no longer be one page on a fund project (accepted). **Step 6 (Excel export rows, 2026-08-10):** the diagnosis found MORE THAN HALF of it already shipped, because the Excel P&L and Cash Flow tabs render from the same shared builders as the screen and the PDF, so the five fee lines with rate + base inline, `Total Fund Management Fee`, the single EBITDA struck after it, the Step 5b Fund Fee Basis block and the `Fund Management and Other Expenses` row in operating cash flow all arrived free at Steps 3 and 5b. What was genuinely missing was the M5 fund surface: the Returns tab read `rs.waterfall` / `rs.resultNetDividends` / `rs.feeEarners` nowhere. Step 6 adds ONE section, `3. Fund Layer`, APPENDED rather than interleaved (interleaving would open a section band inside `1. Returns` and renumber RE Metrics on a fund project only), carrying gross-vs-net KPI tiles, the terms applied, a gross-vs-net table, the ten waterfall rows in the reference's exact order (the three BALANCE rows carry no lifetime total), fee income by earner with the unallocated remainder SHOWN, the shared basis table, and per-period fee income. `fundRow` is a SEPARATE emitter on purpose: the existing local `moneyRow` is shared with the FCFF/FCFE stream rows that already pass `style:'subtotal'`, so widening it would have moved rows on a standalone project. Rows stay CONSTANTS like their neighbours, so `verify-excel-export`'s pinned "zero formula cells" holds. **One real defect fixed:** the Excel P&L passes `fmt: String(v)` so a `totalOverride` round-trips to a number, and `fundFeeLineLabel` used that same fmt, so the workbook had been reading `1.00% of Fund size 154519446.40625` since Step 5b (Excel-only; the PDF passes `fmt.money`). `M4ReportCtx` gained an optional `labelFmt` defaulting to `fmt`. **Toggle-off PROVEN not asserted:** a full cell-level fingerprint (8,615 entries: value + fill + numFmt, all 17 sheets) captured before any Step 6 code is byte-identical after. `verify-fund-excel` **69**, teeth proven by four sabotages each caught by the right check. Known gaps: fund terms were absent from the Inputs tab (CLOSED 2026-08-11, see below), and the phase-filtered Direct CF shows the full project fee in every phase (pre-existing, so do Total Revenue Received / Tax Paid / Total Capex there). **Step 5b (fee basis display + MODEL-DERIVED fund size, mig 211)** reversed a decision this guideline defends, so read it before touching fee bases: fund size is no longer a typed input, it is resolved from the model (equity + debt, LIFETIME total) by `resolveFundSize` in the FEE-FREE pass and FROZEN before the solver, exactly as opening NAV has been since Step 3. The invariant is narrow: the base KIND `fund_size` is linear and allowed, the VALUE is resolved once and frozen, and `fund_size_solved` (a live solved figure read INSIDE the loop) stays in `CIRCULAR_FEE_BASES` and stays forbidden. `fundSizeOverride` pins a typed target instead, and the typed figure is still the fallback when the model raises no capital. Five invariants worth knowing before you read anything else: (1) toggle OFF is byte-identical to today, pinned by `verify-fund-layer-guard` against a fully populated but DISABLED block; (2) every fee base is linear and the rule is a DATA STRUCTURE (`FUND_FEE_SPECS` + `LINEAR_FEE_BASES` / `CIRCULAR_FEE_BASES`), so the verifier fails on the registry, not on a comment; (3) the fee schedule is FROZEN before the iterative solver, which is what stops funding raising the fees; (4) the Step 4 WATERFALL (`src/core/calculations/returns/waterfall.ts`, pure + primitives-only) runs over the M5 Distributed-Equity stream on the REFERENCE structure, rebuilt 2026-08-05 from an earlier three-tier shape: `accrued = (unpaidBoP + equityDrawn) x rate`, `owed = equityDrawn + unpaidBoP + accrued`, `paid = MIN(dist, owed)`, `excess = dist - paid`, `fee = excess x pct`, so there is NO return-of-capital tier (equity drawn folds into ONE hurdle balance), NO catch-up and NO residual split, and the fee is FLAT on the excess. The accrual compounds AND charges the same-period draw, collapsing to `owed = (BoP + drawn) x (1+r)`, so a draw held n periods owes `C(1+r)^(n+1)` and an investor paid exactly the hurdle earns MORE than the hurdle rate as an IRR (a stated accrual convention, not an IRR the payment reproduces). It is NOT in the funding solve (the fee splits cash that already left the project), which `verify-fund-waterfall` proves by holding the full financials snapshot identical across three hurdle/fee settings; gross streams and `result` are untouched, net rides alongside as `netDividendStreamPerPeriod` + `resultNetDividends`; (5) **Step 5 (2026-08-05) made it VISIBLE**: two toggle-gated sections on the M5 Returns tab (Fund Distribution Waterfall with gross-vs-net IRR/MOIC and the reference row order, balances carrying no lifetime total; Fund Fee Income per earner), plus `feeEarners` on the snapshot from a SEPARATE pure engine (`src/core/calculations/returns/feeEarners.ts`). Fee earners sit BESIDE the equity partners: `PartnersSection` and `computePartnerReturns` were not edited at all, the Fund Manager takes 100% of the five management fees plus its matrix share of the performance fee, and matrix shares are NEVER normalised (an 80% matrix allocates 80% and reports the remainder). `verify-fund-fee-income` pins that changing the matrix leaves the whole partners block and every gross stream byte-identical; note that all 556 pre-existing checks PASS when the Fund Manager is leaked into the partner roster (a zero-equity partner keeps Sigma partners == consolidated true), so only this verifier sees it. UI is asserted at SOURCE level only, a real render under tsx is blocked by the CSS-module import; (6) the ENGINE reads `Project.fundTerms` in the version snapshot, `refm_fund_terms` is the tab's durable store, and the server three-tier probes 210 -> 209 -> 208. **Ten-item export review (2026-08-11), items 1 to 9, all measured on the real project before any code changed:** (1) **Checks tab reported CHECK on a healthy model.** The tolerance was `maxBsDiff < 1`, an ABSOLUTE one-currency-unit band on a seven-billion balance sheet (1.4e-10 relative), so a residue of 360.792 at 5.1e-8 failed. It is now RELATIVE (1e-6 of the PEAK magnitude, not the worst period's value, because net cash flow crosses zero and produced "5.0e+0 of 0.0 m"), the status reads OK with the residue and its scale stated, and the other two rows, which were the string `'OK'` with an unrelated magnitude in the note column (one printed total capex, -3,561,517,930, beside a green OK), are now real comparisons: closing cash vs BS cash, and Direct CF vs Indirect CF. All three read the PLATFORM snapshot; `addChecks` no longer destructures `lm` at all. (2) **The workbook shipped TWO models.** Summary and the Returns tab's own "Returns Metrics" block read `lm.*` from `lib/excel/liveModel.ts`, the simplified twin left over from when this workbook emitted live formulas, whose IRR streams carry no inception outflow and none of the historical development investment: Summary said Project IRR **177.3%** / Equity IRR 38.3% / multiple 7.31x while the grid ten rows above said **10.9%** / 7.0% / 2.33x, and the Checks tab hyperlinked to the wrong one. Summary, the Returns metrics block and the Checks headline now all read `rs` (the platform returns engine) and every label names its basis. The third figure the review flagged, Distributed Equity 6.8%, was NOT an error: it is `rs.result.dividends`, a genuinely different basis. **liveModel is no longer a reporting source anywhere**; it still backs the formula-cached cells. (3) **Min DSCR 0.43x is REAL**, not a period artefact: per-period 0.56 / 2.17 / 3.19 / 0.56 / 0.43 / 1.52, three of six debt-service years below 1.00x, while ICR over the same years runs 0.56 to 25.63, so interest is covered and the amortisation is not. Flagged in check-red on the Summary highlight (with a sentence naming the count), on the Returns coverage tile, and per year in the DSCR row. (4) **A base is a STOCK.** The Total column summed the per-period bases, so an annual fee on total equity printed 36,858.3m against a 5,466.8m fund size on the Excel P&L, the Excel Returns tab AND both PDFs. `FundFeeBasisRow` gained `basisDisplay` + `basisIsPerPeriod` so the rule is encoded ONCE; the text surfaces render "2,632.7 x 14" via the shared `fundFeeBasisText`, Excel keeps a plain NUMBER in the cell (the display scale cannot reach text) and puts the period count in the row label. (5) **Raw-vs-millions was in the LABELS, not the values.** Every fee-basis cell was measured at `full` and `millions` scale in Excel and both PDFs and carries the same money format as its neighbours; no string cell anywhere holds a 7+ digit run. The real defect was `labelMoney`, hardcoded to millions regardless of export scale, so a full-unit export read "0.50% of Total equity 2,632.7 m" beside a cell of 13,163,667. It now follows `displayScale` (threaded through `EmitCtx.labelMoney`). (6) **Hurdle Accrued lost its lifetime total** (4,496.1m), joining the three balance rows. It is an accrual on a compounding balance printed between two balance rows. Noted for reversibility: the sum DOES reconcile (accrued 4,496.1 + drawn 2,632.7 == paid 5,651.8 + unpaid 1,477.0), so this is presentation judgement, not an identity. `FUND_WATERFALL_BALANCE_ROWS` is now `FUND_WATERFALL_NO_TOTAL_ROWS` (the old name is a deprecated alias). (7) **Gross == net is explained**, from a shared `fundGrossNetNote` that renders on the M5 screen, Excel Returns, Excel Summary and both PDFs and returns '' the moment a fee arises. (8) **FUND INPUTS band added to the Inputs tab**: the toggle, five rates, other expenses, hurdle, performance fee, Fund Manager, both override flags, the three resolved capital bases (marked computed, not input) and the un-normalised distribution matrix. (9) **The Cover claim was partly WRONG and is corrected here:** the Cover already listed "Fund Fee Basis" and "3. Fund Layer" as hyperlinked sub-entries; the first scan reported zero only because hyperlink cells hold `{text,hyperlink}` objects that stringify to `[object Object]`. What was genuinely missing was a top-level signal, now a "Fund layer active" marker on the identity strip, plus an exemption so fund sections can never be dropped by the 14-section-per-tab cap (they sit at the END of both P&L and Returns). Registering the Summary tab's front-matter bands as sections was TRIED AND REVERTED: every section link is an `!A<row>` anchor whose column A must hold the title, and a front-matter canvas keeps column A as a margin, so it broke the anchor invariant workbook-wide to gain three entries. **Toggle-off PROVEN on the FIXTURE** (the real project is edited in place and is not a stable baseline): 17,230 cells across 17 sheets at two display scales, both PDFs at 8,316 decoded lines, byte-identical except Checks / Returns / Summary, which are exactly items 1 to 3, and zero fund labels anywhere. **Basis block readability follow-up (2026-08-11b):** (a) the labels were being TRUNCATED by the 34-character label column ("Custody and admin fee: basis charged on (per period, 14 periods)" is 64 chars and rendered as "...basis charged on (p", hiding the very count it was added to show). Labels are now `<fee>: basis` / `<fee>: charged` (longest 34 of 34) and the period count moved to the **Base** column, which is 30 wide and held only "Total equity", so it reads "Total equity x 14". (b) The **Rate column was 2 characters** and could not overflow (the Total cell beside it is never empty), so "0.50%" was a sliver; widened to 10, and only when the fund block renders so a standalone project keeps its geometry. **ExcelJS trap worth remembering: `isCustomWidth` is `width !== DEFAULT_COLUMN_WIDTH` and that constant is 9, so a column set to exactly 9 is dropped from `<cols>` entirely and the width silently does not apply.** First attempt used 9 and did nothing; the verifier now asserts `>= 8 && !== 9`. (c) A **flat-amount fee is now ONE row**: its basis and its charge are the same quantity (3.0m per period charged on a basis of 3.0m per period), so the pair said the same thing twice. `FundFeeBasisRow.hasRate` carries that semantically rather than inferring it from `rate === '-'`; rate-based fees keep both rows. **A reported defect that was NOT one:** "Other expenses prints raw 3000000 while neighbours are in millions" was investigated to the byte (file round-trip, raw sheet XML, styles.xml numFmtId resolution, shared-strings scan) and the cells carry style `s=105` -> `numFmtId=166` -> the millions format, IDENTICAL to every neighbouring fee row; the reporter was reading stored values programmatically rather than the rendered cell. Recorded so it is not re-investigated. `verify-fund-excel` 70 -> **75**, teeth proven by four sabotages (no collapse, collapse everything, long labels, width back to 9). `verify-excel-export` 301 -> **304**, `verify-fund-fee-income` 107 -> **109**, `verify-tab2-fixes` 47. Verifiers `verify-fund-layer-guard` 75, `verify-fund-terms` 182, `verify-fund-fees` 136, `verify-fund-waterfall` 382, `verify-fund-pdf` 49, `verify-fund-e2e` 86
> - [CLAUDE-DB.md](CLAUDE-DB.md), Database tables, storage buckets, migrations log
> - [CLAUDE-FEATURES.md](CLAUDE-FEATURES.md), Feature status, archived phase narratives (M1.R → M1.13d pre-M2.0 + M2.0 → M2.0i post-rebuild)
> - [CLAUDE-ROUTES.md](CLAUDE-ROUTES.md), All page routes, API routes, components, lib structure
> - [CLAUDE-TODO.md](CLAUDE-TODO.md), Pending work, backlog, legacy reference. **START HERE is current as of 2026-08-13: the export review is DONE (four passes; passes 1 to 3 deployed, PASS 4 IS UNCOMMITTED AND HELD FOR REVIEW), and THE EXPORTS HAVE NOT BEEN RE-READ SINCE PASS 3 LANDED. Nothing from the 2026-08-11 or 2026-08-12 sessions has been browser-verified (the strategy-change dialog and banner most of all, then both PDFs). Module 7 fixes are further down. The `AI_REVIEW_GUIDE.md` audit (untracked, untrusted, per-finding status recorded) is parked above it.**
> - [ARCHITECTURE.md](ARCHITECTURE.md), Three-tier folder rationale, alias guide, boundary rules
> - [HANDOFF.md](HANDOFF.md), AUTO-GENERATED live status snapshot (commit, stack, latest migration, templates, verifiers, routes, env). Never hand-edit; regenerate with `npm run handoff` (`scripts/generate-handoff.ts` derives it from git + the repo, so it cannot go stale like the old PROJECT_HANDOFF.md did).

---

## STRICT SESSION RULES, READ FIRST

### Writing rule: NEVER use the reference client's name

The user has shared private reference Excel models that we use as a verification benchmark only. The client / model name **must never appear in this codebase**, including:
- UI strings (tooltips, captions, hints, formula text, error messages, labels)
- Code comments
- Variable / function / file names
- Commit messages, PR descriptions, branch names
- Project documentation, MD files, README
- Test fixtures (use generic names like `refAsset`, `refPhases`, "the reference fixture")

If you need to refer to a behaviour learned from the reference Excel, use phrasing like "the reference model", "the reference benchmark", or "the reference design", never the client's actual name. Any historical references that slip through must be scrubbed when found.

The reference Excel files at the repo root are `.gitignore`-listed and must stay that way. Do not commit them.

### Writing rule: NEVER use em-dashes

**NEVER use em-dashes (the long dash, U+2014) anywhere.** This applies to ALL output: code, code comments, UI strings, JSX text, tooltips, error messages, commit messages, documentation markdown, prompts, agent briefs, anything.

Use one of these instead, depending on intent:
- A comma, when separating clauses (most common substitute).
- A colon, when introducing a list or definition.
- Parentheses, for asides.
- "and" or "or" or "and/or", when joining alternatives.
- A period plus new sentence, when the second clause stands alone.

Em-dash sweep across `src/` + `scripts/` is complete (zero remaining as of 2026-05-20). Do not introduce new ones.

### Scoping: Read ONLY the files for your task domain

| Task | Read these paths + MDs |
|------|------------------------|
| Training (auth / dashboard / assessment / certificate) | `app/training/` `app/api/training/` `app/api/t/` `src/lib/training/` `src/components/training/` |
| Modeling Hub auth | `app/modeling/signin/` `app/modeling/confirm-email/` `app/api/auth/` `src/lib/shared/` |
| Modeling Hub wrapper (sidebar, platform catalog, admin sync) | **+ CLAUDE-MODELING-HUB.md.** `app/admin/platform-modules/` `app/api/platforms/` `app/api/admin/platform-module-pages/` `src/lib/modeling/platform-modules/` `src/components/refm/` `src/hubs/modeling/` `app/modeling-hub/` |
| REFM platform (Real Estate, Modules 1-7) | **+ CLAUDE-MODELING-HUB.md + CLAUDE-REFM.md.** `app/refm/` `app/modeling/` `src/components/refm/` `src/lib/modeling/` `src/hubs/modeling/platforms/refm/` `src/core/calculations/` |
| Admin panel | `app/admin/` `src/components/admin/` `app/api/admin/` |
| AI (client / registry / metering / grounding / IC narrative) | **+ CLAUDE-AI.md + docs/AI_FOUNDATION_GUIDELINE.md.** `src/shared/ai/` `app/api/admin/ai-features/` `src/hubs/modeling/platforms/refm/lib/ai/` |
| Email system | `src/shared/email/` |
| Landing pages / CMS | `app/(portal)/` `app/about/` `app/articles/` `app/pricing/` `src/components/landing/` `app/api/cms/` |
| Shared (auth / formatters / device trust) | `src/lib/shared/` `src/core/` |
| Navbar / layout | `src/components/layout/` |

Read only the rows your task touches. Per-platform MD loads only when working on that platform; the global CLAUDE.md stays lean.

### End-of-session rule
**ALWAYS update CLAUDE.md files at the end of every session** to reflect:
- Any new files created (add to the correct folder list in CLAUDE-ROUTES.md)
- Any feature status changes (update the Feature Status table in CLAUDE-FEATURES.md)
- Any new environment variables added
- Any new database tables or migrations (add to CLAUDE-DB.md)
- Platform-specific work: update the relevant `CLAUDE-{platform}.md`, NOT the root CLAUDE.md

### Do NOT touch list
- `next.config.ts`, subdomain routing is live and correct; clean auth URL rewrites + redirects added; app. `/register` rewrite goes to `/modeling/register` (dedicated page, NOT `/modeling/signin?tab=register`)
- `src/middleware.ts`, **DEAD CODE, verified 2026-07-30: this file has NEVER run in production.** Next resolves middleware at the PROJECT ROOT (`app/` is the source root and there is no `src/app`), so `src/middleware.ts` is never compiled: `.next/server/middleware-manifest.json` is `{"middleware":{}}` and the build output never mentions it. Live proof: `/admin/cms`, `/admin/users`, `/admin/revenue` all return **200 unauthenticated**. The `/login` + `/admin/login` 307s that make it look alive come from `next.config.ts` redirects, not this file. **No privileged data is exposed**, re-measured live 2026-08-01 across all **121** `app/api/admin/*` route files (118 was the count on 2026-07-30; three have been added since): **69 -> 401, 44 -> 403, 8 -> 200**. Each of the 8 is a read-only GET of data the public site already renders, with every write method on the same route guarded: `asset-types` (GET public by design since Pass 46, non-admins see only `visible` rows), `modules`, `training` (course catalog, no PII), and five `*-coming-soon` flag readers that public pages poll. So the precise claim is "every admin WRITE is guarded", not "every admin route is guarded". Admin PAGES return 200 unauthenticated but render only the generic marketing shell (no admin nav, no data); the client `useRequireAdmin` hook bounces non-admins, so page-level enforcement is CLIENT-side, and the documented server-side guarantee does NOT exist. Do not "fix" it by moving the file to the root without a deliberate JWT `role`-claim test first: that would ACTIVATE a gate that has never run and could lock admins out. See CLAUDE-TODO.md.
- `app/globals.css`, design system tokens, do not restructure
- `vercel.json`, deployment config is live
- `supabase/migrations/`, never edit existing migrations; create new ones only
- Any feature marked Complete unless explicitly asked by the user
- Cross-feature shared files (`src/lib/shared/`, `src/shared/email/`) without explicit instruction

---

## Project Overview

**Financial Modeler Pro**, Multi-hub SaaS platform with three web properties:

| Property | Domain | Purpose |
|----------|--------|---------|
| Main site | `financialmodelerpro.com` | Marketing, admin, portal, auth |
| Training Hub | `learn.financialmodelerpro.com` | Financial modeling courses |
| Modeling Hub | `app.financialmodelerpro.com` | Interactive financial modeling tools |

**Stack: Next.js 16 (App Router) + TypeScript strict + Tailwind CSS 4 + Supabase**

---

## Tech Stack

Next.js 16 (App Router) + TypeScript strict + Tailwind 4 + Supabase. State Zustand, charts Recharts, NextAuth (Modeling Hub) + custom session (Training Hub), Brevo email, hCaptcha, Anthropic SDK, ExcelJS + react-pdf for exports. Full version pinning in `package.json` + extended detail in [ARCHITECTURE.md](ARCHITECTURE.md).

## Authentication (summary)

Two independent stacks. Full per-flow detail (sign-in / register / OTP / device trust / admin bypass) in [CLAUDE-FEATURES.md](CLAUDE-FEATURES.md).

- **Training Hub** (learn.): custom session `training_session` cookie + bcrypt password in `training_passwords`. Source of truth = Google Apps Script roster. `email_confirmed` is `!== false`-truthy (null counts as confirmed for pre-migration-027 students).
- **Modeling Hub** (app.): NextAuth.js Credentials JWT (1hr) + scrypt password in `users.password_hash`. Admin role bypasses both EmailNotConfirmed + DEVICE_VERIFICATION_REQUIRED checks.
- **Device trust**: shared `fmp-trusted-device` cookie + `trusted_devices` table (30-day, keyed by email not UUID).

## Subdomain Routing (`next.config.ts`)

- `learn.fmp.com/` -> rewrites to `/training`. `app.fmp.com/` -> rewrites to `/modeling`. URL unchanged in both.
- Clean auth URLs `/signin`, `/register`, `/forgot` resolve per-subdomain to the matching training / modeling page.
- Cross-domain main-site paths redirect to `financialmodelerpro.com`.

Navbar uses plain `<a>` with absolute URLs via `NavbarServer.absolutizeHref()` + file-level `APP_URL` / `LEARN_URL` constants (never raw `process.env`).

## Design System

Single source of truth: `app/globals.css`. Tokens: `--color-primary*`, `--sp-1..5` (8px grid), `--font-h1..micro`. Component classes: `.card`, `.kpi-card`, `.btn-primary`, `.table-standard`, `.input-assumption` (yellow assumption cells). Per-platform input variants documented in the platform's MD (REFM FAST navy-pale input lives in `src/hubs/modeling/platforms/refm/components/modules/_shared/inputStyles.ts`). Never use Tailwind utility classes for layout tokens.

## Deployment + Health

Vercel auto-deploys on push to `main`. Environment variables documented in `.env.example` + the Vercel dashboard. Key ones: `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` / `NEXTAUTH_SECRET` / `NEXTAUTH_URL` / `BREVO_API_KEY` / `HCAPTCHA_SECRET_KEY` / `ANTHROPIC_API_KEY` / `ANTHROPIC_MODEL` (optional, overrides the AI default model at deploy time) / `CRON_SECRET` / `FMP_PUBLIC_API_KEY` (shared secret for the public partner page feed; UNSET means that endpoint refuses every request, it fails closed).

```bash
npm run type-check   # tsc --noEmit (zero errors required)
npm run build        # next build --webpack (avoids MAX_PATH on Windows/OneDrive)
npm run verify       # type-check + lint + build
```

`GET /api/health` -> `{ status: 'ok', platform: 'financial-modeler-pro', version: '3.0', commit, timestamp }` (`commit` = `VERCEL_GIT_COMMIT_SHA`, so the deployed revision is verifiable: compare to `git rev-parse HEAD`). Per-platform verifier scripts live in the platform's MD (REFM = `npx tsx scripts/verify-*.ts`; current script list + counts in [CLAUDE-REFM.md](CLAUDE-REFM.md)).
