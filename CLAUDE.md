# Financial Modeler Pro, Claude Code Project Brief
**Last updated: 2026-08-30 (restructured: dated session narrative moved to [CHANGELOG.md](CHANGELOG.md)). Platform state as of 2026-08-30.**

**Module status (REFM platform). Full narrative + commits live in [CLAUDE-REFM.md](CLAUDE-REFM.md) and the dated change record in [CHANGELOG.md](CHANGELOG.md), NOT here:**
- **Module 1 (Setup / Costs / Financing): CLOSED 2026-08-17** after 28 defects found by entering a real project end to end, followed by the 2026-08-18/19 statement, returns and reconciliation corrections (all in [CHANGELOG.md](CHANGELOG.md)). **No further Module 1 changes before launch.** Base lock unchanged at M2.0 Pass 58. The recurring shape across the 28: one rule written in more than one place, with more than one answer. Read [docs/TRAPS.md](docs/TRAPS.md) section 7 before touching Module 1 (7.12, 7.15, 7.16, 7.19, 7.21 are all from this block). Browser-verification status per change is recorded in its CHANGELOG entry and in [CLAUDE-TODO.md](CLAUDE-TODO.md).
- **REFM standing invariants** (each born from a measured defect; the full dated story is in [CHANGELOG.md](CHANGELOG.md), the general lessons in [docs/TRAPS.md](docs/TRAPS.md)):
  - **Asset strategy changes are a MODEL operation**, not a field write: `lib/state/strategySwitch.ts` (pure) parks the outgoing strategy's sub-units, opex and companion in `Asset.retainedByStrategy` and restores or seeds the incoming set; `state.subUnits` / `costLines` / `costOverrides` hold ONLY the active strategy's rows. Dialog and banner are gated on `assetHasStrategyAssumptions`; the dropdown previews via a DRY RUN of the same pure function the store commits.
  - **Allocation shares never silently sum to zero**: every share basis falls back to `equalPhaseShare` when its denominator is zero (six call sites in `resolveAllocationFactor` / `resolveDriverFactor`); factors are proven to sum to exactly 1. Never write `return total > 0 ? mine / total : 0`.
  - **`marketing` is a COST STAGE** (`CostStage = land | hard | soft | marketing | operating`), so construction cost excluding land also excludes marketing. When adding a stage: only `Record<CostStage, number>` sites are compiler-guarded, two inferred literals in `Module1Costs.tsx` will silently drop it, and `deriveCostType` defaults an unhandled stage to `'hard'`.
  - **A parcel is PROJECT-WIDE**: the explicit parcel lookup is project-wide; the implicit rules (autoByBua / percent / equal share) stay phase-scoped. `computeAssetLandBreakdown` returns `rateIssue` so there is no silent zero land value.
  - **One placement / window rule each**: `phaseLocalToProjectIndex` is the ONE Y0 upfront-lump rule (all five sites call it); `deriveCostWindow` is the ONE seeded cost-window rule (seed + store re-derive); a followed phasing source (collections, land cash) replaces the WINDOW, not just the shape, and the row renders `resolvedWindowByLineId`, what the engine spent on.
  - **The capex table is an ordered, user-classified list**: `% of selected lines` is POSITIONAL and upward-only (`src/core/calculations/selectedBase.ts`, shared by picker and engine); stage is a per-line `stageOverride` read through `deriveCostStage` (the id map outranks `stage`); order edits go through `insertCostLineNear` / `moveCostLine`; case overrides are id-keyed.
  - **A cost line is a catalog entry plus a label** (`CostLine.catalogId`, `lib/state/costCatalog.ts`, mig 214; **ACCOUNT-scoped since mig 241**, one vocabulary per client firm shared by every member, author kept with SET NULL): selecting an entry STAMPS behaviour onto the LINE; the catalog is never read by the engine, reports or exports; identity resolves from the base id via `resolveCatalogId` when `catalogId` is absent. Never resolve identity a second way (the minted `custom-<ts>__<phase>` shape defeats base-id-only rules).
  - **The TOP-DOWN area chain is ONE pure function that ONE engine file reads, for ONE figure** (`src/core/calculations/landChain.ts`, land planning step 2): land x utilisation x coverage / FAR gives footprint, landscape, retail and lobby GFA, total and main GFA, net saleable, units, slots and total BUA, pinned to the cent against two real reference-workbook rows. It imports nothing. **"Nothing reads it" was true until 2026-09-09 and consolidation step 5 RETIRED it**: the retail land carve-out's share is retail GFA over total GFA, which are the chain's own figures, and restating that formula in the engine would have bought a green check with TWO definitions of retail GFA. So the invariant NARROWED rather than going: `src/core/calculations/index.ts` alone may read it, only inside `retailCarveContext`, and only `retailGfaSqm` + `totalGfaSqm`. `verify-land-chain` C1/C1b/C1c fail on a second reader, on any other chain figure reaching the engine, and on the share being restated; `verify-asset-type-standards` A2 carries the same allowance BY NAME and A2c fails if it goes stale. Every other chain figure (units, parking, landscape, NSA, total BUA) is still read by nothing that computes money. **THE CARVE HONOURS MASSING INHERITED FROM THE TYPE** (2026-09-11): coverage, FAR and the service share default from the asset type, and the carve read the PLOT alone, so an inherited FAR gave an undefined total GFA and the host kept 4,094,123.49 of land its companion should have taken WHILE THE PROJECT TOTAL STILL FOOTED. `withInheritedMassingAll` merges the type in over COPIES at FIVE NAMED DOORS (the composer, which covers everything it computes, plus the shared capex report and the three screens that enter the land engine themselves); `verify-retail-companion` J goes through the composer because every other section calls the engine directly and could not see it, and J5 fails on a sixth door. C1 and A1 widened BY ONE VERB for this and no more: C1d proves neither new file runs a chain, A1b proves neither names a value except through `chainMassingFor`. **Total GFA is UTILISED LAND x FAR** (not footprint, not gross), landscape is a share of the UTILISED area, **retail parking divides by the GROUND-FLOOR RETAIL TYPE's own sqm-per-slot ratio** (`resolveRetailSlotArea`, the ONE reader: a plot column until 2026-09-09, a project field until 2026-09-10, and both were a second home for a number the type table already had a cell for; the lifted 40 was NOT carried across, it is the area a slot occupies where the reference divides by 25), **a ratio stated in sqm per slot DIVIDES the main asset GFA and a TYPED ZERO derives zero slots** (the reference multiplies units x ratio on EVERY row, so its own m2/slot types derive no parking at all, all 14 of them: that is its shortcut, not a rule, and the divergence is stated AT the branch and pinned by A13/A14), **the parking basis is STORED, never displayed-but-unset** (hydrate stamps what the chain already assumed, so it moves no number), units ARE rounded (2026-09-08; the workbook carries fractional keys and everything downstream here follows the whole count), and with zero retail the main asset keeps the whole GFA. **Vocabulary: reference NetSaleable = platform NSA, reference TotalGFA = platform BUA, reference BUA = platform GFA** (the outermost tiers swap names).
  - **MASSING IS DERIVED ON THE ASSETS TAB, AND A SCENARIO DOES NOT RE-DERIVE IT** (2026-09-11, survey item 7, decided against wiring the chain into the engine): the chain runs on the tab and reaches the engine only through what it WRITES (sub-unit areas via shares, the derived support row, and since 2026-09-12 the full `derivedAreas` bag, filled on load and save by the one shared call site rather than by a tab), so a case override is value-only and re-derives none of it. Every `assets[].landChain.*` lever is therefore INACTIVE, and the reason states what is TRUE rather than a blanket "moves nothing": measured one input at a time, coverage, FAR and the retail share each move the retail land CARVE and nothing else, while utilisation and the service share move nothing at all, because the engine reads the chain for one figure, the retail share. `landChain.retailAreaPerSlotSqm` is RETIRED and `landChain.maxFloors` is read by nothing, and both say so in their own words. **Wiring the chain into the engine was rejected**: the chain produces ROWS THE USER THEN PRICES, not numbers, so a synthesised sub-unit has nowhere for a rate to live, every saved snapshot already contains the derived rows, and the chain would become a second opinion on area beside `resolveAssetAreaMetrics`. **If scenario massing later matters, the honest version is a case carrying its OWN derived rows by re-running the tab's planner**, which reuses the planner unchanged, keeps one definition of every area and keeps prices where a user can type them. `verify-module6-pipeline` asserts the reason text differentiates; `verify-retail-companion` J6 asserts the claim against the engine, so the sentence cannot go stale.
  - **A LINE ALWAYS HAS SOMETHING TO PRICE, AND WHICHEVER OF SHARE OR AREA THE USER TYPED IS THE STATEMENT** (2026-09-10, `planLineSubUnits` in `_shared/assetTableModel.ts`, applied by `syncLineSubUnits`): table 5 lists SUB-UNITS, so a line with none had no row, could not be priced and earned nothing (five live assets, four types, including the two Standalone Commercial lines). A line with none is SEEDED with one, named for its first plot, priced at zero, stating a 100% share; NOT seeded with no NSA, and on a count-metric asset ONLY as a keys row when its type states a unit size (2026-09-12: hospitality counts keys by default, count = NSA over the type's unit size, rounded; no size, no seed). `SubUnit.nsaSharePct` stores a typed share and the store re-derives its area whenever the line's NSA moves. **EVERY PRE-EXISTING ROW IS CONVERTED to the share it already showed** (2026-09-10, founder's call): the conversion writes the share and never an area, so it is byte-identical on the engine (Marina Gate: 55.00% / 45.00%), and it is scoped to lines with a RESOLVABLE NSA, AREA rows only, and NOT Support (which is not part of NSA). The metric is resolved through `effectiveMetric` (the ASSET's wins, TRAPS 7.32) or the one live row carrying metric 'units' under an area asset is silently skipped. The plan SETTLES (re-planning asks for nothing) or it would mark a project dirty for ever. **UNLIKE THE RETAIL COMPANION THIS MOVES MONEY**: a seeded row sits on an ordinary asset, so an asset that had no area now has one (Marina Gate: four lines, debt +8.02bn). **The TYPE is offered when the asset is ADDED**, through the same `assetTypePatch` the row dropdown uses, and stays on the asset, never the plot.
  - **Ground-floor retail is an ASSET, and it carves its land out of its hosts** (consolidation steps 4/5, 2026-09-09; `src/core/calculations/retailCompanion.ts`): ONE pooled Lease companion per LINE (not per plot), `companionType: 'retail'`, reusing the existing `isCompanion` mechanism and carrying NO `parentAssetId` (a line has many hosts), so both Operate sync passes skip it. It holds the pooled retail GFA, a derived sub-unit so the rent can be set (refreshed only while it is the companion's ONLY row, after which the split is the user's), and **takes a share of each host's land equal to retail GFA over that host's total GFA, per plot at that plot's own rate**. Both halves come from ONE function returning both, measured separately, because a project land total that still foots while individual assets are wrong is the failure mode: the reconciliation is PER ASSET in `verify-retail-companion` I2, and I4 pins that `computeAssetLandSqm` and `computeAssetLandBreakdown` never disagree. **CARVED LAND MUST BE ON SCREEN WHERE IT LANDED** (2026-09-10): the companion's share shows PER PLOT in table 3 (from `splits`, since it pools hosts on different plots) and per companion in table 4, land ONLY (its floor area is already inside its hosts' rows), and both tables carry a TOTAL row from ONE rule (`totalsFromRows` in `_shared/assetTableModel.ts`, so a verifier RUNS it) that foots to the plots. The plot check reads the GROSS draw and kept saying 'ok' while the visible column was short: I5 pins the visible sum per plot, U12i pins that the tables render it. **"Companion" no longer means "Operate"**: seven classifiers read the STRATEGY now, since a Lease retail companion would otherwise file into hospitality revenue and opex (an EIGHTH, the Revenue tab's own `isHospitality`, was found in step 6). **STEP 6 (2026-09-10) GIVES IT COST AND RENT**: the cost short-circuit is the OPERATE companion's alone, so the strip charges the phase's ordinary lines (its carved land, its retail GFA, its parking area, the same soft percentages) at its own rate through the ordinary per-asset override, capitalises and depreciates because `isDepreciableAsset` already reads the strategy, and earns per sqm per year from the sub-unit step 4b gave it once the factory seeds the `revenue.lease` block every Lease asset needs. Marina Gate: **+45,731,506.02, all of it on the two strips, every other asset 0.00** (`verify-retail-companion` F3c pins that per asset).
  - **EVERY REFERENCE POINTS AT SOMETHING THAT EXISTS, AND THE CHECK IS IN THE PERSISTENCE LAYER, NOT ON A TAB** (2026-09-12, `src/core/calculations/projectIntegrity.ts`, [docs/TRAPS.md](docs/TRAPS.md) 7.37 and 7.38): `removeParcel` was the ONLY delete in the store that cascaded to nothing, so an asset created on a plot kept pointing at it after the plot was deleted, the label resolver fell back to the phase, and the row reappeared downstream under a new name (FMP - MARINA GATE, `asset_1789041611899`, one `add` and no `remove` in 446 change-log entries). **DELETING A PLOT CASCADES, AFTER A CONFIRM THAT NAMES WHAT GOES** (2026-09-12 evening, founder's direction, replacing the same morning's refusal): `cascadeAssetRemoval` (core) is the ONE rule for an asset and a plot alike (Operate companion, sub-units, per-asset lines and overrides, the retail strip's host list or the strip itself when it was the last host, tranche and equity scopes cleared, scenario overrides dropped), and `repairProjectIntegrity` now removes every orphan dependent on load, so nothing is hunted for afterwards. `repairProjectIntegrity` runs in `hydrate` and `extractPersistSnapshot`, **repairs REFERENCES and never values** (the pointer goes, the sqm stays; a `__` sentinel is an answer, not a pointer), and **SETTLES**: it returns the INPUT OBJECT when nothing is wrong, so opening a clean project can never mark it dirty (measured by opening and saving with no edit: RE HUB 0 changes, Marina Gate 2 before and 3 after, the addition being the repair). **THE FOUR RECONCILIATIONS STILL LIVE IN `Module1Assets` useEffects and moving them is HELD**: `syncLineSubUnits` moves money and deserves its own before and after. `verify-project-integrity` 13.
  - **A CAPEX ROW IS A LINE, ON EVERY SURFACE, AND A COMPANION IS A ROW** (2026-09-11/12): `planCapexSummaryLines` is the ONE row planner, shared by table 1's schedule, the three summary tables, the consolidated view and both exports, so no two capex surfaces can disagree about what a row is. **THE LINE HEADS EVERY BLOCK AND NOTHING NESTS UNDER IT** (2026-09-12, superseding the plot nesting of the day before): each cost line is ONE row with the line's plots' schedules added, the plots named once in the heading (`assetPlotLabel`), on screen and in the report alike (P4q); **Table 6 files every line under Residential, Hospitality, Retail or Other** (`assetCapexCategory`, by type LABEL, a strip always Retail) and foots to Tables 2 and 4. **THE CAPEX INPUT IS THE LINE TOO** (2026-09-12): one section per merged line, breakdown summed and areas pooled across its plots, an override typed on the line fanned out to every plot (storage stays per asset), one navigation row per phase; and **an asset with nothing to price (`assetHasSubstance`: no plot, sub-unit, chain input, typed area, and not a companion) is a row on table 2 only**, read by table 4 and the capex tab through the one rule. **A COMPANION SITS BESIDE THE LINE IT CARVES FROM, retail and Operate both, and carries its OWN strategy.** Excluding companions from the consolidated view was harmless until consolidation step 6 gave the retail strip real cost, after which that view was short by **50,142,866.82** on Marina Gate, the two strips to the cent, on every commit from `5ad4518e` until it was fixed. `verify-capex-report` 28, `verify-consolidated-view` 27.
  - **AN ASSET IS CALLED BY WHERE IT IS AND WHAT IT IS** (2026-09-10, `src/core/calculations/assetName.ts`, [docs/TRAPS.md](docs/TRAPS.md) 7.36): `assetLabel(asset, { parcels, phases })` is the ONE label, plot where there is one (else phase), then type, then phase again on a multi-phase project, then a companion marker; the context is REQUIRED so the compiler enumerates every reader. **`Asset.name` is RETIRED, not deleted**: declared and stored, read by nothing, no migration touches it, and the assets table offers no name to type. A LABEL IS NOT AN IDENTITY, so every lookup keys on `assetId` (the Excel model matched `a.name === m.name` until this change), and the Capex SUMMARY consolidates by phase and type through `groupAssetsForConsolidation` itself, so it and the assets tab's table 4 cannot disagree; an asset in no group (the retail companion) keeps its own row. `verify-asset-label` 41 renames every asset in the fixture and fails if any label in the snapshot, the capex report or the 16-tab workbook moves.
  - **AN ASSET TYPE BELONGS TO THE PROJECT, NAME AND VALUES BOTH** (migs 242-244 + the move of 2026-09-10, Module 1 tab 4; `lib/state/assetTypeStandards.ts`): `project.assetTypes` holds the names (label, category, order) and unit size, parking ratio + basis, build cost per sqm, revenue rate + its UNIT and the parking area per slot live in the SNAPSHOT (`project.assetTypeValues` keyed by entry id, `project.parkingAreaPerSlotSqm`) and so version, diff and change-log like any other input. **THAT IS WHY NOTHING IS STAMPED**: `Asset.assetTypeStandards` is deleted (and stripped on hydrate), `Asset.assetTypeId` is the lookup key, and a firm editing its list can never leave a model stale. A BLANK value is an ABSENT key, never null and never 0. **Unit size resolves sub-units FIRST (the type average is the fallback); parking inherits from the type and a sub-unit may OVERRIDE it, a typed 0 being a real override.** **COVERAGE, FAR AND SERVICE DEFAULT FROM THE TYPE TOO** (2026-09-10, `resolveChainDefaults`): the plot wins, absent inherits, a typed 0 is an override; FAR shows on the plot row as an inherited placeholder because it is a constraint of GROUND and the reference's own runs 1.2 to 5 within one type; the RETAIL SHARE never defaults. **An edit on that tab now MOVES THE MODEL** and the tab says so. Nothing is engine-wired yet, so Module 6 gates them as INACTIVE (economic but unread), not non-economic. **THE LIST MOVED ONTO THE PROJECT 2026-09-10 and that CLOSED [docs/TRAPS.md](docs/TRAPS.md) 7.35**: a firm's projects come from different land owners, each names its types as its scheme requires, and an edit inside one project reached every other (adding rows to their tables, orphaning their values under a banner blaming a deletion nobody made). The list is keyed in the SAME object as the values it keys, so no cross-scope lookup is left to get wrong; the whole tab now locks with the project and has NO Save button on either half; the change log names WHICH type moved (`PER_ELEMENT_ARRAYS.assetTypes`); and a NAME is gated out of the Module 6 picker as non-economic, unlike the values, which stay INACTIVE (economic but unread). **`refm_asset_types` SURVIVES AS A TEMPLATE**: seeded from ON DEMAND (never at creation, when nobody yet knows what the project is) and pushed back explicitly one way, both ends scoped to the account that OWNS the project and REFUSING rather than falling back to the caller's. A project that predates the move is backfilled at hydrate from what it already REFERENCES (its assets' types plus its own value keys), never from an account, so the repair cannot reintroduce the bug; measured on both live projects, five types each, every value key attached, and the engine fingerprint byte-identical. `verify-asset-type-standards` 121.
  - **Cost of sales is computed ONCE, by Module 2** (`lib/costOfSales.ts`): the capex base, the capitalised IDC inside it and the spread on the recognition share are assembled there and nowhere else, and the P&L, the balance-sheet inventory, the Module 2 screen, both PDFs and the workbook all READ `snap.byAssetCostOfSales`. A second engine (`costOfSalesV2`) drove the screen and the exports until 2026-08-30 and was 407,131,731 from the P&L in one year while agreeing to 0.24% over the lifetime; it is deleted. `computeAssetCost.perPeriod` now sums to `.total` (it truncated any line distributed past its window), and a Sell asset inventory roll-forward closes at ZERO, which is the cheap reconciliation that exposed all of it. The tab's FIRST table is the year-by-year BUILD of the base (Module 1 capex + capitalised IDC = total base, then the recognition share and the charge, footed by a check row); both halves and the share are CARRIED on the result, never recomputed by a surface. See [docs/TRAPS.md](docs/TRAPS.md) 7.31, 3.8 and 3.9.
  - **Selling costs belong to revenue**: scope via `deriveAssetScope` (ONE implementation in `selectedBase.ts`), enforced in `assetVisibleLines`, which the Costs tab, the engine and the copy planner all call, so shown is charged and hidden is not. Bases come from the revenue module; **zero is not absent** (a held asset's sale base is 0, never a fallback). `computeAssetCost` takes the revenue snapshot and resolves bases internally (`core/calculations/revenue/sellingCosts.ts`, zero imports); every reachable call site values a line the same way.
  - **No country gate, no silent lock**: `assetVisibleLines` takes no country (the gate is RETIRED; RETT is a catalog entry the user adds); view mode is visibly read-only, `modelLocked` -> `viewLocked` is ONE derivation, inputs are locked by CSS + capture handlers, and a mutating button must declare `data-view-mutates="true"` to be locked (buttons are opt-IN, inputs opt-OUT via `data-view-editable`).
  - **A facility share is used exactly as typed** (`resolveFacilityShares`, verbatim, floored at zero; equal split only when NO share is typed anywhere); a wrong sum is STATED with a one-click repair, never rescaled. A facility's schedule is project-wide (share x requirement), so identical tables are shares, not double counts.
  - **Migrations spread the raw object and normalise on top**, never enumerate the fields they keep (three fields were lost to the whitelist shape); no legacy rename key may be a current catalog id. `Project.country` is an ISO code selected on Project & Phases; `countryMatches` resolves BOTH sides of any comparison.
  - **Depreciation starts when the asset is available for use** (`offset + cp - 1`, in PERIODS, `computePhaseTimeline` needs `modelType`), for BOTH streams that feed `AssetPL.daPerPeriod` (own capex AND capitalised IDC).
  - **Returns chain**: FCFF is UNLEVERED FULL COST (in-kind land charged, no interest / IDC row); FCFE builds VISIBLY from FCFF with the terminal swap as two rows; the finance-cost ledger foots `Opening + Charge - Paid = Closing` at 0.00; interest is one line per origin. The deficit sizing has NO finance cost in it, gates on `capex > 0`, and measures IDC headroom pre-interest (Schedules R112-R124). The management fee funding toggle (`fundTerms.managementFeeFunding`, deficit default | equity) lives on Financing AND Fund Terms, one field; the equity-funded fee is its own capped `dedicatedEquity` draw and the reconciliation compares against BOTH halves per period. In-kind equity reads `capex.perPeriod.landInKind` verbatim (no parcel-walk fallback); the in-kind pair sits INSIDE the totals. **THE BALANCE-SHEET CLAIM IS CORRECTED (2026-09-09): "max |Assets - L&E| is 0.00 on both live projects" is NO LONGER TRUE of the current snapshots.** Measured: `bsReconciliation.bsDifferencePerPeriod` peaks at about **1.35bn on FMP RE HUB** and **548m on FMP - MARINA GATE**, and it read the same BEFORE and AFTER every change on 2026-09-09, so nothing that day caused it. What still holds, and is the invariant worth quoting: **`unexplainedPerPeriod` is 0.0000** on all six projects (every movement is bridged), and the reference fixture balances to **0.0000**, so the ENGINE is sound and this is the live data, both projects being mid-edit. Do not restore the old sentence without re-measuring; diagnosis is open in [CLAUDE-TODO.md](CLAUDE-TODO.md) START HERE 2026-09-09 item 7.
- **A SUB-UNIT'S METRIC IS THE ASSET'S, IN THE ENGINE AS WELL AS THE SCREENS** (2026-09-10, `resolveSubUnitMetric` in `src/core/calculations`, the ONE home; [docs/TRAPS.md](docs/TRAPS.md) 7.32): `Asset.subUnitMetric` is an asset-level override and the row's own metric is never read alone. The asset is a REQUIRED parameter of `computeSubUnitArea`, which is how all 37 call sites were enumerated by the compiler. Worth **10.09bn on one live asset**, where an area was read as a count of units and multiplied by the unit size.
- **THE ASSETS TAB IS THE ONE SOURCE OF AREA, AND `Asset.derivedAreas` IS THE ONE BRIDGE TO CAPEX** (2026-09-12, superseding the 2026-09-10 typed-first rule at the founder's direction; [docs/TRAPS.md](docs/TRAPS.md) 7.38): the bag carries the FULL Table 4 set (net developable, footprint, landscape, total GFA, main asset GFA, retail GFA, lobby, service, net saleable, parking area and slots, retail parking area and slots), written by ONE chain call site, `computeAssetChain` in `_shared/assetTableModel.ts`, which the assets tab's row builder AND the store both call, so the bag is filled on `hydrate` and `extractPersistSnapshot` and never only while one tab is open. **DERIVED WINS, ABSENT FALLS BACK**: `resolveAssetAreaMetrics` prices the chain's figure where the chain produced one and the sub-units and typed fields where it did not, so a project with no chain inputs (every asset on FMP RE HUB) is byte-identical. **NSA STAYS THE SUB-UNITS'** (table 5): the store keeps a line's sub-unit areas equal to the line's NSA through their shares, so they ARE the statement. **EACH FIGURE IS CHARGED ONCE ACROSS A HOST AND ITS RETAIL STRIP**: a host's Total GFA excludes the retail GFA its companion carries, `rate_x_parking_area` is main parking and reads 0 on a strip, `rate_x_retail_parking_area` is the strip's and reads 0 on a host, exactly as the land carve splits the plot; **`rate_x_retail_gfa` is the strip's floor area and 0 on a host** (2026-09-12, later the same day), because the reference prices superstructure on MAIN ASSET GFA, parking and landscape on their own areas, and the retail strip on RETAIL GFA at its own rate, so those are the bases a line should be on and Rate x Total GFA (which still holds lobby and service) is not. **The picker offers the area methods in TABLE 4's column order** (`COST_METHODS`, P4j reads the tab's header row). **A NEW PHASE SEEDS THE REFERENCE BASES WITH BLANK RATES** (Main Asset GFA, Parking Area, Landscape Area, Plot Area; no retired method), **Main Asset GFA falls back to Total GFA where the chain derived nothing** (the reference's own zero-retail rule, so a typed-area plot is never priced on nothing), **a strip's row always writes its OWN override** (it never owns the phase line; P4k-m), **the standard construction lines are put on the reference bases on load and save** (`src/core/calculations/costBases.ts`, settles, P4n; a parking line on slots or NSA and every custom line are left alone), **the phasing curve is ONE PER PHASE** (P4o), and **land cash and in-kind are ONE per-phase engine series** (`CapexAggregate.landByPhase`, summing to `perPeriod.landCash` / `landInKind`) shown on Results Table 5 and read by Financing section 4, which no longer computes plot x rate for itself (P4p); **LAND VALUE IS THE TWO STANDARD LAND LINES BY IDENTITY** (`isLandValueLine`), never the land stage, so RETT stays a cost in the stage and out of the value (P4r); **a following cost window SETTLES on load and save** (`settleFollowingCostWindows`, P4s), since `updatePhase` alone left a three-period phase on 1 to 4 and **a strip the store adds is seeded on Rate x Retail GFA + Rate x Retail Parking Area** (`planRetailCompanionOverrides`, core). `rate_x_main_asset_gfa` is new; **`rate_per_nda` STAYS RETIRED**. `useDerivedAreas` now governs ONLY the derived support ROW in table 5. Measured: Marina Gate 753,585,717.75 to 903,521,562.37 (Land 2 alone was a 5,000 sqm plot the tab derives 11,900 sqm of GFA on that capex priced at zero), RE HUB unmoved on every figure. A method is registered in FIVE places or it is half added (`verify-capex-structure` P4); `verify-land-chain` U78 to U80 pin the set and the settle.
- **Module 2 (Revenue + CoS + Schedules + Escrow)**: LOCKED at Pass 9N.
- **Module 3 (Operating Expenses)**: LOCKED at Pass 5d.
- **Module 4 (Financial Statements)**: DONE. Schedules / P&L / CF / BS. **Fund-fee statement position (2026-08-05):** the five fund fees total into a **Total Fund Management Fee** line and **EBITDA is struck AFTER it** (one EBITDA row, already net of fees; `ebitdaBeforeFundFeesPerPeriod` holds the pre-fee measure and is what the PHASE-level P&L uses, since fees are project-level with no phase allocation; `ebitdaAfterFundFeesPerPeriod` is a retained alias). Cash flow carries a visible **Fund Management and Other Expenses** row inside operating activities above Cash Flow from Operations. Both come from the shared `m4Reports.ts` builders, so screen + PDF + Excel move together. **Fund Fee Basis (2026-08-05, pushed 2026-08-09):** a shared `buildFundFeeBasisRows` builder states what each fee is charged ON (base, rate, basis amount, fee charged) beside the statement, rendered by `FundFeeBasisTable` on the consolidated P&L and the M5 fee income section and emitted as a Fund Fee Basis block on the Excel P&L tab (meta columns B/C, so the period axis at column F does not shift), because a fee reading zero previously gave no way to tell whether the RATE was zero or the BASE was empty. Consequence: ICR/DSCR read `ebitdaPerPeriod` so they are now after-fee on a fund project. BS balances by construction; Direct CF == Indirect CF every period. Phase-filtered P&L stops at EBITDA; phase CF shows Operations+Investing; BS is consolidated-only. P&L/CF/BS render from shared pure builders (`lib/reports/m4Reports.ts`) that the on-screen tabs AND the PDF both consume.
- **Module 5 (Returns & RE Metrics)**: DONE. Sponsor-IRR view (FCFF / FCFE / Distributed Equity + terminal value + RE metrics + per-asset + Case Comparison). RE Metrics tab carries editable Lender Covenants (DSCR / ICR / Debt Yield + LTV at peak debt = debt / GDV, since LTV at exit is a trivial 0%); pure display layer over the snapshot, no engine change. **Per-partner returns (2026-07-09):** each equity holder shows FCFE-based + DDM-based IRR/MOIC/equity-multiple driven by ONE time-weighted (dollar-years) shareholding (manual override = agreed share), each stream = agreedShare x the exact consolidated stream so Sigma partners == consolidated per period; total returns byte-identical (horizontal side-by-side tables + green/amber reconciliation chip). Equity partners can be **linked to M1 Parties** (equity-role party -> stores `partyId` + snapshots the name; engine never reads partyId).
- **Module 6 (Scenario Analysis)**: LIVE. A surface over the case engine (no engine rebuild): case list, multi-case assumptions grid (model-aware curated levers + config-inert gating, exhaustively field-audited on the live project), comparison matrix, and a Year-on-Year Impact tab (per-period divergence per case; debt/equity split deduped to one block; debt drawdown is principal, excludes IDC). Presentation in `lib/cases/assumptionGrid.ts` + `lib/reports/caseYoYReport.ts`; engine `applyOverrides`/`snapshot-diff` untouched. Was "Reports"; swapped with Module 7. **TWO GATE FUNCTIONS, TWO MEANINGS**: `nonEconomicLeverReason` REMOVES a field from the picker (hidden, unreachable by search or Show all); `inactiveLeverReason` leaves it SHOWN with the amber "not used under current settings" badge and drops it only from the curated defaults, so an annotated field is offered, not concealed. `verify-module6-field-census` measures the REFERENCE project (`scripts/fetch-census-fixture.ts`, gitignored fixture), only the first counts as hiding, annotated movers are pinned by pattern, and a picker string lever with NO declared override domain FAILS rather than reading as dead (a field probed with zero candidates is not evidence of inertness). Detail in [CHANGELOG.md](CHANGELOG.md) 2026-09-01e.
- **Module 7, ADDING A SLIDE TEMPLATE IS A TWO-STEP CONTRACT (2026-08-15)**: a deck is saved per project, so an existing deck only gains new slides through `upgradeDeckLayout`, which offers exactly the ids registered in `TEMPLATES_BY_VERSION` ABOVE the deck's own `schemaVersion`. You must **bump `DECK_SCHEMA_VERSION` AND register the new id under that number**. Doing only the first silently lifts every deck's version and adds nothing; doing only the second never fires. Nothing enforced this, so a template added to the library and left unregistered reached freshly seeded decks and was **invisible to every existing one, permanently and silently**. That is what happened to the three fund slides (shipped 2026-08-13, complete and correctly gated, on a fully populated model): a real 36-slide deck exported with zero fund content, and the only route to them was a rebuild that discards arrangement, edits, text boxes and AI narrative. Now `TEMPLATES_BY_VERSION` is COMPLETE (every `SLIDE_TEMPLATES` id under exactly one key, v1 originals included and inert) and **`verify-deck-template-registry` fails on any unregistered template and on a newest-key that does not equal `DECK_SCHEMA_VERSION`**. Detail in [CLAUDE-REFM.md](CLAUDE-REFM.md) 2026-08-15c.
- **Module 7 (IC Presentation Builder)**: LIVE. PowerPoint-style slide editor (navigator / 16:9 canvas / properties); **slides hold BINDING KEYS, never copied numbers**, so the deck follows Modules 1-6 with no sync step and an unresolved binding renders a visible amber UNLINKED state. Direct manipulation, 18+ institutional templates that auto-omit on `available(model)`, paginated full schedules, live ToC, native PPTX + PDF from ONE shared resolved-export contract. Free-form AI drafting on ANY selected block, grounded via the same `collectGrounding` whitelist, all-or-nothing refusal sentinel (`CANNOT_ANSWER_FROM_MODEL:`), metered through `ic_narrative`. Three toggle-gated fund slides fed by `fundReports.ts` / `m4Reports.ts`; toggle off is byte-identical. Report versions are auto-named server-side and the last one opens with the project; report versioning is fully separate from model versioning (migs 199, 207). Detail in [CLAUDE-REFM.md](CLAUDE-REFM.md) + [CLAUDE-AI.md](CLAUDE-AI.md); dated entries in [CHANGELOG.md](CHANGELOG.md).
- **Cases (scenarios)**: Management base + override cases; the active case drives the WHOLE model + all exports (not just the comparison); topbar switcher + Returns Case Comparison + the Module 6 page; per-input "≠ Management" override badge; scenario edits in the change_log; viewing a case never starts an edit session. Engine `lib/cases/applyOverrides.ts` (value-only overrides, base never mutated; per-element grammar via `snapshot-diff.PER_ELEMENT_ARRAYS`).
- **Platform infra**: project-switch state-leak fixed; session-based versioning + per-version change_log. **View/edit lock**: a project opens read-only until Edit (edit in place / different version / new version; `startEditInPlace` / `saveAsNewVersion` in `lib/persistence/module1-sync.ts`); schema-tolerant reads; paginated version history. **PDF full-project report** (`lib/pdf/generateProjectPdf.ts`, pdf-lib) mirrors every module tab via shared `lib/reports/` builders, with part picker + number-scale + version picker in `ExportModal`. **Excel MODEL export** (`lib/excel/buildModelWorkbook.ts`): a HARDCODED platform snapshot (no live formulas), 16 tabs in module order built from the SAME shared builders and snapshots the screens use; locked navy palette (`ALLOWED_FILLS` in `excel/styles.ts`); A4 print setup; per-tab sub-TOC + basis-of-calc blocks; Scenarios tab mirrors Module 6. **Four export review passes** (2026-08-12/13: arithmetic, readability, consistency, presentation) produced the shared builders `streamReports.ts` / `checksReport.ts` / `assetNotes.ts` and verifiers `verify-report-{arithmetic,readability,consistency,presentation}` (78/55/59/92). **Platform guide + guided tour** (2026-08-20; 34 steps at build, 35 since the Module 1 standards tab landed 2026-09-07, and the count is DERIVED from the registries, never hardcoded, so do not restate it as a fixed number): ONE content source `lib/guide/guideContent.ts` rendered VERBATIM by the tour, structure from the live registries via `lib/moduleTabs.ts`. **Trial exports** carry a server-resolved watermark (admin control on /admin/plans); Excel gates on `excel_snapshot`; OpEx seeds ZERO. Full dated narrative in [CHANGELOG.md](CHANGELOG.md) and [CLAUDE-REFM.md](CLAUDE-REFM.md).

- **Public read-only page API**: `GET /api/public/pages/[slug]` serves whitelisted CMS pages to the partner site (PaceMakers). `SLUG_WHITELIST` lives in `src/shared/api/publicPagesConfig.ts`, shared with the `/admin/api-keys` screen; anything not whitelisted or not published 404s. Auth is `x-api-key`, constant-time, **fails CLOSED**. **Key rotation rule (mig 213, read before touching either route)**: the env var `FMP_PUBLIC_API_KEY` is consulted only when `public_api_keys` holds NO ROW AT ALL for the key id; an active row supersedes it permanently; a retired-only history CLOSES the endpoint; an unreadable key store REFUSES; resolution is never cached; only a SHA-256 + 14-char prefix are stored, so reveal is refused for a rotated key. Rotation is one transaction with a partial unique index enforcing at most one active key. Verifiers: `verify-public-pages-api` 43, `verify-admin-api-keys` 80, `verify-api-key-rotation` 85 (probe key id only, never the real one). Full dated narrative in [CHANGELOG.md](CHANGELOG.md) (2026-08-10/11/16).

- **Entitlements & Pricing (LIVE)**: admin-controlled plan/feature system on `features_registry` + `plan_permissions` + `entitlement_plans` + `user_permissions` (migs 158-165). Resolver `resolveEffectiveFeatures` (override-wins) is REUSED everywhere; the live REFM gate (`resolveUserGate` + `gate.ts`, reads the live `users` row, not the JWT) enforces modules/exports/scenarios/save/branding/sensitivity, the project cap and trial expiry. Admin never blocked; unknown plan = access-preserving safety net. Plans: trial/solo/pro/firm; **pricing single source of truth = `entitlement_plans`**; ONE shared `PricingPageBody` + session-aware `PricingExplorer` at path-based `/pricing/<segment>` (segment source-derived in `platforms.ts`); ONE plan-setting path `setUserPlan`. **Payments**: provider-agnostic `src/shared/payments/` (Paddle implemented, sandbox; PayPro stub); checkout -> overlay via client token; webhook verifies HMAC + reuses `setUserPlan`; `paddleEnvMismatch` guards token/env mismatch. Verifier counts in the CHANGELOG entry; per-route/migration detail in [CLAUDE-ROUTES.md](CLAUDE-ROUTES.md) + [CLAUDE-DB.md](CLAUDE-DB.md).

- **Subscription lifecycle (billing, grace, emails, discounts, launch banner)**: self-service billing tab + admin billing on top of the single plan path; `setUserPlan` converges the per-platform `user_platform_subscriptions` row on EVERY plan write, so the gate (store A) and billing (store B) never diverge; live-Paddle users are blocked from silent manual changes. **Post-expiry is a three-state lapse model** (active / grace read-only for one calendar month / lapsed), computed from the plan's expiry anchor by shared `gate.ts` helpers and enforced server-side at every write choke point (create/duplicate/versions/save/export); data is never deleted. **Emails**: nine-plus branded Brevo templates (welcome, trial, renewal notice, dunning, canceled, plan-changed, grace) dispatched at triggers + a daily reminder cron, idempotent via `subscription_email_log` with PER-EVENT dedupe keys; manual plans get a branded receipt PDF (`manual_invoices`, private bucket); the Paddle ledger (`payment_transactions`) self-heals on read so invoice history survives source flips. **Discounts**: Paddle is the single source of truth (`resolveCouponForCheckout` / `loadActivePublicPromo`, live list, 60s cache); only the featured-promo CHOICE is stored locally; `nullableNumber` preserves absence so an unlimited discount never reads as used up. **Launch countdown banner**: three-state (countdown/launched/hidden) resolver over `training_settings.modeling_hub_launch_date` (the SAME key the auto-launch cron reads), admin-editable copy, allowlist of five exact paths. Full dated narrative for every one of these in [CHANGELOG.md](CHANGELOG.md) (2026-06-29 through 2026-08-09); per-route/migration detail in [CLAUDE-ROUTES.md](CLAUDE-ROUTES.md) + [CLAUDE-DB.md](CLAUDE-DB.md).

- **Admin email campaigns (2026-08-30, mig 225 APPLIED)**: `/admin/campaigns` emails Modeling Hub users (audience -> template -> preview -> confirm). `src/shared/email/campaigns.ts` owns each rule ONCE: admins are excluded unless explicitly picked, the unsubscribed are always excluded and reported, every merge value is escaped once, and unsubscribe is an HMAC of the user id (nothing stored). Sending reuses the shared Brevo path via new `sendEmailPerRecipient` (same wave pacing as `sendEmailBatch`, but a per-recipient outcome so one failure cannot stop the rest and every recipient is logged). The send RE-RESOLVES the audience and refuses if the count moved since the preview. `verify-admin-campaigns` 44; detail in [CHANGELOG.md](CHANGELOG.md) 2026-08-30k. The signup industry question now covers real estate OR hospitality (stored field `works_in_real_estate` unchanged).

- **Module 10 COLLABORATION, steps 0 to 2 of 9 (2026-09-02). MEMBERSHIP IS THE ACCESS CHECK (mig 231 APPLIED)**: `refm_project_members` (PK `project_id, user_id`) decides who can reach a project; every existing owner was seeded as an Owner member, and a live comparison of the old owner rule against the new membership rule returns IDENTICAL sets, so nothing changed for a single-user account. `refm_projects.user_id` REMAINS authoritative for ownership, the project cap, the purge and the cascade. **NON-OWNERS ARE READ-ONLY until the edit lock ships (step 5)**, because there is no server-side lock and two autosaves would overwrite each other silently; `getProjectForWrite` is a SEPARATE function from `getProject` so the verifier can ENUMERATE every write handler and fail if one gates on the read resolver. Denial is the default everywhere, and a pre-231 database falls back to the OWNER check, never an open one. Registry driven (four `members*` columns, all-or-nothing). Admin assigns from the HUB dashboard, not inside REFM. `verify-project-membership` 29. **Step 3 (mig 232 APPLIED): `priority` and `sort_order` moved to the MEMBERSHIP row**, so ordering and urgency are per user; the project-level columns are DEPRECATED, not dropped; **`status` did NOT move** (it is a property of the project, not of a person); the ROW SHAPE is unchanged, so no client learned a new field; zero rows to migrate, measured first and re-checked by a guard at apply time. `verify-member-ordering` 35. **Step 4: THE ROLE IS ENFORCED.** Every gated call site NAMES the permission it needs (the compiler enumerated all 15 by refusing a two-argument call, and found DELETE gated as an edit). **TWO GATES, KEPT SEPARATE**: the permanent matrix (`roleCan`) and the TEMPORARY owner-only narrowing (`roleMayWrite`), which step 5 removes; merging them would hide which restriction is which. **EXPORT IS A READ** and gates on the matrix alone (`getProjectForAction`), so a Reviewer may export. `can()` reads the RESOLVED role through the SAME matrix the server uses, and the UI is a courtesy, not the boundary. The editor/Returns exclusion was an OMISSION from a renumbering (the original map's own legend granted the analyst "Module 4 (Returns)"; Returns later became module5 while the positional list kept excluding it) and is FIXED. `verify-role-enforcement` 37. **Step 5: THE EDIT LOCK (mig 233 APPLIED).** One row per project, `project_id` is the PRIMARY KEY so **two holders are impossible by construction**; taken ONLY through `refm_acquire_project_lock`, ONE atomic INSERT ... ON CONFLICT DO UPDATE (**proved: 25 simultaneous races, exactly one winner every time**). **HEARTBEAT, never an unload handler** (which does not fire on a crash, a killed tab or a dropped network); stale release by the NEXT ACQUIRER, not a cron. `RETURNS SETOF` is load-bearing: a scalar return gives a row of NULLs on refusal, which reads as a WIN. Which writes need the lock is DATA (`LOCK_REQUIRED`); project management is deliberately exempt so an owner can still archive a project someone has open. **`roleMayWrite` is RETIRED and an Editor can now write; the matrix is untouched.** A lock refusal is a 409 naming the holder, never a 404. Pre-233 degrades to no locking, never to no editing. `verify-edit-lock` 41. Step 1 gave versions a `created_by` (mig 230, never backfilled: NULL means unknown). Detail in [CHANGELOG.md](CHANGELOG.md) 2026-09-02b. **Step 6: THE APPEND-ONLY CHANGE LOG (mig 234 APPLIED)**, `refm_project_changes`: who changed what, when, appended by the save path and NEVER recomputed, so it stands beside `change_log` (a recomputed diff with no author and no timestamp) rather than replacing it, and `admin_audit_log` is not reused (its `admin_id` is NOT NULL). **The append-only trigger guards CONTENT, not the operation, and it HAD to: `ON DELETE SET NULL` IS an UPDATE, so a blanket ban made a version undeletable and an account uncloseable** (caught by the applier dry run); an FK may be released to NULL, never re-pointed. Each save logs ITS OWN delta (diffed against the STORED snapshot, not the base version, which would re-log the session every 1.5 seconds); past 40 paths it SUMMARISES with the true count rather than truncating; a failed append never fails the save. **It surfaces as an Activity tab in the version manager, and EVERY MEMBER SEES THE SAME ROWS: no role narrowing on the read, and an admin sees no more than a member on any project they can open.** `verify-change-log` 57. **Step 7: COMMENTS (mig 236 APPLIED)**, `refm_project_comments`: a person's words, on the PROJECT, a VERSION or a FIELD, and everything is nullable except the project and the body because a project-wide comment is the common case. `path` REUSES the snapshot-diff grammar (never a second vocabulary); `version_id` is SET NULL and nothing filters by it, so a comment stays visible after a newer save and shows what it was written against. **REPLIES ARE ONE LEVEL, enforced by `trg_refm_comments_one_level`**, because a depth rule in application code is one the next writer breaks. DELETE IS SOFT and the server STRIPS the body on read; a deleted root with live replies leaves a tombstone. **Edit and delete match the author IN THE WHERE CLAUSE; RESOLVE DOES NOT** (a reviewer raises, an editor closes) and targets ROOTS only. `canAddComments` is finally enforced (Owner/Editor/Reviewer write, Viewer does not) while ALL FOUR ROLES READ THE SAME THREAD, and **the edit lock is deliberately not involved**: a comment is not a model edit. Surfaced as a Comments tab beside Activity. No notifications, no email, no unread counts, no jump-to-field. `verify-comments` 50. **Step 8: SEATS ARE ENFORCED (mig 237 APPLIED)**: a seat is a DISTINCT PERSON ON AN ACCOUNT across every project it owns, **the OWNER consumes one**, so pro 1 means a single-person account (trial/solo/pro 1, firm 10; 237 declares these, correcting 158's `pro = 3` seed WITHOUT editing an applied migration). **THE COUNT lived in the registry layer** (`src/shared/admin/seats.ts`, iterating `PROJECT_SOURCES`) until account-model step 3 (2026-09-04) recounted it DIRECTLY from the account's people (`users.account_id`), platform-agnostic by construction; it is COMPUTED live, never stored. A soft-deleted project's members do not count, archived ones do. **A ROLE CHANGE IS NOT A NEW SEAT**: the question is "already a member anywhere on the account", not "will a row be inserted", because the write is an UPSERT. **ONE enforcement point**, the single upsert in `POST /api/admin/project-members`, after the user lookup and before the write; a counting failure REFUSES. The limit belongs to the ACCOUNT (the project owner), not the acting admin, with a platform-admin holder exempt per the standing "admin is never blocked" rule. The per-client raise is the EXISTING `user_permissions` override and REUSES `resolveEffectiveFeatures`; no new table, no new admin UI. The block is OPERATOR-facing (names the account, its limit and `/admin/access`); no end-user copy, no Paddle purchase, no Owner-adding path. `verify-seats` 42. **Step 9: DELETE IS A REQUEST FOR AN EDITOR (mig 238 APPLIED)**, `project_delete_requests`. The OWNER keeps DIRECT delete (soft, 30-day window); an EDITOR, who could not delete at all, may now ASK; Reviewer and Viewer get neither. New matrix permission `canRequestDelete`, and **owner is FALSE on it deliberately**: they hold `canDeleteProject`, so a request would be a slower road to the same place. **PLATFORM AGNOSTIC COSTS THE FK**: platform + project_id is polymorphic, so the cascade is a TRIGGER, which is also the only thing that catches the `users` cascade deleting projects inside Postgres where no route can see it (a new platform adds one trigger; the verifier fails if it does not). `requested_by` is SET NULL so the request outlives the account, losing MEMBERSHIP does not remove it, and the queue SHOWS whether the requester is still a member. ONE open request per project (partial unique index); the decline fields survive a later approval. **THE DESIGNED-AGAINST BUG**: approving an already-deleted project would have updated zero rows and reported SUCCESS, because `softDeleteProject` filters `deleted_at IS NULL` and a service-role write reports no rows-affected, so approval READS the project first and refuses with `already_deleted`, leaving the request pending. A decline REQUIRES a reason, since with no notification system that sentence on the requester's own card is how they find out. Queue lives on the PROJECTS BROWSER beside the Deleted bin (which already existed; a step-9 diagnosis claim that it did not was WRONG and is corrected in CHANGELOG). The Dashboard Delete button is now gated PER CARD via `roleCan`, closing the same class of defect as the Edit and Team Access buttons. No email, no approval for archive, no Owner request path. `verify-delete-approval` 49.
- **THE ACCOUNT MODEL, steps 1-7 (2026-09-04, migs 239 + 240 APPLIED)**: every user has an `accounts` row (the account is ITS OWN ROW, never a pointer to a person's row; exactly one `internal` = FMP's own, the rest clients; held by an AFTER INSERT trigger for every creation path), every holder points back at their own, and a holder whose account still has members cannot be deleted (engine 409 + DB backstop). **THE BOUNDARY IS ENFORCED (step 2)**: a project member candidate must share the OWNER's account (`src/shared/admin/accountBoundary.ts`, the ONE statement of the rule; 403 in `POST /api/admin/project-members`, checked BEFORE the seat check so cross-account never reads as "no seats"); a platform admin is exempt as candidate AND as owner, but only account members are OFFERED (the list is narrower than the write, never the reverse); the Team access dropdown lists through the SAME rule via `?candidatesFor` and is a courtesy, not the scope. **SEATS COUNT THE ACCOUNT'S PEOPLE (step 3)**: `countAccountSeats` reads `accounts.owner_user_id` then `users.account_id`, never a project or membership table (`verify-seats` B3 fails if that returns); PROJECT membership never creates a seat; parity with the old walk measured live before commit, 8 of 8 accounts identical. **THE MEMBER GATE (step 4)**: a member inherits the HOLDER's plan, lapse and grace through `resolveAccountHolder`, called by `resolveUserGate` and NOTHING else (verify-accounts D1 fails on a second caller); members get `projectLimit 0`, keep their own role, and drop out of every no-plan chaser (the access-reminder scan, `'none'`-plan campaign audiences, reported as `membersExcluded`); proven live with a probe member on the real pro plan while all 8 real users resolve to themselves. **INVITES (step 5, mig 240)**: a client invites by email from the dashboard "Your team" card; the SEAT IS RESERVED AT CREATE (people + open unexpired invites + 1 vs the limit) and re-checked at accept; redemption is ONE transaction (`redeem_account_invite`: lock, email match, attach as MEMBER, consume; a failure leaves neither behind); the register fork sits after captcha and BEFORE the pre-launch gate (the client pays), the ordinary path byte-identical, and the redeemed email joins the signin whitelist. **HOLDER SELF-SERVICE (step 6)**: `/api/account/team` (engine `src/shared/account/team.ts`) gives the holder the admin screen scoped to THEIR projects, THEIR people (same `listAccountCandidates` rule) and the roles; only the holder manages the team, `candidate_admin` is refused there (FMP staff are attached by FMP), the owner is immutable, a foreign project gets one `no_project` answer, and assignment is seat-free by construction; the admin screen stays untouched as the operator fallback. **THE DELETE QUEUE REACHES THE HOLDER (step 7)**: `src/shared/account/deleteQueue.ts` filters the admin queue read to the holder's own projects and calls the SHARED approve/decline engine verbatim (already-deleted refusal, required decline reason, one `no_request` answer for anything not theirs); the admin queue still sees everything. **TEAM ACCESS NOTIFIES BY EMAIL** (2026-09-04): all four membership write sites (admin route + holder engine, grant + removal) call `src/shared/email/teamAccessEmails.ts` AFTER the successful write; a failed send never undoes access (the notifier never throws); dedupe = change detection on real prior state + the lifecycle `subscription_email_log` claim reused via the exported `dispatch`; `.invalid` targets are skipped before any claim, which keeps the suite mail-free; **every Modeling Hub email states its OWN footer reason** (`fmpLayout(content, reason)`; the bare `baseLayoutBranded` default is TRAINING copy and belongs only on genuine training emails) and the project-access emails name the PLATFORM from `PROJECT_SOURCES.label`, never the hub (the invite deliberately keeps hub naming: an invite is account-level). **THE TEAM SURFACE IS A SIDEBAR TAB**: Team access shows for every ACCOUNT HOLDER, never a member; the ADMIN, who is also a holder, gets BOTH surfaces there (their own account's team, then the operator panel below a labelled divider) (`ResolvedUserGate.accountMember`, exposed through the entitlements route/hook); a one-seat holder sees the tab with the standard Firm-upgrade path (`canInvite` computed server-side with the SAME `seatsAllow` as the create) instead of the invite box. `verify-accounts` 124 pins the reader allow-list (three files; the member route reads only through the helper and MUST fail S10 if it ever touches the column raw), the decision offline, and the refusal LIVE over real rows; `verify-seats` 43. Later steps (invites, holder self-service) ordered in [CLAUDE-TODO.md](CLAUDE-TODO.md); detail in [CHANGELOG.md](CHANGELOG.md) 2026-09-04.
- **MODULE 10 HAS A SCREEN (2026-09-04)**: `Module10Collaborate.tsx`, per project: who has access (the one new read, `GET /api/refm/projects/[id]/members`, served to ANY member via `getProject`), who is editing (the topbar's own lock state), comments and activity via the SAME panels the Version modal renders (`components/collab/CollabPanels.tsx` + `useCollabData.ts`, lifted verbatim; the modal tabs stay; `verify-comments`/`verify-change-log` re-aimed). The screen READS; membership writes stay on the admin route and the holder team engine. Detail in [CHANGELOG.md](CHANGELOG.md) 2026-09-04.
- **A MODULE'S NUMBER ON SCREEN IS A POSITION; ITS IDENTITY IS THE SLUG (2026-09-03, mig 235)**: `orderModulesForDisplay` drops `status: 'hidden'` rows and numbers the rest 1..N, so with Portfolio (8) and Market Data (9) hidden, `module_10` Collaborate RENDERS as "Module 8" everywhere (sidebar, Plan Builder, pricing) and that is correct, not a defect. The entitlement key is slug-derived and never moves, which is what keeps `plan_permissions` stable across a reorder. **A MODULE NAME IS DERIVED TOO, never a hand-written map**: both label maps (shared `UpgradePrompt.FEATURE_LABELS`, platform `FEATURE_DISPLAY_LABELS`) had their module keys REMOVED on 2026-09-03 after the shared one was found with module_4/module_5 and module_6/module_7 crossed since mig 157; the platform resolves name AND number from the live nav list via `lockedFeatureLabel` and passes it in, because `shared` cannot import the registry under the boundaries rule. **Never store the display number either**: `features_registry.label` holds the NAME only (mig 235 stripped eleven frozen "Module N:" prefixes that had agreed with nothing since seed). `module_8` is granted to solo/pro/firm and Portfolio is `hidden` + `needs_build`, so it is SOLD BUT NOT SHIPPED, and the free all-projects hub called "Portfolio Dashboard" is a DIFFERENT, ungated surface. See [docs/TRAPS.md](docs/TRAPS.md) 8.2; `verify-plan-builder-modules` 26.
- **Module 10 COLLABORATION, step 0 of 9 (2026-09-01). IT IS MODULE 10, NOT 8**: `platform_modules` and `features_registry` both say 8 = Portfolio (shipped and sold) and 10 = Collaborate, so "Module 8" would collide with a live feature. The four roles are **Owner / Editor / Reviewer / Viewer**, defined ONCE in `src/core/collab/projectRoles.ts` (core, not shared: the boundaries rule lets core import only core, and two core files re-export it). **OWNER IS NOT ADMIN**: `users.role === 'admin'` is the PLATFORM administrator, compared as a bare string across the auth layer, while owner is a role on ONE PROJECT. Module visibility is PER PLATFORM (`refm/lib/moduleVisibility.ts`), never in the shared role table. **NOTHING IS ENFORCED YET**: `currentUserRole` is client state pinned to owner, so `can()` still returns true for everything; a role has nowhere to be read from until membership (step 2), and enforcement lands in step 4. `verify-collab-roles` 24; plan + diagnosis in [CLAUDE-TODO.md](CLAUDE-TODO.md).
- **A project card carries a STATUS, an URGENT flag and a MANUAL order (2026-09-01, mig 229 APPLIED)**: cards group by status in ONE order (Construction, Operation, Funded, Draft, Completed, Closed, Dropped), then priority WITHIN the group, then manual drag order within the group, then recency LAST, all from one pure `compareProjectCards` in `src/shared/admin/projectStatus.ts` that the server, the grid and the verifier share. **STATUS IS A LABEL and must stay one**: it gates nothing, archiving is the `archived` boolean and deletion is `deleted_at`, and 'Archived' is now removed from the DATABASE enum too (the app dropped it 2026-08-30, the CHECK still carried it), so no status value may free a slot or make a project view-only. The seven REPLACED the five-value approval workflow, which overlapped on 'Draft' alone; every live row was 'Draft', so no row was rewritten. `sort_order` is NULLABLE on purpose: NULL is "never dragged" and falls back to recency, 0 is a real top-of-group position. Reordering is a WHOLE-GROUP dense write on its own route, within a group only, and never changes a status. Registry driven via `statusColumn` / `priorityColumn` / `sortOrderColumn`, so ERM/BVM inherit it. `verify-project-card-ordering` 54; detail in [CHANGELOG.md](CHANGELOG.md) 2026-09-01f.
- **The Portfolio Dashboard shows portfolio money (2026-08-30)**: `GET /api/refm/portfolio` runs the engine per project and `lib/portfolio/portfolioMetrics.ts` aggregates. THE RULES, all pinned: portfolio IRR is computed ONCE over the SUMMED signed streams (an average of IRRs is not an IRR), the equity multiple is a ratio of sums, peak debt is the max of the SUMMED debt series, the year axis is DENSE (a sparse one compresses time and overstates IRR), totals are PER CURRENCY, and un-modelled projects are counted out with "N of M modelled". Basis is the Management BASE case, archived excluded. The status tiles are gone (nothing ever set `status`) and the duplicate `Archived` status value with them; archiving is the `archived` boolean, always. `verify-portfolio-dashboard` 29; detail in [CHANGELOG.md](CHANGELOG.md) 2026-08-30j.

- **Project delete is SOFT, archive unchanged (2026-08-30, mig 224)**: a user's Delete stamps `deleted_at` (hidden, un-openable, out of the cap) and the daily `apply-scheduled-changes` cron hard deletes after `RETENTION_DAYS` (30, defined once in `src/shared/admin/projectSources.ts` with pure `daysRemaining` / `isPurgeDue`); `hardDeleteProject` survives only for the create/duplicate rollbacks and that purge, and a missing column REFUSES rather than deleting permanently. ARCHIVE is untouched: visible, user-reversible, never expires. Restore is ADMIN ONLY (the Projects Browser's Deleted bin), and read-only grace now BLOCKS deletion. Registry driven via `deletedColumn`, so ERM/BVM inherit it. `verify-project-soft-delete` 38; detail in [CHANGELOG.md](CHANGELOG.md) 2026-08-30h.

- **Access requests + account deletion (2026-08-30)**: the dashboard leads with an access card for no-plan users (request button inside, pending state persisted via `trialRequestPending` on the entitlements route) plus a once-only two-day reminder email in the daily cron; account deletion (admin + self) runs through ONE engine, `src/shared/account/deleteUserAccount.ts`, which retains the financial ledger, refuses to orphan a live Paddle subscription, and audits into `account_deletions` (mig 219, APPLIED 2026-08-30, probed live). The live `admin_audit_log` FKs are NO ACTION, not 007's SET NULL (the table predates the migration log, TRAPS 2.9), so the engine nulls `target_user_id` in code before the users delete, proven live; mig 221 (APPLIED 2026-08-30, probed live) aligns the constraints, the engine pre-null stays as defense in depth. Password reset is rebuilt end to end (mig 222 APPLIED 2026-08-30, full chain proven live on the deployed endpoint; `verify-password-reset` 20), and `scripts/audit-schema-drift.ts` now compares every migration against the live schema (findings: CHANGELOG 2026-08-30f, open items in CLAUDE-TODO). Detail in [CHANGELOG.md](CHANGELOG.md) 2026-08-30; verifiers `verify-access-request` 36, `verify-account-deletion` 33. Same day: the admin user list's project count now reads `refm_projects` (the legacy `projects` table is empty and dead), the Projects button opens a read-only per-user modal, duplicate plan/status badges are gone, ACCESS renders only when it diverges from STATUS, and the lockdown banner follows the live mig-136 flags (`verify-admin-users-cleanup` 22). The Projects Browser is rebuilt on the `PROJECT_SOURCES` registry (`src/shared/admin/projectSources.ts`, THE one place a project table is named; ERM/BVM join as one entry each); the legacy `projects` table is DEPRECATED, not dropped (mig 220 APPLIED 2026-08-30), with zero readers or writers left (`verify-admin-projects-browser` 22).

- **AI foundation (COMPLETE 2026-08-01, units 0 to 9)**: a platform-agnostic AI engine, not a one-off feature. One call path (`runAi`, the only reader of `ANTHROPIC_API_KEY`), a feature registry with admin toggles, server-side metering with DB-driven per-plan caps and refund-on-failure, a grounding abstraction that supplies labelled FACTS and audits every figure in the output, an admin control panel, and the first feature on it: **REFM Module 7 IC narrative generation** (six fields, grounded in the computed model, editable draft, never auto-saved). Migrations 203-206 all APPLIED. Features register DISABLED and are switched on in `/admin/ai-features`. **Full detail, invariants and verifier map in [CLAUDE-AI.md](CLAUDE-AI.md); plan and standing rules in [docs/AI_FOUNDATION_GUIDELINE.md](docs/AI_FOUNDATION_GUIDELINE.md).**

Verifier reality + outstanding ops (migrations pending on prod) live in [CLAUDE-REFM.md](CLAUDE-REFM.md). **THE SUITE IS NOT CLEAN AS OF 2026-09-12 session close: 163 pass / 3 FAIL of 166, 9877 individual checks, measured with credentials on a clean tree at b90470ca (the same three as at the 2026-09-12 open, so the day's capex work regressed nothing). The three are `verify-excel-export`, `verify-fund-e2e` and `verify-report-arithmetic` (per-asset cost exceeds total development cost on the FIXTURE, so code not data), all long-standing. **THE 2026-09-09 LINE HERE SAID "160 / 5 of 165, all five pre-date" AND WAS STALE IN BOTH DIRECTIONS.** Two of its five (`verify-accounts` S10, `verify-view-lock`) now pass and are not in the list. FIVE OTHERS had broken since, and were found only because the count moved: `verify-cost-of-sales`, `verify-ic-report`, `verify-report-models-phase2`, `verify-report-presentation` and `verify-tab3-critical`, all GREEN at `c230156d` and all failing by `40221720`, so the 2026-09-10/11 work broke them. ALL FIVE WERE THE SAME DEFECT, an assertion on `Asset.name`, which was retired on 2026-09-10, and **TWO WERE CRASHING RATHER THAN FAILING**, so 53 and 82 checks respectively ran nowhere for two days while the suite counted the file as one failure. They are restored and re-aimed at behaviour (CHANGELOG 2026-09-12). **THE LESSON: a suite count that moves is the only thing that finds a verifier that stopped running**, so state it every session and compare it with the last one. MEASURED WITH CREDENTIALS via the committed runner `npm run verify:suite` (`scripts/run-verifiers.ts`. The alias said a BARE `tsx` until 2026-09-09 and `tsx` is not a dependency of this repo, so on a clean machine the one command this rule points at could not run at all, and backgrounding it made the failure look like a long quiet success; it is `npx tsx` now, matching every other alias. TRAPS 10.21. Never a hand-rolled loop). State the count when you claim it, and say whether credentials were loaded.** **RUN THE COMMITTED RUNNER, NEVER A HAND-ROLLED LOOP.** Until 2026-09-03 there was no runner at all: every session wrote its own `npx tsx scripts/verify-*.ts` loop WITHOUT `--env-file=.env.local`, so the fourteen verifiers with a live half took their credential-gated `else` branch, printed a quiet SKIP and reported a pass. That is how `verify-admin-users-cleanup`, which runs the exact PostgREST embed `/admin/users` depends on, printed `SKIP A4/A5 (no DB creds)` and `20 passed, 0 failed` while the admin user list was blank in production for a full session (TRAPS 3.20, and the outage itself is 2.14). The runner loads `.env.local` by default, REFUSES to run without it unless `--allow-offline` is passed (labelling that result PARTIAL), and FAILS on any skip not named with a reason in `ACCEPTED_SKIPS` (currently two: `verify-psync` needs a dev server, `verify-module6-field-census` reports per-field paths absent from the catalog). The last flake is gone: `verify-public-pages-api` fired 61 live requests at a 60-per-60s limiter and raced the window it was testing; `rateLimit.ts` now exposes `setRateLimitClock` (a TEST SEAM, asserted unreachable from `app/`), the test freezes time, and it reads 47/0 on eight consecutive runs. State the count when you claim it; a blanket unmeasured "full suite green" stood here through 2026-08-31 while nineteen were failing.** The road there, kept because each step was a different kind of wrong: **123 pass / 19 fail at 94134290, then 134 / 8, then 142 / 1, now 143 / 0.** None of the 19 was a live wrong number; two were real defects (an unescaped `href` in `button()`, a grounding audit that could not catch an ungrounded market claim) and both are fixed. Ten verifiers were re-aimed at behaviour rather than at prose, a file location or an exact source string, each sabotage-tested. **The 9 that remain are diagnosed and deliberately open**: the sale-cohort supersession fixtures (`phase-date-preservation` 15, `funding-methods` 4, `returns-buildup` 2), two stale synthetic deck fixtures (3), the OpEx-seeds-ZERO Module 6 fixtures (6), (`tab3-default-seed` is CLOSED 2026-08-31d: it counted the IDENTITY REGISTRY where it meant the SEED SET, and no Module 1 change was needed or made; `rett` is registered-but-not-seeded BY DESIGN and removing it would have broken identity resolution, the row picker and the legacy id rescoping). Full tables in [CHANGELOG.md](CHANGELOG.md) 2026-08-31b/c; per-verifier status in [CLAUDE-TODO.md](CLAUDE-TODO.md). **Do not restore a blanket green claim here: state the count.**

**M2 lock conventions** (apply to M3+ unless overridden):
- The reference Excel at repo root is the verification benchmark, not a behavioural spec. Every reference-specific behaviour stays configurable; never hard-code currency, locale, escrow, or DSO defaults into engine paths.
- Engine storage is project-axis-indexed (`arr[0]` = first active project year). Only the UI window changes per asset / strategy.
- Vintage matrices (cohort year × cash year, cohort year × recognition year, capex year × CoS year) are the canonical mechanic for both recognition and cash distribution.
- PIT recognition handover = LAST construction year (`phaseStart + cp − 1 − projectStart`), NOT first operations year. Verifier A2-1..A2-5 pin this.
- See CLAUDE-REFM.md for the full Pass 7-9 narrative; older pass detail is archived to CLAUDE-FEATURES.md.

> **See also:**
> - **[docs/TRAPS.md](docs/TRAPS.md), the hard-won lessons. READ THIS FIRST; it is short.** Every entry is a failure that reported success: a grep that returned nothing, a constraint that never fired, a column width that silently did not apply, a deploy that silently did not happen. Symptom, mechanism, fix and the measurement that proved it. The individual copies stay in the files below, so nothing is lost either way
> - [CHANGELOG.md](CHANGELOG.md), the dated change record, newest first: every session-log bullet this file used to carry, verbatim. Append new dated entries THERE, never here
> - [CLAUDE-MODELING-HUB.md](CLAUDE-MODELING-HUB.md), Modeling Hub wrapper, platform catalog, P-Sync admin conventions
> - [CLAUDE-REFM.md](CLAUDE-REFM.md), Real Estate Financial Modeling (REFM), platform status (Modules 1-7) + conventions + Excel/PDF export
> - [CLAUDE-AI.md](CLAUDE-AI.md), AI foundation (client, registry, metering, grounding, admin panel, IC narrative). Load when touching anything AI
> - [docs/FUND_LAYER_GUIDELINE.md](docs/FUND_LAYER_GUIDELINE.md), Fund layer (fund fees, hurdle, carry, gross vs net returns). **Load it when touching anything fund; it is the source of truth and is kept current.** Steps 1 to 7 LIVE, FEATURE COMPLETE for v1, migs 208-211 applied; the fund toggle is ON for FMP RE HUB since 2026-08-11. Five invariants before you read anything else: (1) toggle OFF is byte-identical, pinned against a fully populated but disabled block; (2) every fee base is linear and the rule is a DATA STRUCTURE (`FUND_FEE_SPECS` + `LINEAR_FEE_BASES` / `CIRCULAR_FEE_BASES`); (3) the fee schedule is FROZEN before the iterative solver (fund size resolved on the fee-free pass; `fund_size_solved` stays forbidden); (4) the waterfall (`returns/waterfall.ts`, pure) has ONE hurdle balance, no catch-up, flat fee on the excess, and is NOT in the funding solve; (5) fund PRESENTATION has ONE definition, `lib/reports/fundReports.ts`, which the M5 screen, Excel and both PDFs all import (no surface re-declares a row label). The engine reads `Project.fundTerms` from the version snapshot; `refm_fund_terms` is the tab's durable store; `attachToProject` loads it before hydrate. Verifiers: `verify-fund-layer-guard` 75, `verify-fund-terms` 182, `verify-fund-fees` 133 + 3 pre-existing failures, `verify-fund-waterfall` 382, `verify-fund-fee-income` 109, `verify-fund-excel` 75, `verify-fund-pdf` 49, `verify-fund-e2e` 86. Full dated narrative (Steps 5b-7, the ten-item export review, basis fixes, pinned non-defects) in [CHANGELOG.md](CHANGELOG.md) 2026-08-10/11.
> - [CLAUDE-DB.md](CLAUDE-DB.md), Database tables, storage buckets, migrations log
> - [CLAUDE-FEATURES.md](CLAUDE-FEATURES.md), Feature status, archived phase narratives (M1.R → M1.13d pre-M2.0 + M2.0 → M2.0i post-rebuild)
> - [CLAUDE-ROUTES.md](CLAUDE-ROUTES.md), All page routes, API routes, components, lib structure
> - [CLAUDE-TODO.md](CLAUDE-TODO.md), Pending work, backlog, legacy reference. **START HERE is current as of 2026-09-05: MODULE 10 (steps 0-9, migs 230-238) AND THE ACCOUNT MODEL (steps 1-7, migs 239-241, plus notifications, the Team tab, the Collaborate screen and the account-scoped cost catalog) ARE ALL LIVE. SEVEN THINGS ARE OPEN, in order: (1) in-context commenting, steps 1-5 (path-to-screen map first; plan in CLAUDE-TODO); (2) the module renumber, ON HOLD (Collaborate -> 8, Portfolio -> last as a paid add-on; diagnosis complete, silent-entitlement hazard written up); (3) Collaborate screen usability incl. the activity log wording; (4) Module 1 land planning naming and FAR; (5) the escalated rate table in Module 2; (6) cost per sqm in Capex; (7) the full Alma land structure. Standing: per-platform seats when a second platform ships (recorded, not built); founder click-tests owed on the invite + access emails, Team tab, Collaborate screen and shared catalog picker. Below those: the schema-drift findings (2026-08-30, diagnosed not fixed) and the nine deliberately-open verifier fixtures. The 2026-09-04/05 work is verifier-proven but NOT browser-verified; each CHANGELOG entry states which.**
> - [ARCHITECTURE.md](ARCHITECTURE.md), Three-tier folder rationale, alias guide, boundary rules
> - [HANDOFF.md](HANDOFF.md), AUTO-GENERATED live status snapshot (commit, stack, latest migration, templates, verifiers, routes, env). Never hand-edit; regenerate with `npm run handoff` (`scripts/generate-handoff.ts` derives it from git + the repo, so it cannot go stale like the old PROJECT_HANDOFF.md did).

---

## STRICT SESSION RULES, READ FIRST

**Before debugging anything that "should work", read [docs/TRAPS.md](docs/TRAPS.md).** Traps recorded in the module narrative above are indexed there with symptom, mechanism, fix and proof: the ExcelJS width-9 column that silently does not apply (TRAPS 3.1) and `spliceRows` not moving merges (3.2); pdf-lib CID glyph ids defeating a naive PDF grep (4.1); the PostgREST 1000-row silent truncation (2.1); `Number(null)` being `0` and finite, which made an unlimited discount read as fully redeemed (see 2.4 for the family, an absent value collapsed into a real one); `admin_audit_log.admin_id` being NOT NULL (2.5); `requireAdmin` needing to try/catch `getServerSession`, which throws without a request scope (9.4); `src/middleware.ts` being dead code that must not be activated by moving it (9.2); and `Project.fundTerms` only reaching the store when its tab mounted, which is why `attachToProject` loads it before hydrate and before the autosave subscriber. The copies above stay in place.

### Writing rule: NEVER use the reference client's name

The user has shared private reference Excel models that we use as a verification benchmark only. The client / model name **must never appear in this codebase**, including:
- UI strings (tooltips, captions, hints, formula text, error messages, labels)
- Code comments
- Variable / function / file names
- Commit messages, PR descriptions, branch names
- Project documentation, MD files, README
- Test fixtures (use generic names like `refAsset`, `refPhases`, "the reference fixture")

If you need to refer to a behaviour learned from the reference Excel, use phrasing like "the reference model", "the reference benchmark", or "the reference design", never the client's actual name. Any historical references that slip through must be scrubbed when found.

The reference Excel files at the repo root are `.gitignore`-listed and must stay that way. Do not commit them.

### Writing rule: NEVER use em-dashes

**NEVER use em-dashes (the long dash, U+2014) anywhere.** This applies to ALL output: code, code comments, UI strings, JSX text, tooltips, error messages, commit messages, documentation markdown, prompts, agent briefs, anything.

Use one of these instead, depending on intent:
- A comma, when separating clauses (most common substitute).
- A colon, when introducing a list or definition.
- Parentheses, for asides.
- "and" or "or" or "and/or", when joining alternatives.
- A period plus new sentence, when the second clause stands alone.

Em-dash sweep across `src/` + `scripts/` is complete (zero remaining as of 2026-05-20). Do not introduce new ones.

### Scoping: Read ONLY the files for your task domain

| Task | Read these paths + MDs |
|------|------------------------|
| Training (auth / dashboard / assessment / certificate) | `app/training/` `app/api/training/` `app/api/t/` `src/lib/training/` `src/components/training/` |
| Modeling Hub auth | `app/modeling/signin/` `app/modeling/confirm-email/` `app/api/auth/` `src/lib/shared/` |
| Modeling Hub wrapper (sidebar, platform catalog, admin sync) | **+ CLAUDE-MODELING-HUB.md.** `app/admin/platform-modules/` `app/api/platforms/` `app/api/admin/platform-module-pages/` `src/lib/modeling/platform-modules/` `src/components/refm/` `src/hubs/modeling/` `app/modeling-hub/` |
| REFM platform (Real Estate, Modules 1-7) | **+ CLAUDE-MODELING-HUB.md + CLAUDE-REFM.md.** `app/refm/` `app/modeling/` `src/components/refm/` `src/lib/modeling/` `src/hubs/modeling/platforms/refm/` `src/core/calculations/` |
| Admin panel | `app/admin/` `src/components/admin/` `app/api/admin/` |
| AI (client / registry / metering / grounding / IC narrative) | **+ CLAUDE-AI.md + docs/AI_FOUNDATION_GUIDELINE.md.** `src/shared/ai/` `app/api/admin/ai-features/` `src/hubs/modeling/platforms/refm/lib/ai/` |
| Email system | `src/shared/email/` |
| Landing pages / CMS | `app/(portal)/` `app/about/` `app/articles/` `app/pricing/` `src/components/landing/` `app/api/cms/` |
| Shared (auth / formatters / device trust) | `src/lib/shared/` `src/core/` |
| Navbar / layout | `src/components/layout/` |

Read only the rows your task touches. Per-platform MD loads only when working on that platform; the global CLAUDE.md stays lean.

### Verifier practice: the DIRECT verifier before each commit, FULL SUITE once at session end

**Adopted 2026-09-03, narrowed 2026-09-04 (user decision).** Running all verifiers before every
commit costs about fifteen minutes and re-proves untouched things several times a day; the
read-a-changed-file selection still pulled in verifiers that merely mention a touched path. So:

- **Before each commit: run ONLY the verifier that DIRECTLY covers the change** (the one whose
  subject is the thing you built or fixed, e.g. `verify-accounts` for an account-model change).
  Name it and state that it passed. Everything else, including verifiers that happen to read a
  changed file, waits for the session-end suite, which is what catches cross-cutting breakage.
- **At session end: run `npm run verify:suite` once, with credentials, and state the count.**
  (If the npm alias cannot resolve `tsx`, invoke `npx tsx scripts/run-verifiers.ts`, the same script.)

The rules that already applied still apply: run the COMMITTED runner, never a hand-rolled loop
(TRAPS 3.20), and state whether credentials were loaded. **The full-suite claim in this file is a
SESSION-END measurement, not a per-commit one**, so do not cite it as evidence that an individual
commit was fully checked.

### End-of-session rule: where every new entry goes
**ALWAYS update the MD files at the end of every session**, using this placement rule so CLAUDE.md never grows back over its limit (it hit 166.8k of a 150k cap on 2026-08-30 and was restructured):
- **Dated session narrative** (what was found, measured, fixed today) goes to [CHANGELOG.md](CHANGELOG.md), newest first, AND for platform work the full story goes to the relevant `CLAUDE-{platform}.md`. NEVER as a new CLAUDE.md bullet or paragraph.
- **CLAUDE.md may change only by**: flipping a status word on an existing one-line bullet, adding or amending a ONE-LINE standing invariant, or adding a pointer. Anything longer than ~3 lines belongs in a sub-file with a pointer here.
- **Standing lessons born from a defect** go to [docs/TRAPS.md](docs/TRAPS.md) (symptom, mechanism, fix, proof).
- New files: the folder list in [CLAUDE-ROUTES.md](CLAUDE-ROUTES.md). Feature status: [CLAUDE-FEATURES.md](CLAUDE-FEATURES.md). New tables / migrations: [CLAUDE-DB.md](CLAUDE-DB.md). New env vars: the Deployment section here. Fund work: [docs/FUND_LAYER_GUIDELINE.md](docs/FUND_LAYER_GUIDELINE.md). AI work: [CLAUDE-AI.md](CLAUDE-AI.md).
- **EVERY NEW MIGRATION ROW IN [CLAUDE-DB.md](CLAUDE-DB.md) MUST CARRY `APPLIED` OR `PENDING`**, uppercase, in the row itself. `audit-migration-flags` READS those markers out of the document (it used to hold a hardcoded copy, which drifted and reported eleven false stale flags for two weeks), so a row with no marker is a migration the audit cannot judge and reports as "doc states no usable flag". Case matters: lowercase "applied" and "pending" appear all over the prose and are not markers. FORWARD-LOOKING ONLY. **Do not backfill the 153 older unmarked rows**: they predate the discipline and production working is evidence enough for them. Clear a `PENDING` in the same session it is applied, with what was probed.
- **Size check**: run `npx tsx scripts/verify-md-size.ts` at the end of every session. It WARNS when CLAUDE.md exceeds 100,000 characters and FAILS above 140,000 (the hard harness limit is 150,000). On a warning, move the newest dated content to CHANGELOG.md; never trim rules to make room for narrative.

### Do NOT touch list
- `next.config.ts`, subdomain routing is live and correct; clean auth URL rewrites + redirects added; app. `/register` rewrite goes to `/modeling/register` (dedicated page, NOT `/modeling/signin?tab=register`)
- `src/middleware.ts`, **DEAD CODE, verified 2026-07-30: this file has NEVER run in production.** Next resolves middleware at the PROJECT ROOT (`app/` is the source root and there is no `src/app`), so `src/middleware.ts` is never compiled: `.next/server/middleware-manifest.json` is `{"middleware":{}}` and the build output never mentions it. Live proof: `/admin/cms`, `/admin/users`, `/admin/revenue` all return **200 unauthenticated**. The `/login` + `/admin/login` 307s that make it look alive come from `next.config.ts` redirects, not this file. **No privileged data is exposed**, re-measured live 2026-08-01 across all **121** `app/api/admin/*` route files (118 was the count on 2026-07-30; three have been added since): **69 -> 401, 44 -> 403, 8 -> 200**. Each of the 8 is a read-only GET of data the public site already renders, with every write method on the same route guarded: `asset-types` (GET public by design since Pass 46, non-admins see only `visible` rows), `modules`, `training` (course catalog, no PII), and five `*-coming-soon` flag readers that public pages poll. So the precise claim is "every admin WRITE is guarded", not "every admin route is guarded". Admin PAGES return 200 unauthenticated but render only the generic marketing shell (no admin nav, no data); the client `useRequireAdmin` hook bounces non-admins, so page-level enforcement is CLIENT-side, and the documented server-side guarantee does NOT exist. Do not "fix" it by moving the file to the root without a deliberate JWT `role`-claim test first: that would ACTIVATE a gate that has never run and could lock admins out. See CLAUDE-TODO.md.
- `app/globals.css`, design system tokens, do not restructure
- `vercel.json`, deployment config is live
- `supabase/migrations/`, never edit existing migrations; create new ones only
- Any feature marked Complete unless explicitly asked by the user
- Cross-feature shared files (`src/lib/shared/`, `src/shared/email/`) without explicit instruction

---

## Project Overview

**Financial Modeler Pro**, Multi-hub SaaS platform with three web properties:

| Property | Domain | Purpose |
|----------|--------|---------|
| Main site | `financialmodelerpro.com` | Marketing, admin, portal, auth |
| Training Hub | `learn.financialmodelerpro.com` | Financial modeling courses |
| Modeling Hub | `app.financialmodelerpro.com` | Interactive financial modeling tools |

**Stack: Next.js 16 (App Router) + TypeScript strict + Tailwind CSS 4 + Supabase**

---

## Tech Stack

Next.js 16 (App Router) + TypeScript strict + Tailwind 4 + Supabase. State Zustand, charts Recharts, NextAuth (Modeling Hub) + custom session (Training Hub), Brevo email, hCaptcha, Anthropic SDK, ExcelJS + react-pdf for exports. Full version pinning in `package.json` + extended detail in [ARCHITECTURE.md](ARCHITECTURE.md).

## Authentication (summary)

Two independent stacks. Full per-flow detail (sign-in / register / OTP / device trust / admin bypass) in [CLAUDE-FEATURES.md](CLAUDE-FEATURES.md).

- **Training Hub** (learn.): custom session `training_session` cookie + bcrypt password in `training_passwords`. Source of truth = Google Apps Script roster. `email_confirmed` is `!== false`-truthy (null counts as confirmed for pre-migration-027 students).
- **Modeling Hub** (app.): NextAuth.js Credentials JWT (1hr) + scrypt password in `users.password_hash`. Admin role bypasses both EmailNotConfirmed + DEVICE_VERIFICATION_REQUIRED checks.
- **Device trust**: shared `fmp-trusted-device` cookie + `trusted_devices` table (30-day, keyed by email not UUID).

## Subdomain Routing (`next.config.ts`)

- `learn.fmp.com/` -> rewrites to `/training`. `app.fmp.com/` -> rewrites to `/modeling`. URL unchanged in both.
- Clean auth URLs `/signin`, `/register`, `/forgot` resolve per-subdomain to the matching training / modeling page.
- Cross-domain main-site paths redirect to `financialmodelerpro.com`.

Navbar uses plain `<a>` with absolute URLs via `NavbarServer.absolutizeHref()` + file-level `APP_URL` / `LEARN_URL` constants (never raw `process.env`).

## Design System

Single source of truth: `app/globals.css`. Tokens: `--color-primary*`, `--sp-1..5` (8px grid), `--font-h1..micro`. Component classes: `.card`, `.kpi-card`, `.btn-primary`, `.table-standard`, `.input-assumption` (yellow assumption cells). Per-platform input variants documented in the platform's MD (REFM FAST navy-pale input lives in `src/hubs/modeling/platforms/refm/components/modules/_shared/inputStyles.ts`). Never use Tailwind utility classes for layout tokens.

## Deployment + Health

Vercel auto-deploys on push to `main`. Environment variables documented in `.env.example` + the Vercel dashboard. Key ones: `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` / `NEXTAUTH_SECRET` / `NEXTAUTH_URL` / `BREVO_API_KEY` / `HCAPTCHA_SECRET_KEY` / `ANTHROPIC_API_KEY` / `ANTHROPIC_MODEL` (optional, overrides the AI default model at deploy time) / `CRON_SECRET` / `FMP_PUBLIC_API_KEY` (shared secret for the public partner page feed; UNSET means that endpoint refuses every request, it fails closed).

```bash
npm run type-check   # tsc --noEmit (zero errors required)
npm run build        # next build --webpack (avoids MAX_PATH on Windows/OneDrive)
npm run verify       # type-check + lint + build
```

`GET /api/health` -> `{ status: 'ok', platform: 'financial-modeler-pro', version: '3.0', commit, timestamp }` (`commit` = `VERCEL_GIT_COMMIT_SHA`, so the deployed revision is verifiable: compare to `git rev-parse HEAD`). Per-platform verifier scripts live in the platform's MD (REFM = `npx tsx scripts/verify-*.ts`; current script list + counts in [CLAUDE-REFM.md](CLAUDE-REFM.md)).
