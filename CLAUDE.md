# Financial Modeler Pro, Claude Code Project Brief
**Last updated: 2026-08-30 (restructured: dated session narrative moved to [CHANGELOG.md](CHANGELOG.md)). Platform state as of 2026-08-20.**

**Module status (REFM platform). Full narrative + commits live in [CLAUDE-REFM.md](CLAUDE-REFM.md) and the dated change record in [CHANGELOG.md](CHANGELOG.md), NOT here:**
- **Module 1 (Setup / Costs / Financing): CLOSED 2026-08-17** after 28 defects found by entering a real project end to end, followed by the 2026-08-18/19 statement, returns and reconciliation corrections (all in [CHANGELOG.md](CHANGELOG.md)). **No further Module 1 changes before launch.** Base lock unchanged at M2.0 Pass 58. The recurring shape across the 28: one rule written in more than one place, with more than one answer. Read [docs/TRAPS.md](docs/TRAPS.md) section 7 before touching Module 1 (7.12, 7.15, 7.16, 7.19, 7.21 are all from this block). Browser-verification status per change is recorded in its CHANGELOG entry and in [CLAUDE-TODO.md](CLAUDE-TODO.md).
- **REFM standing invariants** (each born from a measured defect; the full dated story is in [CHANGELOG.md](CHANGELOG.md), the general lessons in [docs/TRAPS.md](docs/TRAPS.md)):
  - **Asset strategy changes are a MODEL operation**, not a field write: `lib/state/strategySwitch.ts` (pure) parks the outgoing strategy's sub-units, opex and companion in `Asset.retainedByStrategy` and restores or seeds the incoming set; `state.subUnits` / `costLines` / `costOverrides` hold ONLY the active strategy's rows. Dialog and banner are gated on `assetHasStrategyAssumptions`; the dropdown previews via a DRY RUN of the same pure function the store commits.
  - **Allocation shares never silently sum to zero**: every share basis falls back to `equalPhaseShare` when its denominator is zero (six call sites in `resolveAllocationFactor` / `resolveDriverFactor`); factors are proven to sum to exactly 1. Never write `return total > 0 ? mine / total : 0`.
  - **`marketing` is a COST STAGE** (`CostStage = land | hard | soft | marketing | operating`), so construction cost excluding land also excludes marketing. When adding a stage: only `Record<CostStage, number>` sites are compiler-guarded, two inferred literals in `Module1Costs.tsx` will silently drop it, and `deriveCostType` defaults an unhandled stage to `'hard'`.
  - **A parcel is PROJECT-WIDE**: the explicit parcel lookup is project-wide; the implicit rules (autoByBua / percent / equal share) stay phase-scoped. `computeAssetLandBreakdown` returns `rateIssue` so there is no silent zero land value.
  - **One placement / window rule each**: `phaseLocalToProjectIndex` is the ONE Y0 upfront-lump rule (all five sites call it); `deriveCostWindow` is the ONE seeded cost-window rule (seed + store re-derive); a followed phasing source (collections, land cash) replaces the WINDOW, not just the shape, and the row renders `resolvedWindowByLineId`, what the engine spent on.
  - **The capex table is an ordered, user-classified list**: `% of selected lines` is POSITIONAL and upward-only (`src/core/calculations/selectedBase.ts`, shared by picker and engine); stage is a per-line `stageOverride` read through `deriveCostStage` (the id map outranks `stage`); order edits go through `insertCostLineNear` / `moveCostLine`; case overrides are id-keyed.
  - **A cost line is a catalog entry plus a label** (`CostLine.catalogId`, `lib/state/costCatalog.ts`, mig 214): selecting an entry STAMPS behaviour onto the LINE; the catalog is never read by the engine, reports or exports; identity resolves from the base id via `resolveCatalogId` when `catalogId` is absent. Never resolve identity a second way (the minted `custom-<ts>__<phase>` shape defeats base-id-only rules).
  - **Selling costs belong to revenue**: scope via `deriveAssetScope` (ONE implementation in `selectedBase.ts`), enforced in `assetVisibleLines`, which the Costs tab, the engine and the copy planner all call, so shown is charged and hidden is not. Bases come from the revenue module; **zero is not absent** (a held asset's sale base is 0, never a fallback). `computeAssetCost` takes the revenue snapshot and resolves bases internally (`core/calculations/revenue/sellingCosts.ts`, zero imports); every reachable call site values a line the same way.
  - **No country gate, no silent lock**: `assetVisibleLines` takes no country (the gate is RETIRED; RETT is a catalog entry the user adds); view mode is visibly read-only, `modelLocked` -> `viewLocked` is ONE derivation, inputs are locked by CSS + capture handlers, and a mutating button must declare `data-view-mutates="true"` to be locked (buttons are opt-IN, inputs opt-OUT via `data-view-editable`).
  - **A facility share is used exactly as typed** (`resolveFacilityShares`, verbatim, floored at zero; equal split only when NO share is typed anywhere); a wrong sum is STATED with a one-click repair, never rescaled. A facility's schedule is project-wide (share x requirement), so identical tables are shares, not double counts.
  - **Migrations spread the raw object and normalise on top**, never enumerate the fields they keep (three fields were lost to the whitelist shape); no legacy rename key may be a current catalog id. `Project.country` is an ISO code selected on Project & Phases; `countryMatches` resolves BOTH sides of any comparison.
  - **Depreciation starts when the asset is available for use** (`offset + cp - 1`, in PERIODS, `computePhaseTimeline` needs `modelType`), for BOTH streams that feed `AssetPL.daPerPeriod` (own capex AND capitalised IDC).
  - **Returns chain**: FCFF is UNLEVERED FULL COST (in-kind land charged, no interest / IDC row); FCFE builds VISIBLY from FCFF with the terminal swap as two rows; the finance-cost ledger foots `Opening + Charge - Paid = Closing` at 0.00; interest is one line per origin. The deficit sizing has NO finance cost in it, gates on `capex > 0`, and measures IDC headroom pre-interest (Schedules R112-R124). The management fee funding toggle (`fundTerms.managementFeeFunding`, deficit default | equity) lives on Financing AND Fund Terms, one field; the equity-funded fee is its own capped `dedicatedEquity` draw and the reconciliation compares against BOTH halves per period. In-kind equity reads `capex.perPeriod.landInKind` verbatim (no parcel-walk fallback); the in-kind pair sits INSIDE the totals; max |Assets - L&E| is 0.00 on both live projects.
- **Module 2 (Revenue + CoS + Schedules + Escrow)**: LOCKED at Pass 9N.
- **Module 3 (Operating Expenses)**: LOCKED at Pass 5d.
- **Module 4 (Financial Statements)**: DONE. Schedules / P&L / CF / BS. **Fund-fee statement position (2026-08-05):** the five fund fees total into a **Total Fund Management Fee** line and **EBITDA is struck AFTER it** (one EBITDA row, already net of fees; `ebitdaBeforeFundFeesPerPeriod` holds the pre-fee measure and is what the PHASE-level P&L uses, since fees are project-level with no phase allocation; `ebitdaAfterFundFeesPerPeriod` is a retained alias). Cash flow carries a visible **Fund Management and Other Expenses** row inside operating activities above Cash Flow from Operations. Both come from the shared `m4Reports.ts` builders, so screen + PDF + Excel move together. **Fund Fee Basis (2026-08-05, pushed 2026-08-09):** a shared `buildFundFeeBasisRows` builder states what each fee is charged ON (base, rate, basis amount, fee charged) beside the statement, rendered by `FundFeeBasisTable` on the consolidated P&L and the M5 fee income section and emitted as a Fund Fee Basis block on the Excel P&L tab (meta columns B/C, so the period axis at column F does not shift), because a fee reading zero previously gave no way to tell whether the RATE was zero or the BASE was empty. Consequence: ICR/DSCR read `ebitdaPerPeriod` so they are now after-fee on a fund project. BS balances by construction; Direct CF == Indirect CF every period. Phase-filtered P&L stops at EBITDA; phase CF shows Operations+Investing; BS is consolidated-only. P&L/CF/BS render from shared pure builders (`lib/reports/m4Reports.ts`) that the on-screen tabs AND the PDF both consume.
- **Module 5 (Returns & RE Metrics)**: DONE. Sponsor-IRR view (FCFF / FCFE / Distributed Equity + terminal value + RE metrics + per-asset + Case Comparison). RE Metrics tab carries editable Lender Covenants (DSCR / ICR / Debt Yield + LTV at peak debt = debt / GDV, since LTV at exit is a trivial 0%); pure display layer over the snapshot, no engine change. **Per-partner returns (2026-07-09):** each equity holder shows FCFE-based + DDM-based IRR/MOIC/equity-multiple driven by ONE time-weighted (dollar-years) shareholding (manual override = agreed share), each stream = agreedShare x the exact consolidated stream so Sigma partners == consolidated per period; total returns byte-identical (horizontal side-by-side tables + green/amber reconciliation chip). Equity partners can be **linked to M1 Parties** (equity-role party -> stores `partyId` + snapshots the name; engine never reads partyId).
- **Module 6 (Scenario Analysis)**: LIVE. A surface over the case engine (no engine rebuild): case list, multi-case assumptions grid (model-aware curated levers + config-inert gating, exhaustively field-audited on the live project), comparison matrix, and a Year-on-Year Impact tab (per-period divergence per case; debt/equity split deduped to one block; debt drawdown is principal, excludes IDC). Presentation in `lib/cases/assumptionGrid.ts` + `lib/reports/caseYoYReport.ts`; engine `applyOverrides`/`snapshot-diff` untouched. Was "Reports"; swapped with Module 7.
- **Module 7, ADDING A SLIDE TEMPLATE IS A TWO-STEP CONTRACT (2026-08-15)**: a deck is saved per project, so an existing deck only gains new slides through `upgradeDeckLayout`, which offers exactly the ids registered in `TEMPLATES_BY_VERSION` ABOVE the deck's own `schemaVersion`. You must **bump `DECK_SCHEMA_VERSION` AND register the new id under that number**. Doing only the first silently lifts every deck's version and adds nothing; doing only the second never fires. Nothing enforced this, so a template added to the library and left unregistered reached freshly seeded decks and was **invisible to every existing one, permanently and silently**. That is what happened to the three fund slides (shipped 2026-08-13, complete and correctly gated, on a fully populated model): a real 36-slide deck exported with zero fund content, and the only route to them was a rebuild that discards arrangement, edits, text boxes and AI narrative. Now `TEMPLATES_BY_VERSION` is COMPLETE (every `SLIDE_TEMPLATES` id under exactly one key, v1 originals included and inert) and **`verify-deck-template-registry` fails on any unregistered template and on a newest-key that does not equal `DECK_SCHEMA_VERSION`**. Detail in [CLAUDE-REFM.md](CLAUDE-REFM.md) 2026-08-15c.
- **Module 7 (IC Presentation Builder)**: LIVE. PowerPoint-style slide editor (navigator / 16:9 canvas / properties); **slides hold BINDING KEYS, never copied numbers**, so the deck follows Modules 1-6 with no sync step and an unresolved binding renders a visible amber UNLINKED state. Direct manipulation, 18+ institutional templates that auto-omit on `available(model)`, paginated full schedules, live ToC, native PPTX + PDF from ONE shared resolved-export contract. Free-form AI drafting on ANY selected block, grounded via the same `collectGrounding` whitelist, all-or-nothing refusal sentinel (`CANNOT_ANSWER_FROM_MODEL:`), metered through `ic_narrative`. Three toggle-gated fund slides fed by `fundReports.ts` / `m4Reports.ts`; toggle off is byte-identical. Report versions are auto-named server-side and the last one opens with the project; report versioning is fully separate from model versioning (migs 199, 207). Detail in [CLAUDE-REFM.md](CLAUDE-REFM.md) + [CLAUDE-AI.md](CLAUDE-AI.md); dated entries in [CHANGELOG.md](CHANGELOG.md).
- **Cases (scenarios)**: Management base + override cases; the active case drives the WHOLE model + all exports (not just the comparison); topbar switcher + Returns Case Comparison + the Module 6 page; per-input "≠ Management" override badge; scenario edits in the change_log; viewing a case never starts an edit session. Engine `lib/cases/applyOverrides.ts` (value-only overrides, base never mutated; per-element grammar via `snapshot-diff.PER_ELEMENT_ARRAYS`).
- **Platform infra**: project-switch state-leak fixed; session-based versioning + per-version change_log. **View/edit lock**: a project opens read-only until Edit (edit in place / different version / new version; `startEditInPlace` / `saveAsNewVersion` in `lib/persistence/module1-sync.ts`); schema-tolerant reads; paginated version history. **PDF full-project report** (`lib/pdf/generateProjectPdf.ts`, pdf-lib) mirrors every module tab via shared `lib/reports/` builders, with part picker + number-scale + version picker in `ExportModal`. **Excel MODEL export** (`lib/excel/buildModelWorkbook.ts`): a HARDCODED platform snapshot (no live formulas), 16 tabs in module order built from the SAME shared builders and snapshots the screens use; locked navy palette (`ALLOWED_FILLS` in `excel/styles.ts`); A4 print setup; per-tab sub-TOC + basis-of-calc blocks; Scenarios tab mirrors Module 6. **Four export review passes** (2026-08-12/13: arithmetic, readability, consistency, presentation) produced the shared builders `streamReports.ts` / `checksReport.ts` / `assetNotes.ts` and verifiers `verify-report-{arithmetic,readability,consistency,presentation}` (78/55/59/92). **Platform guide + 34-step guided tour** (2026-08-20): ONE content source `lib/guide/guideContent.ts` rendered VERBATIM by the tour, structure from the live registries via `lib/moduleTabs.ts`. **Trial exports** carry a server-resolved watermark (admin control on /admin/plans); Excel gates on `excel_snapshot`; OpEx seeds ZERO. Full dated narrative in [CHANGELOG.md](CHANGELOG.md) and [CLAUDE-REFM.md](CLAUDE-REFM.md).

- **Public read-only page API**: `GET /api/public/pages/[slug]` serves whitelisted CMS pages to the partner site (PaceMakers). `SLUG_WHITELIST` lives in `src/shared/api/publicPagesConfig.ts`, shared with the `/admin/api-keys` screen; anything not whitelisted or not published 404s. Auth is `x-api-key`, constant-time, **fails CLOSED**. **Key rotation rule (mig 213, read before touching either route)**: the env var `FMP_PUBLIC_API_KEY` is consulted only when `public_api_keys` holds NO ROW AT ALL for the key id; an active row supersedes it permanently; a retired-only history CLOSES the endpoint; an unreadable key store REFUSES; resolution is never cached; only a SHA-256 + 14-char prefix are stored, so reveal is refused for a rotated key. Rotation is one transaction with a partial unique index enforcing at most one active key. Verifiers: `verify-public-pages-api` 43, `verify-admin-api-keys` 80, `verify-api-key-rotation` 85 (probe key id only, never the real one). Full dated narrative in [CHANGELOG.md](CHANGELOG.md) (2026-08-10/11/16).

- **Entitlements & Pricing (LIVE)**: admin-controlled plan/feature system on `features_registry` + `plan_permissions` + `entitlement_plans` + `user_permissions` (migs 158-165). Resolver `resolveEffectiveFeatures` (override-wins) is REUSED everywhere; the live REFM gate (`resolveUserGate` + `gate.ts`, reads the live `users` row, not the JWT) enforces modules/exports/scenarios/save/branding/sensitivity, the project cap and trial expiry. Admin never blocked; unknown plan = access-preserving safety net. Plans: trial/solo/pro/firm; **pricing single source of truth = `entitlement_plans`**; ONE shared `PricingPageBody` + session-aware `PricingExplorer` at path-based `/pricing/<segment>` (segment source-derived in `platforms.ts`); ONE plan-setting path `setUserPlan`. **Payments**: provider-agnostic `src/shared/payments/` (Paddle implemented, sandbox; PayPro stub); checkout -> overlay via client token; webhook verifies HMAC + reuses `setUserPlan`; `paddleEnvMismatch` guards token/env mismatch. Verifier counts in the CHANGELOG entry; per-route/migration detail in [CLAUDE-ROUTES.md](CLAUDE-ROUTES.md) + [CLAUDE-DB.md](CLAUDE-DB.md).

- **Subscription lifecycle (billing, grace, emails, discounts, launch banner)**: self-service billing tab + admin billing on top of the single plan path; `setUserPlan` converges the per-platform `user_platform_subscriptions` row on EVERY plan write, so the gate (store A) and billing (store B) never diverge; live-Paddle users are blocked from silent manual changes. **Post-expiry is a three-state lapse model** (active / grace read-only for one calendar month / lapsed), computed from the plan's expiry anchor by shared `gate.ts` helpers and enforced server-side at every write choke point (create/duplicate/versions/save/export); data is never deleted. **Emails**: nine-plus branded Brevo templates (welcome, trial, renewal notice, dunning, canceled, plan-changed, grace) dispatched at triggers + a daily reminder cron, idempotent via `subscription_email_log` with PER-EVENT dedupe keys; manual plans get a branded receipt PDF (`manual_invoices`, private bucket); the Paddle ledger (`payment_transactions`) self-heals on read so invoice history survives source flips. **Discounts**: Paddle is the single source of truth (`resolveCouponForCheckout` / `loadActivePublicPromo`, live list, 60s cache); only the featured-promo CHOICE is stored locally; `nullableNumber` preserves absence so an unlimited discount never reads as used up. **Launch countdown banner**: three-state (countdown/launched/hidden) resolver over `training_settings.modeling_hub_launch_date` (the SAME key the auto-launch cron reads), admin-editable copy, allowlist of five exact paths. Full dated narrative for every one of these in [CHANGELOG.md](CHANGELOG.md) (2026-06-29 through 2026-08-09); per-route/migration detail in [CLAUDE-ROUTES.md](CLAUDE-ROUTES.md) + [CLAUDE-DB.md](CLAUDE-DB.md).

- **Access requests + account deletion (2026-08-30)**: the dashboard leads with an access card for no-plan users (request button inside, pending state persisted via `trialRequestPending` on the entitlements route) plus a once-only two-day reminder email in the daily cron; account deletion (admin + self) runs through ONE engine, `src/shared/account/deleteUserAccount.ts`, which retains the financial ledger, refuses to orphan a live Paddle subscription, and audits into `account_deletions` (mig 219, APPLIED 2026-08-30, probed live). Detail in [CHANGELOG.md](CHANGELOG.md) 2026-08-30; verifiers `verify-access-request` 36, `verify-account-deletion` 33. Same day: the admin user list's project count now reads `refm_projects` (the legacy `projects` table is empty and dead), the Projects button opens a read-only per-user modal, duplicate plan/status badges are gone, ACCESS renders only when it diverges from STATUS, and the lockdown banner follows the live mig-136 flags (`verify-admin-users-cleanup` 22).

- **AI foundation (COMPLETE 2026-08-01, units 0 to 9)**: a platform-agnostic AI engine, not a one-off feature. One call path (`runAi`, the only reader of `ANTHROPIC_API_KEY`), a feature registry with admin toggles, server-side metering with DB-driven per-plan caps and refund-on-failure, a grounding abstraction that supplies labelled FACTS and audits every figure in the output, an admin control panel, and the first feature on it: **REFM Module 7 IC narrative generation** (six fields, grounded in the computed model, editable draft, never auto-saved). Migrations 203-206 all APPLIED. Features register DISABLED and are switched on in `/admin/ai-features`. **Full detail, invariants and verifier map in [CLAUDE-AI.md](CLAUDE-AI.md); plan and standing rules in [docs/AI_FOUNDATION_GUIDELINE.md](docs/AI_FOUNDATION_GUIDELINE.md).**

Verifier reality + outstanding ops (migrations pending on prod) live in [CLAUDE-REFM.md](CLAUDE-REFM.md). Full suite green via `npx tsx scripts/verify-*.ts`.

**M2 lock conventions** (apply to M3+ unless overridden):
- The reference Excel at repo root is the verification benchmark, not a behavioural spec. Every reference-specific behaviour stays configurable; never hard-code currency, locale, escrow, or DSO defaults into engine paths.
- Engine storage is project-axis-indexed (`arr[0]` = first active project year). Only the UI window changes per asset / strategy.
- Vintage matrices (cohort year × cash year, cohort year × recognition year, capex year × CoS year) are the canonical mechanic for both recognition and cash distribution.
- PIT recognition handover = LAST construction year (`phaseStart + cp − 1 − projectStart`), NOT first operations year. Verifier A2-1..A2-5 pin this.
- See CLAUDE-REFM.md for the full Pass 7-9 narrative; older pass detail is archived to CLAUDE-FEATURES.md.

> **See also:**
> - **[docs/TRAPS.md](docs/TRAPS.md), the hard-won lessons. READ THIS FIRST; it is short.** Every entry is a failure that reported success: a grep that returned nothing, a constraint that never fired, a column width that silently did not apply, a deploy that silently did not happen. Symptom, mechanism, fix and the measurement that proved it. The individual copies stay in the files below, so nothing is lost either way
> - [CHANGELOG.md](CHANGELOG.md), the dated change record, newest first: every session-log bullet this file used to carry, verbatim. Append new dated entries THERE, never here
> - [CLAUDE-MODELING-HUB.md](CLAUDE-MODELING-HUB.md), Modeling Hub wrapper, platform catalog, P-Sync admin conventions
> - [CLAUDE-REFM.md](CLAUDE-REFM.md), Real Estate Financial Modeling (REFM), platform status (Modules 1-7) + conventions + Excel/PDF export
> - [CLAUDE-AI.md](CLAUDE-AI.md), AI foundation (client, registry, metering, grounding, admin panel, IC narrative). Load when touching anything AI
> - [docs/FUND_LAYER_GUIDELINE.md](docs/FUND_LAYER_GUIDELINE.md), Fund layer (fund fees, hurdle, carry, gross vs net returns). **Load it when touching anything fund; it is the source of truth and is kept current.** Steps 1 to 7 LIVE, FEATURE COMPLETE for v1, migs 208-211 applied; the fund toggle is ON for FMP RE HUB since 2026-08-11. Five invariants before you read anything else: (1) toggle OFF is byte-identical, pinned against a fully populated but disabled block; (2) every fee base is linear and the rule is a DATA STRUCTURE (`FUND_FEE_SPECS` + `LINEAR_FEE_BASES` / `CIRCULAR_FEE_BASES`); (3) the fee schedule is FROZEN before the iterative solver (fund size resolved on the fee-free pass; `fund_size_solved` stays forbidden); (4) the waterfall (`returns/waterfall.ts`, pure) has ONE hurdle balance, no catch-up, flat fee on the excess, and is NOT in the funding solve; (5) fund PRESENTATION has ONE definition, `lib/reports/fundReports.ts`, which the M5 screen, Excel and both PDFs all import (no surface re-declares a row label). The engine reads `Project.fundTerms` from the version snapshot; `refm_fund_terms` is the tab's durable store; `attachToProject` loads it before hydrate. Verifiers: `verify-fund-layer-guard` 75, `verify-fund-terms` 182, `verify-fund-fees` 133 + 3 pre-existing failures, `verify-fund-waterfall` 382, `verify-fund-fee-income` 109, `verify-fund-excel` 75, `verify-fund-pdf` 49, `verify-fund-e2e` 86. Full dated narrative (Steps 5b-7, the ten-item export review, basis fixes, pinned non-defects) in [CHANGELOG.md](CHANGELOG.md) 2026-08-10/11.
> - [CLAUDE-DB.md](CLAUDE-DB.md), Database tables, storage buckets, migrations log
> - [CLAUDE-FEATURES.md](CLAUDE-FEATURES.md), Feature status, archived phase narratives (M1.R → M1.13d pre-M2.0 + M2.0 → M2.0i post-rebuild)
> - [CLAUDE-ROUTES.md](CLAUDE-ROUTES.md), All page routes, API routes, components, lib structure
> - [CLAUDE-TODO.md](CLAUDE-TODO.md), Pending work, backlog, legacy reference. **START HERE is current as of 2026-08-13: the export review is DONE (four passes; passes 1 to 3 deployed, PASS 4 IS UNCOMMITTED AND HELD FOR REVIEW), and THE EXPORTS HAVE NOT BEEN RE-READ SINCE PASS 3 LANDED. Nothing from the 2026-08-11 or 2026-08-12 sessions has been browser-verified (the strategy-change dialog and banner most of all, then both PDFs). Module 7 fixes are further down. The `AI_REVIEW_GUIDE.md` audit (untracked, untrusted, per-finding status recorded) is parked above it.**
> - [ARCHITECTURE.md](ARCHITECTURE.md), Three-tier folder rationale, alias guide, boundary rules
> - [HANDOFF.md](HANDOFF.md), AUTO-GENERATED live status snapshot (commit, stack, latest migration, templates, verifiers, routes, env). Never hand-edit; regenerate with `npm run handoff` (`scripts/generate-handoff.ts` derives it from git + the repo, so it cannot go stale like the old PROJECT_HANDOFF.md did).

---

## STRICT SESSION RULES, READ FIRST

**Before debugging anything that "should work", read [docs/TRAPS.md](docs/TRAPS.md).** Traps recorded in the module narrative above are indexed there with symptom, mechanism, fix and proof: the ExcelJS width-9 column that silently does not apply (TRAPS 3.1) and `spliceRows` not moving merges (3.2); pdf-lib CID glyph ids defeating a naive PDF grep (4.1); the PostgREST 1000-row silent truncation (2.1); `Number(null)` being `0` and finite, which made an unlimited discount read as fully redeemed (see 2.4 for the family, an absent value collapsed into a real one); `admin_audit_log.admin_id` being NOT NULL (2.5); `requireAdmin` needing to try/catch `getServerSession`, which throws without a request scope (9.4); `src/middleware.ts` being dead code that must not be activated by moving it (9.2); and `Project.fundTerms` only reaching the store when its tab mounted, which is why `attachToProject` loads it before hydrate and before the autosave subscriber. The copies above stay in place.

### Writing rule: NEVER use the reference client's name

The user has shared private reference Excel models that we use as a verification benchmark only. The client / model name **must never appear in this codebase**, including:
- UI strings (tooltips, captions, hints, formula text, error messages, labels)
- Code comments
- Variable / function / file names
- Commit messages, PR descriptions, branch names
- Project documentation, MD files, README
- Test fixtures (use generic names like `refAsset`, `refPhases`, "the reference fixture")

If you need to refer to a behaviour learned from the reference Excel, use phrasing like "the reference model", "the reference benchmark", or "the reference design", never the client's actual name. Any historical references that slip through must be scrubbed when found.

The reference Excel files at the repo root are `.gitignore`-listed and must stay that way. Do not commit them.

### Writing rule: NEVER use em-dashes

**NEVER use em-dashes (the long dash, U+2014) anywhere.** This applies to ALL output: code, code comments, UI strings, JSX text, tooltips, error messages, commit messages, documentation markdown, prompts, agent briefs, anything.

Use one of these instead, depending on intent:
- A comma, when separating clauses (most common substitute).
- A colon, when introducing a list or definition.
- Parentheses, for asides.
- "and" or "or" or "and/or", when joining alternatives.
- A period plus new sentence, when the second clause stands alone.

Em-dash sweep across `src/` + `scripts/` is complete (zero remaining as of 2026-05-20). Do not introduce new ones.

### Scoping: Read ONLY the files for your task domain

| Task | Read these paths + MDs |
|------|------------------------|
| Training (auth / dashboard / assessment / certificate) | `app/training/` `app/api/training/` `app/api/t/` `src/lib/training/` `src/components/training/` |
| Modeling Hub auth | `app/modeling/signin/` `app/modeling/confirm-email/` `app/api/auth/` `src/lib/shared/` |
| Modeling Hub wrapper (sidebar, platform catalog, admin sync) | **+ CLAUDE-MODELING-HUB.md.** `app/admin/platform-modules/` `app/api/platforms/` `app/api/admin/platform-module-pages/` `src/lib/modeling/platform-modules/` `src/components/refm/` `src/hubs/modeling/` `app/modeling-hub/` |
| REFM platform (Real Estate, Modules 1-7) | **+ CLAUDE-MODELING-HUB.md + CLAUDE-REFM.md.** `app/refm/` `app/modeling/` `src/components/refm/` `src/lib/modeling/` `src/hubs/modeling/platforms/refm/` `src/core/calculations/` |
| Admin panel | `app/admin/` `src/components/admin/` `app/api/admin/` |
| AI (client / registry / metering / grounding / IC narrative) | **+ CLAUDE-AI.md + docs/AI_FOUNDATION_GUIDELINE.md.** `src/shared/ai/` `app/api/admin/ai-features/` `src/hubs/modeling/platforms/refm/lib/ai/` |
| Email system | `src/shared/email/` |
| Landing pages / CMS | `app/(portal)/` `app/about/` `app/articles/` `app/pricing/` `src/components/landing/` `app/api/cms/` |
| Shared (auth / formatters / device trust) | `src/lib/shared/` `src/core/` |
| Navbar / layout | `src/components/layout/` |

Read only the rows your task touches. Per-platform MD loads only when working on that platform; the global CLAUDE.md stays lean.

### End-of-session rule: where every new entry goes
**ALWAYS update the MD files at the end of every session**, using this placement rule so CLAUDE.md never grows back over its limit (it hit 166.8k of a 150k cap on 2026-08-30 and was restructured):
- **Dated session narrative** (what was found, measured, fixed today) goes to [CHANGELOG.md](CHANGELOG.md), newest first, AND for platform work the full story goes to the relevant `CLAUDE-{platform}.md`. NEVER as a new CLAUDE.md bullet or paragraph.
- **CLAUDE.md may change only by**: flipping a status word on an existing one-line bullet, adding or amending a ONE-LINE standing invariant, or adding a pointer. Anything longer than ~3 lines belongs in a sub-file with a pointer here.
- **Standing lessons born from a defect** go to [docs/TRAPS.md](docs/TRAPS.md) (symptom, mechanism, fix, proof).
- New files: the folder list in [CLAUDE-ROUTES.md](CLAUDE-ROUTES.md). Feature status: [CLAUDE-FEATURES.md](CLAUDE-FEATURES.md). New tables / migrations: [CLAUDE-DB.md](CLAUDE-DB.md). New env vars: the Deployment section here. Fund work: [docs/FUND_LAYER_GUIDELINE.md](docs/FUND_LAYER_GUIDELINE.md). AI work: [CLAUDE-AI.md](CLAUDE-AI.md).
- **Size check**: run `npx tsx scripts/verify-md-size.ts` at the end of every session. It WARNS when CLAUDE.md exceeds 100,000 characters and FAILS above 140,000 (the hard harness limit is 150,000). On a warning, move the newest dated content to CHANGELOG.md; never trim rules to make room for narrative.

### Do NOT touch list
- `next.config.ts`, subdomain routing is live and correct; clean auth URL rewrites + redirects added; app. `/register` rewrite goes to `/modeling/register` (dedicated page, NOT `/modeling/signin?tab=register`)
- `src/middleware.ts`, **DEAD CODE, verified 2026-07-30: this file has NEVER run in production.** Next resolves middleware at the PROJECT ROOT (`app/` is the source root and there is no `src/app`), so `src/middleware.ts` is never compiled: `.next/server/middleware-manifest.json` is `{"middleware":{}}` and the build output never mentions it. Live proof: `/admin/cms`, `/admin/users`, `/admin/revenue` all return **200 unauthenticated**. The `/login` + `/admin/login` 307s that make it look alive come from `next.config.ts` redirects, not this file. **No privileged data is exposed**, re-measured live 2026-08-01 across all **121** `app/api/admin/*` route files (118 was the count on 2026-07-30; three have been added since): **69 -> 401, 44 -> 403, 8 -> 200**. Each of the 8 is a read-only GET of data the public site already renders, with every write method on the same route guarded: `asset-types` (GET public by design since Pass 46, non-admins see only `visible` rows), `modules`, `training` (course catalog, no PII), and five `*-coming-soon` flag readers that public pages poll. So the precise claim is "every admin WRITE is guarded", not "every admin route is guarded". Admin PAGES return 200 unauthenticated but render only the generic marketing shell (no admin nav, no data); the client `useRequireAdmin` hook bounces non-admins, so page-level enforcement is CLIENT-side, and the documented server-side guarantee does NOT exist. Do not "fix" it by moving the file to the root without a deliberate JWT `role`-claim test first: that would ACTIVATE a gate that has never run and could lock admins out. See CLAUDE-TODO.md.
- `app/globals.css`, design system tokens, do not restructure
- `vercel.json`, deployment config is live
- `supabase/migrations/`, never edit existing migrations; create new ones only
- Any feature marked Complete unless explicitly asked by the user
- Cross-feature shared files (`src/lib/shared/`, `src/shared/email/`) without explicit instruction

---

## Project Overview

**Financial Modeler Pro**, Multi-hub SaaS platform with three web properties:

| Property | Domain | Purpose |
|----------|--------|---------|
| Main site | `financialmodelerpro.com` | Marketing, admin, portal, auth |
| Training Hub | `learn.financialmodelerpro.com` | Financial modeling courses |
| Modeling Hub | `app.financialmodelerpro.com` | Interactive financial modeling tools |

**Stack: Next.js 16 (App Router) + TypeScript strict + Tailwind CSS 4 + Supabase**

---

## Tech Stack

Next.js 16 (App Router) + TypeScript strict + Tailwind 4 + Supabase. State Zustand, charts Recharts, NextAuth (Modeling Hub) + custom session (Training Hub), Brevo email, hCaptcha, Anthropic SDK, ExcelJS + react-pdf for exports. Full version pinning in `package.json` + extended detail in [ARCHITECTURE.md](ARCHITECTURE.md).

## Authentication (summary)

Two independent stacks. Full per-flow detail (sign-in / register / OTP / device trust / admin bypass) in [CLAUDE-FEATURES.md](CLAUDE-FEATURES.md).

- **Training Hub** (learn.): custom session `training_session` cookie + bcrypt password in `training_passwords`. Source of truth = Google Apps Script roster. `email_confirmed` is `!== false`-truthy (null counts as confirmed for pre-migration-027 students).
- **Modeling Hub** (app.): NextAuth.js Credentials JWT (1hr) + scrypt password in `users.password_hash`. Admin role bypasses both EmailNotConfirmed + DEVICE_VERIFICATION_REQUIRED checks.
- **Device trust**: shared `fmp-trusted-device` cookie + `trusted_devices` table (30-day, keyed by email not UUID).

## Subdomain Routing (`next.config.ts`)

- `learn.fmp.com/` -> rewrites to `/training`. `app.fmp.com/` -> rewrites to `/modeling`. URL unchanged in both.
- Clean auth URLs `/signin`, `/register`, `/forgot` resolve per-subdomain to the matching training / modeling page.
- Cross-domain main-site paths redirect to `financialmodelerpro.com`.

Navbar uses plain `<a>` with absolute URLs via `NavbarServer.absolutizeHref()` + file-level `APP_URL` / `LEARN_URL` constants (never raw `process.env`).

## Design System

Single source of truth: `app/globals.css`. Tokens: `--color-primary*`, `--sp-1..5` (8px grid), `--font-h1..micro`. Component classes: `.card`, `.kpi-card`, `.btn-primary`, `.table-standard`, `.input-assumption` (yellow assumption cells). Per-platform input variants documented in the platform's MD (REFM FAST navy-pale input lives in `src/hubs/modeling/platforms/refm/components/modules/_shared/inputStyles.ts`). Never use Tailwind utility classes for layout tokens.

## Deployment + Health

Vercel auto-deploys on push to `main`. Environment variables documented in `.env.example` + the Vercel dashboard. Key ones: `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` / `NEXTAUTH_SECRET` / `NEXTAUTH_URL` / `BREVO_API_KEY` / `HCAPTCHA_SECRET_KEY` / `ANTHROPIC_API_KEY` / `ANTHROPIC_MODEL` (optional, overrides the AI default model at deploy time) / `CRON_SECRET` / `FMP_PUBLIC_API_KEY` (shared secret for the public partner page feed; UNSET means that endpoint refuses every request, it fails closed).

```bash
npm run type-check   # tsc --noEmit (zero errors required)
npm run build        # next build --webpack (avoids MAX_PATH on Windows/OneDrive)
npm run verify       # type-check + lint + build
```

`GET /api/health` -> `{ status: 'ok', platform: 'financial-modeler-pro', version: '3.0', commit, timestamp }` (`commit` = `VERCEL_GIT_COMMIT_SHA`, so the deployed revision is verifiable: compare to `git rev-parse HEAD`). Per-platform verifier scripts live in the platform's MD (REFM = `npx tsx scripts/verify-*.ts`; current script list + counts in [CLAUDE-REFM.md](CLAUDE-REFM.md)).
