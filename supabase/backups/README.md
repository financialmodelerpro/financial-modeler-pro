# Apps Script roster snapshots

Pre-cutover backup files written by `scripts/backup_apps_script_students.ts`.
Each is a version-controlled snapshot of the Apps Script Google Sheet at
the time the script ran. Keep these forever for audit.
