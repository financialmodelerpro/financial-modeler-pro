-- ============================================================
-- 081: No schema change — tagline is already stored as text
-- in cms_content. Rich text editor now saves HTML to same field.
-- Favicon injection moved from client-side DOM to root layout
-- metadata via generateMetadata().
-- ============================================================
SELECT 1;
