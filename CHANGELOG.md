# Financial Modeler Pro, Change Record

**Dated session entries, newest first.** Moved verbatim out of CLAUDE.md on 2026-08-30 when it exceeded the 150k session limit; each entry below is the exact bullet CLAUDE.md used to carry (the fund-layer entry was a blockquote line there; only its `> ` prefix was stripped). New dated entries are appended HERE at the top of their date, never to CLAUDE.md (see the End-of-session rule in CLAUDE.md).

Entries are grouped by their most recent date. Fuller per-day narrative for REFM platform work lives in [CLAUDE-REFM.md](CLAUDE-REFM.md); per-route and migration detail in [CLAUDE-ROUTES.md](CLAUDE-ROUTES.md) and [CLAUDE-DB.md](CLAUDE-DB.md); the general lessons in [docs/TRAPS.md](docs/TRAPS.md); fund-layer standing rules in [docs/FUND_LAYER_GUIDELINE.md](docs/FUND_LAYER_GUIDELINE.md). This file is loaded on demand only and has no size limit.

## 2026-08-30

- **PASSWORD RESET WORKS FOR THE FIRST TIME, AND THE FAILURE WAS TRIPLE-SILENT (2026-08-30e, `verify-password-reset` 20 NEW, teeth proven by sabotage, migration 222 APPLIED same day and the full chain PROVEN LIVE on the deployed endpoint: token redeems, new password verifies against the stored hash, reuse refused, expiry refused, unknown refused).** **A defect I shipped inside the fix and corrected within the hour**: the availability gate used a HEAD select, and PostgREST answers a HEAD on an ABSENT table with an empty 204 and no error (a fact this same session had already observed and then reused anyway), so the gate could never fire; it is now a GET `.limit(0)`, pinned by check A1b. **Diagnosis**: `password_reset_tokens` does not exist on prod, so mig 008 NEVER RAN (IF NOT EXISTS cannot produce absence; unlike 007 this is not a no-op, it is a never-ran). End to end: forgot-password found the user, the token insert failed with its error UNCHECKED, a real branded email still went out carrying a token that verified against nothing, the route returned `ok:true`, and the page showed "check your email" for ANY response including 5xx; every click then died on "Invalid or expired reset link" because reset-password ALSO ignored its read error. So: false success at request, false success at page level, and a user-blaming message at redemption. **Three more finds**: the reset route has ALWAYS read a `used_at` column that even 008 never declared (a faithfully-applied 008 would still have failed); the 7-day runtime-error window shows NOTHING reset-related because both failure paths neither log nor throw, so whether a real user hit this is UNKNOWABLE from durable evidence; and the Training Hub is UNAFFECTED (its reset is OTP-based on `training_email_otps`, live with 603 rows). **Fix**: mig 222 creates the table as 008 declared PLUS `used_at` (the single-use marker the code always required) + index + RLS; both routes rewritten: store availability checked BEFORE the user lookup so the degraded state answers 503 identically for every address (no enumeration leak), insert and send errors are TOLD to the user (503/502) with a failed send killing its token, single use claimed ATOMICALLY (conditional update on `used_at IS NULL`), expiry enforced, one live link per user, other links revoked on success, and the email's user-supplied name escaped at its one entry point; the page now surfaces non-OK responses. Until 222 is applied the flow reports honest unavailability instead of false success.
- **FULL SCHEMA DRIFT AUDIT, REPORT ONLY (2026-08-30f, `scripts/audit-schema-drift.ts` NEW, findings in `scripts/schema-drift-report.txt`; NOTHING fixed by design).** Every migration (219 files, 107 declared tables) compared against the live schema via PostgREST's OpenAPI output (a real catalog read: tables, columns, NOT NULL, defaults, FK existence + target), with FK ON DELETE / UNIQUE / CHECK / indexes / RLS explicitly UNVERIFIABLE by this method (43 FK ON DELETEs, 19 UNIQUEs, 54 CHECKs declared-unverified; the 2026-08-30 behavioral probes cover 12 of the FKs, carried with evidence in `KNOWN_BEHAVIOR`). **Headline findings**: (1) LIVE-WRONG: the `certificates` table is 002's shape, 005's `certificate_number`/`assessment_id` never landed, AND `/api/training/certificates` reads `users.full_name`, which does not exist either, so the training certificates page silently shows EMPTY for every student (the first bug shields the second from 500ing). (2) Six declared tables MISSING live with ZERO code readers (founder_profile, pricing_plans/features/modules, marketing_designs/brand_kit): dead migrations. (3) Six live tables NO migration declares (legacy `projects`, `training_passwords` (live, in use), `project_scenarios`, `transcript_links`, `certificate_eligibility_raw`, `newsletter_subscribers_legacy`). (4) The known 006-vs-158 and 007 drift confirmed independently; plus `plan_permissions` and `features_registry` live as 158's shape with 006's columns missing (code uses 158's names: latent), `newsletter_subscribers` live as the 091 rebuild (078's name/is_active missing: latent), `training_pending_registrations.course` and four `session_watch_history` columns nullable where declared NOT NULL (latent), `email_branding.signature_html` default absent (harmless), hand-added live columns nobody declared (session_registrations reminder flags, branding_config.platform_id/updated_by: inventory). (5) NOT drift, verified: refm schema_version default 5 comes from `m2_0_module1_rebuild.sql`, and the three version-pointer FKs live in ALTER/DO statements the parser lists as unfolded, their live presence proving those migrations applied. Parser honesty: every unparseable statement is LISTED, never skipped silently.
- **THE MIGRATION FILE IS NOT THE SCHEMA: THE AUDIT-LOG FK BLOCKED EVERY USER DELETE (2026-08-30d, `verify-account-deletion` 33 -> 40, teeth proven by sabotage, PROVEN LIVE end to end, migration 221 APPLIED 2026-08-30 and verified by behavioral probe: bare delete succeeds, both columns SET NULL, admin_id still blocks by design).** A live admin delete failed with `admin_audit_log_target_user_id_fkey`. The earlier diagnosis said the target SET NULLs; that was read from `007_audit_log.sql`, which declares SET NULL on both columns and a NULLABLE `admin_id`, and NONE of it is live: the table predates the migration log, so 007's `CREATE TABLE IF NOT EXISTS` silently no-opped against a differently-constrained table (the same drift class as `users` and the legacy `projects`; TRAPS 2.5's `admin_id NOT NULL` was the clue already on file). **`pg_constraint` could not be read directly** (PostgREST has no catalog read; the direct `DATABASE_URL` password is stale, tenant resolves in `aws-1-ap-northeast-1`), so every user-referencing column was probed BEHAVIORALLY on prod, with the live constraint names captured from the violation messages: **BLOCKING (NO ACTION)**: `admin_audit_log.target_user_id` (the live error; hits any user an admin ever touched, because every plan/role/status change and trial approval writes a target row), `admin_audit_log.admin_id` (also NOT NULL; only ever references admins, who are undeletable, so it cannot block a normal user delete), and `user_permissions.created_by` (006 declared SET NULL but the live table carries 158's bare `REFERENCES` from the entitlements rebuild; admin-only referent, same non-issue for user deletes). **CASCADE as assumed**: refm_projects, trial_requests.user_id, user_permissions.user_id, ai_usage_counters, refm_cost_catalog, enrollments, certificates. **SET NULL as declared**: account_deletions.deleted_by (self-delete audit safe), articles.author_id. **NO FK**: trial_requests.declined_by, payment_transactions, manual_invoices, user_platform_subscriptions, payment_webhook_events. **SELF-DELETE HITS THE SAME WALL** (same engine, same users delete, same target rows). Two adjacent finds: `password_reset_tokens` DOES NOT EXIST on prod (mig 008 never took effect; `/api/auth/forgot-password` inserts into it, so the modeling-hub password-reset flow is broken independently of this, NOT fixed here), and `assessment_attempts` is unprobeable but empty (0 assessments, 0 attempts). **Fix, both layers**: the engine now NULLS `admin_audit_log.target_user_id` for the user IN CODE before the users delete (the SET NULL the schema always intended; the audit rows SURVIVE, and the account_deletions row keeps the identity mapping), so deletion works with or without DDL; migration 221 then aligns the constraints themselves (target_user_id and created_by to SET NULL; `admin_id` DELIBERATELY left NOT NULL + NO ACTION, a loud block on deleting an ex-admin with admin-actor rows beats silently losing who acted). **Proven live on prod**: a probe user with two target audit rows and a project reproduced the exact reported block on a bare delete, then the fixed engine deleted them: user gone, project cascaded, BOTH audit rows surviving with targets nulled, deletion audited, zero probe rows left behind.
- **THE PROJECTS BROWSER READS THE REAL TABLES, THROUGH A PLATFORM REGISTRY (2026-08-30c, `verify-admin-projects-browser` 22 incl. three LIVE checks, teeth proven by two sabotages, migration 220 APPLIED 2026-08-30 (founder-confirmed; comment-only so not probeable via PostgREST, table probed unchanged at 0 rows), NOT browser-verified).** **Diagnosis first.** The legacy `projects` table (pre-migration-log, ZERO rows on prod) had exactly four readers/writers left: the browser API `/api/admin/projects` (list + archive + hard delete), a full CRUD `/api/projects` whose ONLY caller (`src/shared/hooks/useProject.ts`) was imported nowhere (a dead pair, but a live authenticated write surface), the CMS overview stat (showed 0), and `projectsCount` on `/api/user/profile` (declared in Settings' interface, rendered nowhere). To read `refm_projects` the browser needed: `archived` instead of `is_archived` (the legacy archive write would have ERRORED on the real table, its column does not exist), the owner embed (works, mig-149 FK), version counts, and no `platform` column (implied by the table). **The legacy actions were NOT safe to retarget as-is**: archive was one-way (no unarchive) and wrote the wrong column; hard delete would cascade a real model away behind a bare `window.confirm`. **Rebuilt, registry-first**: `src/shared/admin/projectSources.ts` (`PROJECT_SOURCES`) is THE one place a project table is named (key/label/table/ownerColumn/nameColumn/archivedColumn/versionsTable+Fk); the API iterates it (side-by-side rows normalized with platform badge, owner, archived, version count; `?platform=` filters; a failing source degrades to a note), mutations resolve their table through it so an id can never hit the wrong table, and ERM/BVM later = ONE registry entry, zero route or UI changes. Archive/unarchive are reversible and write the platform's own `archived` flag (the same one the user flow and the project cap read); hard delete requires the project NAME typed back, enforced SERVER-SIDE as well as in the modal (which names the project, owner, version count, and what the cascade destroys); all three actions audited with platform + name. The CMS stat and profile count were repointed through the registry (both had been reading 0 off the empty table); the dead `/api/projects` route + `useProject` hook are REMOVED. **The legacy table is NOT dropped**: migration 220 adds a guarded `COMMENT ON TABLE` deprecation recording why (a drop is irreversible; an empty unreferenced table is harmless), and the registry doc states it must never become an entry.
- **ADMIN USER LIST CLEANUP: THE PROJECT COUNT READ THE WRONG TABLE, AND THE PROJECTS VIEW NEVER WORKED (2026-08-30b, `verify-admin-users-cleanup` 22 NEW incl. two LIVE checks, teeth proven by two sabotages, NOT browser-verified; every change is a read, no number moves on any project).** **Diagnosis (1)**: the PROJECTS count in `GET /api/admin/users` embedded `projects(count)`, a LEGACY table that exists on prod with ZERO rows and is created by no migration in this repo; real projects live in `refm_projects` (mig 149), where meetahmadch@gmail.com holds 2, two other users 1 each. Every user read 0 because the count was taken from the wrong, empty table; it had never been right for REFM. **Diagnosis (2)**: the row's Projects button linked `/admin/projects?userId=<id>`, but that page never reads `userId` anywhere, and its `ProjectsBrowser` fetches `GET /api/admin/projects`, which queries the SAME empty legacy `projects` table (with archive/hard-delete actions on it). So it showed nothing for everyone, always; the per-user filter never existed. **Fixes**: the count now embeds `projects:refm_projects(count)` (aliased so the client shape is unchanged; verified LIVE that the embed equals a direct per-user count). The button opens a READ-ONLY `UserProjectsModal` (name, created, last modified, version count, archived badge) fed by new `GET /api/admin/users/[id]/projects`, which is GET-only, admin-guarded, and selects no snapshot content: an admin does not open or edit a user's model from here. The legacy `/admin/projects` Projects Browser page itself is left untouched and is recorded here as a still-broken legacy surface (queries the empty table; its delete actions can never find a REFM project). **(3) Duplicate columns**: the PlanBadge and StatusBadge that repeated the value of the dropdown beside them are REMOVED (the dropdowns are the single statement); the ACCESS column now renders ONLY when a row genuinely diverges from STATUS, which it can: ACCESS is the date-driven lapse state (shared gate helpers), so it differs when a trial/manual/canceled-Paddle expiry date has passed while the stored status still reads active (the gate enforces by date; the stored value only moves via cron or an admin), during the one-month read-only grace window (which STATUS has no vocabulary for), and when the durable cancel marker (mig 183) says canceling/canceled. On a page where no row diverges the column disappears entirely. **(4) Stale banner**: the static "Modeling Hub is in pre-launch lockdown" banner claimed only admins and whitelisted emails could register or sign in; both mig-136 flags are `false` on prod (the hub is live), so it was flatly wrong. It now reads the SAME two flag routes the auth pages enforce and renders only while a surface is actually closed, worded for whichever one is. **(5)** The never-asked real estate dash already carried the tooltip; it was undiscoverable (bare hyphen), so it now has a help cursor + dotted underline and names the state plainly.
- **REGISTERED USERS WHO NEVER REQUEST ACCESS NOW GET A CARD AND ONE REMINDER (2026-08-30, `verify-access-request` 36 NEW, teeth proven by two sabotages, NOT browser-verified).** Diagnosis on the live DB: 13 users, 11 confirmed, and **4 confirmed non-admin users sat on plan `none` having never filed a trial request** (no `trial_requests` row of any status, no subscription row), 3 of them for more than two days; the approval toggle is ON, so requests queue for review. The request button lived only on `/choose-plan` and the pricing cards, behind a slim dashboard banner, and NO user-facing surface read `trial_requests`, so after the one-time toast a pending request was invisible. Now: **(1) the dashboard leads with an access card** for `planKey === 'none'` non-admins ("Your access is not active yet", request button INSIDE the card posting the same `/api/refm/trial` action; instant grant refreshes the gate, a queued request flips the card to "Your access request is with the team", which also survives reload via a new `trialRequestPending` on `GET /api/refm/entitlements`, queried only for no-plan non-admins so entitled users pay nothing). A LAPSED user keeps a renew banner instead: their path is a plan, not a trial request. **(2) One reminder email, ever**: `runAccessReminderScan` joins the existing daily cron; the pure `isAccessReminderEligible` rule (confirmed 2+ days by `confirmed_at ?? created_at`, no plan, no trial request of ANY status, no subscription row, never admins) is exported and pinned; once-only is structural, the `subscription_email_log` key is (`access_reminder`, `once`, confirm-day) and the anchor never moves. The email links straight to `/choose-plan`. No migration needed.
- **ACCOUNT DELETION, BOTH SIDES, WITH THE SEMANTICS STATED (2026-08-30, `verify-account-deletion` 33 NEW behavioral against a fake client, teeth proven by two sabotages, migration 219 APPLIED 2026-08-30 and verified live by probe, NOT browser-verified).** Diagnosis: self-delete existed in Settings as a bare `users` delete relying on FK cascades; there was NO admin delete; a live Paddle subscription was never cancelled (Paddle would keep billing an account that no longer resolves); and the no-FK tables (`user_platform_subscriptions`, `subscription_email_log`, `manual_invoices`, `payment_transactions`, `trusted_devices`) silently orphaned. Cascades already covered the whole project tree, permissions, trial requests, AI counters and the 002/005 training tables; `admin_audit_log` was believed to SET NULL its target (WRONG, corrected 2026-08-30d: that was read from migration 007, which never took effect; the live FK is NO ACTION and BLOCKED deletes, see the 2026-08-30d entry above), which is in any case why a deletion audit cannot live there. Now ONE shared engine (`src/shared/account/deleteUserAccount.ts`) serves both routes. **REMOVES**: the users row + cascades, plus explicit cleanup of subscriptions rows, email log, trusted devices and the avatar file. **RETAINS deliberately**: `payment_transactions` and `manual_invoices` (deleting them would falsify recorded revenue and issued invoices), audit-log rows, authored articles, Training Hub roster records (separate identity), and a new `account_deletions` row (mig 219: raw-copy identity, deleted_by, timestamp, the optional message, `removed` jsonb). **REFUSES**: admin accounts (demote first); a live Paddle subscription without explicit opt-in, and when opted in it is cancelled at Paddle IMMEDIATELY first, with a cancel failure ABORTING the whole delete; and an admin deletion that cannot be audited (503 until 219 is applied; self-delete instead proceeds with a loud log so a user's right to delete does not wait on migration lag). Admin UI: Delete on each `/admin/users` row and a danger zone on the user record, both opening ONE `DeleteUserModal` that names the user, lists removed vs retained, takes the optional message (emailed via `accountDeletedEmail`), and arms only when the user's email is typed. Self UI: Settings now previews what is lost (project/version counts) and a live paid sub adds a required "cancelled immediately" checkbox feeding `acknowledgeSubscriptionCancel`. **Migration 219 was applied by the founder via the dashboard the same day and verified live by probe** (all 11 columns selectable, 0 rows), so admin deletions are fully unblocked; the session itself could not run DDL because `.env.local`'s `DATABASE_URL` is stale (tenant resolves in `aws-1-ap-northeast-1`, password fails auth), which still needs refreshing for future sessions.

## 2026-08-20

- **Platform infra**: project-switch state-leak fixed; session-based versioning + per-version change_log. **View/edit lock**: a project opens read-only (no version churn) until you click Edit, which presents an **Edit choice** (edit this version in place / edit a different version / create a new version) plus a mid-session **Save as new version** (`startEditInPlace` / `saveAsNewVersion` in `lib/persistence/module1-sync.ts`); schema-tolerant reads; paginated version history. **PDF full-project report** (`lib/pdf/generateProjectPdf.ts` on pdf-lib) mirrors every module tab via shared `lib/reports/` builders (m4/opex/capex/financing/cos), with per-module part picker + number-scale + **version picker** in `ExportModal` (file named after the chosen version). **PDF EXPORT REVIEW, three passes (2026-08-12, commits `2f546659` / `3cea19dd` / `76ed9e50`):** the equivalent of the workbook review, over BOTH PDFs at all three display scales. Pass 1 (arithmetic + signs): the shared cash-sweep waterfall ADDED interest and principal instead of subtracting them (both series are stored already-negative and the builder negated each twice, overstating every row below "Cash Available" by 2x interest + 2x principal, and breaking the tie to closing cash that the last row claims); the PDF FCFE build-up started from FCFF and re-added the terminal value; "new debt" had three figures under one name; per-asset economics reported ZERO cost and 100% margin on every asset (a sign-convention mismatch in the returns engine, ships on screen too); the summary P&L printed deductions positive beside a negative fund fee row so the column could not be added; and stock rows were summed across periods. Pass 2 (readability + scale): column geometry is now RESOLVED PER DOCUMENT from the widest cell it will draw, because a full-unit export ellipsised **3,867 numeric cells** and thousands 43 (millions stays 104pp; thousands and full go 104 -> 140pp, so wider numbers cost pages, never digits); three label-truncation bugs, two of them the SAME off-by-padding mistake (measure the text, then hand that width to a `drawCell` that subtracts 4pt a side); per-unit rates stay unscaled but now name their unit; and a value that rounds to zero at the displayed precision prints the en-dash instead of "(0.0)" (126 + 13 cells, fixed in the shared `formatAccounting` so screen and PDF move together). Pass 3 (consistency + missing content): one name per metric with its basis stated, gross-vs-net named on a fund project, a Model Integrity Checks table in BOTH documents, DSCR flagged as a covenant reading with a Lender Covenants table, the model version on the cover and every footer, derived Module 5 tab numbers, and Timeline / Land & Area / Fund Inputs / Sensitivity (entitlement-gated) added. **Two shared builders came out of it**, `lib/reports/streamReports.ts` (the FCFF/FCFE/dividend row lists, previously FOUR hand-maintained copies, which is why one drifted) and `lib/reports/checksReport.ts` (the three integrity identities + a RELATIVE, PEAK-anchored tolerance, now shared by both PDFs AND the workbook Checks tab). New verifiers `verify-report-arithmetic` **78**, `verify-report-readability` **55**, `verify-report-consistency` **59**, each with teeth proven by sabotage; toggle-off byte-identical at all three scales. **PASS 4, PRESENTATION (2026-08-13, six items over BOTH PDFs AND the workbook, `verify-report-presentation` 92):** every item was a figure the model computes correctly and a reader cannot interpret. (1) The fund waterfall printed Hurdle Paid's LIFETIME total (5,668.5m) directly under a Total Hurdle Owed row carrying no total and closing at 4,834.0m, so the column read as more paid than was ever owed; a shared `fundWaterfallTotalsNote` now names which rows are flows and which are balances and quotes both figures FROM THE MODEL. (2) **"Total" meant two things in one document**: the P&L used it for a lifetime sum while the balance sheet used it for the CLOSING balance (TOTAL ASSETS printed 2,512.1m, the at-exit figure) and the cash flow statement used it for both at once. The heading is now DERIVED from the rows via a new `M4Row.totalIsBalance` + `resolveTotalColumnKind`: `Closing` on the balance sheet, `Total / Closing` on cash flow and schedules, `Total` unchanged on the P&L. The classifier is shared with the summary PDF's hand-built rows (which had a parallel copy), proven by a sabotage that now breaks all three surfaces where it used to break only two. NOTE `drawPeriodTable` had hardcoded the literal `'Total'` and ignored `table.columns[1]`, so the builders alone changed nothing; and `resolveMetrics` now measures the HEADING as column content, since it is no longer a fixed 5-character word. (3) **A structural zero now says so**: two live assets reported 0 sqm BUA and zero cost with a 100% margin, both correct (an existing operational asset with 3,682.1m of historical pre-capex and no new build, and a companion whose area and cost sit on its parent). New shared `lib/reports/assetNotes.ts` marks the cell `- [a]` and footnotes the reason, on the composition table, the assets table, per-asset economics (cost, margin AND yield) and the workbook's Returns and Land & Area tabs. Detection is on the asset's DECLARED shape (`isCompanion`, `status === 'operational'` + historical spend), never on the zero itself, or a genuinely empty asset would be explained away; a real figure is never suppressed, and `takeFootnotes()` prints only the markers a table actually raised. (4) The three capital bases sat INSIDE the fee basis table with Timing / Rate (and in Excel, Base) empty and their amount in the column that holds a fee charged, so a quantity of capital read as a fee; they are now their own captioned block above it, tagged `capital base`. **Worth recording: the reported symptom was exact for the WORKBOOK only.** Both PDFs already placed the amount under `Basis charged on` (grid columns are per-table and content-sized, so nothing could shift it); the workbook has one `Total` column serving both the basis and the charge rows, which is where the ambiguity actually lived. (5) Distributed Equity IRR and MOIC appeared three times in a ten-page summary, twice as a BYTE-IDENTICAL card set (`headlineReturnCards` called on both the exec summary and the returns page); the returns page now points at them instead, and a shared `fundHeadlineRestatementNote` captions the fund page as the gross-vs-net split of the same metric rather than a third statement of it. (6) All of it confirmed in the FULL report and the workbook, not only the summary. Teeth proven by nine sabotages (8 / 5 / 19 / 2 / 2 / 1 / 1 / 1 / 4 failures); toggle-off re-proved, and the non-fund fixes are asserted to still render with the fund OFF so half a fix cannot pass. **NOT browser-verified.** **Excel MODEL export** (`lib/excel/buildModelWorkbook.ts`): a HARDCODED platform snapshot (every computed cell is the platform value written as a constant, editing does NOT recalculate, re-export after changing inputs), now a FULL module-for-module mirror of every module (Modules 1-6). 16 tabs in MODULE ORDER (Cover, **Summary**, Inputs, Timeline, Land & Area, Capex, Financing, Revenue, Opex, Schedules, P&L, Cash Flow, Balance Sheet, Returns, **Scenarios**, Checks); each output tab is a section-for-section mirror built from the SAME shared `lib/reports/` builders + snapshots the on-screen tabs use. **Cover + Summary + print setup (2026-07-14 cosmetic pass):** the Cover is a clean, module-grouped **Table of Contents** (numbered, hyperlinked, Module 1-6 + Reference bands + legend; shared front-matter primitives `frontMatterCanvas`/`Banner`/`Band`); a new **Summary** tab (2nd, `always`-part) is a one-page executive summary (key facts, a headline-metric tile wall, two side-by-side highlight tables Development economics / Returns & leverage, all in millions, computed from the same returns engine as the Returns tab). `applyPrintSetup` gives every sheet a print-ready A4 layout (fit-to-width, landscape wide tables repeating header rows 1:4, centred portrait one-page Cover/Summary, page-numbered footer). Inputs holds EVERY input grouped by domain band in module order (Project / Phases / Land / Assets / Sub-units / Returns / Capex, then FINANCING / REVENUE / OPEX INPUTS). **Scenarios tab = Module 6 mirror (2026-07-14):** built from the SAME shared `caseComparisonReport` + `caseYoYReport` that feed the on-screen Module 6 and the PDF: 1. Cases & Assumptions (every case + differing-lever grid), 2. Scenario Comparison (headline KPI matrix, delta vs base), 3. Year-on-Year Impact (each changed input + the per-period outputs it drives, base + each scenario delta). `ExportModal` threads the `caseComparison` bundle into `generateModelWorkbookBuffer` and shows the case picker for Excel too, **defaulting the statement tabs to the Management/base case while the Scenarios tab always compares ALL cases** (the user can pick another case for the statements). Degrades to a "no scenarios" note when no cases. Scenarios is an `outputs`-part sheet (hidden under an outputs-off filter). ONE standard navy palette (navy tabs + totals, deep-navy section bands, pale-navy subtotals, navy-pale inputs, red only for genuine check failures); the verifier locks the palette + tab order. **Export improvements (2026-07-28):** (1) **per-tab sub-TOC**, a nav strip at the top of every data tab listing that tab`s sections as internal hyperlinks plus a link straight to the basis-of-calc block and a "Back to Cover" link (reuses the `setSectionSink`/`sectionReg` + `{text,hyperlink}` mechanism the Cover ToC uses). Placement is structure-aware: rows are RESERVED below the frozen header on gridded tabs via a new `insertRowsAt` helper in `styles.ts` (ExcelJS `spliceRows` shifts values/styles but NOT merged ranges, which corrupts the file, so it unmerges, splices, re-merges shifted and rewrites same-sheet hyperlink targets), and appended at the bottom on the designed one-page canvases (Summary / Checks) whose merges span the insert point. ORDER is load-bearing: `applyTabGuides` -> `applyTabSubToc` (both re-base `sectionReg`) -> Cover/Guide last, so every deep link still lands on its section. (2) **palette LOCKED** in `ARGB` + a new exported `ALLOWED_FILLS`: the off-palette `#0C2340` and the `#1B3A6B` FILLS are gone (`#1B3A6B` survives as a text tone only, by decision), the sanctioned darker band is `#0D2E5A`, the sub-TOC strip is `#DDE7F3`, pass green is `#2E7D52`, and the FAST input fill `#E2EAF4` stays deliberately distinct; the verifier now asserts every fill is in `ALLOWED_FILLS`, the three forbidden fills are absent, and every section band is navy or sectionDark. (3) **basis-of-calc DEEPENED** (it already existed on all 15 data tabs since 2026-07-26; the gap was depth + burial): `TAB_GUIDES` is now `{kind:inputs|logic|feeds}` rendered as a labelled Inputs / Calculation / Feeds mini-table describing the real mechanics per tab, shared with the Guide tab. (4) **Timeline rebuilt** from 2 rows into a dated phase schedule (construction / operations windows + project envelope) plus a cell-based Gantt over the period grid, driven by the canonical `computePhaseTimeline` / `computeProjectTimeline`; ExcelJS 4.4 exposes NO chart API (`worksheet.addChart` is undefined) so a native chart is not possible and coloured cells are the fallback. Timeline columns B/C/D reclaimed from 3-char spacers to readable widths. Verifier `verify-excel-export` 187 -> **304**. Verifier `verify-excel-export` 159/159; two-way Sensitivity grid (Returns) is the one pending unit. **Platform guide REBUILT + guided tour (2026-08-20, detail in CLAUDE-REFM.md 2026-08-20g/h):** full-screen overlay with side nav + search, ONE content source (`lib/guide/guideContent.ts`) rewritten current that the 34-step guided tour renders VERBATIM (string-identity pinned), structure from the live registries via the pure `lib/moduleTabs.ts` (the frozen-copy verifier trap is closed; a new tab with no guide content fails the suite). Tour: auto-runs once per user (mig 217 blob + localStorage fallback, MERGING writes), module-only walk when no project is open, per-tab DOM emptiness skip, first-run guide prompt before the tour offer. **Trial exports (2026-08-20):** every PDF a watermark-listed plan exports (full, summary, IC deck) carries a server-resolved diagonal stamp + footer (admin control on /admin/plans, default ON for trial only, `white_label_pdf` untouched, paid exports byte-identical); the deck route now enforces Module 7 per plan server-side (403 FEATURE_NOT_INCLUDED); Excel gates on `excel_snapshot` (was `excel_formula`, false on every plan, which denied pro/firm an export they are sold; trial stays denied). **OpEx seeds ZERO (2026-08-20):** all 26 seeded builder values are 0, line items intact, stored-but-disabled seed values cleared on hydrate with user-typed values preserved, no number moved on any live project.

## 2026-08-19

- **SELLING COSTS BELONG TO REVENUE, DEPRECIATION STARTS AT OPERATIONS, AND A LOCKED BUTTON LOOKS LOCKED (2026-08-19e to 19h, four shipped blocks, BROWSER-VERIFIED on production).** Detail per block in [CLAUDE-REFM.md](CLAUDE-REFM.md) 2026-08-19e/f/g/h. **(Pass B, 19e) THE VIEW LOCK NOW COVERS BUTTONS, BY DECLARATION.** It covered `input`/`select`/`textarea` only, so the fund fee toggles were clickable in view mode, `setProject` no-opped and the click was discarded in silence (TRAPS 7.20, reintroduced). The rule for buttons is INVERTED from the rule for inputs and has to be, since nothing in the DOM distinguishes a mutating button from a collapsible: a button declares `data-view-mutates="true"` and gets `pointer-events:none`, a grey dashed treatment AND `disabled` (CSS cannot stop a keyboard Enter), while a check asserts the once-reverted blanket lock STAYS reverted. The split brain is gone: `saveFundTerms` is a network call that does not no-op, so a view-mode click had been writing the DURABLE row while the snapshot stayed put, measured live on MARINA GATE (durable `enabled=false, deficit` against a snapshot `enabled=true, equity`); the durable row was aligned TO THE SNAPSHOT, no chosen setting changed, and the snapshot is never written because it is versioned history. **(Pass C, 19f) `computeAssetCost` TAKES THE REVENUE SNAPSHOT** and resolves the bases internally; `collectionsTotal` / `saleRevenueTotal` / `totalRevenueTotal` are DELETED with their three helpers. Pass A wired six sites by hand, which fixed the numbers and left a twelfth free to forget; with ONE input a site either has revenue in scope or it does not. `core/calculations/revenue/sellingCosts.ts` (ZERO imports, so `src/core` consumes the snapshot structurally) owns basis resolution AND the multiplication, the engine does no arithmetic of its own, and Module 2 Revenue renders a **Selling Costs** section with the basis NAMED and held assets showing zero with the reason. `computeAllSellResults` keeps its inputs and reads no cost input, which is load-bearing because `computeAssetCapex` calls BACK into `computeAssetCost`. **(19g) DEPRECIATION STARTS WHEN THE ASSET IS AVAILABLE FOR USE**, not in the last construction year: `offset + cp - 1` is the M2 PIT revenue-recognition handover (verifier-pinned A2-1..A2-5) reused for a different question. **IT TOOK TWO PASSES because there are TWO streams**, the asset's own capex and the IDC capitalised into it, and `AssetPL.daPerPeriod` is their sum; fixing one left the other charging during construction and **my measurement script read only the first and printed "none" while the defect was live**. One shared `operationsStartIndex`, in PERIODS not dates (`computePhaseTimeline` defaults to MONTHLY without `modelType`, which collapsed the index to zero on the first attempt, caught by the fixture). **Measured: 5.791m + 0.654m and 14.294m + 0.076m charged early -> 0; P&L D&A 58.008m -> 51.562m and 1,614.267m -> 1,599.897m; closing NBV rises by the same amount to the cent, so it is conservation, not value lost; BS 0.00 throughout.** **(19h) SELLING COSTS YEAR ON YEAR** in Module 2 (Total column first, reading the engine's own `perLinePerPeriod`), and an **EXIT CAP RATE** terminal method derived as `r - g` unless typed, with a forward-income toggle for the `(1 + g)` step that had been buried inside the perpetuity branch. Terminal EV and both IRRs byte-identical on both projects. **A defect I shipped between the two and fixed the same day: the year-on-year table omitted `collectionsPerPeriod`**, so a selling cost degraded to an even spread and disagreed with Capex while the LIFETIME TOTALS matched (Marina Residences 2027: 1,253,606 against 114,380), and the registry entry I had written for that site said it did not need collections, which is TRAPS 7.26 committed into the registry built to prevent it. Verifiers: `verify-view-lock` 37 -> 57, `verify-selling-cost-scope` 44 -> 91, `verify-fixed-assets` 82 -> 99, `verify-returns-engine` 107 -> 123, `verify-capex-collections` 52 -> 66, every new section proven by sabotages that break the CODE.

- **THE RECONCILIATION KNOWS ABOUT DEDICATED EQUITY, AND EVERY REACHABLE CALL SITE VALUES A COST LINE THE SAME WAY (2026-08-19d, Pass A, `verify-capex-collections` 52 -> 63 and `verify-returns-buildup` 62 -> 66, teeth proven by reverting each fix, DEPLOYED at `2771650e` with health SHA matching HEAD).** Two defects, both introduced the same day by the two commits below it and both LIVE on production when found, fixed as a narrow pass on their own. **(1) A FALSE IMBALANCE.** `reconcile` compared `EquityMovement.totalCash` against `sum(split.equity)`, but cash equity now has TWO halves, the base development draw in `split.equity` and the management fee in `split.dedicatedEquity` drawn outside the ratio, so the check ran short by exactly the fee whenever the fee is equity funded: measured **61,153,555.75 against 52,485,540.76, a gap of 8,668,014.99**, the fee to the cent. It now compares against both halves, plus a new PER-PERIOD check that the halves sum to `cashPerPeriod`, because a lifetime total can agree while the timing does not and the halves are what schedule 9 renders. **Invisible because both live projects are set to `deficit`**, where the dedicated series is all zeros and the two forms of the check agree: the identity was broken and the suite stayed green. **(2) THE MODEL DISAGREED WITH ITSELF ABOUT CAPEX.** 19c wired FIVE of `computeAssetCost`'s ELEVEN call sites; the rest fell back to the old sub-unit product, so one cost line had two values depending on which surface asked. **Measured: financing aggregate vs per-asset Cash Flow, 432,082,531.35 vs 431,585,397.11 on MARINA GATE (497,134.24) and 1,625.87 on RE HUB; both now agree to 0.00.** Five sites wired (`financials-resolvers` per-asset CF, `fixed-assets-resolvers`, `capexReports`, `cosReports`, and `revenue-resolvers.computeAssetCapex` via an optional `revenue` argument); `financing-hooks.ts` deliberately NOT wired because `createFinancingHooks` is imported nowhere in src, app or scripts, and wiring unreachable code would be pretending. RE HUB keeps two PRE-EXISTING unrelated reconciliation issues (facility shares 33.33, a period-0 drawdown mismatch), unchanged. **The guard's shape is the lesson (TRAPS 7.26):** the call-site registry now carries TWO dimensions, `wired` for collections (which drive PHASING) and `bases` for the revenue bases (which drive the AMOUNT), because the single flag had `computeAssetCapex` recorded as `wired: false` with the reason "phasing cannot move a total", which is TRUE and was read as permission to omit the bases as well, which move it by over a million. **STILL OPEN, diagnosed in full and NOT built, preserved in [CLAUDE-TODO.md](CLAUDE-TODO.md): Pass B** (the fee toggles are plain buttons that the view lock does not cover, so a click is silently discarded, TRAPS 7.20 again; and the Financing toggle writes the durable row even when the store write no-ops, a split brain) **and Pass C** (marketing and commission computed and displayed in Module 2 Revenue, with `computeAssetCost` taking the revenue snapshot so no call site can forget a base). **Also logged, out of scope: depreciation starts in the last CONSTRUCTION year**, 5.791m early on MARINA GATE and 14.294m on RE HUB.

- **A SELLING COST APPLIES ONLY TO AN ASSET THAT SELLS, AND A SHARED RATE SAYS SO (2026-08-19, `verify-selling-cost-scope` 44 NEW, teeth proven by two sabotages, NOT browser-verified). NO NUMBER MOVES on either live project, measured to the byte.** Three symptoms reported from the Marketing line. **(1) "A rate typed on one asset appeared on another, and clearing it on the second removed it from the first."** That is what happens: a cost line belongs to a PHASE, and with no per-asset override an edit writes the LINE, which every asset in the phase reads. The design is not the defect; saying nothing about it was. The row now NAMES the other assets a typed value reaches and the Override button says so in its tooltip. **(2) Marketing was charged to Operate and Lease assets.** A commission and a marketing budget are percentages of a SALE, and a held asset carries its own operating expenses with no sale for them to be a percentage of. New `CostLine.assetScopeOverride` + `deriveAssetScope` scope the two selling lines to `Sell` / `Sell + Manage`, resolved from the BASE ID when nothing is stamped so every saved line gains it with no migration (the `stageOverride` precedence, for the same reason). Enforced in ONE place, `assetVisibleLines`, which the Costs tab, the engine and the copy planner all call, so an asset that cannot see the row is not charged for it and the tab STATES the absence; a row-level "Applies to" picker is the escape hatch. This is the reference structure (Schedules R40: its Marketing charges on Sale Revenue / GDV and phases on the sale-revenue curve). **Measured: total capex identical on both live projects** (426,406,950.00 and 4,912,199,955.98 before and after), because the one project with a marketing line carries a zero rate and the other had already been zeroed BY HAND on every operated and leased asset, which is the manual workaround this replaces. **(3) THE AMOUNT WAS WRONG AND IS NOW FIXED (second stage, same day).** `computeAssetRevenue` was the basis every percent-of-revenue method charged on and it sums `metricValue x unitPrice` over Sellable AND Operable AND Leasable; that product is a sale value only for Sellable, is ONE YEAR of rent for Leasable and ONE NIGHT at full occupancy for Operable. **Measured against the revenue engine: a leased asset understated 5x to 11x, a hotel 2,255x to 4,739x** (Hotel Phase 1, a 1.158m basis against 5,488.913m of revenue the model itself computes). **THE BASE NOW COMES FROM THE REVENUE MODULE and the RATE stays an input on the Capex tab**, which is the split asked for: `percent_of_revenue_sale` reads the sale value (GDV), `percent_of_total_revenue` the whole-hold revenue, `percent_of_revenue_cash` the collections it already read (new `saleRevenueTotalForAsset` / `totalRevenueTotalForAsset` beside the collections helper, wired at all five sites that already pass collections; revenue depends on sub-units and phases and never on cost, so there is no circularity). **ZERO IS NOT ABSENT and that distinction IS the fix**: a held asset has no sale value so its base is 0, where `undefined` would fall back to the broken product, which is why a sale-basis line charges zero on a hotel even with the scope forced OPEN and the two fixes are independent defences. The caption names the figure the engine used and, on a held asset, says "no sale value: this asset is held, not sold". **MEASURED: FMP - MARINA GATE total capex 437,844,950.00 -> 438,943,005.32** (+1,098,055.32, all of it Marina Residences' marketing moving from a 571.900m product to a 626.803m sale value, the gap being sale price indexation), **FMP RE HUB +1,625.87 on 4.9bn** (rounding scale: both its selling assets are Sell, so product and sale value already agreed). **A DEFECT I SHIPPED AND THE USER FOUND (same day, TRAPS 7.25): the scope missed the live marketing line.** `deriveAssetScope` resolved identity from the BASE ID alone, a SECOND identity rule that disagrees with the shared `resolveCatalogId` on the shape a real project produces: a line added from the catalog picker mints `custom-<timestamp>__<phase>` and carries its identity in `catalogId`, so its base id matches no selling id. The live row is `custom-1787123860417__phase_1`, catalogId "marketing", and it kept charging Podium Retail **119,700**, while the seeded `commission__phase_2` on the same project was correctly scoped: half the rule worked and the half on screen did not. **The verifier could not see it because its fixture used seeded ids exclusively, so both resolvers agreed on every line in it.** Fixed by having ONE implementation (moved into `selectedBase.ts`, calls `resolveCatalogId`, re-exported from `index.ts`), and pinned by a permanent section H carrying the minted shape, whose teeth were proven by REVERTING the fix (6 checks red, a held asset charged 9,600,000) rather than by mutating the fixture. **Also found: the Costs tab's row list was a FOURTH inline copy of the visibility filter**, which would have shown a marketing row the engine no longer charged (the mirror of the country-gate defect). Three guards earned their keep: `verify-cost-catalog` caught `assetScopeOverride` missing from the migration literal, which would have made it the FOURTH field dropped on every hydrate, and two source-shape checks pinned the old three-argument call and now demand the strategy. `verify-selling-cost-scope` **44 -> 64**, three sabotages (a fourth was deleted for proving nothing). Detail in [docs/TRAPS.md](docs/TRAPS.md) 7.23, 7.24 and 7.25.

- **THE FEE IS DRAWN FROM EQUITY, THE STATEMENTS FOOT, AND THE DDM HAS TWO HALVES (2026-08-19, `verify-returns-buildup` 51 -> 62 and `verify-cf-capex-foot` 63 -> 72, teeth proven by seven and four sabotages, NOT browser-verified).** Six items, read from the reference formulas. **(1) The fee funding toggle is on the FINANCING tab**, beside the funding method, which is where it decides whether the fee sits inside the deficit that method sizes; it was only on Fund Terms, which is why it could not be found. One field, both screens, written to the snapshot AND the durable row. **(2) The fee equity draw is CAPPED.** It was the whole fee in every period, so it drew throughout the project life; it is now drawn only while construction is SPENDING (the R116 gate) and only as far as keeps cash at the minimum after the base funding, so after construction the fee is paid from cash. Measured on the fixture: 5.079m drawn of a 17.478m fee. **(3) It is its own row, not part of the ratio.** `DebtEquitySplit.dedicatedEquity` and `EquityMovement.developmentPerPeriod` / `managementFeePerPeriod`; it renders as the THIRD row of schedule 9 Total Equity Required and in Equity Movement, and base equity is now exactly the ratio share of base debt with nothing folded in. **(4) The deficit schedule IS the reference schedule** (Schedules R105 to R124): cash capex, operating inflows BY ASSET CLASS (residential cash collection / hospitality EBITDA / retail NOI, with the fee as a line when the deficit funds it), pre-financing net cash, minimum, opening, the gated funding need split at the ratio, then the debt drawdowns (capex, IDC) and the equity drawdowns (development, management fee). **Every "NOT in sizing" memo line is gone**: each row is now in the arithmetic of the row beneath it. **(5) The in-kind pair is INSIDE the totals**, Land In-Kind inside Total Capex and In-Kind Equity inside Cash Flow from Financing, so both sections foot on the FULL cost, the two net to zero and Net Cash Flow is unchanged; they were memos outside the totals. **Interest Paid is ONE line per origin again** (Returns R65), the IDC / operating split being a classification that belongs on the Financing schedules. **(6) TWO DDMs, in the reference order** (Returns R114 to R139): the DDM before the performance fee with its IRR / MOIC, then the hurdle waterfall, then the DDM after the fee against the same equity investment with the post-fee IRR / MOIC (new shared `buildDdmPostFeeRows`). **THE FEE BASES NOW FOLLOW THE REFERENCE**: fund size is the SELECTED METHOD'S FUNDING REQUIREMENT split at its base ratio, not every draw the model ever made, so IDC drawdowns, the fee's own equity draw, in-kind land and pre-existing capital are all outside it; still resolved on the fee-free pass and FROZEN before the solver, so `fund_size_solved` stays forbidden. The 18c pro rata equity attribution (four memo lines ESTIMATING what a draw funded) is retired: the engine now supplies the real split.

- **THREE CORRECTIONS AGAINST THE REFERENCE (2026-08-18f, `verify-returns-buildup` 47, six sabotages, migration 215 APPLIED 2026-08-19 and verified live by probe).** **(1) The in-kind credit is REMOVED from FCFE**: built in 18e, never asked for. FCFF charges full land incl. in-kind and FCFE inherits it, as the reference does, so FCFE is the return on TOTAL equity. Equity IRR 109.63% -> **37.96%** (MARINA GATE), 14.37% -> **7.44%** (RE HUB); project IRR unchanged. **(2) The management fee funding toggle, at the ENGINE, both paths.** The reference has NO such toggle (its fee feeds the deficit at the project ratio); ours did neither path cleanly (18b added the equity draw AFTER the engine ran, so the fee was funded twice, and there was no UI). Now `fundTerms.managementFeeFunding` = `deficit` (default, reference, every project unchanged) or `equity`, which removes the fee from the deficit sizing and hands it to the engine as `dedicatedEquityByPeriod`, drawn as equity on top of the ratio split. Toggle on the Fund Terms tab, four mappers, fifth server tier (215). RE HUB equity path: net cash required 420,532k -> 354,839k, equity drawn exactly the 347,832k fee. **(3) Cash deficit funding, reported LINE BY LINE against Schedules R102-R124, NO change made.** Three differences, two of them the same thing: the reference keeps ALL finance cost out of the sizing (R112 has none, R123 measures IDC headroom from pre-interest R115 + R116) where ours subtracts cash finance cost from cash available AND feeds it into the IDC budget; and the reference gates the need on `cash capex > 0`. MARINA GATE identical either way; RE HUB 420,532k ours vs 263,780k reference rule, the gap being the existing hotel loan interest, which is 18d Item D, still HELD pending the operating-shortfall decision. Detail in [CLAUDE-REFM.md](CLAUDE-REFM.md) 2026-08-18f. **Not browser-verified.**

## 2026-08-18

- **Module 1 (Setup / Costs / Financing): CLOSED 2026-08-17** after **28 defects found by entering a real project end to end** (seven batches over one day, every one reported from the SCREEN rather than by a check), plus two more the new verifiers found themselves. Every fix is browser-verified against the live project and a throwaway, each is pinned by a verifier proven by sabotage, and every one that could move a saved number was measured before and after. **Three structural outcomes are load-bearing and are detailed in the bullets below: the country-gated RETT mechanism is REMOVED (no cost line can be present but invisible, and RETT is no longer seeded), VIEW MODE IS VISIBLY READ-ONLY (an input that silently discarded an edit is the same defect as a screen showing a number the model does not use), and the legacy migration no longer enumerates the fields it keeps.** The recurring shape across the 28: **one rule written in more than one place, with more than one answer.** Read [docs/TRAPS.md](docs/TRAPS.md) section 7 before touching Module 1: 7.12 (a fix applied to one copy leaves the others wrong), 7.15 (silent normalisation), 7.16 (a whitelist is the wrong shape for a migration), 7.19 (a gate that hides a row is a defect generator) and 7.21 (a list that mirrors another list diverges) are all from this block. **No further Module 1 changes before launch.** Base lock unchanged at M2.0 Pass 58. **Asset strategy changes are a MODEL operation (2026-08-11), not a field write** (`lib/state/strategySwitch.ts`, pure): the Strategy dropdown used to write one field, and since the engine gates on `asset.strategy` in every resolver, the old strategy's assumptions stopped feeding the model while the new strategy's never started. Measured: EVERY change dropped the asset's revenue to zero (a Sell asset switched to Operate took project revenue 14,055.0m -> 11,233.0m with no warning) and Sell <-> Sell + Manage was a model no-op. Two causes: `asset.revenue` is partitioned (.sell/.operate/.lease) but an asset carries only the one it was built with, and SUB-UNITS are not partitioned at all (one `category` each), so an Operate resolver counted ZERO keys and even a seeded config would compute nothing. Leaving Sell + Manage additionally HARD DELETED the companion asset with its sub-units, cost lines and cost overrides. Now: the outgoing strategy's sub-units, opex and companion are PARKED in `Asset.retainedByStrategy` and the incoming set is restored, or seeded from the outgoing one (names, counts and areas carry; the RATE does not, because a sale price is not an ADR); Support sub-units belong to no strategy and never move. Retention sits OFF TO ONE SIDE deliberately: `state.subUnits` / `costLines` / `costOverrides` still hold ONLY the active strategy's rows, so none of the ~40 engine and UI readers had to learn to filter. The dropdown now previews via a DRY RUN of the same pure function the store commits (so the dialog cannot promise something else), then a `strategyReview` banner persists across navigation until dismissed, because the empty assumptions live on other tabs. **Both the dialog and the banner are GATED on `assetHasStrategyAssumptions` (2026-08-15)**: an asset is created as `Sell`, so the user's first pick from the dropdown was technically a change and fired a four-item review on an asset with nothing on it. `needsReview` cannot answer this itself (it means "active but empty", which on a new asset is everything), so the gate is a separate pure predicate read from the state BEFORE the switch, shared by the store and the dropdown so they cannot disagree. The switch still RUNS when suppressed (Sell + Manage still seeds its companion). Detail in CLAUDE-REFM.md 2026-08-15. `verify-strategy-switch` **84 passed, 0 failed** (CORRECTED 2026-08-18. The recorded "54 passed + 1 PRE-EXISTING FAILURE" was an artefact of running it WITHOUT `--env-file=.env.local`, which the script documented but did not do for itself: it fell back to a fixture with no Sell + Manage asset, and EIGHT companion-retention checks silently did not run while the output read "56 passed, 1 failed". It now loads the env itself and seeds a Sell + Manage asset when the loaded model has none, so both paths exercise the branch; sabotage confirms deleting the companion instead of parking it now fails three checks that previously could not fire. See docs/TRAPS.md 10.10). Teeth proven by three sabotages (4, 12 and 7 failures). **Not browser-verified.** Funding Methods 2/3 gap-sized; conditional IDC + iterative fixed-point solver. Financing sub-tabs: Inputs / Schedules / Funding Gap / Cash Sweep. Dividends are ONE project-level after-debt policy (`Project.dividendPolicy` + `dividendStartYear`) with a terminal 100% payout in the engine.

- **THE DEFICIT SIZING RULE, APPLIED FROM THE REFERENCE FORMULAS (2026-08-18g, `verify-returns-buildup` 51, `verify-m4-bs-reconciliation` section Q rewritten).** Three rules from Schedules R112-R116 and R123, on instruction: **(a) no finance cost of any kind enters the sizing** (nor before-sweep dividends; interest is paid from the cash the project has); **(b) the `capex > 0` gate**, so once construction spend stops no development funding is raised and the forward walk carries the true cash; **(c) IDC headroom measured PRE-interest after the development draw**, where before the cash interest fed back into its own headroom. MARINA GATE unchanged in returns (26.30% / 37.96%), requirement 172,152k -> 182,642k. **RE HUB requirement 420,532k -> 263,244k and closing cash -230,355k -> -288,172k: the honest number**, the difference being construction funding that was servicing the existing hotel loan interest, which construction capital does not do; RE HUB was already negative before any of this. Supersedes 18d Item D (HELD, now applied and stronger) and makes the 2026-08-04 known issue BY DESIGN for RE HUB. Detail in [CLAUDE-REFM.md](CLAUDE-REFM.md) 2026-08-18g. **Not browser-verified.**

- **THE REFERENCE FORMULAS, READ DIRECTLY (2026-08-18e, `verify-returns-buildup` REWRITTEN to 39, teeth proven by six sabotages). REVERTS 18d ITEM A.** The reference model was placed in the repo and its formulas read (Schedules R100-165, Returns R93-109). Its closing cash ADDS THE TOTAL DRAWDOWN (capex + IDC) AND SUBTRACTS THE FULL FINANCE COST, so 18d's "interest is cash-only, IDC drawdown is a memo" was the opposite of the reference and is reverted: the whole charge is paid, the whole drawdown comes in, nothing is a memo. **The Finance Cost ledger was the thing at fault**: it deducted BOTH Paid and Capitalized from a payable, and once the charge is paid in full the capitalised figure is a DRAWDOWN, so it removed the same money twice (stuck at -21,525k). It is now Opening + Charge - Paid = Closing, footing at 0.00. **Four items:** (1) the reference line structure, ONE drawdown line, one repayment, and the finance cost as TWO genuine charges (IDC / operating), not the same movement three ways; (2) a construction period is where construction COST is incurred, verified on MARINA GATE 2030 (79,254k of cost, zero drawdown, 11,299k IDC), no change needed; (3) in-kind land CHARGED in FCFF and CREDITED in FCFE, both changed together, so FCFE is the return on CASH equity (equity IRR 109.63% / 14.37%, chosen with that number in view; the reference does NOT credit it, recorded as a deliberate departure); (4) the equity draw attributed across what it funded, ONE line whose total never changes plus a MEMO pro rata across capex / fund fees / operating shortfall / finance cost, summing exactly. **FCFE is FOUR STEPS** from the PRE-TERMINAL FCFF, no removal rows. Project IRR 26.30% / 7.15%. **The reference file was one `git add -A` from being committed** (root `.xlsx` was not ignored); root `.xlsx` is now ignored by default with the two tracked ones re-included by name. Detail in [CLAUDE-REFM.md](CLAUDE-REFM.md) 2026-08-18e. **Not browser-verified.**

- **THE FINANCE-COST CHAIN, SIX ITEMS ON THE REFERENCE STRUCTURE (2026-08-18d, `verify-returns-buildup` 37, teeth proven by five sabotages). CORRECTS THE BULLET BELOW, SAME DAY.** **(A) The Finance Cost ledger was removing the capitalised slice TWICE.** `interestPaid` had been set to the WHOLE accrued charge with the capitalised part treated as a matching drawdown: cash-neutral, and wrong exactly as M4 Pass 2P was wrong about capex and in-kind equity, both gross lines overstated by the same amount so the error cancelled in closing cash. The ledger's own invariant `Opening + Accrued - Capitalised - Paid = Closing` walked to a stuck **-21,525k** on MARINA GATE, and two definitions of cash interest disagreed by exactly that (waterfall 21,305k right, `interestPaid` 42,829k wrong). **`interestPaid` is CASH ONLY again**; the capitalised slice grows the balance and leaves as PRINCIPAL; the roll closes at **0.00** on both projects; and the cash statement stops presenting the IDC drawdown as a financing inflow. **(B) FCFF is UNLEVERED**: the IDC line added hours earlier comes OUT, matching both reference models. In-kind land STAYS (a real resource, not a financing charge). **Project IRR 25.25% -> 28.64% (3.113x -> 3.487x) on MARINA GATE, 7.04% -> 7.15% on RE HUB.** **(C) The whole finance cost flows through FCFE**, gross, with the IDC drawdown beside it netting to the cash actually paid. **FCFE does not move (41.75% / 6.453x, 7.44% / 2.129x)**, which is the guard: the gross presentation is only right if the capitalised slice returns later as principal. **(D) HELD, NOT APPLIED.** Sizing the deficit on the IDC half only would drop RE HUB's requirement 420,532k -> 263,244k and take closing cash to -276,696k / -288,172k. **RE HUB is ALREADY negative** (-230,371k measured at `0ce10096`), so the shortfall is pre-existing; item D would deepen it, not create it, and auto-funding an operating shortfall with construction capital is a separate decision. The split series are exposed; only the sizing is held, one line away. **This reopens the 2026-08-04 known issue for RE HUB**, whose earlier closure was correct for the fixture only. **(E)** Revenue and opex stay visible per asset, with a per-class subtotal struck from them (Residential Cash Collection / Hospitality EBITDA / Retail NOI), additive so the existing subtotals keep their meaning. **(F)** `fundTerms.managementFeeFunding`, absent means `debt` so nothing moves; `equity` draws the fee as its own equity line AND credits it as cash available, without which the fee is funded twice. Measured on RE HUB: equity drawn 0 -> 347,832k, net cash required 420,532k -> 354,839k. **Guards added: UNLEVERED PURITY** (no interest / IDC / finance-cost row in FCFF, on the row list and the arithmetic) **and THE FINANCE COST ROLL FOOTS** (closes at zero, cash-paid equals accrued less capitalised, capitalised non-zero so neither is vacuous). Detail in [CLAUDE-REFM.md](CLAUDE-REFM.md) 2026-08-18d. **Not browser-verified.**

- **FCFF IS FULL COST, FCFE CHAINS FROM IT, AND IDC HAS ONE TREATMENT (2026-08-18, `verify-returns-buildup` 30 NEW, teeth proven by five sabotages). MOVES PROJECT IRR ON BOTH LIVE PROJECTS, deliberately.** Three corrections matching the reference. **(1) FCFF deducts FULL-COST capex.** It was `CFO + CFI` with CFI on the CASH basis, so the unlevered return was measured against a cost base excluding the land contributed in kind and the interest capitalised while building; the card even claimed "less in-kind land contributed", which described the FCFE line. **Measured: project IRR 41.21% -> 25.25% (MOIC 4.909x -> 3.113x) on FMP - MARINA GATE, 11.31% -> 7.04% (2.401x -> 1.763x) on FMP RE HUB.** **FIGURES CORRECTED 2026-08-18: the first MARINA GATE measurement was taken against a snapshot the user then edited IN PLACE (version v2 is rewritten in place, so its content changes without a new version number). Re-measured before and after at ONE instant against the current snapshot; the deltas and the FCFE-unchanged invariant are unaffected. See TRAPS 5.1, which already records this for FMP RE HUB.** **(2) FCFE now builds VISIBLY from FCFF** (it was built in parallel, deliberately, because an earlier PDF chained from FCFF and forgot to back out the enterprise terminal), with the terminal swap as TWO VISIBLE ROWS. **The consistency trap is why this had to be one change:** FCFE carried its own in-kind row, so making CFI full-cost while leaving it would charge the sponsor TWICE for land contributed once, silently, and the same applies to the finance cost (FCFF absorbs the IDC half, so FCFE deducts only the OPERATING half). The guard is an algebraic identity, not a row count: FCFE must equal the equity holder's actual net cash rebuilt from the cash statement, and sabotaging either double-charge trips it by exactly the amount. **FCFE does not move on either project** (41.75% and 7.44% before and after), which is the proof the pair stayed consistent. CFO was ALREADY pre-interest and is now labelled so. **(3) IDC, one treatment.** It never reached `capex.perPeriod` (capex = cash + in-kind exactly) though it did reach CWIP; under `debt_drawdown` the balance rolled up with `interestPaid` left at ZERO, so the interest appeared in the cash statement NOWHERE. **The reported "no IDC paid in 2030": construction WAS still running** (79,254k of spend, Phase 1 cp=4 to 2030), and that year's interest WAS classified as IDC internally, and the defect was that **every row labelled IDC carried only the CAPITALISED slice** while 2030 was the one year the whole IDC was paid from surplus cash, so every IDC line read zero. Now the split follows construction SPEND rather than a phase-date window (RE HUB's 2030 gains 9,971k it was missing), IDC is PAID in the period it arises, debt is drawn only for what cash cannot cover, and the two drawdowns are separate lines. **Cash-neutral and proven: closing cash and the balance sheet unchanged, max |Assets - L&E| 0.00 on both.** Total drawn 142,031k against 142,031k repaid on MARINA GATE, the reference invariant that base plus IDC is all repaid. `idcConfig.capitalize` and `fundingMode` are RETIRED (a saved project carrying `capitalize:false, fundingMode:cash` is proven to compute identically); `allocationBasis` stays because it changes no project-level total. A found defect went with them: the Excel Inputs tab printed the funding mode defaulting to `debt_drawdown` while the engine defaulted to `conditional`. **The IAS 23 question is RESOLVED 2026-08-18, with NO engine change: the STRICT qualifying-asset test applies, so interest is IDC only to the extent it funds an asset still UNDER CONSTRUCTION, and a specific borrowing against a complete, operating asset stays an operating finance cost however much is being built elsewhere. Both live projects were already correct under it. The capex-share allocation was designed, measured and REJECTED (it swept 604,800k of a pre-existing hotel loan into IDC on RE HUB and peeled 9,971k onto the operating phase); `tranche.phaseId` is logged as an unusable attribution key and deliberately not fixed. Pinned by three new `verify-returns-buildup` checks proven by sabotage.** Detail in [CLAUDE-REFM.md](CLAUDE-REFM.md) 2026-08-18c. **Not browser-verified.**

- **A CASH FLOW SECTION MUST FOOT, AND ONE QUANTITY MUST HAVE ONE RECOGNITION RULE (2026-08-18, `verify-cf-capex-foot` 57 NEW, teeth proven by four sabotages plus a permanent counterfactual section). FIXES A REAL BALANCE SHEET IMBALANCE ON BOTH LIVE PROJECTS.** Reported from the screen: the Cash Flow's Investing section did not add up, `Total Capex` showing 366,407.0k under asset rows summing to 426,407.0k. **The 60,000.0k gap was the LAND CONTRIBUTED IN KIND**, and chasing it found a much larger defect underneath. **(1) The section now foots.** The subtotal has been the CASH basis since M4 Pass 2P (in-kind land never leaves the bank; it is recognised as Land and Share Capital at once) while the asset rows were the FULL cost, so two bases sat under one heading; the phase filter made it worse by using the full cost when filtered and the cash basis when not, so changing the FILTER changed the BASIS. `AssetCF` gained `landInKindPerPeriod`, the rows and both subtotals are the cash slice, and the in-kind land is stated on its own `(memo) Land In-Kind` row rather than dropped. `AssetCF.capexPerPeriod` is deliberately UNCHANGED and still the full cost, because inventory, per-asset returns and the IC report are the asset's CARRYING value, not its cash. **(2) THE BALANCE SHEET WAS OUT BY 25,000,000.** In-kind LAND is capitalised by the capex engine in the CONSUMING asset's window; in-kind EQUITY was stamped by a separate walk over the parcels at the OWNING parcel's phase. Those are the same quantity described twice, and they agreed only for as long as a parcel belonged to its phase: **since 2026-08-17 a parcel is PROJECT-WIDE**, so a Phase 2 asset drawing on Phase 1 land splits them. Measured live: equity recognised the whole 60,000,000 at t=0 while the land arrived 35,000,000 at t=0 and 25,000,000 at t=1, and `bsDifferencePerPeriod` was -25,000,000 in the construction year and zero after. `computeDebtEquitySplit` now reads `capex.perPeriod.landInKind` verbatim, with **no fallback** to the parcel valuation (land no asset capitalises is not on the Balance Sheet, so crediting equity for it is exactly the imbalance being removed), and its now-dead `phases` / `project` parameters are gone. **Measured after: max |Assets - L&E| is 0.00 on BOTH live projects** (FMP - MARINA GATE 25,000,000.00 -> 0.00; FMP RE HUB 360.79 -> 0.00). **That 360.79 was recorded in this file and in the fund guideline as a pre-existing solver-convergence residue and pinned as a non-defect; it was this, and both records are corrected in place.** The only figure that moves besides the imbalance is FMP RE HUB's total in-kind equity, by 400 currency units on 1.35bn, because it now equals the in-kind land actually capitalised. Also pointed the per-asset capex loop at the shared `phaseLocalToProjectIndex` instead of spelling the rule out again (TRAPS 7.12). **Why the suite stayed green:** every pre-existing fixture puts the parcel in the SAME phase as the asset consuming it, and the defect is unreachable in that shape; it also needs the later phase's land line off phase-local index 0, since index 0 clamps to `offset - 1` and makes the two rules agree by accident. Detail in [docs/TRAPS.md](docs/TRAPS.md) 7.22. **Not browser-verified**, but verified through the real shared row builders (`buildDirectCFRows` / `buildIndirectCFRows`, which the screen, both PDFs and Excel all render from) against both live saved snapshots. Note `verify-fund-fees` is **133 passed + 3 PRE-EXISTING FAILURES**, confirmed by stashing these changes and re-running; the long-recorded 136 was stale.

- **Entitlements & Pricing (LIVE, 2026-06-22)**: admin-controlled plan/feature system on `features_registry` + `plan_permissions` + `entitlement_plans` + `user_permissions` (migs 158-165). **Resolver** `src/shared/entitlements/resolveOverrides.resolveEffectiveFeatures` (plan coverage + per-user overrides, override-wins, expiry-ignored) is REUSED everywhere; the live REFM gate (`resolveUserGate` + `gate.ts`, reads the live `users` row not the JWT) enforces module/export/scenarios/save+versioning/branding/sensitivity, the project cap (active = non-archived, mig 161 `refm_projects.archived`), and trial expiry. Admin never blocked; unknown plan = access-preserving safety net. **Admin (one consolidated Users tab + Plan Builder)**: `/admin/users` (+ `/admin/users/[id]` = the merged former "User Access" panel: resolved entitlements, grant/revoke overrides, plan assign, trial) and `/admin/plans` (features + limits + price + Most-Popular/badge + non-module visibility + Trial **length (days)** + coupons). **ONE shared plan-setting path** `setUserPlan`. **Plans reconciled** to trial/solo/pro/firm (mig 160). **Pricing single source of truth = entitlement_plans** (prices mig 162, popular/badge mig 163, feature visibility mig 164, trial_days mig 165). **UNIFIED + path-based (2026-06-25):** ONE shared `app/pricing/PricingPageBody.tsx` (server-loads via `loadPricingCatalog`) renders the polished `PricingExplorer` (platform picker -> per-platform `LivePlanCards` + comparison) in BOTH contexts. The `PricingExplorer` island is SESSION-AWARE: logged-out keeps the `/register` handoff, logged-in runs in-app checkout/trial + resume (no separate marketing-vs-app design or URL). **Clean per-platform URL `/pricing/<segment>`** (`app/pricing/[platform]/page.tsx`): segment SOURCE-DERIVED via `platformPricingSegment`/`platformSlugForSegment` in `src/hubs/modeling/config/platforms.ts` (shortName lowercased, REFM->refm, slug fallback; unknown -> picker). Bare `/pricing` = picker. Dashboard "Get access" + live-platform marketing CTA both link the SAME `/pricing/<segment>`; old `?platform=`/`/modeling/pricing` redirect (next.config). hero/FAQ stay Page Builder; footer legal links publish-status driven. `platform_pricing` + `/admin/pricing` retired. **Payments LIVE (Paddle adapter implemented, sandbox):** provider-agnostic `src/shared/payments/` (adapters paddle[implemented]/paypro[stub], registry, config, signature); checkout route -> active provider adapter -> Paddle.js overlay (`paddleBrowser.ts`, client token only); webhook `/api/payments/webhook/[provider]` verifies HMAC + reuses `setUserPlan` (idempotent). `paddleEnv.paddleEnvMismatch` guards sandbox/live token mismatch; `/admin/payments` badge = client_token+webhook_secret (Paddle Billing has no api_secret). Verifiers: `verify-entitlement-gate` 92, `verify-payments-structure` 79, `verify-subscription-management` 204, `verify-trial-days` 7, `verify-pricing-display` 23, `verify-user-overrides` 13, `verify-footer-legal-links` 12, `verify-plan-builder-modules` 16, `verify-pricing-application-ready` **49** (DIAGNOSED and fixed 2026-08-18: the markup and the behaviour were always correct; the check demanded the literal `onClick={() => setSelected(p.slug)}` and the handler had been extracted to `selectPlatform(p.slug)`, so it was a refactor, not a regression. The originally recorded 49 was right all along), `verify-feature-description` 41. See [CLAUDE-ROUTES.md](CLAUDE-ROUTES.md) + [CLAUDE-DB.md](CLAUDE-DB.md) for the per-route/migration detail.

- [docs/FUND_LAYER_GUIDELINE.md](docs/FUND_LAYER_GUIDELINE.md), Fund layer (fund fees, hurdle, carry, gross vs net returns): scope, the linear-fee-base decision, the 7-step sequence, standing rules and known gaps. **Load it when touching anything fund; it is the source of truth and is kept current.** State as of 2026-08-11 (see section 8 of the guideline for the presentation corrections and the toggle-now-ON state): **Steps 1 to 7 LIVE, the fund layer is FEATURE COMPLETE for v1**, migs 208/209/210/211 APPLIED (Steps 4, 6, 6b and 7 needed none). **The ONLY remaining work is browser verification**, which Step 7 explicitly did not close. **Step 7 (end-to-end verify, 2026-08-10):** `scripts/verify-fund-e2e.ts` **86 checks, 0 failures**, run against the REAL project **FMP RE HUB** (3 phases, 8 assets, 14 periods) read from its saved version snapshot and NEVER written back; falls back to the fixture without DB creds and says so. **CORRECTED 2026-08-10:** an earlier version of this bullet said `refm_fund_terms` had ZERO rows. Wrong, and the error is worth remembering: the probe selected a column named `enabled`, the column is `fund_enabled`, the query errored and the empty fallback was reported without checking the error. **FMP RE HUB is CONFIGURED but DISABLED**, carrying a fully populated row written 2026-08-05 (structure fee 0.50%, management 0.25%, custody 0.50%, debt arranging 0.50%, other expenses 3m pa, 8% hurdle, 20% carry, named Fund Manager, 3-row fee matrix) with `fund_enabled` **false**. An empty export is therefore the CORRECT tested behaviour (it is exactly Step 1's fully-populated-but-disabled case), not a defect; flipping only that flag yields 403.844m of fees and 57 fund rows (P&L 17, Cash Flow 4, Returns 36). **SUPERSEDED 2026-08-11: the toggle IS now on.** `refm_fund_terms.fund_enabled` is true and the latest saved version (labelled 1.0, 2026-08-10) carries `project.fundTerms.enabled: true`, so FMP RE HUB exports a full fund workbook: 359.941m of fees, 5,651.850m distributed, ZERO performance fee (the 8% hurdle is never cleared, 1,477.007m unpaid at exit). Both earlier sentences in this paragraph, that the toggle had never been on and that every saved version predates fund terms, are now false and are kept only so the correction is legible. NOTE the project snapshot is edited in place by the user, so a fingerprint captured against it is NOT a stable baseline: use `buildExcelSampleState` for any before/after identity proof. Proves: toggle-off byte-identical across engine + screen builders + Excel + BOTH PDFs (terms fully populated but disabled, and separately at zero rates); the cash chain as an EXACT identity `CFO_on = CFO_off - fee + taxShield` (worst residue 2.4e-7 on 2,040m; **taxPaid is stored NEGATIVE so the shield ADDS**); **the freeze proven decisively**, the structure fee charging on the FEE-FREE fund size 5,466.799280m to 0.000e+0 while the with-fees figure is 5,626.859262m; waterfall conservation and hurdle roll-forward every period; post-fee IRR/MOIC recomputed independently to 1e-9; changing the hurdle leaving FCFF/FCFE/gross dividends byte-identical; and row order + totals agreeing across all four surfaces. **`ebitdaBeforeFundFeesPerPeriod` is NOT the standalone EBITDA** (5,021.992m vs 5,053.947m fund-off, a 31.956m gap): the fee raises funding, which capitalises +35.212m of IDC into Sell-asset inventory, which releases +31.956m to cost of sales. Not a circularity breach (fees are exactly 1,047.144m either way); compare two RUNS, not two fields. One non-defect pinned so it is not re-reported: waterfall `unallocated` is -9.5e-7 on 4,437m (2.2e-16, one machine epsilon). **A SECOND was pinned here and was WRONG: the 360.8-unit BS residue, recorded as pre-existing solver convergence because it was small and identical fund off vs on, was a REAL defect, fixed 2026-08-18 (see the Cash Flow bullet below) and now 0.00 on both live projects.** **The real project does not clear an 8% hurdle** (gross DDM IRR 4.38%), so the carry path only runs in the verifier's second cleared-hurdle case; keep such a case in any future edit or the section passes vacuously. **Step 6b (PDF exports + shared presentation builder, 2026-08-10):** the full PDF already had the fee lines / total / EBITDA-after / cash flow row free from the shared `m4Reports` builders but NO waterfall, gross-vs-net, fee income or basis block (the word "hurdle" appeared nowhere in the document); the **summary PDF had nothing and was actively misleading**, printing an `ebitdaPerPeriod` already net of fees with no fee line, so revenue less CoS less opex missed the printed EBITDA by exactly the fee (-35.98m vs -56.76m, 20.78m unexplained). Fixed by extracting `lib/reports/fundReports.ts` as the ONE definition of fund PRESENTATION (row order, labels, which rows are balances, gross-vs-net, earner grid, headline cards); the M5 screen, Excel, the full PDF and the summary PDF ALL render from it and the Step 6 inline Excel rows were DELETED rather than left as a copy. Formatting stays per-surface (each passes its own money/pct/mult); STRUCTURE is what must not drift. **Measurement note worth reusing:** pdf-lib embeds Inter as a CID font, so PDF text is hex GLYPH IDS and a naive grep silently returns nothing (a first diagnosis pass did exactly that and was worthless); `scripts/pdfTextExtract.ts` maps glyph ids back through the embedded font. The check that earns its place is `verify-fund-pdf` section 2: every surface must IMPORT the builder and re-declare NO row label, so two hand-maintained copies cannot pass. Toggle-off proven across all three exports at once by an 8,660-line fingerprint (Excel cells + fill + numFmt + `pageSetup`, plus both PDFs' decoded text), identical at HEAD and after. `verify-fund-pdf` **49**, teeth proven by three sabotages. Also repointed two `verify-fund-fee-income` checks from grepping the M5 component source to asserting the builder output (107 -> 108) and widened a hole in the `verify-fund-excel` toggle-off regex that did not match `Fund Management and Other Expenses`.   **Fee bases corrected to THREE DISTINCT bases (2026-08-10):** total equity 2,632.733m (cash + in kind + existing), debt facility 2,834.066m (existing + draws + capitalised IDC), fund size 5,466.799m = the sum, so a reader can add two and get the third. Management + custody charge on TOTAL EQUITY, structure on FUND SIZE, arranging on the DEBT FACILITY. **The arranging fee is NOT on the facility limit**: that resolves from an LTV cap to 4,273.822m vs 2,834.066m raised, a 1,439.756m gap, so it charged a ceiling never drawn; pinned that facilityLimitOverride and a stated principal do NOT move the fee. `totalEquity`/`debtFacility` resolve INDEPENDENTLY of fundSize (a fundSizeOverride zeroes its components) and the reconciliation display suppresses itself under that override. Interim step the same day, superseded: the two annual fees charged on OPENING NAV; the engine was correct (`fee_t == openingNAV_t x rate`, max single-period NAV 3,953.2m = 0.72x fund size, so NAV never exceeded capital) but the base did not match the model (which sizes a fund by TOTAL period debt+equity funding) and the DISPLAYED basis summed a balance over 14 years (41,752.1m vs 5,466.8m fund size, ~7x, reading as a fault). Both now charge on `fund_size`, the same frozen constant the structure fee uses: all three capital fees share ONE base, the annual fees are now charged in period 0 too (fund size is known at the start where opening NAV was zero), and the basis shows the per-period constant + period count ("0.50% of 5,466.8m, 14 periods"). `opening_nav` stays a legal linear base kind, unused. **Export fix (same day, found by a user export not a verifier):** `Project.fundTerms` only reached the store when the Fund Terms tab MOUNTED, so opening a project and exporting produced a file with no fund content and no warning; `attachToProject` now loads it before hydrate (and before the autosave subscriber, or it causes version churn), filling only an ABSENT value. ExportModal now checks the SELECTED version and warns plainly when it carries no fund terms. The stale "terms do not yet flow into the model" notice and module header (true at Step 2, false since Step 3) are rewritten. Known: the full PDF truncates the inline fee labels in its narrow label column (hence the columnar basis block), and the Excel Summary tab can no longer be one page on a fund project (accepted). **Step 6 (Excel export rows, 2026-08-10):** the diagnosis found MORE THAN HALF of it already shipped, because the Excel P&L and Cash Flow tabs render from the same shared builders as the screen and the PDF, so the five fee lines with rate + base inline, `Total Fund Management Fee`, the single EBITDA struck after it, the Step 5b Fund Fee Basis block and the `Fund Management and Other Expenses` row in operating cash flow all arrived free at Steps 3 and 5b. What was genuinely missing was the M5 fund surface: the Returns tab read `rs.waterfall` / `rs.resultNetDividends` / `rs.feeEarners` nowhere. Step 6 adds ONE section, `3. Fund Layer`, APPENDED rather than interleaved (interleaving would open a section band inside `1. Returns` and renumber RE Metrics on a fund project only), carrying gross-vs-net KPI tiles, the terms applied, a gross-vs-net table, the ten waterfall rows in the reference's exact order (the three BALANCE rows carry no lifetime total), fee income by earner with the unallocated remainder SHOWN, the shared basis table, and per-period fee income. `fundRow` is a SEPARATE emitter on purpose: the existing local `moneyRow` is shared with the FCFF/FCFE stream rows that already pass `style:'subtotal'`, so widening it would have moved rows on a standalone project. Rows stay CONSTANTS like their neighbours, so `verify-excel-export`'s pinned "zero formula cells" holds. **One real defect fixed:** the Excel P&L passes `fmt: String(v)` so a `totalOverride` round-trips to a number, and `fundFeeLineLabel` used that same fmt, so the workbook had been reading `1.00% of Fund size 154519446.40625` since Step 5b (Excel-only; the PDF passes `fmt.money`). `M4ReportCtx` gained an optional `labelFmt` defaulting to `fmt`. **Toggle-off PROVEN not asserted:** a full cell-level fingerprint (8,615 entries: value + fill + numFmt, all 17 sheets) captured before any Step 6 code is byte-identical after. `verify-fund-excel` **69**, teeth proven by four sabotages each caught by the right check. Known gaps: fund terms were absent from the Inputs tab (CLOSED 2026-08-11, see below), and the phase-filtered Direct CF shows the full project fee in every phase (pre-existing, so do Total Revenue Received / Tax Paid / Total Capex there). **Step 5b (fee basis display + MODEL-DERIVED fund size, mig 211)** reversed a decision this guideline defends, so read it before touching fee bases: fund size is no longer a typed input, it is resolved from the model (equity + debt, LIFETIME total) by `resolveFundSize` in the FEE-FREE pass and FROZEN before the solver, exactly as opening NAV has been since Step 3. The invariant is narrow: the base KIND `fund_size` is linear and allowed, the VALUE is resolved once and frozen, and `fund_size_solved` (a live solved figure read INSIDE the loop) stays in `CIRCULAR_FEE_BASES` and stays forbidden. `fundSizeOverride` pins a typed target instead, and the typed figure is still the fallback when the model raises no capital. Five invariants worth knowing before you read anything else: (1) toggle OFF is byte-identical to today, pinned by `verify-fund-layer-guard` against a fully populated but DISABLED block; (2) every fee base is linear and the rule is a DATA STRUCTURE (`FUND_FEE_SPECS` + `LINEAR_FEE_BASES` / `CIRCULAR_FEE_BASES`), so the verifier fails on the registry, not on a comment; (3) the fee schedule is FROZEN before the iterative solver, which is what stops funding raising the fees; (4) the Step 4 WATERFALL (`src/core/calculations/returns/waterfall.ts`, pure + primitives-only) runs over the M5 Distributed-Equity stream on the REFERENCE structure, rebuilt 2026-08-05 from an earlier three-tier shape: `accrued = (unpaidBoP + equityDrawn) x rate`, `owed = equityDrawn + unpaidBoP + accrued`, `paid = MIN(dist, owed)`, `excess = dist - paid`, `fee = excess x pct`, so there is NO return-of-capital tier (equity drawn folds into ONE hurdle balance), NO catch-up and NO residual split, and the fee is FLAT on the excess. The accrual compounds AND charges the same-period draw, collapsing to `owed = (BoP + drawn) x (1+r)`, so a draw held n periods owes `C(1+r)^(n+1)` and an investor paid exactly the hurdle earns MORE than the hurdle rate as an IRR (a stated accrual convention, not an IRR the payment reproduces). It is NOT in the funding solve (the fee splits cash that already left the project), which `verify-fund-waterfall` proves by holding the full financials snapshot identical across three hurdle/fee settings; gross streams and `result` are untouched, net rides alongside as `netDividendStreamPerPeriod` + `resultNetDividends`; (5) **Step 5 (2026-08-05) made it VISIBLE**: two toggle-gated sections on the M5 Returns tab (Fund Distribution Waterfall with gross-vs-net IRR/MOIC and the reference row order, balances carrying no lifetime total; Fund Fee Income per earner), plus `feeEarners` on the snapshot from a SEPARATE pure engine (`src/core/calculations/returns/feeEarners.ts`). Fee earners sit BESIDE the equity partners: `PartnersSection` and `computePartnerReturns` were not edited at all, the Fund Manager takes 100% of the five management fees plus its matrix share of the performance fee, and matrix shares are NEVER normalised (an 80% matrix allocates 80% and reports the remainder). `verify-fund-fee-income` pins that changing the matrix leaves the whole partners block and every gross stream byte-identical; note that all 556 pre-existing checks PASS when the Fund Manager is leaked into the partner roster (a zero-equity partner keeps Sigma partners == consolidated true), so only this verifier sees it. UI is asserted at SOURCE level only, a real render under tsx is blocked by the CSS-module import; (6) the ENGINE reads `Project.fundTerms` in the version snapshot, `refm_fund_terms` is the tab's durable store, and the server three-tier probes 210 -> 209 -> 208. **Ten-item export review (2026-08-11), items 1 to 9, all measured on the real project before any code changed:** (1) **Checks tab reported CHECK on a healthy model.** The tolerance was `maxBsDiff < 1`, an ABSOLUTE one-currency-unit band on a seven-billion balance sheet (1.4e-10 relative), so a residue of 360.792 at 5.1e-8 failed. It is now RELATIVE (1e-6 of the PEAK magnitude, not the worst period's value, because net cash flow crosses zero and produced "5.0e+0 of 0.0 m"), the status reads OK with the residue and its scale stated, and the other two rows, which were the string `'OK'` with an unrelated magnitude in the note column (one printed total capex, -3,561,517,930, beside a green OK), are now real comparisons: closing cash vs BS cash, and Direct CF vs Indirect CF. All three read the PLATFORM snapshot; `addChecks` no longer destructures `lm` at all. (2) **The workbook shipped TWO models.** Summary and the Returns tab's own "Returns Metrics" block read `lm.*` from `lib/excel/liveModel.ts`, the simplified twin left over from when this workbook emitted live formulas, whose IRR streams carry no inception outflow and none of the historical development investment: Summary said Project IRR **177.3%** / Equity IRR 38.3% / multiple 7.31x while the grid ten rows above said **10.9%** / 7.0% / 2.33x, and the Checks tab hyperlinked to the wrong one. Summary, the Returns metrics block and the Checks headline now all read `rs` (the platform returns engine) and every label names its basis. The third figure the review flagged, Distributed Equity 6.8%, was NOT an error: it is `rs.result.dividends`, a genuinely different basis. **liveModel is no longer a reporting source anywhere**; it still backs the formula-cached cells. (3) **Min DSCR 0.43x is REAL**, not a period artefact: per-period 0.56 / 2.17 / 3.19 / 0.56 / 0.43 / 1.52, three of six debt-service years below 1.00x, while ICR over the same years runs 0.56 to 25.63, so interest is covered and the amortisation is not. Flagged in check-red on the Summary highlight (with a sentence naming the count), on the Returns coverage tile, and per year in the DSCR row. (4) **A base is a STOCK.** The Total column summed the per-period bases, so an annual fee on total equity printed 36,858.3m against a 5,466.8m fund size on the Excel P&L, the Excel Returns tab AND both PDFs. `FundFeeBasisRow` gained `basisDisplay` + `basisIsPerPeriod` so the rule is encoded ONCE; the text surfaces render "2,632.7 x 14" via the shared `fundFeeBasisText`, Excel keeps a plain NUMBER in the cell (the display scale cannot reach text) and puts the period count in the row label. (5) **Raw-vs-millions was in the LABELS, not the values.** Every fee-basis cell was measured at `full` and `millions` scale in Excel and both PDFs and carries the same money format as its neighbours; no string cell anywhere holds a 7+ digit run. The real defect was `labelMoney`, hardcoded to millions regardless of export scale, so a full-unit export read "0.50% of Total equity 2,632.7 m" beside a cell of 13,163,667. It now follows `displayScale` (threaded through `EmitCtx.labelMoney`). (6) **Hurdle Accrued lost its lifetime total** (4,496.1m), joining the three balance rows. It is an accrual on a compounding balance printed between two balance rows. Noted for reversibility: the sum DOES reconcile (accrued 4,496.1 + drawn 2,632.7 == paid 5,651.8 + unpaid 1,477.0), so this is presentation judgement, not an identity. `FUND_WATERFALL_BALANCE_ROWS` is now `FUND_WATERFALL_NO_TOTAL_ROWS` (the old name is a deprecated alias). (7) **Gross == net is explained**, from a shared `fundGrossNetNote` that renders on the M5 screen, Excel Returns, Excel Summary and both PDFs and returns '' the moment a fee arises. (8) **FUND INPUTS band added to the Inputs tab**: the toggle, five rates, other expenses, hurdle, performance fee, Fund Manager, both override flags, the three resolved capital bases (marked computed, not input) and the un-normalised distribution matrix. (9) **The Cover claim was partly WRONG and is corrected here:** the Cover already listed "Fund Fee Basis" and "3. Fund Layer" as hyperlinked sub-entries; the first scan reported zero only because hyperlink cells hold `{text,hyperlink}` objects that stringify to `[object Object]`. What was genuinely missing was a top-level signal, now a "Fund layer active" marker on the identity strip, plus an exemption so fund sections can never be dropped by the 14-section-per-tab cap (they sit at the END of both P&L and Returns). Registering the Summary tab's front-matter bands as sections was TRIED AND REVERTED: every section link is an `!A<row>` anchor whose column A must hold the title, and a front-matter canvas keeps column A as a margin, so it broke the anchor invariant workbook-wide to gain three entries. **Toggle-off PROVEN on the FIXTURE** (the real project is edited in place and is not a stable baseline): 17,230 cells across 17 sheets at two display scales, both PDFs at 8,316 decoded lines, byte-identical except Checks / Returns / Summary, which are exactly items 1 to 3, and zero fund labels anywhere. **Basis block readability follow-up (2026-08-11b):** (a) the labels were being TRUNCATED by the 34-character label column ("Custody and admin fee: basis charged on (per period, 14 periods)" is 64 chars and rendered as "...basis charged on (p", hiding the very count it was added to show). Labels are now `<fee>: basis` / `<fee>: charged` (longest 34 of 34) and the period count moved to the **Base** column, which is 30 wide and held only "Total equity", so it reads "Total equity x 14". (b) The **Rate column was 2 characters** and could not overflow (the Total cell beside it is never empty), so "0.50%" was a sliver; widened to 10, and only when the fund block renders so a standalone project keeps its geometry. **ExcelJS trap worth remembering: `isCustomWidth` is `width !== DEFAULT_COLUMN_WIDTH` and that constant is 9, so a column set to exactly 9 is dropped from `<cols>` entirely and the width silently does not apply.** First attempt used 9 and did nothing; the verifier now asserts `>= 8 && !== 9`. (c) A **flat-amount fee is now ONE row**: its basis and its charge are the same quantity (3.0m per period charged on a basis of 3.0m per period), so the pair said the same thing twice. `FundFeeBasisRow.hasRate` carries that semantically rather than inferring it from `rate === '-'`; rate-based fees keep both rows. **A reported defect that was NOT one:** "Other expenses prints raw 3000000 while neighbours are in millions" was investigated to the byte (file round-trip, raw sheet XML, styles.xml numFmtId resolution, shared-strings scan) and the cells carry style `s=105` -> `numFmtId=166` -> the millions format, IDENTICAL to every neighbouring fee row; the reporter was reading stored values programmatically rather than the rendered cell. Recorded so it is not re-investigated. `verify-fund-excel` 70 -> **75**, teeth proven by four sabotages (no collapse, collapse everything, long labels, width back to 9). `verify-excel-export` 301 -> **304**, `verify-fund-fee-income` 107 -> **109**, `verify-tab2-fixes` 47. Verifiers `verify-fund-layer-guard` 75, `verify-fund-terms` 182, `verify-fund-fees` 138, `verify-fund-waterfall` 382, `verify-fund-pdf` 49, `verify-fund-e2e` 86

## 2026-08-17

- **THE CAPEX TABLE IS AN ORDERED, USER-CLASSIFIED LIST, and a parcel is PROJECT-WIDE (2026-08-17, `verify-capex-structure` 115, teeth proven by four sabotages, BROWSER-VERIFIED on the live project and on a throwaway one).** Thirteen defects found by entering a real project end to end, treated as one piece because the last five change the same structure. **(1) A parcel is project-wide.** The explicit parcel lookup was scoped to the asset's own phase in the UI AND in three engine sites, so a Phase 2 asset drawing on the Phase 1 land resolved `undefined` -> rate 0 -> land value 0, silently, and it was reachable by doing nothing: `handleAddAssetToPhase` seeds `parcelId` from `phaseParcels[0] ?? parcels[0]`, and that fallback crosses phases, so the asset was BORN holding a reference the lookup could not resolve. Only the EXPLICIT lookup widened; the implicit rules (autoByBua / percent / equal share) keep their phase scoping, so nothing that was resolving moves. **(2) Weighted average has two scopes**, `__weighted__` (this phase, unchanged) and the new `__weighted_all__`, and every option in the dropdown now prints the rate it resolves to, which is what "shows no rate" actually meant. **(3) No silent zero:** `computeAssetLandBreakdown` returns `rateIssue` (`parcel_missing` / `zero_parcel_rate` / `no_parcels_in_scope` / `zero_custom_rate`) and the card prints the reason beside the number. Third of this family after the allocation-share zero and `Number(null)`. **(4) The period window follows the phase.** `deriveCostWindow(baseId, cp)` is the ONE definition, used by the seed and by the store's re-derive on a phase-length change; seeded lines carry `windowFollowsConstruction` and typing a Start or End clears it. The 1-to-25 the user saw was ONE caller: `buildWizardSnapshot` called `makeBlankCostLines(p.id)` without the phase length, so the parameter default of 24 applied to every wizard-created project. `repairStaleWizardCostWindows` repairs existing ones, running on the RAW snapshot BEFORE the chain (see TRAPS 7.8) and recognising the clamped shape; measured on the live project, 8 lines repaired and the hand-set phase untouched. The default now ends AT `cp`, not `cp + 1` (that buffer made every seeded line render a permanent "extends into operations" warning). **(5) A followed source owns the window:** `AssetCostBreakdown.resolvedWindowByLineId` reports what the engine spent on, and the row renders THAT instead of two editable boxes, so marketing shows "from collections" or "no collections yet" rather than a construction window it does not use. **(6) Stage is a per-line choice** via a new `stageOverride` (NOT a write to `stage`: the id map outranks `stage`, and that precedence is how the marketing reclassification reached saved models with no migration). `byStage` and the `percent_of_construction` base now read `deriveCostStage`, which also settles `pre-operating`, seeded soft and mapped operating since the map was written (see TRAPS 7.10). **(7) Order is editable:** `insertCostLineNear` + `moveCostLine` (within the phase, swapping with the row the user can SEE, since a stage filter can hide the array neighbour), and a line whose selection has fallen below it says so on the row. Case overrides are id-keyed so a reorder is safe, and `snapshotsEqual` is `JSON.stringify` so a reorder is detected and saved. **(8) Catalog order** is land -> hard -> soft -> **developer fee** (new, zero rate, charging on the hard and soft lines above it) -> contingency (its base now includes the fee) -> **marketing LAST**, which by the positional rule puts marketing OUTSIDE the fee and contingency bases: a deliberate departure from the reference, chosen because a visible positional rule beats a hidden exception. NEW SEEDS ONLY; reordering saved projects would change their numbers unasked. **(9) `% of selected lines` shows its base** (`selectedBaseByLineId`), which the caption had always been able to print and had always been handed `undefined`. **(10) The `InputLabel` help icon leaves the tab order** (`tabIndex={-1}` + `title`): the label renders before its input, so every second Tab landed on an icon and popped a tooltip over the next field. Measured after: 54 icons on the page, 0 tab stops. **(11) A sub-unit area typed before a unit size is HELD, not discarded** (the Pass 57 no-op protected a hand-typed count and silently threw away the area), and converts to keys the moment a size arrives.

- **NO COST LINE IS PRESENT BUT INVISIBLE, AND A LOCKED INPUT LOOKS LOCKED (2026-08-17c, `verify-no-hidden-cost-lines` 53 NEW (replaces `verify-country-gate`), `verify-view-lock` 37 NEW, teeth proven by four sabotages, BROWSER-VERIFIED). NO NUMBER MOVES on either live project.** **(1) THE COUNTRY GATE IS RETIRED.** `CostLine.requiresCountry` let a line exist in the model while being absent from the screen, and that one property produced two silent money defects in a week, in opposite directions: the engine CHARGED a hidden transfer-tax row (937,500 on a Phase 2 hotel, on top of the user's own line), and once the gate was honoured, SELECTING A COUNTRY made that row appear and charge, doubling a cost already entered by hand. The fix for a defect on both sides of a gate is to remove the gate. RETT is **no longer seeded** (the seed is 12 lines, not 13); it is a catalog entry the user adds when the project needs it, and `stampFromEntry` puts `phasingSource: 'land_cash'` on the line so it still lands with the land cash. `assetVisibleLines` no longer takes a country: visibility depends on phase and target asset and nothing else, so **shown is charged**. `retireCountryGatedLines` (raw-snapshot repair, runs on every hydrate) clears saved rows with TWO cases and neither moves a number: not chargeable today -> REMOVED with its overrides (it was invisible and contributing zero), chargeable today -> KEPT with the dead flag stripped (it is already charged, so removing it would delete a live cost). **Measured on both live projects: total capex identical to the byte** (FMP - MARINA GATE 426,406,950 before and after, lines 23 -> 21; FMP RE HUB unchanged, it never had a gated row). `requiresCountry` stays on the type, DEPRECATED, so the repair can find it. The country select stays (it drives statement terminology and records where the project is) but creates and activates nothing. **(2) VIEW MODE NOW LOOKS READ-ONLY.** A project opens read-only until Edit, and the lock lived only at the store's model mutators, which no-op SILENTLY: an input accepted a change, showed it, and discarded it on the next render. That is how the phasing curve appeared to "revert". The panel-level `pointer-events` lock was tried once before and removed because it killed collapsibles, selectors and view toggles, so the lock is on the CONTROLS: `input` / `select` / `textarea` get `pointer-events: none` plus a muted dashed not-allowed treatment (globals.css, `!important` because these carry inline styles), buttons and rows are untouched, and capture-phase handlers swallow every key except Tab, Escape and the copy shortcuts (CSS cannot stop typing into a Tab-focused field). `data-view-editable` opts out the four controls that filter a view rather than change the model (Capex stage filter, the two sensitivity axes, the Module 6 field finder). **The store lock and the screen lock are now ONE derivation** (`modelLocked` -> `viewLocked`), so a field cannot look editable while the store refuses the write. Module 7 is exempt: the deck carries its own state and its own save. Browser-verified: typing into the project name while locked leaves the value untouched, the stage filter stays live, tabs and expand-all still work, and after Edit the same field takes the text. Detail in [docs/TRAPS.md](docs/TRAPS.md) 7.19 (a gate that hides a row is a defect generator) and 7.20 (a control that accepts a change and discards it is a lying screen).

- **A COUNTRY IS A SELECTED VALUE, AND A SAVED FIELD MUST SURVIVE BEING LOADED (2026-08-17b, `verify-country-gate` 15 -> 51, `verify-snapshot-field-survival` 27 NEW, `verify-capex-phasing` 84 -> 91, `verify-capex-structure` 126 -> 133, teeth proven by six sabotages, BROWSER-VERIFIED on the live project and a throwaway).** Four screen-found defects. **(1) `Project.country` had NO EDITOR ANYWHERE**, on any screen or wizard step, while two behaviours read it: the `requiresCountry` cost-line gate and the default statement terminology (Zakat). The visible field is `location` ("free-text city / country / region, display only"), which is where the user had typed "Jeddah, Saudi Arabia" while `country` sat at its default `''` on every project this app has created. So the gate could never match and the Capex tab correctly said the country was not set. **The earlier-flagged risk (a gate matching free text against the literal 'Saudi Arabia') was NOT the cause**, and fixing that would have left the field unreachable. Now a `<select>` on Project & Phases writes an ISO code from `src/core/countries.ts` (249 entries) and ONE comparison, `countryMatches`, resolves BOTH sides, so a line saved as 'Saudi Arabia' matches a project storing 'SA' with nothing migrated either side and an unrecognised string still comparing as text. The location can offer its country as a ONE-CLICK suggestion (`guessCountryFromLocation`, matched on comma-separated parts so a substring cannot resolve); it never infers. **Closing a gate opens its mirror:** users added their own copy of the line they could not see, so setting a country now DOUBLES that cost. Measured on the live project: a user RETT line at 5% charges 1,375,000 (phase 1) and 937,500 (phase 2), and the seeded `rett__phase_2` carries a stale 5% that goes live the moment a country is set. So the country field names what it switched on, and the Capex tab carries a `costs-duplicate-charge-notice` keyed on CATALOG IDENTITY plus phase (the two rows are named differently), stating the duplication and deleting nothing. **(2) RETT was not following the land cash.** The shape a `land_cash` follower follows was built by scanning for `method === 'percent_of_cash_land'`, which is ALSO RETT's own method (a transfer tax is a percentage of the cash land value), so RETT was half of the shape it was following. Measured: phase 2 land cash sits entirely in period 1, and RETT resolved across 1..3 as 625,000 / 156,250 / 156,250; it now resolves to 1..1 as 937,500. `isParcelDrivenLandLine` is the one definition of which lines are the land value and its own docstring warns that method is the wrong discriminator; the site now calls it. **(3) THE ASSET PHASING CURVE WAS SAVED AND THEN DESTROYED ON READ.** `migrateLegacyToV8` rebuilt every phase, parcel, asset and cost line from a literal naming a FIXED set of fields, so `asset.capexPhasing` was dropped on every open; the database row really does carry it. **Third field lost this way** (after `windowFollowsConstruction` and `phasingSource`), and the guard added last round covers `CostLine` only. Fixed by SHAPE, not by a fourth name: every record spreads the raw object and normalises on top, which also makes the loose path agree with the v8 path (which has always spread and runs the identical chain). Note every app-saved snapshot takes this path, because `extractPersistSnapshot` writes no version marker. **A second live defect fell out of the new verifier:** the v6 rename map turned `marketing` into `commission` and `project-management` into `professional-fee`, and both are CURRENT catalog ids (2026-08-15 and 2026-08-17), so a marketing line saved by a new project was renamed and then deduped away on the next open. Both keys removed and the invariant checked: no rename key may be a catalog id. **(4) Area-mode sub-units had no unit size**, so the area x unit size = count identity was only enforceable in Units mode (three of the four sub-units on the live project are Area-metric). Area mode now takes a Unit Size and derives a read-only count; the stored quantity is unchanged (`metric` stays 'area', `metricValue` stays the total sqm) and the whole cost breakdown is proven byte-identical with a unit size set, because `computeSubUnitArea` and `makeSubUnitMaterial` both branch on `metric` and never read `unitArea` there. Also noted, not fixed: in VIEW mode every input is live-looking and silently discards the edit (`MODEL_MUTATORS` no-op on `viewLocked`). Detail in [docs/TRAPS.md](docs/TRAPS.md) 7.16 (a whitelist is the wrong shape for a migration), 7.17 (a legacy rename must never target a line that exists today) and 7.18 (a field with no editor is not a default, it is unreachable).

- **A FACILITY SHARE IS USED EXACTLY AS TYPED (2026-08-17, `verify-facility-shares` 29 -> 55, teeth proven by restoring the rescale (12 failures reproducing the reported 66.67 / 33.33), BROWSER-VERIFIED). MOVES LIVE NUMBERS, deliberately.** `normaliseFacilityShares` RESCALED: any share set whose sum was not 100 was divided through by that sum, so typing 100 into the first of two facilities (the second still at 50) produced 66.67 and 33.33 on every recompute, one facility could never carry the whole requirement, and the card printed "Normalised: 66.67%" under the typed 100, presenting the divergence as a fait accompli. A second defect in the same function: ONE missing share triggered an equal split across ALL of them, so a deliberate 70 became 50 because a neighbour had no value. And `reconcile` already carried `Facility shares sum N (expected 100)`, a check that **could never fire** because the function it guarded guaranteed 100 by construction. Now `resolveFacilityShares` is verbatim (floored at zero); the equal split survives ONLY when no share is typed anywhere (nothing to override); an absent share beside a typed one resolves to zero, which is what makes "set one to 100 and the others go to zero" work; and a wrong sum is STATED, by the reconciliation and by a banner naming the direction ("raises more debt than the project needs" / "leaves part of the requirement unfunded") with a one-click explicit repair. **Consequence to expect on any project whose shares do not sum to 100: the drawdown changes.** Measured live, a project carrying 100 and 50 drew 214,852 under the rescale and draws 322,279 (150%) as typed, with the banner explaining it and the button fixing it in one click. Detail in [docs/TRAPS.md](docs/TRAPS.md) 7.15, whose general form is the point: **normalising an input behind the user's back is worse than accepting it or rejecting it.**

- **TWO IDENTICAL SENIOR DEBT TABLES ARE NOT A DOUBLE COUNT (2026-08-17, `verify-facility-shares` 29, teeth proven by two sabotages, BROWSER-VERIFIED). NO NUMBER MOVED, and that is the finding.** The Financing tab rendered the Senior debt schedule twice with the same opening, drawdown, IDC and closing, and Combined Debt Service showed 214,852 against 107,426 in each, which reads exactly like the RETT defect. **It is not.** There are genuinely two tranches (the wizard seeded one PER PHASE) and a facility's schedule is PROJECT-WIDE: since Pass 28 (2026-05-14) the engine deliberately stopped windowing a tranche to `tranche.phaseId`, because a bank funds drawdowns in every phase and the interest on all of them is IDC. So a facility draws the project debt requirement TIMES ITS SHARE, and `normaliseFacilityShares` splits equally when no share is set. **Measured: 107,426,203 x 2 = 214,852,407 = the debt requirement exactly, and collapsing the two facilities into one leaves capex drawdown, IDC, interest expensed, principal repaid and the requirement byte-identical.** The real defect was seeding one facility per phase for an engine that finances the project as a whole, so a per-phase facility was an unlabelled 50% share. Now: the wizard seeds ONE facility (new projects only, the schema is unchanged and `phaseId` stays because `removePhase` cascades on it); every facility table names its share when there is more than one; and a note states that identical schedules are shares of one requirement, that the combined is their SUM, and that deleting all but one is number-neutral. The note keys on the SYMPTOM (more than one new facility with identical draw schedules), not on "no share is set", because the live project carries explicit 50 and 50. Detail in [docs/TRAPS.md](docs/TRAPS.md) 7.14, whose lesson is the point: **the fix for a genuine double count and the fix for a split presented badly are opposite actions.** **Alongside it: two project-total tiles on Development Costs**, Total Project Cost Incl. Land and Total Project Cost Excl. In-Kind Land, derived from `COST_STAGES` (so a future stage cannot be silently left out of the headline) with the in-kind slice read from the engine's own `perPeriodLandInKind`. They are the same two measures the Capex summary tables report as "Incl All Land" and "Excl Land In-Kind", so the tile bar and the schedule cannot disagree: verified live at 429,932 and 369,932 against a 60,000 in-kind land slice.

- **A COUNTRY GATE MUST STOP THE MONEY, NOT JUST THE ROW (2026-08-17, `verify-country-gate` 15, teeth proven by one sabotage, BROWSER-VERIFIED). A WRONG NUMBER, now corrected.** `CostLine.requiresCountry` gates a country-specific line (the catalog's RETT). Three input-side filters in the Costs tab honoured it; `computeAssetCost` built its own list filtering on phase and target asset only, so a gated line was **CHARGED while invisible, uneditable and undeletable**, and the same function respected the gate for the percent-of-selected base thirty lines below. Reaching the broken state takes nothing unusual: type a rate while the country matches, then change the country. **Measured live: the Phase 2 hotel was charged 937,500 for a RETT row it did not show, on top of the user's own RETT at the same rate, so the transfer tax was double-charged into the financing schedule, the statements, the returns and both exports; the asset subtotal falls from 146,889 to 145,952 exactly.** Now the engine builds its list from the shared `assetVisibleLines`. Because closing a silent overcharge opens the mirror image, the tab now states any gated line that carries a rate it cannot use. Detail in [docs/TRAPS.md](docs/TRAPS.md) 7.13. **Alongside it: (1) COPY IS A PURE PLAN** (`lib/state/costCopyPlan.ts`), extracted from the JSX click handler that made it untestable (which is how "copy reproduces the line set" passed as a source-string grep while the live project still did not do it), and **both sides now use the same visibility rule**: the source list dropped country-gated lines while the target list counted them, so a hidden `rett__phase_2` matched the user's own RETT, nothing was created and the override landed on a row they cannot see. Verified by running the planner against the live line sets: all 10 source lines map to real counterparts, including "Permits & Approvals" to "Permits and Approvals" by catalog id despite the different names. **(2) AREA x UNIT SIZE = COUNT, so only two of the three are inputs.** All three were editable and each edit rewrote another, so typing a count silently rewrote the area. Area and Unit Size are authoritative, Count is DERIVED and read-only, and the one exception is the same rule: with no unit size there is nothing to divide by, so the count becomes the input (the existing-operations case Pass 57 added). A count is a whole number, so an area that does not divide evenly SNAPS, and the row now says "you entered 13,300; 160 x 83 = 13,280" instead of silently changing a number the user typed.

- **THE Y0 UPFRONT LUMP HAS ONE PLACEMENT RULE (2026-08-17, `verify-y0-lump-placement` 23, teeth proven by two sabotages, BROWSER-VERIFIED).** A cost line's phase-local index 0 is the upfront lump (land cash, land in-kind, RETT) and belongs in the period BEFORE the phase starts, clamped to the first project period for a phase starting with the project. That rule was written out in FIVE places with TWO answers: M4 Pass 2W (2026-05-24) clamped it in the financing aggregate and the equity engine, and the Costs tab's own schedule kept `if (offset > 0)`, which added a PHASE 1 lump to **no column at all**. Reported from the screen as "Phase 1 land shows a total but no period allocation, while Phase 2 phases correctly". **Measured before the fix: 70,000,000 of phase 1 land, present in the model, in the financing schedule and in the capex table's Total column, and absent from every period column of that table**, so the row did not foot; and one of the three tables accumulated its row total INSIDE the dropped branch, so it under-reported rather than merely misplacing. Now `phaseLocalToProjectIndex` in `capexPhasing.ts` is the one definition and all five sites call it. The financing docstring, which described the pre-Pass-2W behaviour and claimed the two surfaces matched, was false on both counts and is rewritten. Detail in [docs/TRAPS.md](docs/TRAPS.md) 7.12.

- **A COST LINE IS A CATALOG ENTRY PLUS A LABEL (2026-08-17, migration 214, `verify-cost-catalog` 194, teeth proven by four sabotages, BROWSER-VERIFIED on the live project and a throwaway).** The defect class: a row's behaviour lived in its `id` (the seeded method, `STANDARD_STAGE_BY_ID`, the seeded `phasingSource`) while its label was free text, so a live project carried a row labelled "Permits and approvals" that was the seeded Commission line, following sales collections, with nothing on screen saying so. **Now `CostLine.catalogId` names the entry**, selecting one STAMPS method + stage + `stageOverride` + phasing source + basis onto the LINE, and renaming changes the label only. **The line owns its behaviour, not the catalog**: the entry is written onto the line at selection time and is never read by the engine, the reports or the exports, so an unreachable catalog can never change a number (at worst a caption reads "Custom line"). Identity RESOLVES from the base id when `catalogId` is absent, so every existing line declares itself with no migration and no data change. **Built-ins live in code** (`lib/state/costCatalog.ts`, 17 entries covering a real development; the four new ones, engineering supervision / design consultancy / permits and approvals / project management, are SELECTABLE BUT NOT SEEDED, so the 13-line seed is unchanged and a new project gains no extra zero rows). **User additions are shared per user** in `refm_cost_catalog` (migration 214), added in place from the row that needs them, and whoever adds one sets its method and stage because a name with no behaviour is the problem being solved; the API fails SOFT (absent table returns `available:false` and the built-ins carry on). A reassignment states what it will change before doing it, and the amber "renamed" flag fires only when a label is ANOTHER entry's name (comparing to its own entry's label marked every seeded synonym, measured: "Construction (BUA)" against the entry "Superstructure"). **Four defects fixed alongside it. (1) ONE money strip.** Two strips were guarded off two different variables (`displayPhasing` vs `effPhasing`), so an inherited manual curve rendered the real amounts and an even spread beneath; the row now renders `perLinePerPeriod` and calls no distribution function at all (TRAPS 7.11). **(2) `phasingSource` was being DESTROYED** by the loose-path field whitelist, measured on the live project (v1 `collections` / `land_cash`, working version ABSENT), which is why marketing phased on the build. Restored where absent, never where the user chose, and a new verifier section fails on ANY `CostLine` field the whitelist does not name so a third field cannot go the same way (TRAPS 7.9). **(3) Copy between assets reproduces the LINE SET**, matched by catalog identity plus occurrence rather than by display name (a renamed or added line had no name twin and was silently skipped, so a target phase kept the seeded catalog while the panel reported success); missing lines are created with selections remapped into the target phase, removal is opt-in, the counts are reported, and the dialog says that cost lines belong to a PHASE so the change reaches every asset in it. **(4) Deleting a row is undoable**, restoring at its index with its overrides, and the confirm dialogs are gone.

## 2026-08-16

- **`marketing` is a COST STAGE (2026-08-16)**: `CostStage` is `land | hard | soft | marketing | operating`. A selling cost is not a construction cost, so **construction cost excluding land also excludes marketing**, while the developer fee and contingency still charge ON marketing because bases are SELECTIONS of named lines and never consult a stage. A stage rather than an exclude-from-totals flag, deliberately: the stage states something true, a flag would be a general mechanism for omitting any line from any total. **Traps when adding a stage:** only the sites typed `Record<CostStage, number>` are compiler-guarded, and two INFERRED literals in `Module1Costs.tsx` would silently drop the new stage from the Capex-by-Stage table and its Total; and `deriveCostType` defaults to **`'hard'`**, so an unhandled stage reads as a hard cost. The `Total Capex Excl. Land` tile is now `Construction Cost Excl. Land`. Detail in [CLAUDE-REFM.md](CLAUDE-REFM.md) 2026-08-16b.

- **`% of Selected Lines` is POSITIONAL (2026-08-16)**: a cost line may charge on any line **ABOVE it** on that asset, in display order (order is array order; the Capex table filters `costLines` and never sorts), and on nothing else. One shared rule in `src/core/calculations/selectedBase.ts` used by the picker AND enforced in the engine's Pass 3, so the list offered and the base computed cannot diverge. This **replaced a blanket ban on the `percent_of_selected` method**, which had blocked the ordinary cascade a budget is built from (a developers fee on hard plus softs, then a contingency including that fee) and even stopped the standard catalog putting professional fee inside contingency's base. Upward-only references make a cycle unrepresentable, so no method ban is needed. **Position was already load-bearing in the engine but unguarded**: a downward reference to a DIRECT-method line read a real number and produced a wrong base (measured 150 vs 100), while one to another percent line silently read zero. No saved model moves (audited all 1400 versions). Detail in [CLAUDE-REFM.md](CLAUDE-REFM.md) 2026-08-16.

- **Public read-only page API (2026-08-10)**: `GET /api/public/pages/[slug]` serves selected CMS pages to **PaceMakers Business Consultants**, so the partner site renders FMP page content without a database connection or a shared deploy. Returns `{version:1, page:{slug,title,meta_title,meta_description,og_image_url,status,updated_at}, sections:[{section_type,content,styles,display_order}]}`, sections filtered to `visible=true` and sorted by `display_order`; no internal columns (row id, is_system, the visible flag, created_at) are in the response. **The public slug is NOT the internal slug**: `SLUG_WHITELIST` in the route file maps `modeling-hub`->`modeling`, `refm`->`modeling-real-estate`, `training-hub`->`training`, because the three names in the spec matched no live `cms_pages` row and a literal whitelist would have 404ed on all of them; the mapping also means a CMS rename cannot break the partner. Anything not in that constant 404s, and so does a draft, so unpublished work cannot be probed. Auth is `x-api-key`, constant-time, resolved by the shared `src/shared/api/publicApiKeys.ts` and **fails CLOSED** (no usable key refuses every request). Since 2026-08-16 the key is ROTATABLE and `FMP_PUBLIC_API_KEY` is only the PRE-ROTATION fallback: see the rotation paragraph at the end of this bullet before assuming the env var is what the endpoint accepts. Rate limit 60/min/IP, in-memory and therefore PER INSTANCE on serverless, so the effective ceiling is a multiple of that. Success carries `Cache-Control: public, max-age=60, s-maxage=60, stale-while-revalidate=300`; errors are `no-store`. Rejections write `public_api_unauthorized` to **`public_api_audit` (migration 212)**, a NEW table because `admin_audit_log.admin_id` is NOT NULL (proven by a live insert probe) and an unauthenticated caller has no admin to attribute; the audit write is best effort and never turns a 401 into a 500, so the route works before the migration is applied. Note the section `content` jsonb is passed through VERBATIM, including any nested `visible:false` items the FMP renderer would hide, so a consumer must respect those itself. Verifier `scripts/verify-public-pages-api.ts` **43** (calls the real handler against the live DB). **Admin > API Keys (2026-08-11):** `/admin/api-keys` (System group) shows the key masked with a reveal toggle and a copy button, the endpoint URL, the whitelisted slugs and what the key grants, so the value never has to be dug out of an env file. `SLUG_WHITELIST` moved to `src/shared/api/publicPagesConfig.ts` so the screen and the endpoint read ONE constant. The rule that makes it an API keys screen rather than an arbitrary environment reader: **the client never names an environment variable**, it sends a registry id that the route resolves against a literal constant, so `SUPABASE_SERVICE_ROLE_KEY` and friends are unreachable. GET carries a character count and no part of the value; POST returns the value and writes `admin_audit_log` action `api_key_revealed`, and COPY goes through that same POST so it cannot bypass the record. `requireAdmin` try/catches `getServerSession`, which THROWS without a request scope and would otherwise turn a missing session into a 500. **KEY ROTATION (2026-08-16, migration 213, `public_api_keys` + `rotate_public_api_key`):** the screen can now regenerate the key, which required moving the live value out of the environment: a button cannot write a Vercel environment variable, and could not reach a running deployment if it could, so a rotate button that only generated a value could never make the old key stop working. **THE RESOLUTION RULE, read this before touching either route:** the environment variable is consulted only when the table holds **NO ROW AT ALL** for the key id, not "no active row". An active row supersedes the environment permanently; a **retired-only history closes the endpoint** rather than resurrecting the key a rotation retired; an **unreadable key store REFUSES** rather than being read as "no rows"; and a table that is absent (a deploy landing before the migration) falls back to the environment so the partner keeps working. Rotation is ONE transaction (`rotate_public_api_key` retires the active row and inserts the new one, so there is never a moment with two valid keys or none) and the database enforces at most one active key per id with a **partial unique index**, so "the old key stopped working" is a schema guarantee rather than a careful caller. **Resolution is NOT cached**: an in-memory TTL would let a retired key keep working on a warm serverless instance, which is exactly what rotation must prevent, so the public route pays one indexed read per request. **Only a SHA-256 and a 14-character prefix are stored**, so the value exists for exactly one HTTP response, Reveal is REFUSED for a rotated key (`409 hashed_not_revealable`) rather than returning an empty string that reads as a broken button, and the one-time panel on the screen must be dismissed by hand rather than sharing the 60-second auto-hide a revealed key uses (a revealed key can be revealed again; a rotated one cannot). Rotation requires `confirm:true` in the body as well as the dialog, and writes `admin_audit_log` action `api_key_rotated` carrying PREFIXES only, reporting `audited:false` to the screen if that write fails rather than pretending. **Cutover is immediate with no overlap window** and the dialog says so: the consumer gets 401s until they are sent the new value. `REGISTRY` and `requireAdmin` moved to `src/shared/api/apiKeyRegistry.ts`, since a `route.ts` may export only its handlers and there are now two routes needing both. Verifiers `verify-admin-api-keys` 55 -> **80** and NEW `verify-api-key-rotation` **85** (behavioural, against the live database under a PROBE key id, never the real one, because a verifier must not be able to hand the partner a 401 and a rotation cannot be undone; the probe rows are removed and the real key id's rows are proven byte identical to before the run). Teeth proven by six sabotages (3 / 7 / 3 / 1 / 2 / 1 failures). `verify-public-pages-api` stays **43** but now SKIPS its 200-path checks when a rotated key is live, since no plaintext exists for it to present: rotation working must not read as the partner feed being broken. **Not browser-verified.**

## 2026-08-15

- **COST ALLOCATION, SILENT MONEY LOSS, FIXED 2026-08-15 (predates all of the above; read this before touching `resolveAllocationFactor` / `resolveDriverFactor` in `src/core/calculations/index.ts`)**: every share basis was written `return total > 0 ? mine / total : 0`, which returns 0 for **EVERY** asset when the denominator is zero. The shares then sum to **0 instead of 1**, so a project-level lump sum on that basis was allocated to NOBODY and **left the model with no warning anywhere**, no banner, no check failure, no zero-looking cell to notice. **Measured**: one asset, a phase with no land, a `fixed` cost line of 1,000,000 moved total capex by **0.000m** on `land_share`, against the full 1,000,000 on `bua_share` or `per_asset`. Reachable on any phase whose basis quantity is genuinely zero (no parcels entered yet, an operational asset with no new build, areas not filled in), so it had been losing money quietly for as long as those bases have existed. Now every share path falls back to a shared `equalPhaseShare` equal split (SIX call sites: bua / gfa / land on the basis path, bua / land / value_share on the driver path), which is the convention `computeAssetLandSqm` and the `category` basis already used; companions are excluded and the factors are proven to sum to exactly 1. Only the `fixed` method consults it (`FIXED_METHODS_NEEDING_ALLOC`), since every `rate_per_X` method already returns an asset-scoped contribution. **The fix can move existing models, upward only, wherever a cost was being lost.** Pinned by `verify-new-project-defaults` section E; full narrative in [CLAUDE-REFM.md](CLAUDE-REFM.md) 2026-08-15b.

- **Capex phasing inheritance + hard/soft (2026-08-15)**: ONE curve per asset (`Asset.capexPhasing`) that every cost line inherits, with per-line break-out and two DERIVED sources: **RETT follows the land cash outflow** (a transfer tax is due when the land cash is paid, so it must never take the construction curve) and **marketing + commission follow sales collections** (paid out of cash received, so they arise when collections arrive). Built on a SHARED generic primitive `lib/state/inheritance.ts` (own -> derived -> group -> fallback), deliberately free of capex vocabulary because the consolidated input matrices reuse it. Resolution lives INSIDE `computeAssetCost` (`capexPhasing.ts`) because that function has TEN call sites and resolving at them would let the screen and the export disagree; it is skipped when `capexPhasingIsInert`, so an existing project is byte-identical. **A followed source replaces the WINDOW, not just the shape.** `rett` + `marketing` added to the standard catalog at zero rate (RETT country gated). Hard/soft: the row marker deleted on 2026-05-11 is restored, and the shared report layer gained stage SUBTOTALS (it had no hard/soft aggregation at all, which is why no export carried the split). Additive schema, no migration. `verify-capex-phasing` 69. Detail in [CLAUDE-REFM.md](CLAUDE-REFM.md) 2026-08-15d.

## 2026-08-13

- **Module 7 (IC Presentation Builder)**: LIVE. **Free-form AI drafting + fund sections (2026-08-13, `verify-ic-freeform` 72 / `verify-ic-fund-sections` 84, teeth proven by eight sabotages, NOT browser-verified).** (A) The AI panel no longer offers only six fixed narrative fields: select ANY block and give a free instruction ("write this up", "explain this IRR"). Targeting resolves from the SELECTED OBJECT, deliberately not from `findNarrativeTargets`, which is template-driven and returns nothing for a blank slide. **Grounding is unchanged**: same `collectGrounding`, same whitelisted model facts, no new source, and the verifier MEASURES that by capturing the real request rather than reading the source. `buildGroundedRequest` puts `GROUNDING_RULES` in the system prompt and repeats the figure reminder after the task, so a user instruction sits INSIDE rules it cannot reach around, which is why a user-authored task is safe to send at all. **The refusal contract is stricter than the standing rules**: rule 4 permits a partial answer, which is wrong for an arbitrary instruction because a half-grounded paragraph hides which half was real, so free-form is ALL OR NOTHING via a machine-detectable sentinel (`CANNOT_ANSWER_FROM_MODEL:`) and the review offers NO Apply on a refusal. A refusal returns 200 with an empty draft and KEEPS its counted generation (the call was made; refunding it would make refusing free and answering expensive). **An empty block on a blank slide is described as empty in the prompt**, with its own branch, because a model handed a silent empty string invents a subject. Metered through the SAME `ic_narrative` feature, cap and toggle: a free instruction is a different way to ask, not a second allowance. Draft identity in the review moved from the field key to an explicit `id` (the field key for a fixed draft, the object id for a free-form one), since a block-targeted draft has no field. (B) The fund layer reaches the deck: three toggle-gated slides (Fund Terms and Fees, Fund Distribution Waterfall, Fund Returns and Fee Income) covering all five fund sections, fed by a new `fund` block on `ICReportModel` that reads ENTIRELY from the shared `fundReports.ts` / `m4Reports.ts` builders, following the `schedules.fcff` precedent. Toggle off is byte-identical, proven by seeding absent-vs-populated-but-disabled decks and comparing in full. Detail in [docs/FUND_LAYER_GUIDELINE.md](docs/FUND_LAYER_GUIDELINE.md) section 8b and [CLAUDE-AI.md](CLAUDE-AI.md). A PowerPoint-style slide editor (navigator / 16:9 canvas / properties) that replaced the old form-plus-fixed-report UI. **Slides hold BINDING KEYS, never copied numbers**, so the deck follows Modules 1-6 with no sync step and an unresolved binding renders a visible amber UNLINKED state rather than a stale figure. Direct manipulation (drag / resize / snap / inline edit / undo), 18 institutional templates that auto-omit on `available(model)`, full year-by-year schedules paginated across slides, a live ToC, and native PPTX + PDF export from ONE shared resolved-export contract so a file figure cannot drift from the screen. AI drafting on the narrative blocks (see CLAUDE-AI.md). Free text boxes, presentation versions (migration 207) and a save choice on EVERY save (update the version on screen in place, or save as a new version) rather than a silent overwrite. **Report versions are AUTO-NAMED and the last one opens with the project (2026-08-03):** nothing is typed (`{Project}_Presentation_v1.3_08032026`, named server-side, reusing the model version rollover helper as a PURE function), every save keeps a version (an unlinked save creates the first one), and `getDeck` returns the last saved version plus which version it is, so the editor names what is open and the versions modal is for browsing and opening the others. Report versioning stays entirely separate from model/project versioning: its own table, routes, client and UI. Migration 199. **Full narrative + remaining work (Insert menu, image upload, group / align, PNG export) in [CLAUDE-REFM.md](CLAUDE-REFM.md).**

## 2026-08-09

- **Discount normalization fix, an unlimited discount is not a used-up one (2026-08-09)**: a live 25% pre-launch promo showed in the admin discount list but never reached the public pricing page and was never passed to Paddle at checkout, so customers were charged full price with nothing rejected and nothing logged. **Cause was ONE normalization line**: `shapeDiscount` (`paddleApi.ts`) read Paddle's nullable numerics as `Number.isFinite(Number(x)) ? Number(x) : null`, and `Number(null)` is `0`, which is finite. So `usage_limit: null`, meaning UNLIMITED, normalized to `usageLimit: 0`, and `isLiveDiscount` then read `timesUsed >= usageLimit` (0 >= 0) as fully redeemed and filtered the discount out. **The two surfaces disagreed silently because only one of them filters**: `loadAdminDiscountView` returns the Paddle list RAW (so admin kept showing it), while the pricing page and checkout both go through `loadActivePublicPromo`, which requires `isLiveDiscount`; a typed code via `resolveCouponForCheckout` would have reported it expired. **Fix**: a `nullableNumber` helper preserves absence as null instead of collapsing it to zero, applied to `usage_limit` and `times_used`. `isLiveDiscount` needed NO change (it already guarded `!= null`), so the bug was never in the predicate: normalization destroyed the null before the predicate saw it. `shapeDiscount` + `isLiveDiscount` are now EXPORTED so the behaviour is pinned rather than trusted from a comment. **Verified live after the fix**: the featured promo resolves (25% off), `restrict_to` is empty so it covers every plan price id, and Paddle applies it by discount id **despite `enabled_for_checkout: false`** (a `pricing-preview` call returned 1,500 -> 1,125 on pro monthly), which confirms the standing assumption that the flag only governs the customer-typed code field. Confirmed end to end by a real checkout. `verify-payments-structure` 65 -> **79** (14 checks: the pin "no usage limit + zero redemptions is LIVE", a code-less checkout-disabled discount still live, and the exhaustion clause still biting so the fix is not just a deleted check; **proven to have teeth, 7 fail against the previous normalization**). Resolver/gate/enforcement UNCHANGED. No migration.

- **Launch countdown banner (2026-08-09)**: a wide (~800px, scaling down), centred, dismissible launch banner announcing the Modeling Hub launch, shown to visitors on the marketing home, both Modeling Hub pages and both Real Estate platform pages. Reads `training_settings.modeling_hub_launch_date`, the SAME key `/api/cron/auto-launch-check` reads, so the countdown a visitor sees and the moment the hub flips are one date rather than two that can drift; nothing is hardcoded. That key had **no admin editor** (the `/api/admin/modeling-coming-soon` route existed but had ZERO callers), so this wires that existing route to a new "Modeling Hub - Public Launch Date" card on `/admin/modules`; the per-surface signin/register dates are a separate thing and are untouched, so the public announcement can be staged apart from when the auth pages open. **Setting the date does NOT schedule an automatic launch**: the cron also requires `modeling_hub_auto_launch`, which is `'false'` and has no UI writing it. **THREE STATES (2026-08-09c)**: `countdown` while the date is in the future, `launched` once it passes (its own editable headline/supporting line + a call to action, and it STAYS until retired rather than vanishing), `hidden` when the admin switches it off or there is no usable date. Only the OFF choice is stored (`modeling_hub_launch_banner_enabled`, **absent means ON** so existing installs are unaffected, checked FIRST so it overrides both date-derived states); countdown vs launched is derived from the date so the two can never disagree with the clock. **The off switch exists to prevent collateral damage**: the only previous way to retire the banner was clearing the launch date, which is the same key the auto-launch cron reads. At zero the banner **switches in place** to the launched message (both copies are sent to the client) instead of disappearing under a visitor. The CTA destination defaults to the platform's own page derived from the config slug (`${APP_URL}/modeling/<slug>`, absolute because the banner also renders on the apex) and is admin-overridable; no destination + no label means no button rather than a dead link. **Session dismissal is keyed on date AND mode**, so dismissing the countdown does not swallow the launch announcement later the same session. **Sizing**: ~800px wide (`min(800px, 100vw - 32px)`), height follows content capped to the viewport with internal scroll; it was square at 400px until 2026-08-09c. `CountdownTimer` caps its own digit grid at `min(440px, 100%)`, so the digits centre in the wide card rather than stretching. Placement is an **ALLOWLIST of five EXACT paths**, not the blocklist `PromoPopup` uses, because this banner covers content: exact matching keeps it off deeper routes, and `'/'` serves both the apex home and the app subdomain root (which rewrites to `/modeling` with the browser path unchanged). Dismissal is **sessionStorage keyed on the launch date**: gone for the session, back next session, and a rescheduled launch is a new announcement. Square on every screen via `min(400px, 100vw - 32px, 100vh - 32px)`. Brand navy gradient + gold accent. Mounted ONCE in `app/layout.tsx` beside `PromoBanner`; pure rules in `launchCountdown.ts`, server read in `LaunchCountdownBanner.tsx`, path/dismissal/tick in `LaunchCountdownPopup.tsx`. **No migration** (`training_settings` is a free-form key/value store, `key text primary key` with no constraint on the key set, so every launch setting is just another row). **Editable copy + platform-aware (2026-08-09b)**: headline and supporting line are admin-editable (`modeling_hub_launch_headline` / `_subline`), and `modeling_hub_launch_platform` stores the platform SLUG only, with the NAME resolved from the platform config at render (`getPlatform(slug).name`, else the first LIVE platform), so no product name is hardcoded in the banner and a rename carries through. Either field may contain a `{platform}` token, mirroring the `{trialDays}` precedent; blank restores the default; an unresolved platform never leaks a literal token to a visitor. **Admin is now ONE dedicated `LaunchBannerCard`** (date + copy + platform + live preview) instead of a second `LaunchStatusCard`, and it carries a **BANNER SHOWING / NOT SHOWING status computed by the same pure resolver the banner uses**, from the SAVED (not draft) date. That status is the fix for the reported "banner does not appear": the date had never been written (`reason: not_set`), and with three cards on the page carrying a launch date and none stating which drove the banner, a save on the wrong card (where "Save" is disabled when the value is unchanged) looked identical to success. `LaunchStatusCard` is back to its pre-banner shape (the two props added earlier were reverted rather than left dead). `verify-launch-banner` **141**. **Not browser-verified.** Note `/modeling-hub` and `/modeling-hub/real-estate` are `revalidate = 60`, so a date change can lag up to a minute there; the other three are `revalidate = 0`.

## 2026-07-07

- **Discounts auto-linked to Paddle (rework, 2026-07-06b)**: Paddle is now the SINGLE SOURCE OF TRUTH for discounts, replacing the manual re-entry of mig 184. The platform reads the LIVE Paddle discount list via the API (`listDiscounts` in `paddleApi.ts` -> `GET /discounts`, server-side, 60s in-memory cache in `coupons.ts`), so a discount created once in Paddle appears in the platform automatically with its real percentage/code/expiry/redemption-limit/status (Paddle enforces those at checkout; the platform never duplicates them). **Minimal local storage**: only the admin's CHOICE of which Paddle discount is the featured PUBLIC promo (discount id + optional label) in `cms_content` (`section='payments'`, `key='public_promo:{platform}'`); NO discount data stored. The dead `/api/admin/pricing/coupons` + `/api/pricing/validate-coupon` routes were REMOVED (2026-07-07); the `coupon_codes` table (mig 076/184) is now orphaned but RETAINED (data, non-destructive). **`coupons.ts` rewritten, same public signatures** (so checkout/`coupon/resolve`/`pricingCatalog` are unchanged): `resolveCouponForCheckout` matches the typed code against the live Paddle list; `loadActivePublicPromo` returns the featured Paddle discount (only when still active/enabled). **Public auto-apply fixed**: the featured promo now applies by `discountId` with NO code (the old bug: the local row often had no `dsc_` id so nothing was passed and Paddle showed its code field). **Admin** `/admin/plans` CouponManager is now a live Paddle discount viewer + "Feature as public promo" picker (via new `GET/POST /api/admin/payments/discounts`), no re-entry; shows code/amount/expiry/uses/checkout-enabled/status. **Banner site-wide**: new server `PromoBanner` (`src/hubs/main/components/pricing/PromoBanner.tsx`) on the main marketing home page (renders nothing when no promo); the pricing page keeps its in-`LivePlanCards` banner. Private codes still typed at checkout -> matched to a Paddle discount -> applied by Paddle. Resolver/gate/enforcement UNCHANGED; a 100%-off Paddle discount still grants the plan at zero charge. No migration.

## 2026-07-06

- **Discounts / coupons (Model 1: Paddle owns the discount, 2026-07-06)**: the platform REFERENCES, displays, and passes Paddle discounts; it does not compute them (a discount affects PRICE only, never entitlements/gate). **Schema (mig 184)**: `coupon_codes` gains `paddle_discount_id` (the `dsc_...` the code references; NULL = display-only, will not reduce the charge), `kind` (`public` auto-apply / `private` code), `display_label`, `starts_at`. **Resolver** `src/shared/payments/coupons.ts`: `resolveCouponForCheckout` (code -> Paddle discount id, validity + platform scoped, clear rejection reason on every failure) + `loadActivePublicPromo` (newest active public promo, ONLY when it has a `paddle_discount_id`, so a displayed promo always really applies). **Checkout** (`POST /api/payments/checkout`): accepts `coupon_code`; a typed code resolves server-side (invalid -> `{status:'error'}` rejection, no overlay), else the active public promo auto-applies; the resolved `discountId` flows through `CheckoutRequest`/`CheckoutResult` -> the Paddle adapter's `open_overlay` -> `paddleBrowser.openPaddleCheckout` -> `Paddle.Checkout.open({ discountId })`, where Paddle validates + applies it (a rejected discount surfaces via the existing `checkout.error` handler). A discount id is client-safe; the API key stays server-side. A 100%-off Paddle discount yields a zero charge while Paddle still creates the subscription and the webhook grants the plan (entitlements unaffected). **Checkout coupon field** (`app/pricing/CouponInput.tsx`, controlled): logged-in only (logged-out hands off to `/register`); "Apply" gives immediate feedback via `POST /api/payments/coupon/resolve` (no charge/state); the code is held in `PricingExplorer` and passed to checkout. **Marketing/pricing display**: `loadPricingCatalog` returns the active public `promo`; `LivePlanCards` shows a promo banner + the discounted (struck-through) price for percentage promos; `PricingPageBody` threads it through (the Paddle discount id stays server-side, only label/percent reach the client). **Admin** (`/admin/plans` CouponManager): each entry has the Paddle discount id (with a clear "must exist in Paddle, else it only displays" warning), kind, display label, active toggle, start/expiry; a **Not wired** badge flags rows with no `paddle_discount_id`. **Manual plans**: unchanged, the discount is just the amount the admin enters and the manual receipt shows it (no Paddle). Resolver/gate/enforcement UNCHANGED (`verify-entitlement-gate` 92, `verify-pricing-display` 23, `verify-pricing-application-ready` 48 + 1 pre-existing failure, see the Entitlements bullet). Mig 184 APPLIED 2026-07-06.

- **Subscription-email gap closure (2026-07-06)**: fixes the six gaps from the email-system audit; resolver/gate/enforcement UNCHANGED. **P1 (1) Period-end convert-to-manual is no longer silent**: `applyScheduledManualConversion` (webhook `subscription.canceled` + the cron backstop) now sends the manual welcome + issues the branded receipt (when an amount is owed), matching the immediate path; startedAt is anchored to `conv.effectiveAt` so the welcome/receipt dedupe token is STABLE across the webhook/cron race (one email, one receipt). **(2) Welcome/receipt self-heal**: new `selfHealWelcomePaddleEmail` sends the welcome+invoice for a live paid Paddle sub whose `activated` webhook never delivered. It first checks for an existing `welcome_paddle` marker on the STABLE `evt:{subId}` threshold (IGNORING anchor_day), so already-welcomed subs (incl. all existing prod rows) are skipped, no mass re-send. Wired at the billing-panel live read (`GET /api/payments/subscription`, active non-canceled only) AND daily in the reminder cron Pass B (stored row, no extra live call). **P2 (3) Dunning**: adapter now maps `subscription.past_due`/`transaction.payment_failed` -> new neutral `payment_failed`; webhook sends `paymentFailedEmail` (update-card), NO plan change (Paddle retries, access continues), deduped PER BILLING PERIOD (`evt:{subId}` + period-end anchor so the several retry events collapse to one). Retain coexistence: safe alongside Paddle Retain; to hand dunning fully to Retain, drop the `sendPaymentFailedEmail` call in the webhook. **(4) Renewal receipt**: adapter splits `transaction.completed` by `origin` (`subscription_recurring` -> new neutral `renewed`, else `activated`), so a renewal sends the new `renewalReceiptEmail` (not the welcome copy); the `renewed` webhook branch records the ledger txn + sends the receipt with the invoice attached, and does NOT call setUserPlan (plan unchanged). **P3 (5) Double-submit guard**: admin plan route's fallback `started_at` is now DATE-ONLY (`slice(0,10)`), so a rapid double-submit resolves to one dedupe token -> one welcome + one receipt. **(6) Paddle-dashboard cancel**: the webhook `updated` branch now sends the cancellation confirmation when it carries a scheduled cancel-at-period-end, deduped on the SAME `evt:{accessUntil}` key the in-app cancel route uses (no double for in-app cancels). New parsed-event fields `billingPeriodEnd` (+ `renewed`/`payment_failed` types); webhook also resolves the user by stored `paddle_subscription_id` as a fallback for thin renewal/dunning events. All sends go through the existing `[sub-email]` dedupe dispatch (logged sent/skipped/FAILED). Two new templates (`renewalReceiptEmail`, `paymentFailedEmail`), FMP/PaceMakers branding + per-platform `/pricing` links. Verifiers `verify-subscription-emails` 113, `verify-subscription-management` 210, `verify-entitlement-gate` 92. No migration.

## 2026-07-02

- **Email/invoice testing fixes (2026-07-02)**: (1) transactional email dedupe is now PER-EVENT not per-type-per-day (`subscriptionEmails.ts`): `welcome_manual` keys on the assignment `started_at`, `manual_invoice` on `started_at:amount` (claimed BEFORE the receipt is created, so a double-click makes no second receipt while a genuine new assignment does), `canceled` on the period-end, mirroring `plan_changed`. `issueManualInvoice` routes through the dedupe dispatch, RETURNS a result, and `dispatch` logs every outcome ([sub-email] sent id= / skipped / FAILED via console.error) with the Brevo message id, so failures are visible not swallowed. Manual receipt still gated on amount>0 with an admin-UI note ("No amount: no receipt"). New `sendPlanEndedEmail`/`planEndedEmail`: removing a manual plan (admin sets none for a user who had a real plan) sends the manual user a cancellation confirmation (the Paddle-only cancel route never fires for them). (2) Full invoice history survives source flips: Paddle history reads from the DURABLE `payment_transactions` ledger (`listPaddleLedgerInvoices`) not the live API alone; the invoices route merges manual + ledger Paddle + live enrichment (deduped by txn id); the Paddle PDF route authorizes from the ledger (`userOwnsPaddleTransaction`) + builds cfg without a live sub, 404s gracefully when a PDF is unavailable. (3) Immediate plan changes attach the Paddle proration invoice (shared `fetchPaddleInvoiceAttachment`, newest txn -> getInvoicePdfUrl -> base64); scheduled downgrades have none. Verifier `verify-subscription-emails` 113. Resolver/gate/enforcement UNCHANGED.

- **Convert/links/plan-change fixes (2026-07-02)**: (B) **convert-to-manual** no longer re-cancels an already-canceled Paddle sub (the `subscription_update_when_canceled` / "paid through n/a" failure that left users on Trial after Firm): the route reads the LIVE Paddle state and, when `canceled`/no-active-period, assigns the manual plan DIRECTLY via `setUserPlan(source:'manual')` (single converge path; active subs keep period-end/immediate); a `when_canceled` race recovers instead of 502. The webhook baseline-drop now CLEARS the dead Paddle ids (`config.clearPaddleSubscriptionIds`) so a canceled row can't masquerade as live (which mis-rendered billing and blocked manual-assign recovery). Gate inputs/convergence untouched. (C) renew/plan links are now **per-platform** `/pricing/<segment>` (source-driven `platformPricingSegment`, real-estate->refm): email `pricingUrl(platform)` at all call sites, the REFM grace banner, and the dashboard grace banner; pickers stay bare `/pricing`. (A) new **plan-change confirmation email** (`planChangedEmail` + `sendPlanChangedEmail`) on upgrade/interval (immediate) + scheduled downgrade (next cycle), triggered at the change-plan route only (one change = one email), deduped via `subscription_email_log` (marker encodes plan+interval, keyed by timing). Verifiers `verify-subscription-management` 210, `verify-subscription-emails` 90, `verify-entitlement-gate` 92.

- **Email reliability + durable billing history (2026-07-02c)**: (1) **Welcome (Paddle) + trial-started emails now dedupe PER-EVENT** (`welcome_paddle` keyed on the subscription/transaction id, `trial_started` on the trial end date), matching the welcome_manual/canceled pattern. They were on `threshold:'once'` + `anchor_day: today`, so a second purchase/trial the SAME day was swallowed as a duplicate (the "was receiving, now not" on repeated testing). (2) **Assigning a user to `trial` via the admin plan route now sends the trial-started email** (`sendManualPlanWelcomeEmail` skips trial by design and nothing else fired). (3) **Invoice-attachment fetch made fast/best-effort** so it can't stall the send: welcome uses ONE attempt (no sleep, keeps the webhook under Paddle's timeout so a stalled send never leaves the dedupe marker claimed + drops the email), plan-change uses 2 short attempts; the email always sends with the billing link even when the PDF is not yet generated. (4) **Durable Paddle billing history**: the combined `/api/payments/invoices` (manual + Paddle ledger + live) and the panel (renders it in BOTH the manual + Paddle branches) were already correct, but the Paddle `payment_transactions` ledger was only populated by the webhook's `transaction.completed` (unreliable in sandbox), so Paddle history vanished after convert-to-manual ("manual only"). Now the ledger SELF-HEALS: the invoices route backfills settled live Paddle transactions on read (idempotent on txn id), and convert-to-manual captures the Paddle invoice history to the ledger BEFORE the sub goes away, so both sources persist and show together. Founder note: if the `[payments-webhook]` logs show events are not delivered in sandbox, the welcome email + ledger still depend on a live read to populate (self-heal on billing-panel view / convert); verify webhook delivery separately. Resolver/gate/enforcement UNCHANGED. verify-subscription-emails 113, verify-subscription-management 210, verify-entitlement-gate 92.

- **Plan-change invoice + admin cancel visibility (2026-07-02)**: (1) **Immediate plan-change / conversion invoice now attaches reliably.** Root cause was timing + silent swallow: right after a `prorated_immediately` change the proration transaction exists but Paddle has not yet generated its invoice PDF, so `getInvoicePdfUrl` returned `invoice_not_available` and `fetchPaddleInvoiceAttachment`'s bare `catch` dropped it with no log. Fix: `changeSubscriptionPlan`/`shapeSummary` now surface `immediate_transaction.id` (passed as the exact proration txn, subscription-newest-txn as fallback); `fetchPaddleInvoiceAttachment` gained short retry-with-backoff (invoice generation lag) + VISIBLE `[sub-email] invoice-attach ...` logging on every failure reason; `sendPlanChangedEmail` logs `plan_changed invoice NOT attached` when an immediate change sends without one. Welcome-paddle routed through the same shared helper. Conversion emails already attach the manual receipt PDF (`issueManualInvoice`); no Paddle charge applies on convert (Paddle canceled). (2) **Admin now shows Canceling / Canceled** (mig 183 `user_platform_subscriptions.scheduled_cancel_at`, a dedicated column since the `subscription.updated` webhook re-converges store-B `status`). Persisted on self-service cancel (`markSubscriptionCanceling`) + the webhook's parsed `scheduled_change` cancel; cleared on (re)activation. `/api/admin/users` derives `cancelState` (`canceling`/`canceled`) + `cancelAt` from the durable marker (own schema-tolerant query) and supports `?cancel=canceling|canceled` for retention outreach; the list shows a Canceling/Canceled badge + the end date, the detail panel surfaces the live `scheduledCancelAt`. **List/detail convergence (2026-07-02b):** the list reads the DURABLE marker while detail + the user billing panel read LIVE Paddle, so a cancel made before mig 183 (or Paddle-side) left the list showing plain "active". Both live-read routes (`GET /api/payments/subscription` + the admin detail `GET /api/admin/entitlements/user`) now SELF-HEAL the durable marker via `markSubscriptionCanceling(scheduledCancelAt ?? null)` (reusing the live call already made, null clears on resume), so the durable single source converges and the list badge matches the detail. Resolver/gate/enforcement UNCHANGED. Verifiers `verify-subscription-emails` 113, `verify-subscription-management` 210, `verify-entitlement-gate` 92. Mig 183 APPLIED 2026-07-02.

## 2026-07-01

- **Subscription email lifecycle (2026-07-01, Brevo, branded from financialmodelerpro.com)**: full lifecycle emails via the existing `src/shared/email` stack (`baseLayoutBranded` + `sendEmail`, sender `FROM.noreply`; `sendEmail` gained additive `attachments`, `FROM.support` added). Nine branded templates in `templates/subscription.ts`; dispatch + cron scan in `subscriptionEmails.ts` (self-contained, NEVER throws to caller). **Transactional** (wired at the trigger): welcome/active Paddle (webhook `activated` only, with the invoice PDF fetched SERVER-SIDE via `getInvoicePdfUrl` and attached + a `/dashboard#billing` link), welcome/active manual (admin plan route + convert-to-manual immediate, start/expiry, team-managed no-invoice, skips none/trial), canceled confirmation (self-service cancel route, "access until [period end]"), trial started (self-serve `startTrialForUser` + admin shortcut + approval queue). **Time-based** (daily cron `GET /api/cron/subscription-reminders`, CRON_SECRET, 10:00 UTC, added to vercel.json) at 1 week + 1 day before the date: trial ending (`trial_ends_at`), auto-renewal charge notice ("you'll be charged [amount], cancel before then") for auto-renewing Paddle only, ending-plan expiry ("access ends, renew") for manual + canceled Paddle. **Grace** emails: grace started (on first run after expiry) + grace ending (1wk/1d before grace end = expiry + 1 calendar month), reusing the gate's `computeLapseState`/anchor helpers (NO gate change). Auto-renew vs ending are DISTINCT (classified via a server-side `getSubscription()`; charge notice suppressed on uncertainty so a canceled sub never gets "you'll be charged"). **Idempotent + deduped** via `subscription_email_log` (mig 181, APPLIED 2026-07-01): claim-then-send keyed on (user, platform, type, threshold, anchor_day), so no double-sends and the cron is safe to re-run; anchor_day lets a renewal reminder re-fire for a new period. All Paddle calls server-side. Verifier `verify-subscription-emails` 45 (renders templates + asserts the charge-vs-ends distinction + every trigger wiring + dedupe + cron). Resolver/gate/enforcement UNCHANGED (`verify-entitlement-gate` 92).

- **Billing display fixes + manual invoices (2026-07-01)**: (1) subscription email footer standardized to the FMP company line "A PaceMakers Business Consultants Platform" (matching the pricing credibility line) via a `baseLayoutBranded` overrides param + a `subLayout` wrapper, replacing the Training Hub tagline. (2) **Paddle-wins source fix**: `storeUserPlatformSubscription` now sets `source:'paddle'` (+ clears stale manual `expires_at`/`amount_minor`/`note`) on activation, and `loadUserPaddleContext` treats a row as manual ONLY when there is no `paddle_subscription_id`, so a Paddle sub created over a prior manual record shows the Paddle subscription (not the stale "Managed by your team"). Billing-panel resolution only, NOT the gate. (3) **Combined invoices**: `/api/payments/invoices` merges Paddle invoices + manual receipts into one normalized `{id,source,...}` list; the panel renders it (+ viewer) in both the manual and Paddle branches, routing View by source. (4) **Manual invoices** (mig 182 `manual_invoices` + private `invoices` bucket): admin manual-assign / convert-to-manual-immediate with an amount generates an FMP + PaceMakers branded receipt PDF (`src/shared/payments/manualInvoice.ts`, pdf-lib) with the legal SELLER block (PaceMakers Business Consultants LLP, LLP Reg No 0200688, Gulberg III Lahore address, FBR NTN 6899301 for identification only, NO tax line), bill-to (name/email/company), and the plan + coverage period line item; stores it privately, emails it (PDF attached), and lists it in billing; served via ownership-checked `GET /api/payments/manual-invoice/[id]` (short-lived signed URL). All Paddle calls server-side; resolver/gate/enforcement UNCHANGED. Verifier `verify-subscription-emails` 65 (renders a real receipt PDF). Mig 182 APPLIED 2026-07-01.

- **Account area cleanup (2026-07-01)**: the grace renew banner now ALSO renders on the modeling **dashboard** (`app/modeling/dashboard/page.tsx`, `data-testid="dashboard-grace-banner"`, gated on `ent.lapseState === 'grace'` from `useEntitlements`), so a grace user sees read-only-until-[date] + Renew on login, not only inside the workspace; lapsed still routes to `/choose-plan`. **Settings** (`app/settings/page.tsx`): the redundant Subscription card (Plan/Status/Projects/Upgrade) was REMOVED, it read the legacy `users.subscription_plan` off the NextAuth session (default `'free'`, a dead source separate from the entitlement gate, the cause of the stale "Plan: Free" display, and the ONLY user-facing reader of it), so the dashboard Billing tab is now the single subscription surface. Profile card gained editable **Company + Job Title (required) + Phone + City (optional)** and **profile-image upload/replace** (initials fallback). New authenticated `POST /api/user/avatar` -> `avatars` storage bucket (2 MB, jpg/png/webp) + `users.avatar_url`, shown in Settings + dashboard header/sidebar; `/api/user/profile` GET/PATCH extended (schema-tolerant, `action=profile`). No migration (avatar_url mig 002, company/job_title 172, phone/city 027 already exist). Resolver/gate/enforcement UNCHANGED; `verify-entitlement-gate` still 92.

## 2026-06-30

- **Subscription Management & Billing (2026-06-29, sandbox-first)**: full self-service + admin billing on top of the single plan path. **Diagnostic webhook logging** (`[payments-webhook]` arrived/verified/rejected). **In-dashboard billing tab** (modeling dashboard in-page `activeView`; `BillingView` + `SubscriptionPanel`): per-platform subscriptions (mig 177 `user_platform_subscriptions`, one section per live platform, source-driven), view/cancel-at-period-end/invoices (in-app iframe viewer)/update-card (Paddle hosted), upgrade/downgrade with the target plan's full feature list + a Paddle **preview** differential (PATCH /subscriptions/{id}/preview) + monthly<->annual interval change, and the **timing rule** (upgrade immediate / downgrade deferred to next cycle via mig 178 scheduled cols + the `apply-scheduled-changes` cron, daily in vercel.json). **Single source of truth** (mig 179): `setUserPlan` keeps the per-platform row (store B) in sync on EVERY plan write so the gate (store A) + billing panel + admin views never diverge: a MANUAL plan writes the full row (source manual, dates, amount); every other caller (trial shortcut, self-serve trial, approval queue, the webhook's setUserPlan call) does a PARTIAL `syncPlatformSubscriptionFields` UPDATE-only of `plan_key`+`status` that PRESERVES webhook-owned metadata (source, paddle ids, Paddle dates) and is a no-op when no row exists (no fabricated `paddle`-defaulted row). All trial paths now also apply the SAME live-Paddle guard the plan route uses (shared `PADDLE_BILLED_BLOCK_MESSAGE` + `isUserLivePaddle`, 409) so a Paddle-billed user is never silently moved to trial; the `/admin/users` status dropdown converges store B status manual-only (never clobbers Paddle status). gate honors `expires_at` (additive only; mirrors trial). (2026-06-30: convergence-at-source fix, closes the admin-Trial-vs-billing-Firm divergence from any path; gate access logic unchanged.) **Manual (offline/bank) plans**: admin assigns plan+start+expiry+amount in `/admin/users/[id]`; live-Paddle users are BLOCKED from a silent change. **Admin Revenue** (`/admin/revenue`, mig 180 `payment_transactions` ledger): total + Paddle(reconcilable)/manual split, by month/year/custom + by plan, DB-aggregated. **Convert-to-manual** (safe): period-end default (cancel Paddle at period end + scheduled conversion applied by the subscription.canceled webhook / cron) or immediate (warned). All Paddle calls server-side; key never client-side. Resolver/gate core + admin bypass + none/unknown UNCHANGED. Verifiers: `verify-subscription-management` 204, `verify-entitlement-gate` 92. **Migs 176-180 each were applied to prod as shipped (176-180 done).** Founder TODO: set the server Paddle API key in Admin>Payments (scopes subscription.read+write, transaction.read) to light up the Paddle-side flows. Per-route/migration detail in [CLAUDE-ROUTES.md](CLAUDE-ROUTES.md) + [CLAUDE-DB.md](CLAUDE-DB.md).

- **Post-expiry access (read-only grace + lapse, 2026-06-30)**: a plan that expires no longer drops to instant lockout. The gate now computes a THREE-STATE lapse model purely from the plan's expiry date (the "lapse anchor"): **active** (`now < expiry`, full access), **grace** (`expiry <= now < expiry + 1 calendar month`, READ-ONLY: can log in + view projects, but edit / save / export / create / archive denied + a renew banner), **lapsed** (`now >= expiry + 1 month`, NO platform access like `none`, account still logs in, data NEVER deleted). Anchor priority (`resolveLapseAnchorMs` in `gate.ts`, shared by the live gate AND the admin list): trial -> `users.trial_ends_at`; manual -> `user_platform_subscriptions.expires_at`; canceled/expired Paddle -> `current_period_end` (active renewing -> no anchor). Pure helpers `computeLapseState` + `addCalendarMonths` + `writeBlockReason` (`READ_ONLY_GRACE` | `LAPSED`) in `gate.ts`; `computeGate` gains `lapseState`/`readOnly` (grace KEEPS the feature map so projects stay VIEWable; lapsed = denied like none). **Server-side read-only enforcement** at every write choke point: `POST /api/refm/projects` (create), `.../[id]/duplicate`, `.../[id]/versions` (save = the edit choke point), and the `/api/export/{pdf,excel}` routes via shared `src/shared/entitlements/exportGuard.assertExportAllowed` (the live export is client-side, so `ExportModal` read-only gate is the genuine point + the routes are defense-in-depth). `/refm` layout redirects LAPSED (not grace) to `/choose-plan` via `isNoPlanLockedOut(plan, isAdmin, lapseState)`; dashboard treats lapsed like no-plan. UI: grace renew banner + workspace read-only lock + suppressed Edit/Save/Export/New in `RealEstatePlatform`. **Admin user list** (`/admin/users`): new **Access** (auto active/grace/expired-lapsed by date) + **Expires** columns, computed server-side in `/api/admin/users` with the SAME shared helpers (no divergence from the live gate, correct between cron runs). **Admin bypass + none/unknown distinction UNCHANGED; no migration (reuses migs 177/179 columns); no data ever deleted.** Backward-compat: the legacy `trialExpired`/`planExpired` booleans (no `lapseState`) still map a full expiry to `lapsed`. Verifier `verify-entitlement-gate` 92 (adds grace/lapsed/read-only/anchor/admin-bypass-survives-grace cases).
