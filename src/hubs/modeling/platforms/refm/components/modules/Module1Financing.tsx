'use client';

/**
 * Module1Financing.tsx, Tab 4 placeholder (rebuild in progress, 2026-05-13).
 *
 * Original 2,480-line implementation deleted in Commit 1 of the full
 * Tab 4 rebuild. The new engine (src/core/calculations/financing/)
 * and UI ship in Commits 2 and 3. Schema (module1-types.ts, v8) is
 * preserved unchanged so saved snapshots keep loading.
 */

import React from 'react';

export default function Module1Financing(): React.JSX.Element {
  return (
    <div
      style={{
        padding: 'var(--sp-3)',
        background: 'var(--color-surface)',
        border: '1px dashed var(--color-border)',
        borderRadius: 'var(--radius)',
        color: 'var(--color-text-muted)',
        fontFamily: 'Inter, sans-serif',
        fontSize: 13,
        textAlign: 'center',
      }}
    >
      Tab 4 Financing is being rebuilt. The new engine and UI ship in the next two commits.
    </div>
  );
}
