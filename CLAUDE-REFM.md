# Real Estate Financial Modeling (REFM), Claude Code Project Brief
**Last updated: 2026-08-17. Lock status: M1 CLOSED 2026-08-17 (28 defects found in end-to-end use, all fixed and browser-verified; base lock unchanged at M2.0 Pass 58) + Parties tab (mig 190), M2 LOCKED (Pass 9N), M3 LOCKED (Pass 5d), M4 DONE, M5 DONE (Returns & RE Metrics + Lender Covenants + per-partner FCFE/DDM + M1-Parties link), M6 Scenario Analysis LIVE, M7 Reports LIVE (IC / Lender / One-Pager preview + PPT export, migs 191+192). Full verifier suite green via `npx tsx scripts/verify-*.ts`.**

> **TRAPS RECORDED IN THIS FILE ARE COLLECTED IN [docs/TRAPS.md](docs/TRAPS.md). Read that first; it is short.** The copies below stay in place, so nothing is lost if you open this file instead. Index of what this file records: the REFM shell `zoom: 0.8` making `vh` and media queries lie (TRAPS 6.1); drag/resize needing pointer capture, not window listeners (6.2); the ExcelJS width-9 column that silently does not apply (3.1); pdf-lib CID glyph ids defeating a naive grep (4.1); the PostgREST 1000-row cap (2.1); export fingerprints needing the FIXTURE, never the live project (5.1); a zero-valued seed hiding a whole code path (7.1); a stage list written as a literal defeating exhaustiveness (7.3); a two-step template registration that fails silently and permanently (8.1); and the verifier rules (a grep proves presence not firing; never gate an assertion on the thing it asserts; prove teeth by sabotage) in TRAPS section 10.

## 2026-08-18: The Cash Flow Investing section foots, and in-kind land has ONE recognition rule

Reported from the screen: the Cash Flow's Investing section did not add up. Its per-asset capex
rows summed to 426,407.0k while the `Total Capex` subtotal printed under them read 366,407.0k.
`verify-cf-capex-foot` **57 NEW**, teeth proven by four sabotages (15 / 1 / 15 / 8 failures) plus
a permanent counterfactual section. **NOT browser-verified**, but verified through the real shared
row builders against both live saved snapshots.

**Measure first, and the 60,000.0k gap named itself: it was the LAND CONTRIBUTED IN KIND**, to the
currency unit, arriving 35,000.0k in t=0 and 25,000.0k in t=1. That pointed at a second, larger
defect that the reporter had also noticed: the Balance Sheet was out by 25,000,000.

**Defect 1, presentation.** The `Total Capex` subtotal has been the CASH basis since M4 Pass 2P
(`capex.perPeriod.exclLandInKind`), because land contributed in kind never leaves the bank: it is
recognised as Land and Share Capital simultaneously. The asset rows above it were the FULL cost.
Two bases under one heading, so the column could not be added up. The phase filter compounded it:
`capexFiltered` summed the full cost while the unfiltered branch used the cash basis, so changing
the FILTER changed the BASIS rather than the scope. `AssetCF` gained `landInKindPerPeriod`; the
rows, the bucket headers and both filtered and unfiltered subtotals are now the cash slice, and
the in-kind land is stated on a project-level `(memo) Land In-Kind (non-cash, not in Total Capex)`
row rather than dropped. **`AssetCF.capexPerPeriod` is deliberately unchanged** and still carries
the full cost, because inventory, per-asset returns and the IC report want the asset's CARRYING
value, not its cash; the verifier asserts that explicitly so a future tidy-up cannot net it down.

**Defect 2, and this is the money one.** In-kind LAND is capitalised by the capex engine, per
asset, in the CONSUMING asset's window. In-kind EQUITY was stamped by a separate walk over the
parcels in `computeDebtEquitySplit`, at the OWNING parcel's phase. **Those are the same quantity
described twice**, and they agreed for as long as a parcel belonged to its phase. Since
2026-08-17 A PARCEL IS PROJECT-WIDE, so a Phase 2 asset can draw on Phase 1 land, and from that
day the equity credit and the asset it creates could land in different columns. Measured live:
equity recognised the whole 60,000,000 at t=0 while the land arrived 35,000,000 / 25,000,000, and
`bsDifferencePerPeriod` was -25,000,000 in the construction year and zero in every period after.

**The fix is to delete the copy.** `computeDebtEquitySplit` reads `capex.perPeriod.landInKind`
verbatim. **No fallback** to the parcel valuation when that series is empty: land that no asset
capitalises is not on the Balance Sheet, so crediting equity for it is precisely the imbalance
being removed. Its `phases` and `project` parameters became dead and are gone, along with the
arguments the one call site was carefully passing. The per-asset capex loop in
`financials-resolvers` also now calls the shared `phaseLocalToProjectIndex` instead of spelling
the rule out a second time (TRAPS 7.12).

**Measured after: max |Assets - L&E| is 0.00 on BOTH live projects.** FMP - MARINA GATE
25,000,000.00 -> 0.00; FMP RE HUB 360.79 -> 0.00. **That 360.79 had been recorded in CLAUDE.md and
in docs/FUND_LAYER_GUIDELINE.md as a pre-existing solver-convergence residue and explicitly pinned
as a non-defect so it would not be re-reported. It was this defect.** Both records are corrected
in place rather than deleted, because reading a small residue as numerical noise (it was small, it
was relative 5.1e-8, and it did not move with the feature under test) is exactly how a genuine
imbalance survives a verifier looking somewhere else. The only other figure that moves is FMP RE
HUB's total in-kind equity, by 400 currency units on 1.35bn, because it now equals the in-kind
land actually capitalised rather than the parcel's own valuation.

**Why the entire suite stayed green while a live project was out by 25m.** Every pre-existing
fixture puts the parcel in the SAME phase as the asset that consumes it, and several verifiers
already assert the Balance Sheet balances, so the defect is simply unreachable in that shape. It
also needs the later phase's land line to sit off phase-local index 0: index 0 is the upfront lump
and clamps to `offset - 1`, which makes the two rules agree by accident for a phase one year out.
The new fixture is built for the shape (`sqm` allocation mode, an explicit cross-phase
`parcelId`, and Phase 2's land window typed to period 1, all of which the live project has) and
reproduces the live 35,000,000 / 25,000,000 split exactly. Because section A's Balance Sheet check
cannot be sabotaged after the fact (the sheet is already built by the time the harness runs), the
verifier carries a permanent COUNTERFACTUAL section that recomputes what the retired parcel rule
would have produced on the same fixture and fails if the two AGREE, so the fixture cannot quietly
stop reproducing the defect.

Also noted: `verify-fund-fees` is **133 passed + 3 PRE-EXISTING FAILURES**, confirmed by stashing
these changes and re-running (identical figures before and after). The long-recorded 136 was stale.

Trap recorded as [docs/TRAPS.md](docs/TRAPS.md) 7.22.

## 2026-08-17: MODULE 1 CLOSED, 28 defects found by entering a real project end to end

One day, seven batches, all reported from the SCREEN rather than by a check. Two more were found
by the new verifiers themselves. Every fix is browser-verified against the live project (FMP -
MARINA GATE) and a throwaway, pinned by a verifier proven by sabotage, and every fix that could
move a saved number was measured before and after. **No further Module 1 changes before launch.**

**The recurring shape, across almost all 28: one rule written in more than one place, with more
than one answer.** The Y0 lump placement rule lived in five places with two answers. The
"which lines are the land value" rule was written once as a shared predicate and once as a method
test, and the method test also caught RETT. The visibility rule was applied on one side of the
copy planner and not the other. The view lock was written once for the store and once for the
screen. Where the second copy could be deleted it was; where it could not, the two are now
derived from one expression.

**Three structural outcomes.**

1. **THE COUNTRY-GATED RETT MECHANISM IS REMOVED.** `CostLine.requiresCountry` let a line exist
   in the model while being absent from the screen, and that one property produced two silent
   money defects in a week, in opposite directions: the engine charged a hidden transfer-tax row
   (937,500 on a Phase 2 hotel, on top of the user's own line), and once the gate was honoured,
   selecting a country made that row appear and charge a second time. **The fix for a defect on
   both sides of a gate is to remove the gate.** RETT is no longer seeded (the seed is twelve
   lines); it is a catalog entry the user adds when the project needs it, and selecting it stamps
   `phasingSource: 'land_cash'` onto the line so it still lands with the land cash.
   `assetVisibleLines` no longer takes a country, so SHOWN IS CHARGED.
   `retireCountryGatedLines` clears saved rows on every hydrate: not chargeable today means
   REMOVED with its overrides, chargeable today means KEPT with the dead flag stripped. Measured
   on both live projects, total capex identical to the byte (426,406,950 either way on MARINA
   GATE, 23 lines to 21).

2. **VIEW MODE IS VISIBLY READ-ONLY.** The lock lived only at the store's model mutators, which
   no-op silently, so an input accepted a change, displayed it, and discarded it on the next
   render. That is the same defect class as a screen showing a number the model does not use, and
   it is why the asset phasing curve appeared to "revert". The lock is now on the CONTROLS
   (`input` / `select` / `textarea` get a muted, dashed, not-allowed treatment plus
   `pointer-events: none`, and capture-phase handlers stop the keyboard), never on the panel: a
   panel-level lock was tried once before and removed because it killed collapsibles, selectors
   and view toggles. `data-view-editable` opts out the four controls that filter a view rather
   than change the model. The store lock and the screen lock are ONE derivation.

3. **THE LEGACY MIGRATION NO LONGER ENUMERATES WHAT IT KEEPS.** `migrateLegacyToV8` rebuilt every
   record from a literal naming each field, and every app-saved snapshot goes through it, so
   three real fields were destroyed on load over three weeks (`windowFollowsConstruction`,
   `phasingSource`, `capexPhasing`). Records now spread and normalise on top, which also makes
   the loose path agree with the v8 path.

**The batches, in order.** (1) Thirteen defects across Assets and Capex, treated as one piece
because five of them change the same structure: a parcel is project-wide, the capex table is an
ordered user-classified list, no silent zero, the period window follows the phase. (2) Four more
plus the structural change that made a cost line a CATALOG ENTRY plus a label (migration 214).
(3) The Y0 upfront lump had one placement rule written five times with two answers. (4) Three:
area x unit size = count, copy reproduces the line SET, and the RETT double charge. (5) Two:
project-total tiles, and two identical senior debt tables that were NOT a double count (they were
two 50% shares of one requirement, and removing one would have halved a correct model). (6)
Facility shares are used exactly as typed. (7) Four: the country field had no editor at all, RETT
was following a shape that included itself, the phasing curve was saved and then destroyed on
read, and area-mode sub-units had no unit size.

**Verifiers added or grown in this block:** `verify-capex-structure` 134, `verify-cost-catalog`
207, `verify-y0-lump-placement` 23, `verify-facility-shares` 55, `verify-no-hidden-cost-lines` 53
(replaces `verify-country-gate`, which pinned a mechanism deliberately removed),
`verify-snapshot-field-survival` 27, `verify-view-lock` 37, `verify-capex-phasing` 91,
`verify-selected-base` 68, `verify-new-project-defaults` 97. Every trap from the block is in
[docs/TRAPS.md](docs/TRAPS.md) section 7 as 7.12 and 7.15 through 7.21.

## 2026-08-15d: Capex phasing, one curve per asset + derived sources, and the hard/soft split restored

Four related changes to Module 1 Capex, built on ONE shared mechanism because the consolidated input matrices will reuse it. `verify-capex-phasing` **69**, teeth proven by four sabotages (2 / 1 / 1 / 2 failures). **NOT browser-verified.**

**Diagnosis first, and three findings changed the work.** (1) **The engine needed no new maths**: `distributeItemCost` already reads `phasing` / `distribution` / `startPeriod` / `endPeriod` off the resolved line, and `manual` plus a weight array expresses any curve, so this was a RESOLUTION problem throughout. (2) **Both "follow" sources already existed**: the land cash outflow is already computed per period including deferred parcel schedules (`expandDeferredSchedule`), and collections are `sell.cashCollectedPerPeriod`, computed at `financials-resolvers.ts:1418` BEFORE capex at `:1568`, by an engine that reads no cost input, so following collections is acyclic. (3) **Hard/soft was never missing from the data**: `CostStage`, `deriveCostStage` and `byStage` carried it throughout and the per-line stage already reached the PDF and the Excel Inputs tab as a column. Exactly two things were missing, and the brief was right that it "is no longer present": the **per-row marker**, deliberately deleted 2026-05-11 (`M2.0L Pass3 Fix 13`, comment still in the file) leaving only an unlabelled background COLOUR, and **any aggregation in the shared report layer**, where `lib/reports/` contained ZERO references to hard or soft, which is why no export could show a subtotal.

**RETT and Marketing did not exist as capex lines at all.** RETT appeared only as design intent (the `requiresCountry` gate, comment "drive conditional cost lines (e.g. RETT for KSA)") and as free text in the IC report; the only "Sales & marketing" was a Module 3 OPEX default. Both are now seeded standard lines at a zero rate, RETT country gated through the mechanism M2.0c added for exactly it. **SUPERSEDED 2026-08-17c: the country gate is REMOVED and RETT is NOT SEEDED. Gating a line on a project attribute meant a row could be present but invisible, which charged money nobody could see and later doubled a cost when a country was selected. RETT is a catalog entry the user adds; marketing stays seeded. Kept here so the correction is legible.** Commission DID exist and ran `cp/2 -> cp+1`, spread across the back half of the BUILD, which is the wrong shape the brief describes.

**The shared mechanism** (`lib/state/inheritance.ts`) is generic and carries NO capex vocabulary, enforced by a check, because the input matrices are the next consumer. Resolution order is **own -> derived -> group -> fallback**, and the fallback step is the invariant that makes adoption safe: with nothing set anywhere every member resolves to its own stored value. **A follower whose source yields nothing FALLS THROUGH rather than resolving to empty**, degrading to the group then the fallback and reporting `degraded`, so the UI says "set to follow collections, but there are none yet" instead of showing an unexplained curve. `Resolution` carries WHY as well as WHAT so the UI badge and the number cannot disagree.

**Placement, which was the architectural crux.** `computeAssetCost` has **TEN production call sites**; resolving at the call sites would be ten adoption points and one missed site would make the screen and the export disagree about WHEN money moves. The resolution therefore sits INSIDE `computeAssetCost`, immediately before the only loop that reads a curve and after the totals are final (phasing can never move a total, pinned by a check). It is **SKIPPED ENTIRELY when nothing opts in**, checked via `capexPhasingIsInert` rather than trusted to be an identity. **Worth recording: a sabotage removing that guard left the byte-identical checks PASSING**, so the resolution genuinely IS an identity when inert and the guard is belt and braces, not the only thing holding the line.

**A FOLLOWED SOURCE REPLACES THE WINDOW, not just the shape.** If collections arrive in periods 8 to 14 the line spends in 8 to 14. Reshaping inside the line's old construction window would leave a sales cost sitting on the build programme, which is the defect being fixed.

**Two things the verifier work turned up.** (a) A sabotage showed that an **all-zero** source (what a sell asset with no collections inside its own window actually looks like) was covered by NOTHING: the missing-source checks only exercised `undefined`. C9/C10 were added; without them a follower could have collapsed into period 0 silently. (b) **The seeded `commission` line computes to ZERO out of the box**, being `percent_of_selected` with an empty `selectedLineIds` (pre-existing, and the seed comment says the revenue source ships in M2.1). Found because the first draft of E10 used commission and **passed vacuously**. Its collections default is correct but inert until that line has a base.

**Schema is additive and optional throughout**: `Asset.capexPhasing`, `CostLine.phasingSource`, `CostOverride.phasingSource`, all absent on every existing record, and absent means `inherit`, which with no asset curve means "keep what you had". No migration.

## 2026-08-16c: the marketing stage CRASHED the financing capex aggregate (shipped, found by diagnosis)

**A defect introduced by 2026-08-16b and pushed to production.** `aggregateProjectCapex` built `perStagePerPeriod` from a **hand-written literal** (`{ land, hard, soft, operating }`) and incremented `bucket[projIdx]` with **no guard**, so a `marketing`-stage line found no bucket and threw `TypeError: Cannot read properties of undefined (reading '0')`.

**Why the whole 111-verifier suite passed over it twice:** the seeded Marketing line carries a **zero rate**, and `computeAssetCost` skips zero-total lines before building `perLinePerPeriod`, so the line never reached the bucket. **The crash needed a user to type a marketing rate on a new project.** No fixture did, because none existed.

**Fixes, all three in one pass:** buckets are now DERIVED from `COST_STAGES` so a future stage cannot reintroduce it; the increment carries a `if (!bucket) continue` guard so an unregistered stage (a hand-built fixture, a legacy snapshot) is skipped rather than crashing the model; and `lineStage` now uses **`deriveCostStage`** instead of the stored `cl.stage`, because the financing path was the one surface bucketing by the raw field while the screen, the report builders and the engine's own `byStage` all use the derived one.

**`verify-capex-stage-buckets` 14**, and the fixture that was missing now exists: a NON-ZERO marketing line, plus a per-stage-to-`inclAllLand` reconciliation. Teeth proven by two sabotages, and the pair is instructive: restoring the literal WITH the guard does not throw, it **silently drops 30,000** (caught by the reconciliation check B5); restoring the literal WITHOUT the guard reproduces the original crash (caught by B1). A guard alone would have converted a loud failure into a quiet one.

**Lesson worth keeping: a zero-valued seed hides a whole code path.** Every seeded line now ships at a zero rate (2026-08-15), which is right for the user and means fixtures must deliberately give lines a value or entire branches go unexercised.

## 2026-08-16b: marketing is its OWN COST STAGE, which closes the subtotal gap

Follow-up to the cascade work below, which left one thing unexpressible: a reference budget totals CONSTRUCTION COST EXCLUDING LAND as hard cost plus engineering, design, permits, developer fee and contingency, leaving sales marketing OUT of that total while still charging the developer fee and the contingency ON it. The bases were expressible (they are selections); the TOTAL was not, because every subtotal the platform reports is the asset total or a STAGE subtotal, and marketing was a soft cost, so it necessarily fell inside "development cost (excl. land)". Measured gap: 1574.37 reported against the reference 1524.37, exactly the 50.00 of marketing.

**A STAGE, not an exclude-from-totals flag or user-defined subtotal groups.** Marketing is a SELLING cost, not a construction cost, so a stage states something true about it, where a flag would be a general mechanism for omitting any line from any total: a much larger claim, and one nothing else needs.

**Both halves proven on the cascade fixture.** Construction cost excl land is now **1524.37**, matching the reference exactly; marketing is reported as its own 50.00 subtotal; construction + marketing + land reconciles to the total, so nothing is hidden. And the cascade is UNDISTURBED: developer fee 71.40 and contingency 74.97 to the last decimal, because bases are selections of named lines and have never consulted a stage. That second half is the check that matters, since a stage change that quietly altered the fee would be a regression dressed as a fix.

**Three things TypeScript could NOT catch**, found by mapping the blast radius before editing rather than after: (1) two INFERRED object literals in `Module1Costs.tsx` (the Capex-by-Stage table and the tile accumulator) would have silently dropped marketing from the table AND its Total column, since only the sites typed `Record<CostStage, number>` are compiler-guarded; (2) `deriveCostType` has no marketing member and its default branch returns **`'hard'`**, so an unhandled stage would have made a selling cost read as a HARD cost wherever `CostType` is consulted (mapped to `soft`, the honest nearest neighbour, pinned by F3); (3) the `Total Capex Excl. Land` tile needed RENAMING to `Construction Cost Excl. Land` with an "excludes marketing" caption, because leaving the old label on a narrowed figure is worse than the original gap.

**No saved project reclassifies**: all 1400 versions scanned, 0 lines affected, because `marketing` only entered the catalog on 2026-08-15 and seeds on new projects only. **One existing check needed updating**, `verify-capex-phasing` D9 ("Marketing as soft"), which was my own assertion from the previous pass now pinning the superseded classification.

`verify-selected-base` 51 -> **67**, `verify-capex-phasing` **84**. Exports green (excel 304, pdf 58, capex-report 14). **NOT browser-verified.**

## 2026-08-16: `% of Selected Lines` is POSITIONAL, and the cost cascade composes

**Reported:** the picker on Contingency listed only standard catalog lines, so a Developers fee (charged on hard cost plus the softs above it) could not be included in Contingency's base and the chain could not be built.

**Diagnosis, with one correction to the premise.** The picker filtered on `c.method !== 'percent_of_selected'` (commented "we don't allow recursive references"), a **blanket ban on the whole method**. A developers fee IS a `percent_of_selected` line, so it was excluded by METHOD, not for being custom. **Custom lines were never excluded** (`targetAssetId === undefined || === asset.id` passes project-wide and this asset's own), and there was **no ordering filter at all**, so the picker offered lines BELOW as well as above: simultaneously too strict about method and too loose about position. The ban also stopped the STANDARD catalog putting professional fee inside contingency's base.

**Measured engine behaviour before the fix**, on build 1000 / dev fee 10% / contingency 5%: dev fee ABOVE contingency gave 55.00 (correct); moved BELOW it gave **50.00**, the fee silently counted as zero. A self-reference resolved to "ignore myself" by accident, and a two-line cycle produced 500 and 750, both artefacts of array position. Pass 3 is a single forward sweep reading a map filled as it goes, so **position was already load-bearing, unguarded and unreported**.

**Fix:** shared `src/core/calculations/selectedBase.ts` (`assetVisibleLines` / `eligibleBaseLines` / `allowedSelectedIds`). A line may charge on anything strictly ABOVE it on that asset, in display order (order is array order: the Capex table filters `costLines` and never sorts). The picker offers exactly that set, including custom, country-gated and `percent_of_selected` lines; the ENGINE filters the stored selection through the SAME function. Upward-only references make a cycle unrepresentable, so no method ban is needed to stay safe.

**A correction I had to make to my own claim, and the verifier gap that hid it.** I first wrote that enforcing the rule "moves no existing model", and section C of the verifier passed a sabotage that removed the engine filter entirely. Both were wrong for the same reason: a downward reference to another PERCENT line already contributed zero (the forward pass read an absent entry), but a downward reference to a **DIRECT-method** line is different, because `directTotals` is fully populated in Pass 1 and the old code read a REAL number. On build 1000 / contingency 10% selecting a 500 line below it, the base was 1500 and the answer 150; it is now 1000 and 100. `C2a` is the check that distinguishes enforcement from accident, and it fails with `150` under sabotage.

**No saved model actually moves.** Audited **all 1400 versions of both projects** (28,210 selection-carrying lines): 2 downward/self references, **0 that move a number**, and both turn out to be DANGLING ids pointing at cost lines deleted from MARINA GATE's phase 1, not downward references. **Method note worth keeping: the first audit silently hit the PostgREST 1000-row cap** and reported 1002 versions when FMP RE HUB alone has 1398; redone with explicit `.range()` pagination and jsonb field selection (`snapshot->costLines`) so 1400 snapshots did not have to be pulled whole.

**The reference cascade composes exactly**, built by selection with nothing hardcoded, and is pinned as a permanent check (section E): hard 1300 (superstructure 1000 + parking 200 + landscape 100), engineering 26 (2%), design 39 (3%), permits 13 (1%), sales marketing 50, developer fee **71.40** (5% of 1428 = hard + the three softs + marketing), contingency **74.97** (5% of 1499.40, including the developer fee), total excl land and excl marketing **1524.37**. RETT 150,000 (5% of land cash 3,000,000) sits in neither base. Every base was checked OFFERABLE in the picker, not merely computable, because a cascade the engine resolves but the UI will not let you build is not usable.

**KNOWN GAP (being handled as its own pass):** the reference total excludes sales marketing while including it in the fee and contingency bases. The bases are expressible; the TOTAL is not, because every subtotal the platform reports is the asset total or a STAGE subtotal and marketing is a soft cost, so it falls inside "development cost (excl. land)". Platform reports 1574.37 against the reference 1524.37, a difference of exactly the 50.00 of marketing. Recorded in `selectedBase.ts` and pinned by `E9` so it is a stated limitation rather than a surprise. **Decision taken: give marketing its OWN STAGE** rather than an exclude-from-totals flag or user-defined subtotal groups, because a stage states something true about a selling cost instead of adding a mechanism that lets any line be omitted from any total.

`verify-selected-base` **47**, teeth proven by sabotage (removing the engine filter fails C2a).

## 2026-08-15e: the inherited asset curve was MISALIGNED (money on a curve nobody typed), and land was wrongly in the scheme

Two defects in the 2026-08-15d work, both reported from the screen, both diagnosed by MEASURING THE ENGINE rather than reading the UI.

**1. The asset curve reached the money rows, and reached them WRONG. A correctness bug, not a display one.** With a typed curve of `0 / 10 / 30 / 40 / 20` and a line spanning P1..P4, the engine distributed `0 / 12.5 / 37.5 / 50`. **Cause: `distribute('manual', span, weights)` reads `weights[i]` counting from the START OF THE LINE'S WINDOW, while the asset curve is authored against ABSOLUTE periods P0..Pn** (which is how the control labels it). Handing the whole array to a line starting at P1 applied the P0 weight to P1, shifted every later weight one period early and dropped the last weight off the end. Fixed by slicing the curve to the line's window. **The fix created a hazard that had to be closed in the same change**: a window the curve does not cover, or an all-zero slice, yields a zero distribution, which distributes NOTHING and makes the line's money vanish silently. It now degrades to the line's own curve instead. The row was ALSO stale (it rendered the STORED weights whatever was driving), so the display and the numbers were both wrong and differently wrong; the row now renders the curve IN FORCE, labelled `Asset curve %` and READ-ONLY, because an editable box that does not drive the number is worse than no box. A derived-source line gets a caption naming the source instead of numbers this row cannot compute.

**2. Land was in the phasing scheme at all.** Land cash timing comes from the parcel schedule, so a curve and a "not inheriting" badge implied a decision the user does not have. New shared `isParcelDrivenLandLine`, used by the ENGINE and the ROW from one definition. **Identified by base id, deliberately NOT by stage or by method: both shortcuts would wrongly catch RETT**, which is a land-stage cost charged on land value that genuinely must follow the land cash outflow. Pinned by a check, and a sabotage adding `rett` to the predicate fails it. Land money never actually moved (5.00m at P0 either way), so this was presentational plus latent.

**A gap the new checks found in the SHARED mechanism.** It collapsed "no group set" and "a group exists but says nothing for this row" into one outcome, so the reason read "the asset curve is not set" when it plainly was. Values were right, the explanation misdirected. `InheritanceRequest.groupEmpty` now separates them; the consolidated input matrices will hit the same case.

`verify-capex-phasing` 69 -> **84**, teeth proven by two more sabotages (reinstating the prefix bug fails C2a to C2d with its exact signature `[[0,10,30,40]]`; catching RETT in the land predicate fails E12b). Engine-sensitive verifiers re-run green. **NOT browser-verified, and the display half is exactly what only a browser confirms.**

## 2026-08-15c: the IC deck never offered the fund slides to an existing deck, and nothing could have caught it (HELD FOR REVIEW, UNCOMMITTED)

**Reported symptom:** the exported IC deck for FMP RE HUB had 36 slides and NO fund content at all, the only occurrence of any fund term being the word "hurdle" inside the sensitivity commentary.

**Diagnosis, all three questions measured rather than reasoned about.** (1) The fund block WAS reaching `ICReportModel`: built from the project's current saved version, `fund.active` true, `hasFeeIncome` true, 3 terms rows, 5 fee-basis rows, a 10-row waterfall with `hasData`, 2 gross-vs-net rows, 4 earner rows, both notes populated. (2) The three slides were NOT gated out: all three `available(model)` returned **true** on that exact model, and a fresh seed yields 32 available templates including all three. (3) The deck was seeded before the feature shipped and **the upgrade path never offers them**.

**Three independent gates, all closed, any one sufficient**: `DECK_SCHEMA_VERSION` was still **3** (the fund work of 2026-08-13 added three templates without bumping it); `TEMPLATES_BY_VERSION` carried keys **2 and 3 only**, so the fund ids appeared nowhere in it and a bump ALONE would have changed nothing (`candidates` empty, the upgrade silently lifting the version and adding no slides); and `Module7Deck.tsx` short-circuits before even calling `upgradeDeckLayout` when `schemaVersion >= DECK_SCHEMA_VERSION`, which was true at 3 >= 3. **The fix therefore needed BOTH edits, not one.**

**What it cost users:** the only route to the fund slides was "Rebuild every slide from the library", whose own confirm text says it replaces the current arrangement. It DELETEs the deck and re-seeds, discarding arrangement, edits, free text boxes and any AI-drafted narrative. So a user who had invested work in a deck could not get the fund sections without throwing that work away.

**Fix:** `DECK_SCHEMA_VERSION` 3 -> **4**, `4: ['fund_terms','fund_waterfall','fund_returns']` registered, and a `CLOSING_TEMPLATE_IDS` anchor so the block lands at the END OF "The case" (before Key Risks / Recommendation / Appendix) exactly where `seedDeck` puts it, rather than being appended after the Appendix.

**THE REAL DEFECT, which is the reason this entry exists.** Registering a template was a **two-step contract (bump + register) that NOTHING enforced**. A template added to the library and left unregistered reached freshly seeded decks and was invisible to every existing one, permanently and silently: no error, no notice, no failing check. The fund slides were its first casualty and would not have been its last. Closed by `verify-deck-template-registry`: **`TEMPLATES_BY_VERSION` is now COMPLETE**, every id in `SLIDE_TEMPLATES` appears under exactly one key including the v1 originals that no upgrade will ever offer (`Number(v) > from` is false for key 1 even on a legacy deck defaulting to 1, so listing them is inert), and the verifier fails when any template id is unregistered, when a registered id does not exist, when an id is registered twice, when the registry and library sizes disagree, or **when the newest key does not equal `DECK_SCHEMA_VERSION`** (which is the bump half of the contract, and it fails independently of the register half, proven by two separate sabotages).

**The non-destructive claim is PROVEN on the real saved deck, not asserted.** Section B loads FMP RE HUB's actual 36-slide deck from the database (read-only, never written back), forces it to schemaVersion 3 to replay what a user's deck does on open, and asserts: all three fund slides added; every original slide still present BY ID; every original slide byte-identical on a fingerprint that excludes only the section-number chip that `renumber` legitimately rewrites; the original slide ORDER preserved; every original text object (which is where free text and AI narrative live) surviving byte-identical; section numbers still contiguous; the fund block before the closing slides and in library order; a second open adding nothing; **a user-deleted fund slide NOT resurrected**; a fund-OFF project gaining no fund slides but still being lifted so it is not re-checked forever; and the upgraded deck carrying every template a fresh rebuild would. `verify-deck-template-registry` **28 with DB creds, 8 without** (section A only, and the skip now SAYS it skipped: an earlier draft printed "falls back to a synthetic deck" and then simply returned, which would have let a green run read as though the non-destructive claim had been tested). Teeth proven by four sabotages (10 / 8 / 1 / 6 failures). **One verifier robustness fix came out of the fourth**: a destructive upgrade CRASHED the fingerprint check on an undefined slide instead of failing cleanly, which would have made a genuinely broken fix look like a broken verifier. **NOT browser-verified.**

## 2026-08-15b: the rest of the create-path pre-fills, and the degenerate allocation basis underneath them (HELD FOR REVIEW, UNCOMMITTED)

Follow-up to 2026-08-15 (below), same principle applied to the three create-path pre-fills that were flagged but not changed in that pass. **The land one could not be done safely until an ENGINE defect underneath it was fixed, and that defect is the more important half of this entry.**

**THE ENGINE DEFECT: a share basis with a zero denominator DROPPED the cost.** Every share basis in `src/core/calculations/index.ts` was written as `return total > 0 ? mine / total : 0`, which returns 0 for **every** asset when the total is zero. The shares then sum to 0 instead of 1, so a project-level lump sum allocated on that basis is allocated to **nobody** and leaves the model with no warning. **Measured before the fix**, one asset, a phase with no land, a `fixed` cost line of 1,000,000: on `land_share` total capex moved by **0.000m**, while the identical line on `bua_share` or `per_asset` moved it by the full 1,000,000. This is pre-existing and reachable on any phase whose basis quantity is genuinely zero (no parcels yet, an operational asset with no new build, areas not entered yet); a new project defaulting to an empty land row makes it the ORDINARY case, which is why it had to be fixed first rather than used as a reason to keep the pre-fill. Fix: a shared `equalPhaseShare(asset, phaseAssets)` fallback at all **six** call sites (`bua_share` / `gfa_share` / `land_share` on `resolveAllocationFactor`, and `bua_share` / `land_share` / the `value_share` tail on `resolveDriverFactor`). **The equal split is not a new convention**: `computeAssetLandSqm` already falls back to `phaseLandSqm / phaseAssets.length` when total BUA is zero, and the `category` basis is already `1 / sameCat.length`. **Companions are excluded** (they carry no land, BUA or GFA by construction), so the eligible assets split the whole cost and the factors still sum to exactly 1, proven by a check rather than asserted. Zero eligible assets still returns 0, the one case where dropping is the only option. **Scope note: `resolveAllocationFactor` is only consulted for the `fixed` method** (`FIXED_METHODS_NEEDING_ALLOC`), because every `rate_per_X` method already returns an asset-scoped contribution. **This can MOVE NUMBERS on existing models**, but only upward and only where a cost was previously being lost.

**The three pre-fills**, all of which fed the model directly: (1) the wizard's initial land row was **100,000 sqm at 500/sqm**, so clicking through the wizard untouched put 50m of land cost into the project; now 0 x 0. (2) `handleAddParcel` was 50,000 sqm at 500/sqm (25m per click); now 0 x 0. (3) `handleAddSubUnit` was **50 units of 100 sqm at 1,000,000 each**, which is 5,000 sqm of BUA and 50m of revenue per click, plus a 65% occupancy and a 35% margin on the operating strategies; all now zero. `makeDefaultParcel` (which backs the store default state and the migrator's no-parcels fallback) is zeroed at the factory. **Only the STRUCTURE is still chosen for the user**: the sub-unit's category and metric, both derived from the asset's strategy.

**The empty land row would have become a WALL, and that is worth recording.** The wizard's step 2 gate read `p.area > 0 && p.rate > 0`. That was harmless while the row arrived pre-filled (it could never fail), and it turned into a required-entry block the moment the row shipped empty: Next was disabled until the user typed land. Relaxed to `>= 0`, because the wizard **already ships `assets: []` and `subUnits: []` and defers them to Tab 2**, so land is a row to fill in on the same terms, and a zero-land project computes cleanly now that the allocation fallback exists. The split gate is untouched and still has to sum to 100. **General lesson: when you blank a default, check every validator that was silently satisfied by the old value.**

**The one pre-filled field deliberately NOT zeroed, flagged for the user's call:** the parcel's `cashPct` / `inKindPct` 60/40 split. It ROUTES value rather than creating it, so at a zero rate it moves nothing and the "nothing enters unless typed" rule is already satisfied; zeroing both halves would make land cost vanish entirely once a rate WAS typed; and moving it to 100/0 would be a modelling-policy change (no in-kind land equity by default) that was not asked for. A check pins that it still sums to 100 so the intent reads as deliberate rather than an oversight.

`verify-new-project-defaults` 71 -> **97**, teeth proven by four more sabotages (5 / 2 / 3 / 1 failures). **Full suite re-run after the engine change: 108 verifiers, 4 non-zero exits, and all four are the known pre-existing failures** (`verify-module6-scenarios` 127+1, `verify-pricing-application-ready` 48+1, `verify-tab2-pass2` 16+5, and `verify-strategy-switch` on its credential-less fixture path). Nothing regressed, which matters here because the allocation fallback is an ENGINE change: had any fixture carried a degenerate basis with a cost silently parked on it, this is where it would have surfaced. **NOT browser-verified.**

## 2026-08-15: three new-project defects (blank capex seed, strategy alert on first pick, the version selector that never named a version)

Three defects found while creating a new project. All three are **create-path** faults; none of them needed an engine change or a migration. `verify-new-project-defaults` **71**, teeth proven by six sabotages (7 / 2 / 2 / 1 / 5 / 2 failures). **NOT browser-verified.**

**1. Capex arrived pre-filled, and the values were SEEDS, not a template.** `makeDefaultCostLines` (`module1-types.ts`) hardcoded the reference benchmark rates into the standard 10-line catalog (Construction BUA 4,500/sqm, Parking 25,000/bay, Infrastructure 250, Landscaping 75, Pre-operating 3%, Professional Fee 6%, Commission 4%, Contingency 5%), and **all four callers run on a new project**: the wizard (one catalog per phase), the store default state, `addAsset` (first asset in a phase with no lines) and the hydrate-time seed migration (which fires for ANY phase with no lines, so it is also the path a phase added after create takes). Confirmed against the live DB: the Thursday project's wizard-written v1 carries **0 assets and 20 cost lines** already rated. They reach the costing silently because the rows arrive **switched On** (`Module1Costs.tsx`, `checked={!effDisabled}` with `disabled` unset) and are deletable rather than locked, so a seeded rate is indistinguishable from a typed one. Fix: `makeDefaultCostLines` gained a `CostLineSeedValues` mode; `'reference'` is UNCHANGED and is what the 16 fixture-building verifiers still use, and the new `makeBlankCostLines` emits the **structurally identical** catalog (same ids, names, methods, stages, allocation bases, period windows, `selectedLineIds`) with every editable rate at zero. All four product paths use it. **The two LOCKED land rows are a deliberate exception and keep their 100**: they are not cost assumptions but the derivation carrying the parcel value the user typed into the model, they cannot be unchecked or deleted, and zeroing them would drop land out of the project entirely. The discriminator is `isLocked`, never an id list, so a third derived row added later is protected automatically.

**2. The strategy-change alert fired on asset CREATION.** An asset is created as `'Sell'` (`Module1Assets.tsx handleAddAssetToPhase`), so the user's FIRST pick from the Strategy dropdown is technically a change and got the full confirm dialog plus the persistent review banner. It fired hardest where there was least to say: `needsReview` means "what is now active but empty", and on a blank asset that is everything (measured: four items). **The report cannot answer this on its own**, which is why the gate is a separate predicate rather than a tweak to `applyStrategySwitch`. New pure `assetHasStrategyAssumptions(state, assetId)` in `strategySwitch.ts` is true only when the asset carries sub-units (ANY category, Support included), opex lines, a revenue config, parked `retainedByStrategy`, a companion, a cost override, or an asset-targeted cost line. **Useful life is deliberately excluded**: it is not strategy scoped and survives every switch. The store measures it against the state BEFORE the switch and only then attaches `strategyReview`; the dropdown gates on the SAME predicate, so the dialog and the banner cannot disagree. **Suppressing the banner does not suppress the model operation**: Sell + Manage still seeds its companion on a bare asset, pinned by a check.

**3. The version selector read "Unsaved draft" always, and the auto-name never rolled over.** ONE root cause: `projectsToStorageShape` writes `versions: {}` (the project LIST endpoint returns a version COUNT, never the rows), and four surfaces looked the active version up in that map, so the lookup could only ever miss. Consequences: the **Topbar** and **Sidebar** read `activeVersionData?.name ?? 'Unsaved draft'` with the amber warning chip permanently, on every project in every state (this never worked, it is not state dependent); **`NameVersionModal.existingVersions`** was fed the same empty map, so `getNextVersionNumber([])` returns `'1.0'` every time and **the X.Y rollover has never advanced** (confirmed in the DB: exactly one row across all projects carries a `version_label`, and it is 1.0); **EditChoiceModal** could not name the version about to be overwritten in place; and **ExportModal** lost the version label. Separately, `handleCreateFromWizard` never called `setActiveVersionId` at all. Fix: the loaders hand the identity back instead of making the shell find it (`AttachResult.version`, `loadVersionInto(...).version`), both named through a shared `resolveVersionDisplayName` in `versionNaming.ts` (label -> `v1.3` -> `Version 3` -> null); the shell holds `activeVersion` state set on open / create / load / load-for-edit / auto-started session / explicit save and cleared on close, guarded on the id so a name cannot outlive its version; and the naming modal reads a real `listVersions` fetch refreshed after every version creation. **It reads as a draft only when there is genuinely no version row.** The end-to-end check stubs `fetch` and drives the REAL sync layer, so it asserts the NAME rather than the wiring that produces it.

**The other two versioning rules were NOT broken** and were verified against the live DB, not assumed: the last saved version opens with the project (`POST /versions` bumps `current_version_id`; the GET prefers it and falls back to latest), and other versions are browsable (`VersionModal` reads `GET /versions` directly). Autosave also works: it PATCHes the session's version row every 1.5s and the rows show the work landed. **A reported data loss was withdrawn by the user as a mis-identified project, and the DB agrees.**

**Two existing verifiers were updated, both because they pinned the OLD contract:** `verify-tab3-default-seed` asserted `construction-bua value === 4500` (now asserts the blank rate plus "no editable seeded line carries a rate", so the shape stays pinned), and `verify-strategy-switch`'s "the store records the report as a review banner" was a **grep for the literal `strategyReview: { ...res.report`** that broke the moment the assignment became conditional even though the behaviour was intact. It is now behavioural and asserts BOTH halves (banner on a real change, no banner on a first pick), 82 -> **84 with DB creds, 0 failures**; on the credential-less fixture path it is 56 passed + the 1 long-documented pre-existing failure.

**Pre-existing failures confirmed by stashing (NOT caused by this work):** `verify-module6-scenarios` 127 passed + 1 ("Module 7 is Reports and live (enabled)") and **`verify-tab2-pass2` 16 passed + 5**, which was previously UNRECORDED anywhere. `verify-pricing-application-ready` 48 + 1 is the already-documented one.

## 2026-08-12: PDF export review, three passes (arithmetic, readability, consistency) + two shared builders

The workbook got its review on 2026-08-11; this is the equivalent pass over **both PDFs**, the full project report and the executive summary, every section from the cover onward, at all three display scales. Diagnosis first (no fixes), then three reviewed passes. Commits `2f546659` (pass 1), `3cea19dd` (pass 2), `76ed9e50` (pass 3 + the checks fold-in).

**Measurement note that made the review possible, and that cost a wrong first answer.** pdf-lib writes CID glyph ids, and Inter substitutes CASE-ALTERNATE forms for `( ) [ ] - :` when they sit beside digits or capitals. Those alternates decode through `scripts/pdfTextExtract.ts` to PRIVATE USE codepoints (U+E081, U+E082, U+E083, U+E084, U+E088, U+E092), so a naive extract silently **drops every parenthesis and turns negatives into positives**. The first diagnosis pass read "Cost of sales 4,507.6" as a sign error when the value was `(4,507.6)` and correct. Every verifier added here normalises the PUA range before matching.

### Pass 1 (`2f546659`), wrong numbers and sign conventions

Six defects, all of which the existing suite passed over because each check asserted a row EXISTS, not that a column ADDS UP.

1. **The cash sweep waterfall added interest and principal instead of subtracting them.** `directCF.interestPaidPerPeriod` and `directCF.debtRepaymentPerPeriod` are stored ALREADY NEGATIVE (the snapshot field comments say so; returns-resolvers, icReport and the on-screen waterfall all treat them that way), and `buildCashSweepTables` negated each a second time. Overstatement was 2 x interest + 2 x principal: on FMP RE HUB "= Cash Available" printed 2,041.2 in 2030 against a true 1,800.0, and the row captioned "ties to CF + BS" no longer did. **The on-screen waterfall was already correct** (it has its own inline implementation), so only Excel and the PDF were wrong; the shared builder now matches the screen's formula. The minimum cash reserve is also a RESERVATION, never spent, so deducting it into a subtotal the next subtotal ignored broke the chain even with correct signs; it is now an indented memo pair. The chain is `Opening + Ops + Inv + Equity + Debt Draw + Interest = Cash Available`, then `Cash Available + Debt Paid + Dividend = Closing Cash`, an exact identity in every period.
2. **The FCFE build-up did not foot, PDF only.** It started from FCFF, which already carries the terminal ENTERPRISE value, then added the terminal EQUITY value on top. The two are equal whenever debt at exit is zero, which is why it read plausibly. The screen, Excel and the IC report all had the correct list; the PDF was a fourth copy. See the shared builder below.
3. **Three different "new debt" figures.** `528.4` = cash drawdown + capitalised interest = debt actually raised (what Sources & Uses uses); `469.2` = cash drawdown only. The executive summary printed the second under the first's name. It now prints 528.4 labelled "incl. capitalised interest"; the FCFE row keeps 469.2 labelled "(cash)", because capitalised interest is not a cash flow. **Correction worth keeping:** "only 528.4 reconciles peak debt" was true on this project but is NOT the general invariant. A project that draws and repays concurrently legitimately raises more than its peak. The verifier checks the roll-forward instead (`existing + raised - repaid == closing`).
4. **Per-asset economics reported ZERO cost, 100% margin and a null yield on cost for every asset** on a project with 4.9bn of development cost. `perAssetCF.capexPerPeriod` is a POSITIVE magnitude while the project-level `directCF.capexPerPeriod` is negative, and the field carried no sign comment either way, so the engine's `Math.max(0, -sum(capex))` clamped everything to zero. Fixed at the call site with the convention now documented on both ends. Ships on screen too. Two rows still read 100% and are CORRECT: an existing operational asset whose spend is historical pre-capex, and a companion asset whose capex sits on its parent.
5. **The summary PDF's P&L could not be added.** It printed cost of sales, opex, D&A, interest and tax POSITIVE beside a NEGATIVE fund fee row, so the column summed to 22,724.7 against a printed EBITDA of 4,683.6, and the one negative row made it look deliberate. The full report's P&L renders from the shared builder and was correct.
6. **Cash balances and the standing minimum reserve were summed across periods** (the 50.0 reserve totalled 700.0). Stock rows now carry an explicit closing-value Total, on screen as well.

`scripts/verify-report-arithmetic.ts` **78**, teeth proven by six sabotages.

### Pass 2 (`3cea19dd`), readability and display scale

**The export offered three display scales and only one produced a readable document.** Measured with the embedded font: the widest period cell needs 45.3pt at millions, 55.3pt at thousands and 71.5pt at full, against a hardcoded 52pt. A full-unit export ellipsised **3,867 numeric cells** in a real 14-year report ("323,870,..."), thousands ellipsised 43. Column geometry is now RESOLVED PER DOCUMENT from the widest cell it will actually draw, which required building all module content before rendering (the builders are pure, so it costs ordering only). **The trade is pages, and it falls only where the problem was: millions unchanged at 104pp, thousands and full 104 -> 140pp.** Wider numbers cost pages, never digits.

**Labels: four causes, not one.** (a) the label column was too narrow (now 230pt minimum, growing to the document's longest label up to 320pt, then shrinking to 6.5pt before it will ever truncate; numeric cells never shrink, so a column stays uniform); (b) **the Inputs legend measured its text then handed that width to `drawCell`, which subtracts 4pt of padding a side** and so was always 6pt short, truncating on every Inputs page of every report ever produced; (c) **the executive summary narrative had the SAME bug**, wrapping to `CONTENT_W - 4` while drawing into `CONTENT_W - 8`; (d) grid tables used a fixed 28%/equal-share split which cut whichever column held the long strings. Result: zero ellipses anywhere, either document, all three scales.

**Per-unit rates are deliberately NOT scaled.** Scaling an ADR of 268 renders "0.0", which destroys the figure rather than scaling it. What read as broken was an unlabelled 2,590,000 under a band saying "All figures in SAR millions", so every such column now names its unit: `Unit price / ADR (SAR/unit)`, `Rate (SAR/sqm)`, `ADR (SAR/night)`, `Indexed rate (SAR/sqm)`.

**Signed zeros are gone.** A value that ROUNDS to zero at the displayed precision now prints the same en-dash an exact zero does, fixed in the shared `formatAccounting` so screen and PDF move together. 126 such cells in the full report and 13 in the summary, including a Balance Sheet cash line reading "(0.0)" and the reconciliation row captioned "Unexplained (must be 0)" showing "(0.0)".

`scripts/verify-report-readability.ts` **55** over three scales x two documents x fixture and real project, teeth proven by six sabotages. Toggle-off proven byte-identical at ALL THREE scales, where it had only ever been checked at millions.

### Pass 3 (`76ed9e50`), consistency and missing content

- **F1/F3, one name per metric.** Two cards both read "Equity Multiple" (2.10x FCFE, 2.22x distributions), the second with no basis; and the same metric was "Dividend IRR" in the executive summary and "Distributed Equity IRR" on the next page. Fixed at the definition: the exec summary and the summary page now take the SHARED card list instead of writing their own.
- **F2, gross vs net.** On a fund project every headline was GROSS while the Fund Layer page two pages later showed NET, with nothing saying which. Proven by forcing the hurdle to 0 (fee 623.6m): gross 7.1% / 2.22x against net 6.2% / 1.98x. Headlines are now net where a fee arises, captioned as such, with the gross named beside them; where no fee arises the document says so.
- **F4**, every margin states its basis, and the appraisal profit against profit after tax (3.6x apart on adjacent pages) carries a bridge naming the three items an appraisal does not deduct. **F5**, the live terminal-value parameter is marked `(applied)` and the other `(not applied)`.
- **G1/G2/G3**, both documents carry a Model Integrity Checks table (the full report had one BS row buried inside the balance sheet; the summary had nothing). Tolerance relative and PEAK-anchored. A sub-1.00x DSCR carries the breached-year count and a sentence, and a Lender Covenants table states threshold vs modelled vs verdict from the same `evaluateCovenant` the screen and workbook use.
- **H1**, the model version prints on the cover, in every footer, and the comment when supplied. `ExportModal` already computed the label (it queries the chosen version row for it); the generator simply never read it, so the version picker changed only the filename.
- **H2**, Module 5 tab numbers are DERIVED. Hardcoded, every project without scenarios printed headers and a ToC reading Tab 1, 2, 4, 5.
- **H3**, Timeline, Land & Area, Fund Inputs, Lender Covenants and the two-way Sensitivity grid added. **Sensitivity is ENTITLEMENT-GATED**: it defaults to omitted and `ExportModal` passes the live gate, because an export must not contain what the plan cannot open on screen.
- **H4/H5**, the executive summary states the fund fee in its cost story with its own KPI card, and the funding claim is scoped to NEW funding with the real capital mix beside it ("funded 100% debt / 0% equity" sat directly above a table showing 2,550.7 of equity).

`scripts/verify-report-consistency.ts` **59**, teeth proven by five sabotages.

## PDF + workbook export review, pass 4: PRESENTATION (2026-08-13)

Six items, all raised against BOTH PDFs and the workbook, all measured on the real project before any code changed. Every one is a number the engine gets right and a reader cannot interpret.

- **P1**, the fund waterfall's Total column mixed lifetime flows with untotalled balances silently. Hurdle Paid printed **5,668.5m** (fourteen years of payments summed) directly under Total Hurdle Owed, which carries no total and closes at **4,834.0m**. Scanning down, more was paid than was ever owed. Blanking the balance totals is the right call and stays; what was missing was the sentence. A shared `fundWaterfallTotalsNote` now sits under the table on all four surfaces, naming which rows are flows and quoting both figures **from the model**, so the caption cannot drift from the table above it.
- **P2**, **"Total" meant two things in one document.** The P&L used it for a genuine lifetime sum; `buildBSRows` sets every `totalOverride` to the FINAL period, so TOTAL ASSETS printed **2,512.1m**, the at-exit figure, under a heading claiming a sum; and the cash flow statement used it for both at once (four summed flows plus closing cash). The heading is now DERIVED from the rows: new `M4Row.totalIsBalance` on all 47 point-in-time overrides, plus `resolveTotalColumnKind` / `totalColumnHeading` / `totalColumnNote` in `m4Reports.ts`. Balance sheet reads **Closing**, cash flow and schedules read **Total / Closing** with a note naming which lines are which, the P&L is untouched. **Two traps.** `drawPeriodTable` had hardcoded the literal `'Total'` and never read `table.columns[1]`, so changing the builders alone produced no visible change at all; and `resolveMetrics` measured only CELLS, so a 14-character heading would have been shrunk to fit rather than given a column, and the heading is now measured as content. The summary PDF's statements are hand-built rows rather than M4Rows and had a parallel `pdfTotalHeading`; it now calls the same `resolveTotalColumnKind`, which a sabotage confirms (reversing the classifier used to break two surfaces, it now breaks three).
- **P3**, a structural zero now says so. **Hotel Phase 1** is an existing operational asset: no new build, so BUA computes nil, and its **3,682.1m** of cost was spent before period 0 and sits in historical pre-capex, not in any capex line, so per-asset economics reads cost 0.0m and margin 100.0%. **Residential Tower - Operate** is a companion: area and cost both sit on its parent. New shared `lib/reports/assetNotes.ts` prints `- [a]` with a footnote naming the reason, on the composition table, the assets table, per-asset economics (cost, margin AND yield on cost) and the workbook's Returns and Land & Area tabs. Land & Area keeps its FORMULAS (column D feeds the group subtotals, so a text marker there would break them) and gets a named footnote block instead.
- **P4**, the three capital bases read as fees. **The reported symptom was exact for the workbook only** and this is worth recording: both PDFs already placed the amount under `Basis charged on` (grid columns are per-table and content-sized, so nothing can shift a cell into a neighbouring column), while Excel has ONE `Total` column serving both the `: basis` and `: charged` rows, so "Total equity 2,550.7" sat directly above "Fund structure fee: charged 26.9" with Base and Rate empty. They are now their own captioned block above the fee table on all four surfaces, tagged `capital base`, with a shared "amounts of CAPITAL, not fees" note.
- **P5**, Distributed Equity IRR and MOIC appeared three times in ten pages. Two of the three were `headlineReturnCards` rendered **byte-identically** on the executive summary and the returns page. The returns page now omits the pair and states where they are; the fund page keeps its gross-vs-net split and gains a shared `fundHeadlineRestatementNote` saying it is the same metric decomposed, which matters most on this project, where no performance fee arises and all three read the same number.
- **P6**, everything above confirmed in the full report and the workbook, not only the summary.

`scripts/verify-report-presentation.ts` **92**, teeth proven by **nine sabotages** (8 / 5 / 19 / 2 / 2 / 1 / 1 / 1 / 4 failures). Fund toggle-off re-proved (absent vs populated-but-disabled byte-identical on both PDFs), and the NON-fund fixes are separately asserted to still render with the fund off, so a half-landed fix cannot pass. **NOT browser-verified.**

### The two shared builders (the reason pass 1 item 2 happened at all)

**`lib/reports/streamReports.ts`** is now the ONE definition of the FCFF / FCFE / Distributed Equity build-ups. The M5 screen, the Excel Returns tab, the IC report and the project PDF all consume it. The builder owns which rows exist, in what order, and which series each carries; a surface may restyle the wording through a label-override map, which is how the IC deck keeps its sentence case and `= FCFE, levered equity` with byte-identical output. The IC report guarded for an ABSENT `buildup` with optional chaining, so the builder tolerates missing series by contract (the first version did not, and crashed three deck verifiers).

**`lib/reports/checksReport.ts`** is the ONE definition of the three integrity identities and their tolerance, consumed by both PDFs AND the workbook's Checks tab (the workbook's inline copy was folded in, proven byte-identical by a 176-cell fingerprint of the Checks sheet at two display scales). Its money formatter stays pinned to millions there so the extraction changed no rendered text; **that pinning is itself the same display-scale inconsistency pass 2 fixed elsewhere** (a full-unit export prints the residue in units and describes its peak in millions) and is left as a deliberate follow-up. The FAILURE wording now differs from the old Excel text; that branch does not render on a passing model, which is why the fingerprint is clean.

### Two bugs pass 3 created, and one it uncovered

- **The H2 renumbering would have SILENTLY DROPPED Module 5 content from real exports.** `ExportModal` seeds the tab selection from the static `PDF_MODULE_TABS` and the render filter matched the full numbered label, so a renumbered tab stopped matching. Tab identity is now the NAME (`pdfTabKey`), on both sides.
- **PRE-EXISTING, found while fixing that: `Tab 5: Fund Layer` was never in `PDF_MODULE_TABS` at all.** Because the picker SEEDS the selection from that manifest, touching the per-tab picker on a fund project dropped the entire fund section from the exported PDF. Added.
- **The first H2 check had no teeth**: it swept every `Tab N:` in the document, and Modules 1 to 4 supply 1 to 5 themselves, so a Module 5 gap was masked and the sabotage passed 58/58. Now scoped to the Module 5 running header. Same for H1, where the footer satisfied the cover assertion.

Two existing verifiers had their PROXY invalidated rather than their intent, and were made more precise: `verify-fund-pdf`'s EBITDA-ordering used a whole-document first-occurrence search that the new exec-summary sentence "EBITDA is struck after them" inverted (now anchored on the P&L heading), and `verify-pdf-export`'s manifest check compares tab names rather than numbered labels.

### Standing traps this review re-confirmed

- **The working tree is CRLF.** Git Bash's `cat -A` does NOT show the `^M`, so a `\n`-anchored patch matches nothing while looking correct. Cost one silently-failed sabotage restore, caught on the diff.
- **Shell escaping ate backslashes three times** in generated verifier code (`/\s+/` became `/s+/`, `/^Tab \d+:/` became `/^Tab d+:/`, and a `\uE0xx` map lost its keys). Each produced a verifier that ran and reported plausible results while matching nothing. Write generated code from a FILE, never from an inline `node -e` or heredoc with regex literals in it.

## 2026-08-11: ten-item workbook review, the two-model finding, and strategy-change reactivation

A user review of an exported workbook. Nine items on the export, one on model behaviour. Every item was diagnosed and measured on the REAL project before any code changed, and two of the ten turned out not to be what they looked like.

### THE FINDING THAT MATTERS: the workbook shipped TWO models

The Summary tab said **Project IRR 177.3%, Equity IRR 38.3%, multiple 7.31x**. The Returns tab, ten rows above its own metrics block, said **10.9% / 7.0% / 2.33x**. Both figures were in the same file.

Cause: the Summary tiles, the Summary highlights, the Returns tab's own "Returns Metrics" block and the Checks tab's headline all read `lm.*` from `lib/excel/liveModel.ts`. That module is a **deliberately simplified twin** left over from when this workbook emitted live Excel formulas; its own header says so. Its IRR streams are `[0, cfo+cfi, ...]` with no inception outflow and none of the historical development investment that the platform's FCFF build-up carries, so a near-zero early outflow inflates the IRR by an order of magnitude.

The workbook is now a hardcoded platform snapshot, so nothing had reconciled the two since that change. **All four surfaces now read `rs` (the platform returns engine), the same source as the Returns grid, and every label names its basis** ("Project IRR (FCFF, unlevered)"). The third figure the review flagged, Distributed Equity 6.8%, was NOT an error: it is `rs.result.dividends`, a legitimately different basis, and is unchanged.

**`liveModel` is no longer a reporting source anywhere.** It still backs the formula-cached cells, so it remains a latent second model in the file. If those cells are ever removed, delete it.

### The other eight export items

Numbered as the review numbered them, so item 2 above is the missing one.

**Item 1. The Checks tab reported CHECK on a healthy model.** The tolerance was `maxBsDiff < 1`, an ABSOLUTE one-currency-unit band on a seven-billion balance sheet (1.4e-10 relative), so a residue of 360.792 at 5.1e-8 failed. Now relative to the PEAK magnitude (not the worst period's value, which produced "5.0e+0 of 0.0 m" on a net cash flow that crosses zero). Two of the three checks were also the literal string `'OK'` with an unrelated magnitude in the note column, one printing total capex (-3,561,517,930) beside a green OK; all three are now real comparisons and all three read the platform snapshot.

**Item 3. Min DSCR 0.43x is REAL**, not a period artefact. Per period: 0.56 / 2.17 / 3.19 / 0.56 / 0.43 / 1.52, so three of six debt-service years are uncovered, while ICR over the same years runs 0.56 to 25.63. Interest is covered; the amortisation is not. Flagged in check-red on the Summary highlight (with the count in a sentence), the Returns coverage tile, and per year in the DSCR row.

**Item 4. A base is a STOCK.** The fee-basis Total column summed the per-period bases, so an annual fee on total equity printed 36,858.3m against a 5,466.8m fund size, on the Excel P&L, the Excel Returns tab AND both PDFs. Encoded once in the shared builder as `basisDisplay` + `basisIsPerPeriod`.

**Item 5. NOT A DEFECT, and worth not re-investigating.** "Other expenses prints raw 3000000 while neighbours are in millions" was chased to the byte: file round-trip, raw sheet XML, `styles.xml` numFmtId resolution, shared-strings scan. Those cells carry style `s=105` -> `numFmtId=166` -> the millions accounting format, IDENTICAL to every neighbouring fee row, and no 7+ digit run exists as text anywhere in the workbook. The reporter was reading stored values programmatically rather than the rendered cell. A REAL adjacent defect was found and fixed on the way: `labelMoney` was hardcoded to millions regardless of export scale, so a full-unit export read "0.50% of Total equity 2,632.7 m" beside a cell of 13,163,667.

**Item 6. Hurdle Accrued lost its lifetime total**, joining the three balance rows. Noted for reversibility: its sum DOES reconcile (accrued + drawn == paid + unpaid at exit), so this is presentation judgement, not an identity.

**Item 7. Gross equals net is now explained** from a shared `fundGrossNetNote` on all four surfaces, empty the moment a fee arises.

**Item 8. A FUND INPUTS band** was added to the Inputs tab, which had carried no fund content at all.

**Item 9. PARTLY WRONG as reported.** The Cover already listed "Fund Fee Basis" and "3. Fund Layer" as hyperlinked sub-entries; the first scan reported zero only because hyperlink cells hold `{text,hyperlink}` objects that stringify to `[object Object]`. What was genuinely missing was a top-level signal, now a "Fund layer active" identity marker, plus an exemption from the 14-section-per-tab cap. **Registering the Summary tab's front-matter bands as sections was TRIED AND REVERTED**: every section link is an `!A<row>` anchor whose column A must hold the title, and a front-matter canvas keeps column A as a margin, so it broke the anchor invariant workbook-wide to gain three entries.

### Strategy changes now reactivate the right assumptions (`lib/state/strategySwitch.ts`)

The Strategy dropdown wrote one field and nothing else. Since the engine gates on `asset.strategy` in every resolver, the old strategy's assumptions stopped feeding the model the instant the field changed, and the new strategy's never started.

**Measured on the real project: EVERY strategy change dropped the asset's revenue to zero with no warning** (a Sell asset moved to Operate took project revenue 14,055.0m to 11,233.0m), and Sell to Sell + Manage was a model no-op.

Two causes. `asset.revenue` is already partitioned (`.sell` / `.operate` / `.lease`) but an asset carries only the sub-config it was built with. And **sub-units are not partitioned at all**: each holds one `category`, so a Sell asset switched to Operate kept its `Sellable` rows and the Operate resolver counted ZERO keys. Even a seeded revenue config would have computed nothing. Separately, leaving Sell + Manage **HARD DELETED** the companion asset with its sub-units, cost lines and cost overrides, so that was the one strategy whose assumptions did not survive a round trip.

Now the outgoing strategy's sub-units, opex and companion are PARKED in `Asset.retainedByStrategy` and the incoming set is restored, or seeded from the outgoing one on first visit (names, counts and areas carry; the RATE does not, because a sale price is not an ADR and printing one as an ADR is worse than blank). Support sub-units belong to no strategy and never move.

**The design point: retention sits OFF TO ONE SIDE, not as a filter over the live arrays.** `state.subUnits` / `costLines` / `costOverrides` still contain ONLY the active strategy's rows, exactly as before, so none of the roughly forty engine and UI readers had to learn to filter, and none can drift. The dropdown previews via a DRY RUN of the same pure function the store commits, so the dialog cannot promise something other than what happens, then a `strategyReview` banner persists across navigation until dismissed, because the empty assumptions live on other tabs.

`verify-strategy-switch` **54 passed + 1 PRE-EXISTING FAILURE** ("the fixture exercises Sell + Manage (a companion exists to lose)", the fixture builds no companion so the Sell + Manage clauses never run; measured 2026-08-12 and confirmed pre-existing by stashing unrelated changes. The long-recorded 82 was stale). Teeth proven by three sabotages (4, 12 and 7 failures). The round-trip identity deliberately EXCLUDES `retainedByStrategy`: after A to B to A the asset correctly remembers B, so that a second visit restores rather than re-seeds. The bank is asserted separately so the exclusion hides nothing.

### Fund fee basis block, follow-up the same day

The labels were being TRUNCATED by the 34-character label column: "Custody and admin fee: basis charged on (per period, 14 periods)" is 64 characters and rendered as "...basis charged on (p", hiding the very count it was added to show. Labels are now `<fee>: basis` / `<fee>: charged` (longest 34 of 34) and the period count moved to the **Base** column, which is 30 wide and held only "Total equity", so it reads "Total equity x 14".

The **Rate column was 2 characters** and could not overflow, because the Total cell beside it always has a value; widened to 10, and only when the fund block renders. **ExcelJS trap worth remembering: `isCustomWidth` is `width !== DEFAULT_COLUMN_WIDTH` and that constant is 9, so a column set to exactly 9 is dropped from `<cols>` entirely and the width silently does not apply.** The first attempt used 9 and did nothing; the verifier now asserts `>= 8 && !== 9`.

A **flat-amount fee is now ONE row**: its basis and its charge are the same quantity, so the pair said the same thing twice. `FundFeeBasisRow.hasRate` carries that semantically rather than inferring it from `rate === '-'`, because a display string is not a fact about the fee.

### Proof

**Toggle-off proven on the FIXTURE, not the real project.** The saved FMP RE HUB version is edited IN PLACE (same label, same `created_at`, different `snapshot`), so a baseline captured against it drifts mid-session and reads as an engine regression. Measured: 17,230 cells across 17 sheets at two display scales plus both PDFs at 8,316 decoded lines, identical except Checks / Returns / Summary, which are exactly items 1 to 3, with zero fund labels anywhere. A later run at 18,020 entries including every column width was byte-identical.

`verify-excel-export` 301 -> **304**, `verify-fund-excel` 69 -> **75**, `verify-fund-fee-income` 107 -> **109**, `verify-tab2-fixes` 47, `verify-strategy-switch` **54 + 1 pre-existing failure** (new; see the Module 1 entry above, the 82 recorded here at the time was stale). Commits `9eb7f9ab` and `7b9deb2a`.

## 2026-08-04: Fund Terms tab, Fund Manager entity + model-read facility limit

Three fixes on the Fund Terms tab. **Migration 210 APPLIED 2026-08-04** (two `ADD COLUMN IF NOT EXISTS`) and live-verified behaviourally: 18 probes on a throwaway project covering the two defaults, the full app payload round trip with the reserved `__fund_manager__` row surviving unmangled, and `resolveFeeEarners` off a real stored row putting the manager first on 100% of the management fees.

**1. The Fund Manager is a first-class entity, and deliberately NOT a party.** It lives in `fundTerms.fundManagerName`, set on the Fund tab, because it only exists while the fund layer is on: putting it in `refm_parties` would give every standalone project a counterparty its model has no concept of. It earns **100% of all five fund management fees, unsplit** (so there is no share to store, the entitlement is total by definition), and takes its share of the performance fee through a row in the distribution matrix carrying the reserved id `__fund_manager__`. Reserved id rather than a uuid on purpose: recognisable in stored jsonb and incapable of colliding with a refm_parties uuid. The matrix stays ONE uniform structure keyed by party id, with `isFundManagerRow` as the predicate, rather than a special case bolted alongside.

**THE STEP 5 CONTRACT, and why it is not `PartnerInput`.** M5 partners are defined by the equity they contributed: each gets one time-weighted shareholding and earns `shareholdingPct x the consolidated stream`, which is what makes Sigma partners == consolidated hold by construction. The Fund Manager contributes NO equity, so dropped into `PartnerInput` it would take a 0% shareholding, a zero invested base, an undefined IRR, and would break that reconciliation. So `resolveFeeEarners(terms)` returns `FeeEarner[]` (`entityId`, `name`, `kind`, `managementFeeShare`, and the three matrix shares), Fund Manager always first, present even with an empty matrix. Step 5 consumes that alongside the equity partners rather than inventing its own shape. The verifier pins that a `FeeEarner` carries no equity-contribution fields.

**2. The facility limit is read from the model.** Diagnosis first: there is NO facility limit the engine enforces. `FinancingTranche.principal` is an input ("absolute principal, overrides ltvPct") but is **dead** (nothing reads it); `ltvPct` is an input but `@deprecated` for per-facility scaling; `FacilityResult.outstanding` / `drawSchedule` are **SOLVED OUTPUTS** that come straight from the funding gap. So `resolveFacilityLimit` walks inputs only: stated principal (summed) -> LTV cap (`ltvPct% x capex`, and `CapexAggregate` carries no IDC so capex does not move with the funding solve) -> the typed figure, with `facilityLimitOverride` to pin the typed one. The drawn balance is never touched, and the verifier greps the fee layer's CODE (comments stripped) for `outstanding|drawSchedule` to keep it that way.

**The UI honesty problem worth noting:** the tab cannot cheaply compute a capex total, and re-deriving capex in the UI would be a second implementation free to drift from the engine's. So `resolveFacilityLimit` takes `capexTotal: number | null` and returns `amountKnown: false` when the source is the LTV cap but the amount cannot be computed there; the tab then shows the percentage and says the amount comes from the model. One implementation, one priority order, no lie on screen.

**3. Fund size stays typed, and the tab now says why**: fund size is equity plus debt, the debt is decided by the funding requirement, and these fees raise that requirement, so reading it from the model would make the fee change its own base. `fund_size_solved` is already in `CIRCULAR_FEE_BASES`; the tab copy is what stops a later step "fixing" the input as though it were an oversight.

**Schema tolerance is now THREE tiers** (`210 -> 209 -> 208`) in `fundTerms-server.ts`, walking down on a missing column for both reads and writes, because the Fund Manager columns can lag exactly as the 209 columns did. Whatever a lower tier cannot hold still rides in the version snapshot, which is what the engine reads.

`verify-fund-terms` 128 -> 173, `verify-fund-fees` 64 -> 73 (the arranging fee now charges the model-resolved limit, proven distinct from both the typed figure and the drawn balance), `verify-fund-layer-guard` 73 -> 75 (its disabled block now carries a Fund Manager and an override flag and is still byte-identical). Engine regression: returns-snapshot 99, m4-bs-reconciliation 187, cases 35, excel-export 304. Type-check + build clean, no new lint.

## 2026-08-04: Fund layer Step 3, fund fees in M4 (FIRST ENGINE-ADJACENT STEP)

Fund fees are now booked in the financial statements. **No migration**: every input already lives in migs 208/209, the fees are computed.

**Where they sit.** A new P&L line BELOW EBITDA and ABOVE the tax/Zakat line: `pl.fundFeesPerPeriod` + `pl.ebitdaAfterFundFeesPerPeriod`, so EBITDA itself is untouched and the fee flows EBIT -> PBT -> tax -> PAT. Cash side: `directCF.fundFeesPaidPerPeriod`, paid in the period charged with NO accrual, which is what keeps the BS balanced by construction (expense hits retained earnings through PAT, cash falls the same amount the same period). The Indirect CF needs no add-back for the same reason, and Direct == Indirect still holds.

**How the fees reach the funding requirement: they already did.** `computeFundingGap` builds Method 3's cash-available line from the Direct CF arrays, so putting the fee inside `cashFromOps` is the whole mechanism. Verified: with no tax, operating cash falls by exactly the fee each period, and the period-0 funding requirement rises by EXACTLY the period-0 fee (74.72m -> 83.47m against an 8.75m fee).

**THE DESIGN DECISION THAT MATTERS: the fee schedule is FROZEN BEFORE the solver.** `computeFinancialsSnapshot` is an iterative fixed point (gap sizing, conditional IDC and the sweep are fed back until they converge). A fee computed INSIDE that loop from anything the solver moves would drift each iteration, and a NAV including funded cash would mean more funding -> bigger fee, which is the circularity the guideline forbids. So the public entry now runs TWO stages when the toggle is on: a fee-free pass produces the NAV path, `computeFundFeeSchedule` builds the schedule from it, and that schedule is passed unchanged into every iteration of `computeFinancialsSnapshotSolved`. It is never part of `derived`, so no iteration can revise it. Belt and braces: NAV is `bs.totalEquityPerPeriod` (NET assets), so a debt drawdown does not move it at all. Cost is one extra snapshot when the fund is ON; when OFF the path is untouched and single-pass. (Watch the recursion: the fee-free pass calls the SOLVER, not the public entry, or it re-enters the fund branch forever.)

**Each fee's timing and base come from `FUND_FEE_SPECS`, not from engine code**, exactly as Step 2 intended: one-time fees land in period 0, annual fees recur, NAV fees charge `NAV[t-1]` (zero at t=0, since the fund holds no net assets before its first period; inventing a base there would be the circular shortcut).

**The P&L now RENDERS the fees** through the shared `m4Reports.buildPLRows`, so screen, PDF and Excel all pick them up from one place: one row per charged fee, a Total Fund Fees subtotal, an EBITDA after fund fees line, and nothing at all on a standalone project. The local `ebit` in that builder had to be struck AFTER the fees or the rendered statement would stop footing from EBITDA to PAT.

**A SPECIFIED CHECK THAT COULD NOT BE MET, reported rather than fudged.** Step 3 asked to verify "a funding-gap project stays cash-non-negative with fees on". **That property does not hold, and it does not hold WITHOUT fees either**: the baseline fixture troughs at about -9.8m in the first operating period because the gap-sized drawdown does not fully meet the computed requirement once construction capex stops. Raising the facility LTV from 60 to 95 does not change it, so it is not a facility-size cap. This is pre-existing financing-engine behaviour, independent of the fund layer, and Step 3 is additive so it is not licensed to change drawdown sizing. The verifier therefore asserts what the fund layer IS responsible for, and says so in a comment at the check: the baseline already troughs below zero, the model draws MORE funding with fees on (118.70m -> 128.02m), the extra draw does not exceed the fees it covers (no amplification), and cash is never worse than the cumulative fee cost. If drawdown sizing is ever made to meet the requirement in full, the non-negativity check belongs there and will then mean something.

New verifier `verify-fund-fees` 64. Regression: `verify-fund-layer-guard` 73 (toggle off still byte-identical), returns-snapshot 99, m4-bs-reconciliation 187, m4-reconciliation-broad 24, funding-methods 93, fixed-assets 82, escrow 46, idc-depreciation 141, cost-of-sales 24, cases 35, covenants 41, excel-export 304, pdf-export 58, deck-financials 72, ic-report 82. Type-check and build clean.

## 2026-08-04: Fund layer Step 2 EXTENDED, the real fee set + per-party distribution (inputs only)

The tab shipped on 2026-08-03 carried ONE management fee on a choice of two bases, and split it by ROLE. A real fund carries several fees with different timings and different bases, and splits several fee types across the actual parties in the deal. Rebuilt to match. Still inputs only: nothing here changes a number, and the tab says so.

**Fee set:** fund structure fee (one time, % of fund size), fund management fee (annual, % of OPENING NAV), custody and admin fee (annual, % of OPENING NAV), debt arranging fee (one time, % of facility LIMIT), other expenses (flat per annum), plus the performance fee and hurdle IRR.

**LINEARITY IS NOW A DATA STRUCTURE, not a comment.** Every fee is a `FeeSpec` in `FUND_FEE_SPECS` carrying its `timing` and its `base`; `LINEAR_FEE_BASES` is the allowed set (fund size, opening NAV, facility limit, flat amount, all known BEFORE the period's cash moves) and `CIRCULAR_FEE_BASES` names what must never appear (closing NAV, average NAV, drawn debt, total sources, funding requirement, and a SOLVED fund size, the trap one careless line away from the allowed user-input fund size). `verify-fund-terms` fails on the REGISTRY, so a bad fee added to the DATA is caught as well as one added to the UI. Proven: injecting `base: 'closing_nav'` on the management fee failed 4 checks including the named opening-NAV one; reverted and re-verified clean. The tab renders its fee rows FROM the registry and shows each fee's timing and base, so the user sees what each fee charges on, and Step 3 reads the same registry rather than re-deciding per fee in engine code.

**Distribution matrix:** rows are the project's LIVE parties (loaded from the Parties tab), columns are Performance Fee / Developer Fee / Commission, each column carries a live total chip that WARNS at other than 100% rather than blocking. Cells key on `partyId` and SNAPSHOT `partyName`, the M5-partner shape, so a rename or deletion never blanks a saved row. Shares belonging to deleted parties are NOT silently dropped: they are counted, named in an amber note, and removed only on an explicit save.

**Migration 209 APPLIED 2026-08-04** and live-verified BEHAVIOURALLY (25 probes on a throwaway project, deleted afterwards): new columns defaulted correctly with the retired 208 columns still present, the new CHECKs rejected out-of-range rates and negative amounts (23514), the boundary rate 1.0 was accepted, and the EXACT payload `toRow` produces round-tripped through `fromRow` with the whole distribution matrix intact (`partyId` linked, `partyName` snapshotted) and the toggle staying OFF. Probe 5 (re-run is a no-op) was NOT exercised, since the JS client cannot issue DDL; it is guarded by construction but unobserved. Eight `ADD COLUMN IF NOT EXISTS`, nothing dropped or renamed. Range CHECKs go in `pg_constraint`-guarded DO blocks rather than inline, because `ADD COLUMN IF NOT EXISTS` silently skips its inline CHECK when the column already exists, which would leave a re-run with an unconstrained column and no error. The 208 columns `management_fee_pct` / `fee_base` / `committed_capital` / `fee_shares` are RETIRED BUT KEPT (additive-only, and prod may hold typed values); `carry_pct` and `hurdle_rate_pct` keep their exact meaning, so no data moves.

**Schema tolerance is the part that keeps prod safe.** 208 is applied and 209 is not, so a fixed column list would 42703 and take Module 1 down. `fundTerms-server.ts` now two-tier probes (FULL column set, falling back to the 208 set on "column does not exist", cached per process), exactly as `server.ts` does for the mig-153 columns, and writes mirror it via `toLegacyRow`. Before 209 is applied the extended values do not persist to the TABLE but still ride in the version snapshot, which is what the engine reads, and the tab says so in its own amber note.

**Step 1's guard was strengthened again:** its populated-but-disabled block now carries every new field (both bases, all five fees, the performance fee, and a two-party matrix), so "stored but inert" is pinned for the real fee set rather than for a bare toggle. 72 -> 73.

`verify-fund-terms` 88 -> 128. Engine untouched: `verify-returns-snapshot` 99, `verify-cases` 35, `verify-covenants` 41, type-check + build clean, no new lint problems (one genuine new error, a `setState` inside an effect in the percent input, was fixed with React's adjust-during-render pattern rather than suppressed).

## 2026-08-03: Fund layer Step 2, M1 Fund Terms tab + storage (inputs only)

Second step. Inputs only: nothing entered here changes a number anywhere, and the tab says so. The fee reaches M4 at Step 3, the waterfall M5 at Step 4.

**New M1 tab 3, Fund Terms** (`Module1FundTerms.tsx`), directly after Parties because the fee share is split by PARTY ROLE. M1 tab labels renumbered 1 to 6. Holds the toggle, management fee, fee base, hurdle, carry, committed capital and fee share per role. Rates are stored as DECIMAL FRACTIONS and shown as percentages, so the x100 lives in one component and nowhere else.

**Fee base is an enum with exactly TWO linear options** (`committed_capital`, `total_development_cost`), enforced in three places that must agree: the TS union, `FEE_BASES` + `coerceFeeBase`, and the migration CHECK. Fund size is absent by design (it feeds the fee back into its own base and pulls the layer into the M4 circular solve) and is actively COERCED away rather than silently honoured if it ever arrives from a future build or a hand-edited row.

**THE DESIGN DECISION WORTH KNOWING: the tab writes TWO stores, on purpose.** `refm_fund_terms` (migration 208) is the durable per-project row. `Project.fundTerms` in the version snapshot is the ENGINE-FACING copy, and from Step 3 the engine reads ONLY that. M1 storage already splits this way: engine inputs (`returns`, `partners`, `covenants`, `dividendPolicy`) live in the snapshot, identity data (`refm_parties`) lives in a table explicitly outside it. Fund terms are engine inputs, so a table-only design would mean a six-month-old version shows today's fee, Module 6 scenarios could never vary the fee (`applyOverrides` operates on the snapshot model), and the Excel/PDF version picker would export the wrong fee for old versions. The precedent for writing both is the M5 partner, which links to a party by `partyId` but SNAPSHOTS the name so the engine never depends on a mutable side table. The tab seeds the snapshot from the row ONCE per project and only when the snapshot carries no terms, so opening the tab on an old version never overwrites what the user is looking at.

**Migration 208 APPLIED 2026-08-03** and live-verified BEHAVIOURALLY, not by grepping the DDL: 20 probes on a THROWAWAY project (created for the probe, deleted at the end, so no real project was touched). The CHECK rejected `fund_size` (23514), a 150% fee, a negative carry and negative committed capital; defaults came back `false` / `committed_capital` / `[]` / 0; a duplicate `project_id` was rejected (23505); the exact app payload upserted with the jsonb fee shares and roles intact and the toggle staying OFF through a populated write; the boundary rate 1.0 was accepted (the CHECK is inclusive by design); and deleting the project cascaded the row away. Reads stay schema-tolerant regardless, so the tab degrades rather than failing on any database that lacks the table.

**Step 1's guard was STRENGTHENED, not merely kept green.** It compared a bare `{enabled: false}` against no fund block at all. It now compares a FULLY POPULATED disabled block (2% fee, 8% hurdle, 20% carry, 250m committed, a 60/40 split), because that is the state a real user reaches: fill the tab in, leave the toggle off until ready. "Stored but no effect" has to hold for those values, not for an empty object. `verify-fund-layer-guard` 71 -> 72.

New verifier `verify-fund-terms` 88: the two-option enum (including that the migration's CHECK clause excludes `fund_size` while the file still DOCUMENTS the probe proving it is rejected), input coercion at every boundary (NaN fee, 300% carry, negative capital, unknown role, duplicate roles), row-and-snapshot round-trip agreement, every selected column existing in the migration, and Step 1's contract surviving. Storage rules are asserted against the migration TEXT because the table is not applied; the four behavioural probes at the foot of the SQL are what will actually prove the constraints have teeth.

Engine untouched. `verify-fund-terms` 88, `verify-fund-layer-guard` 72, `verify-returns-snapshot` 99, `verify-cases` 35, `verify-covenants` 41, `verify-version-edit-choice` 38, type-check + build clean.

## 2026-08-03: Fund layer Step 1, the toggle-off regression guard

First step of the fund layer (management fee, preferred return, carry, gross versus net returns). Scope, the linear fee base decision and the 7-step sequence live in [docs/FUND_LAYER_GUIDELINE.md](docs/FUND_LAYER_GUIDELINE.md), which is the source of truth; this entry records only what shipped.

**Guard before feature code, deliberately.** Nothing of the fund layer was built here except the toggle and the verifier that proves it is inert. `Project.fundTerms?: { enabled?: boolean }` is a new optional field alongside `returns?` / `partners?` / `covenants?`, plus a pure `lib/fundTerms.ts` (`resolveFundTerms` / `isFundLayerActive`) with NO consumers. Default is off BY OMISSION: nothing stamps the field, and only a literal `true` resolves to on, so an older snapshot or hand-edited jsonb cannot switch the layer on by accident.

**No migration, and none is needed.** The project rides inside the `refm_project_versions.snapshot` jsonb and hydration spreads it whole (`snap.project as Project`), so the toggle persists with zero schema change. The fund terms TABLE arrives with Step 2, where the M1 Fund Terms tab needs real storage.

**Why the guard is a comparison, not golden numbers.** `verify-returns-snapshot` (99 checks) is property-based: identities, relationships and ranges at 1e-2 tolerance. A uniform drift, say every distribution three percent lower, passes all 99. Pinning golden values instead would rot on the first legitimate model change and get re-blessed unread. So `scripts/verify-fund-layer-guard.ts` (71 checks) compares the SAME ENGINE RUN WITHOUT THE TOGGLE against the run with it present and off, in one process on identical inputs: exact `Object.is` deep equality (Map-aware, because the financing snapshot carries per-facility Maps that `JSON.stringify` flattens to `{}`) over the FULL financials and returns snapshots, across levered / unlevered / perpetuity-terminal / no-terminal / scenario-override shapes, reporting the exact diverging path. It also runs the entire returns suite twice as a child process (`FUND_LAYER_TOGGLE=off-present`) and requires an identical TRANSCRIPT, not just matching totals.

**Teeth proven, not assumed.** A deliberate 1e-9 drift injected into `resolveReturnsConfig` behind the toggle failed 12 checks with the exact path (`returns.config.discountRate: 0.1 vs 0.100000001`) and propagated to FCFF IRR and the equity multiple. The existing 99-check suite passed that same drift unchanged, which is precisely the gap this guard closes. The injection was reverted and the tree re-verified clean.

**Deliberately NOT asserted:** that the toggle ON changes nothing. That holds only until Step 3 adds the fee, and a guard that has to be deleted to make progress is not a guard.

Engine untouched. `verify-fund-layer-guard` 71, `verify-returns-snapshot` 99, `verify-cases` 35, type-check + build clean.

## 2026-07-09 session: M7 Reports (Phases 1-3a) + M5 per-partner returns + M1-Parties link

**M5 per-partner returns (commit 188950f6).** Each equity holder shows FCFE-based + DDM-based IRR/MOIC/equity-multiple driven by ONE per-partner shareholding = TIME-WEIGHTED average capital balance (dollar-years: `K[i,t]=existing+Sigma(s<=t)(cash+inKind)`, `share=Sigma K[i,t]/Sigma K_total[t]`), manual override = the agreed cap-table share (drives both bases). Each stream = agreedShare x the exact consolidated stream (FCFE passed in as `consolidatedFcfePerPeriod`; DDM reconstructed internally), so Sigma partners == consolidated per period and total returns are byte-identical. Engine `src/core/calculations/returns/partners.ts` kept every prior `PartnerResult` field (so verify-returns-engine 107 / -snapshot 99 unchanged); UI `Module5Returns.tsx` horizontal side-by-side tables + Weighted-Avg/Agreed rows + green/amber reconciliation chip. verify-per-partner-streams 15.

**M5 <-> M1 Parties link (commit d4350e4a).** `ProjectPartner` gains optional `partyId`; the partner name is picked from a dropdown of equity-role M1 Parties (Sponsor/Developer/Investor-Equity Partner) which stores `partyId` + SNAPSHOTS the name (writer-byline pattern). Engine NEVER reads partyId (identity only); rides in the JSON version snapshot, no migration. verify-m5-parties-link 15.

**M7 Reports, snapshot READ-ONLY, per-project (no engine change, no recompute).** **IC REBUILT to full IC-grade depth (Phase A+B, 2026-07-12):** 21 model-driven sections (`IC_SECTIONS`: cover, executive_summary, investment_recommendation, project_overview, master_plan, asset_mix, market_context, development_programme, development_costs, value_economics, sources_uses, financing_structure, returns_analysis, exit_optionality, scenario_cases, scenario_economics, sensitivity, risk_assessment, regulatory_tax, recommendation_approvals, disclaimers). AUTO blocks feed from verified snapshot paths (asset-mix table + BUA-by-strategy, phasing + per-phase capex from `snap.perAssetCF`, sources/uses, value bridge, cost stack, financing summary incl. `FUNDING_METHOD_LABELS`/`minimumCashReserve`, RE metrics, `rs.exitYears` table, `rs.sensitivity` two-way IRR grid, scenario headline + economics via `buildCaseComparisonReport` + a per-column `drivers[]` "what drives each case" matrix built off `buildOverrides` + assumptionGrid descriptors). **AUTO-OMIT** = ONE shared predicate `icSectionOmitted(key,model,inputs)` + `icVisibleSections()` in `lib/reports/icReport.ts`, used by BOTH the preview AND the PPT nav filter (so the deck slide count + TOC stay in lockstep): a section renders only when its model data exists/non-trivial or its FORM field is non-empty; empty FORM never renders a blank in output; cover/returns/investment_recommendation/project_overview/disclaimers never omitted. Structured narrative from the form (mig 193): developmentConcept, marketContext{stats/points/sourcesNote}, keyGates, returns/exit/scenario commentary, risks[], regulatoryTax[] (loadable `KSA_REGULATORY_PRESET`, never a default), conditionsPrecedent[], nextSteps, execPoints[]; legacy free-text kept as fallbacks. **Charts + development-programme Gantt = Phase C+D (TODO).** **Lender Package + Investor One-Pager PARKED** (`PARKED_REPORT_TYPES`/`ACTIVE_REPORT_TYPES`; hidden from the selector, ALL code + sections + form fields + verifier coverage retained). Original build: IC Report (Phase 1, commit a89f02b9), Lender + One-Pager (Phase 2, 79456798), PPT export (Phase 3a, f5e84345). Financials auto from `computeReturnsSnapshot`/`computeFinancialsSnapshot`; narrative + editable header/footer + font from the per-project **report-inputs** form (`refm_report_inputs`, migs 191+192); parties from M1 by role; per-report-type show/hide + reorder. Pure assemblers `lib/reports/{icReport,lenderReport,onePagerReport}.ts` (Lender covenant heatmap reuses `lib/covenants.ts` -> same verdicts as RE Metrics; facility terms from STORE `financingTranches`, cash sweep from `snap.cashSweep.totalSweepPerPeriod`). **PPT (pptxgenjs) generated SERVER-SIDE** via `POST /api/refm/projects/[id]/report-pptx` (Node runtime) because pptxgenjs `node:fs/https` imports cannot browser-bundle and `next.config` is off-limits; the client POSTs the assembled model + inputs (no recompute), the route rebuilds `makeFmt` + streams the .pptx. 16:9 editable master, navy header band, ToC with clickable internal slide links (hyperlink MUST be on the text RUN), per-section divider slides, KPI tiles, named fonts. **Phase 3b (PDF) = same-model pdf-lib renderer** (literal PPT->PDF NOT feasible on Vercel: no LibreOffice). Canonical spot-checks (FMP RE HUB): GDV 14,055, Project IRR 11.9%, Equity IRR 8.3%, MOIC 2.40x (=equityMultiple, NOT fcff.moic), peak debt 2,834.1m. **Nav enablement (commit 0fe3a599):** enabling M7 needed `module7` added to the `ModuleKey` union (`settings.types.ts`) + `MODULE_VISIBILITY` all roles (`core/state/index.ts`) beyond modules-config/MODULE_TABS/render-switch, else `canSeeModule('module7')` was false for every role (admin has NO bypass on role-visibility) and the sidebar greyed it out despite the live DB `platform_modules` ✓. Verifiers verify-ic-report 82, verify-report-models-phase2 26, verify-report-pptx-export 27 (+ verify-returns-snapshot 99 unchanged, engine untouched).

## 2026-06-17 session: version edit choice (edit-in-place / save-as-new)

Ends the "a new version on every Edit" churn from the view/edit lock. Clicking **Edit** now opens a CHOICE step (`components/modals/EditChoiceModal.tsx`, NEW) BEFORE editing starts; it shows the **current version name** clearly and offers: **edit this version in place** (default) / **edit a different version** / **create a new version**. No engine/calculation change, version/persistence + Edit-entry UX only.

`module1-sync.ts` gained two session entry points beside `startEditSession` (which still POSTs a new version = the create-new path):
- **`startEditInPlace()`** points the editing session at the LOADED version (`editingVersionId = sessionBaseVersionId`) so autosaves PATCH that SAME row. No new version is created; the loaded version is overwritten in place. Returns an error when no saved version is loaded (cache-only) so the caller falls back to create-new. Also added module state `sessionBaseLabel` (so in-place adopts the loaded version's name without a rename).
- **`saveAsNewVersion(label, meta)`** POSTs a fresh version from the current working state, branched off the version being edited (`baseVersionId = editingVersionId ?? sessionBaseVersionId`), then re-anchors the session to the new row and CONTINUES editing it. The source version is left untouched.

Wiring: `RealEstatePlatform.handleEnableEditing` opens the choice step; `handleEditChoice` routes in-place / different (history picker in "edit" intent -> `handleLoadVersionForEdit` loads then edits in place) / create-new. `NameVersionModal` gained a `save-as-new` mode (same auto-name + Task + Comment form); `handleNameVersionConfirm` routes it to `saveAsNewVersion`. `VersionModal` gained optional `initialTab` + `loadActionLabel` (edit-intent lands on History with an "Edit this version" action; keyed remount via `key={`versions-${versionPickMode}`}`). `Topbar` gained a **🌿 Save as new version** button (edit mode only, prop `onSaveAsNewVersion`). Non-destructive: editing one version in place never touches others; branching never alters the source; existing history + naming preserved. `scripts/verify-version-edit-choice.ts` 38/38 (real sync vs an in-memory fetch server + UI-wiring source invariants). Commit `6bc35629`.

Also this session (separate commits): assessment-timer silent-drop fix (training, `0e66eff7`) and M6 YoY debt/equity dedup + drawdown-excludes-IDC (`ab4f49ec`).

## 2026-06-16 session: M6 override pipeline (empirical), platform shell, admin module mgmt

Full day, ~12 commits (`0b7f7fe0`..`9bc12e80`). Three themes:

**1. Module 6 scenario override pipeline.** Empirically PROVEN sound: `scripts/verify-module6-pipeline.ts` (19/19) drives the REAL store (`hydrate`/`setActiveCase`/`setCaseFieldValue`) + real `buildCaseComparisonReport` and asserts OBSERVED comparison deltas (the prior `verify-module6-scenarios` tested labels + a `scen.overrides` shortcut, so it passed 127/127 while users saw "no delta"). `applyOverrides` loses nothing; ~60/108 numeric fields move a KPI, the rest are inert by config/semantics, NOT a broken merge. Fixes: (a) opex inflation `normalizeOpexIndexation()` in `core/calculations/opex/defaults.ts` coerces a method-less rate to `yoy_compound` (applied in `opex-resolvers.ts` asset+HQ + mirrored in `Module3Opex.tsx`); (b) "N overrides" counts REAL diffs not stored keys (`caseComparisonReport` + `Module6Scenarios`); (c) **inert-lever gating** `inactiveLeverReason(path, model)` in `assumptionGrid.ts` drops/labels levers dead under the active config (fixed-ratio Debt% under funding Method 2/3, tranche interest/LTV under Method 1, perpetuity growth under exit-multiple, occupancy on Operate assets, opex inflation with no fixed-cost lines, lease baseRate when unitPrice set); (d) null-FCFF headline: comparison leads with Equity IRR (FCFE), `Project IRR (FCFF)` renders "n/a (no unlevered IRR)" via KPI `nullLabel`; (e) added **NPV (FCFF)** row so discount rate has a metric; (f) per-period levers (`occupancyPct`) stripped from grid via `isPerPeriodLever()`; (g) Group B polish: Tax/Zakat off curated, Capex-matched construction labels (`Construction (BUA), per sqm` / `(Parking), per bay`), land-price lever (`parcels[id].rate`), sticky-header grid, comparison metrics (Total Financing Cost / Cap Rate at Exit / Min DSCR, TDC split into Land + Capex). LIVE-PROJECT proof (FMP RE HUB, funding Method 3): debt%/occupancy/opex were inert for real config reasons (gap-sized debt / per-period occupancy / no fixed-cost opex lines); ADR + discount rate move 13/8 KPIs. `verify-module6-scenarios` 128/128, `verify-module6-pipeline` 19/19.

**2. Platform shell.** No default project on sign-in (boot no longer auto-opens cached `readActiveProjectId`); lands on Dashboard. **Projects tab removed** (both nav sources) — Dashboard hub replaces it. `Dashboard.tsx` rebuilt as the project-AGNOSTIC all-projects hub (portfolio tiles + recent activity + project cards w/ Open + Delete-with-confirm). NEW `Overview.tsx` = open-project investor summary (returns / economics / capital-structure donut / timeline / health chips) via `computeFinancialsSnapshot`->`computeReturnsSnapshot`, no engine change, no nav cards. `renderModule` routes `dashboard`->Dashboard, `overview`->Overview (no longer aliased). Universal module lock: Overview + every `module1..11` disabled until a project is open. **View/edit lock**: opening a project lands on Overview in VIEW mode (read-only, `pointer-events:none` on module content + "View only" banner); Topbar/banner **Edit** button runs the save-as / name-version flow then unlocks. **View-mode lock moved to the write layer (2026-07-07, commit `7fe962ea`):** the old panel-level `pointer-events:none` blocked ALL interactions (collapsible result dropdowns, phase/asset selectors, view toggles) not just mutating inputs. It was replaced by a store guard: `module1-store` gained a `viewLocked` flag that no-ops ONLY the ~42 model-mutating setters (an explicit allowlist); the navigation setters (`setActivePhaseId`/`setActiveAssetId`/`setActiveCase`) + `hydrate` are never gated (load/switch use `set(...)`/hydrate directly, not the public setters). So view mode now allows every view/navigation interaction while no model number can change; `RealEstatePlatform` syncs `viewLocked` from the edit state and dropped the overlay (the banner stays). Verifier `verify-view-mode-lock` 13/13 (behavioral); `verify-version-edit-choice` regex loosened to tolerate the grace guard (pre-existing false-negative). This fixes VERSION CHURN: `module1-sync` gained an `editingEnabled` gate (`setEditingEnabled`) so the store subscriber ignores view-mode changes (no auto-session/autosave/version); `startEditSession` + entering edit mode flip it on. New projects open in edit mode on Setup. New Project modal no longer closes on backdrop (X / Create only). Sidebar width 264->304->**325** (`--sidebar-width`; wrapper has `zoom: 0.8`) + widened `.sidebar-nav` scrollbar (11px). Visual design pass on Dashboard + Overview (navy hero band, brand-accented sections, kpi-card pattern, status-accented cards).

**4. Module 1 Parties tab (2026-07-09, identity-only, additive).** New **2nd Module 1 tab** (`Module1Parties.tsx`): `Project & Phases → Parties → Assets/Sub-units → Capex → Financing` (`m1Tabs` renumbered, sidebar auto-picks from `MODULE_TABS`). Each party is ONE row: Name (required), Identifier (optional), and one or more Roles from a fixed set (Sponsor, Developer, Investor/Equity Partner, Advisor, Lender, Prepared-by, Contact, Other) as toggle chips. **Identity data ONLY: the engine never reads it.** Stored in the `refm_parties` table (mig 190), per-PROJECT and OUTSIDE the version snapshot, so old projects load unchanged (empty tab) with no returns regression. CRUD via `/api/refm/projects/[id]/parties` (owner-checked, service-role); the tab receives `activeProjectId` as a prop and is independent of the store/snapshot. Shared consts in `lib/parties.ts`; server helpers in `lib/persistence/parties-server.ts`; client fns appended to `lib/persistence/client.ts`. Anticipates later work: role set covers M5 equity partners (Investor/Equity Partner, replacing the free-text `ProjectPartner` name later) + M7 report identity (Prepared-by / Contact / Sponsor). Live E2E (throwaway project): multi-role persist, edit, delete, project-delete cascade, existing FMP RE HUB stays empty. Not a Module 1 lock break (engine untouched).

**3. Admin module reorder / hide + the read/write disconnect (CRITICAL).** Root cause of "hidden doesn't hide / reorder doesn't propagate": admin WRITES `platform_slug='real-estate'` (the legacy `modules.slug`; migration seeds + re-keys to it) but the workspace READ `usePlatformModules('refm')` -> empty -> hardcoded `STATIC_SIDEBAR_MODULES` fallback (all visible, fixed order). Fix: `export REFM_PLATFORM_SLUG = 'real-estate'` (read == write slug) + pure testable `toSidebarNavList()` (filters Hidden, orders by `display_order`, stable `module${number}` key, position label). Reorder via admin ▲/▼ arrows (`moveModule` rewrites `display_order`; `number` stays the stable routing identity, no migration). Hidden = excluded from sidebar AND non-routable (`renderModule` guard). Marketing pages use position numbering too, but ONLY since 2026-07-30: the public page actually rendered the raw `number` until then, so hidden modules made it show "Module 10: Collaborate" against admin's "Module 8". All surfaces now share `orderModulesForDisplay()` in `src/shared/entitlements/moduleCatalog.ts` (see CLAUDE-MODELING-HUB.md). `verify-psync` **108 pass / 0 fail / 3 skip** (incl. read-slug == seed-slug, a live endpoint check when the dev server is up, canonical name parity between the `platforms.ts` marketing fallback and `modules-config.ts`, and display-numbering parity between sidebar and marketing).

## 2026-06-15 session: Lender Covenants (M5 RE Metrics)

**Lender Covenants section on the RE Metrics tab (`lib/covenants.ts` + `Module5Metrics.tsx`, verifier `scripts/verify-covenants.ts` 32/32, commit `ec9e2e5b`).** Pure display-layer evaluation over the EXISTING returns snapshot — NO engine/snapshot change. Editable covenant thresholds saved to `Project.covenants` (seeded from `DEFAULT_COVENANTS`: DSCR ≥ 1.20x, ICR ≥ 2.00x, LTV ≤ 60%, Debt Yield ≥ 10%); each row shows worst (binding period) / avg + Pass/Breach + a per-period heatmap. DSCR/ICR read straight off the snapshot (worst = engine min, matching the cards); Debt Yield = NOI/debt (n/a where no debt). **LTV is measured at PEAK DEBT, not at exit:** debt is repaid by exit so LTV-at-exit is a trivial ~0% that is meaningless to a lender. The model has no per-period property value, so the value basis is GDV (Gross Development Value): LTV per period = debt outstanding / GDV, and the binding (max) point IS the peak-debt LTV. The basis is surfaced in the UI (`peak debt / GDV` caption + `LTV (peak debt)` label) and in the section description. Where GDV is unavailable it falls back to a single-point LTV at exit, explicitly labelled "LTV at exit" via `CovenantEval.basis`/`basisLabel`, so it cannot be mistaken for a peak-leverage covenant. Derived from existing snapshot series only (`debtOutstandingPerPeriod` + `developmentEconomics.gdv`).

## 2026-06-14 session: Excel MODEL export full module mirror + Module 6 Scenario Analysis

**Excel MODEL export (`lib/excel/buildModelWorkbook.ts`, verifiers `scripts/verify-excel-export.ts` 304/304 + `scripts/verify-fund-excel.ts` 69/69 for the toggle-gated fund rows; the PDF exports are pinned by `scripts/verify-fund-pdf.ts` 49/49, which decodes real generated PDFs via `scripts/pdfTextExtract.ts`; `scripts/verify-fund-e2e.ts` 86/86 runs the whole fund layer end to end against the REAL FMP RE HUB project, read-only, covering the engine and all three exports at once. Fund PRESENTATION for every surface, screen included, comes from the shared `lib/reports/fundReports.ts`).** A hardcoded snapshot of the whole platform, one tab per module surface, tab order = the module sequence (Cover, Inputs, Timeline, Land & Area, Capex, Financing, Revenue, Opex, Schedules, P&L, Cash Flow, Balance Sheet, Returns, Checks). Each output tab is a faithful mirror built from the SAME shared report builders the on-screen tabs + PDF use (`m4Reports` / `cosReports` / `opexReports` / `financingReports` / `capexReports`) and the snapshots (`computeFinancialsSnapshot` / `computeReturnsSnapshot`):
- **Inputs** holds EVERY model input, grouped by domain divider band in module order: Project / Phases / Land parcels / Assets / Sub-units / Returns / Capex cost lines, then `CAPEX` is followed by `FINANCING INPUTS` (Module 1), then `REVENUE INPUTS`, then `OPEX INPUTS`. The module output tabs echo their own input slice marked "from the Inputs tab".
- **Revenue** mirrors all 5 Module 2 sub-tabs (Inputs / Output incl. vintage matrices / Cost of Sales / Schedules / Escrow). **Opex** mirrors the 3 Module 3 sub-tabs. **Schedules** mirrors the 2 Module 4 sub-tabs (Fixed Assets & D&A; BS Schedules A1-A4 / L1-L3 / E1-E2). **P&L / Cash Flow / Balance Sheet** are the full M4 statements (terminology-driven labels). **Returns** mirrors the M5 Returns + RE Metrics tabs section-for-section (KPI cards, sources & uses, funding mix, exposure, debt analytics, returns-by-basis, FCFF/FCFE/Distributed build-ups). Sensitivity grid is a pending unit.
- **One standard navy palette throughout** (no green / per-tab colours): navy tabs + totals, deep-navy section bands, pale-navy subtotals, teal removed; red reserved for genuine check failures. The verifier locks the palette + the tab sequence.

**2026-07-14 update (Module 6 in Excel + cosmetic front matter; verifier now 159/159):** the workbook is now a FULL Modules 1-6 mirror at **16 tabs**. (1) New **Scenarios** tab (after Returns, before Checks) built from the SAME shared `caseComparisonReport` + `caseYoYReport` that feed on-screen Module 6 + the PDF: Cases & Assumptions, Scenario Comparison (KPI matrix, delta vs base) and Year-on-Year Impact; `ExportModal` threads the `caseComparison` bundle in, shows the case picker for Excel, and **defaults the statement tabs to the Management/base case while the Scenarios tab always compares ALL cases**. (2) Cosmetic pass: **Cover** rewritten to a clean module-grouped **Table of Contents** (numbered hyperlinks, Module 1-6 + Reference bands, legend); shared front-matter primitives `frontMatterCanvas`/`frontMatterBanner`/`frontMatterBand`. New **Summary** tab (2nd, always-visible) = one-page executive summary (key facts, headline-metric tile wall, two side-by-side highlight tables Development economics / Returns & leverage, computed from `computeReturnsSnapshot`). `applyPrintSetup` gives every sheet a print-ready A4 layout (fit-to-width, landscape wide tables repeating header rows 1:4, centred portrait one-page Cover/Summary, page-numbered footer). No engine change, no migration; palette + reconciliation locks intact. Memory `[[project_excel_module6_scenarios_2026-07-14]]`.

**Module 6 Scenario Analysis (`components/modules/Module6Scenarios.tsx`, verifier `scripts/verify-module6-scenarios.ts` 79/79). DONE (grid 2026-06-15, b9281cae).** A surface over the EXISTING case engine (do not rebuild it): case list (reuses store actions), comparison matrix (`buildCaseComparisonReport`), and a multi-case **assumptions grid** (Section 2). The grid: rows grouped under Inputs-tab category dividers with plain-English labels + asset/phase/facility/parcel/method attribution (so no ambiguous duplicate rows); columns = every case incl. an editable Management; curated key-driver default view + "show all" toggle + add-row picker. **Presentation layer in `lib/cases/assumptionGrid.ts`** (labels / categories / curated set / `buildGridContext` attribution / format helpers) — the override engine (`applyOverrides` / `snapshot-diff`) is untouched. Construction cost levers are MODEL-AWARE: when a project enters costs per-asset, `curatedDefaultFields` surfaces one row per asset from `costOverrides[assetId::lineId]` (the real rates) instead of the phase-level master (which is 0 or a stale seed); zero/disabled/unused levers are dropped. Per-field percent-scale detection (fractions 0-1: discount/tax/indexation; whole 0-100: debt/occupancy/contingency/pre-op/prof-fee) renders all percents at 2dp via `formatAssumptionValue`/`parseAssumptionInput` (edit in displayed unit); rates/prices keep accounting. Store actions `setCaseFieldValue`/`resetCaseFieldValue` edit ANY case (base reflows the active scenario). Backlog: construction-timeline overrides (need a cascade-on-override). The earlier explicit single-case override editor (16/16) was replaced by this grid. **Exhaustive per-field audit (2026-06-17, on LIVE FMP RE HUB):** every one of the 901 picker fields was overridden + recomputed; 265 are proven KPI movers, the rest are gated (config-inert economic, `inactiveLeverReason` expanded model-aware) or DROPPED from the picker (`nonEconomicLeverReason`, NEW: structural selectors / identity / display / engine-derived geometry / revenueTemplates / legacy per-phase dividends / historical baselines). Found one new occupancy-class wrong-field lever (`adrIndexation.rate` ignored when method is `yoy_per_period`), now gated. Guarded by `verify-module6-field-census.ts`. Flagged for separate engine review (CLAUDE-TODO): historical/existing-operations inputs are not consumed by the compute pipeline. Config swap: `modules-config.ts` Module 6 = Scenario Analysis (enabled), Module 7 = Reports (stub); placeholders renamed `module6-scenario.ts` / `module7-reports.ts`. New `enumerateOverridableFields(model)` in `lib/cases/applyOverrides.ts` mirrors the `diffSnapshots` grammar so the picker only offers fields that round-trip (id / reference fields excluded). New store action `setOverridePath(path, value)` writes the same live override map as auto-capture. Per-element override grammar: `snapshot-diff.PER_ELEMENT_ARRAYS` (parcelFunding by parcelId) + `findElement` generic `field=value` selector so `project.financing.parcelFunding[parcelId=P].debtPct` round-trips. Value-only + base-untouched intact. Coverage audit: sale price / area already round-trip via top-level `subUnits[id=X].unitPrice` / `.unitArea`; per-period curves (cash / recognition / lease occupancy) and per-sub-unit velocity stay whole-array auto-capture. Re-basing = a later unit.

**2026-06-04 session (detail in memory `[[project_pdf_mirror_autosync_2026-06-04]]`):** M4 phase views (P&L→EBITDA / CF→Operations+Investing / BS consolidated-only); SHARED report builders in `lib/reports/` (m4/opex/capex/financing/cos) — both the on-screen M4 tabs AND the PDF render from `m4Reports.ts` (auto-sync mirror), the PDF mirrors M1/M2/M3/M5 via the other builders. `ExportModal` version picker (export any saved version, file named after it; `modelFromSnapshot`). Cases follow-ups A/B/C: viewing a case no longer starts an edit session (`stripVolatile` in module1-sync), per-input "≠ Management" `OverrideBadge` + Reset, scenario edits described in the change_log (`diffCases`). Auto-updating **platform walkthrough guide** (`lib/guide/`, Topbar "Guide" button): in-app view + PDF/Markdown download, structure derived from MODULES + MODULE_TABS.

<!-- Historical narrative (2026-06-01/02: M5 sponsor-IRR rework, funding Methods 2/3 gap-sizing, conditional IDC + iterative fixed-point loop, funding-gap 3-schedule split) retained below; full detail in memory + git. -->

**Module 5 (Returns & Valuation) NEW 2026-06-01 (commits `9095ae7` engine + `19f0292` resolver/UI/sidebar): pure IRR/MOIC/NPV/Payback on FCFF/FCFE/Dividends + terminal value (exit-multiple/perpetuity/none) + RE metrics (Yield on Cost, Cap Rate, DSCR, LTV at Exit, Equity Multiple, Debt Yield, ICR, Cash-on-Cash, Profit Margin). Engine `src/core/calculations/returns/` (pure), resolver `returns-resolvers.ts`, UI `Module5Returns`/`Module5Metrics`, additive `Project.returns` config. Funding Methods 2/3 calculate + GAP-SIZED drawdown via a guarded two-pass in `computeFinancialsSnapshot` (commits `03a18ec` → `7d340fc`); BS balances + Direct==Indirect for all 4 methods. M5 build-up: step-by-step FCFF/FCFE/Dividend derivation tables (commit `1c2d149`). **2026-06-02 — M5 reworked to a SPONSOR-IRR / project-inception view: every stream leads with an INCEPTION period (index 0 = projectStartYear − 1) carrying existing operations. FCFF inception = −existing pre-capex; axis = CFO + CFI (new capex), NO in-kind land line (non-cash, already in CFO via CoS/dep). FCFE inception = −pre-capex + existing debt opening (= −existing equity); axis adds debt draw − principal − interest − in-kind land. In-kind land + existing-debt-opening appear on FCFE only. Per-period bridge FCFE = FCFF + debt draw − principal − interest − in-kind + terminal-equity adj. `fin.existing.{preCapexTotal,debtOutstandingTotal}` feed inception; `ReturnsSnapshot.streamYearLabels` + `buildup.{existingPreCapex,existingDebtOpening,existingEquity}PerPeriod` added; IRR is headline, NPV still computed. verify-returns-snapshot 30→38 (SPONSOR section).** Platform infra: project-switch state-leak fixed + session-based versioning with per-version change_log shipped (commits `ca5c152` + `d25a20b`). **2026-06-02 — Funding Gap sub-tab split into THREE segregated schedules (Method 3 Funding Requirement / Debt Repayment via Cash Sweep / Dividend via Cash Sweep), each ending in its own Closing Cash chained to the Direct CF closing (pre-distribution −sweep = post-sweep −dividend = final); cash-sweep order now EXISTING loans first then priority; NEW `idcConfig.fundingMode: 'conditional'` raises IDC debt only to the extent needed to maintain the minimum cash reserve — in surplus-cash construction periods the interest is PAID IN CASH (still capitalised to the asset basis), via a per-tranche `interestCapitalizedCashPaid` engine reclassification (no new composer balance term: `debtServiceCash = accrued − capitalized + principal` auto-carries the cash outflow). The drawdown ↔ finance cost ↔ balance circularity (the one Excel resolves with iterative calc) is now solved by an ITERATIVE FIXED-POINT LOOP: `computeFinancialsSnapshot` wraps an inner `computeFinancialsSnapshotOnce`, re-deriving `{fundingGap, idcCashBudget}` via `deriveCircularInputs` and re-running until both stop changing (max per-period delta < 1, cap 25 iters); the SAME loop also converges Methods 2/3 gap-sizing (replacing the old single re-pass). Each pass is internally consistent so BS balances + Direct==Indirect at every iteration; convergence pins drawdown + finance cost. Explicit-opts callers still do one pass. BS balances + Direct==Indirect, debt lower than the all-capitalised case.** FULL VERIFIER SUITE GREEN 2026-06-02: 25 scripts, 0 fail (idc-depreciation 113→135 §J conditional IDC, funding-methods 45→59 [IDC] + [CONV] fixed-point, bs-reconciliation +N5 existing-first sweep).**

## Cases (scenario management), shipped 2026-06-03 (commits `bb19ae6` + `2682a35`)

Scenario/case management on the REFM model. A "case" = the base ("Management") model plus a flat map of field OVERRIDES (value changes only, no per-case add/remove of entities). The **Management Case** is the base/reference; **Downside** + **Upside** are seeded override-cases; users rename + add custom cases.

- **Merge engine `lib/cases/applyOverrides.ts`** (pure): `applyOverrides(base, overrides)` = inverse of `diffSnapshots` (deep-clone + set each path; missing entities skipped); `buildOverrides(base, edited)` derives the map via `diffSnapshots` (one path grammar); `getByPath`; `seedCases` / `baseCaseId` / `normaliseCases`. Override paths use the diff scheme (`assets[id=X].revenue.sell.pricePerUnit`, `costLines[id=Y].value`, `costOverrides[a::l].x`, `landAllocationMode`).
- **Store** (`module1-store.ts`): top-level model fields ALWAYS hold the ACTIVE case's merged model, so every component + setter is case-agnostic. New `cases[]` + `activeCaseId` + `baseSnapshot`; methods `setActiveCase` (flush current → load target) / `addCase` / `renameCase` / `removeCase` / `clearCaseOverrides` / `resetOverridePath` / `extractPersistSnapshot`. `hydrate` auto-seeds the Management base + normalises legacy snapshots (additive, schema-tolerant).
- **Persistence** (`module1-sync.ts`): `extractSnapshot` delegates to `extractPersistSnapshot`, so the persisted form is always base model + cases + activeCaseId (never the merged active model). Cases version with the snapshot.
- **UI**: `CaseSwitcher.tsx` in the Topbar (switch/add/rename/delete + overrides list with Reset/Reset-all + amber chip on scenario); `Module5CaseComparison.tsx` = Returns "3. Case Comparison" tab (every case computed via the pure pipeline; KPI rows × case columns + delta vs Management). `m5Tabs` gained `m5-cases`.
- **Verifiers**: `verify-cases.ts` 19/19; `verify-returns-snapshot.ts` +3 real-engine cases checks (89 total).
- **Known/accepted (follow-ups in CLAUDE-TODO.md NEXT SESSION):** switching cases marks the project dirty (persists last-viewed case, can auto-start a session); `diffSnapshots` doesn't yet describe scenario-only edits in the change_log; per-input inline override badges deferred (Case Manager + comparison tab cover visibility).

## Latest financing + returns work (2026-06-02)

Commits this session, on `main`: `2f5adad` consolidated cash waterfall, `045828f`/`042828f` conditional IDC + segregated schedules, `45ecbc8` Method 3 drawdown table restored, `225b66d` M1→M5 audit (9 fixes), `056add3`/`dbbe580` M5 Returns Pass 1 + final revision, `ea7ac63` M5 terminal 100% payout + Method 3 gap excludes debt repayment, `ab451a8` docs optimize, `c6e78f6` dividends-after-debt + terminal payout moved into the engine, `2d400aa` single project-level dividend start year, `0994f76` project-level dividend policy + repo em-dash scrub, `1817c02` Cash Sweep sub-tab, `3301350` per-tranche Debt Paid + pre-sweep opening fix, `5db031d` full-project PDF export, `5d71d4b` Dividend Policy to top of Cash Sweep tab.

- **M5 terminal 100% payout (`ea7ac63` → moved to engine in `c6e78f6`):** at the exit period no minimum cash is retained; all cash above the opening-cash seed is distributed. NOW booked in the financing engine (`computeCashWaterfall`: `terminalPayoutPeriod` = `project.returns.exitYearOffset ?? N-1`, `terminalCashFloor` = `historicalOpeningCashTotal`), added to `totalDividendsPerPeriod[exit]` so it flows through Direct CF + BS + Returns and the cash-sweep Dividends line ties to FCFE / Distributed Equity. The returns-resolver-only fold was removed. New `DividendSnapshot.terminalPayoutPerPeriod`.
- **Dividends are ONE after-debt rule, no toggle (`c6e78f6`):** `statusPriority` is always `'after_sweep'` (before-sweep tier retired). BS Check label now tolerant (balanced if `maxAbsDiff < max(1000, totalL&E×1e-6)`, rounding noise on a billions project).
- **Single project-level dividend controls (`2d400aa` + `0994f76`):** `Project.dividendStartYear` (one start year, default = year after the last construction period) + `Project.dividendPolicy { enabled, payoutRatio, mode }` drive EVERY phase via `computeCashWaterfall` (`dividendStartYear` + `projectDividendPolicy` params); unset => legacy per-phase fallback. Per-phase Dividend Policy table + EBITDA-cap detail table removed from the UI.
- **Method 3 gap excludes existing debt repayment (`ea7ac63`):** `computeFundingGap` no longer subtracts `existingDebtRepaymentPerPeriod` (never raise NEW debt to repay OLD debt). Repayment is serviced from cash on hand.
- **Financing Cash Sweep sub-tab (`1817c02`):** Module1Financing now has Inputs / Schedules / Funding Gap / **Cash Sweep**. `FundingGapView` takes a `view: 'gap' | 'sweep'` prop, sections gated on it (Method 2 + Method 3 → gap; Cash Waterfall + per-tranche sweep + Dividend Policy → sweep). `5d71d4b` then moved the Dividend Policy to the TOP of the sweep tab (inputs above the waterfall they drive).
- **Per-tranche Debt Paid + pre-sweep fix (`3301350`):** Cash Waterfall splits Debt Paid per facility (`facilities.get(id).principalRepaid`) with a reconciling total. `computeCashWaterfall`'s per-tranche `preSweepOutstanding` now = `postSweep + THIS period's sweep` (was `postSweep + cumulative sweep`, so a fully-repaid tranche showed its original opening forever).
- **Full-project PDF report (`5db031d` → REWRITTEN `76aea30` 2026-06-03):** `lib/pdf/generateProjectPdf.ts` (pdf-lib). All-landscape FULL document (not a summary): portrait Cover + auto Executive Summary → one landscape section per selected module split into Inputs/Outputs/Schedules bands in tab order, every period table led by the universal prior-year column, `displayScale` default Millions. Builders return `TaggedItem[]`; modules + parts picked via `opts.moduleSections`. Embeds Inter full (`subset:false` is mandatory — fontkit subsetting emits a subset program Acrobat rejects). NO em-dashes. Pure (reads the snapshots, no new calc). verify-pdf-export 17/17. **Full per-builder coverage + the FAST-shading/whole-platform-coverage follow-ups (`4af1914` + `5b956a3`) are detailed in CLAUDE-ROUTES.md `generateProjectPdf.ts` entries + memory `[[project_pdf_mirror_autosync_2026-06-04]]` / `[[project_pdf_coverage_selection_2026-06-07]]`.**
- **PDF export overhaul (2026-07-07):** (1) renamed "PDF, Full Report" -> **"PDF, Full Financial Model"**. (2) **ExportModal auto-links to the live `platform_modules` registry** (fetch `/api/platforms/real-estate/modules`, same source as the sidebar) instead of the static `MODULES`+`BUILT` list: `status='live'` = selectable, coming-soon greyed, ordered by display_order, keyed to the `moduleN` routing key via `moduleComponentNumber(slug,number)`. So whatever ships / is hidden in the admin Module Manager auto-reflects in the export options. (3) **GROUP-level filter checkboxes** Inputs / Schedules / Output at the top of the modules step, wired to `opts.moduleSections` across every selected module (the top of the filter hierarchy: group -> module -> per-tab, existing checks intact). (4) **Module 6 (Scenarios) is now a BUILT PDF module** (`buildModule6`): Tab 1 Cases & Assumptions (inputs), Tab 2 Scenario Comparison (the shared `buildCaseComparisonMatrix`, also used by M5 Tab 3), Tab 3 Year-on-Year Impact (from `buildCaseYoYReport`); degrades to a note page with no scenarios. Added to `BUILT` + the switch + `PDF_MODULE_TABS.module6` + `collectModuleTabs`. (5) **Admin per-module "Include in PDF export" flag** (`platform_modules.include_in_pdf`, mig 186) toggled in `/admin/platform-modules`; the export list drops modules with it `false` (non-core: Reports/Portfolio/Market-Data/Collaborate/API seeded off). (6) **Executive summary + standalone Executive Summary PDF now carry a Scenario Summary block** (headline KPIs per case) when a case bundle is supplied (`buildExecSummary` gained a `caseReport` param; `generateSummaryPdf` builds + passes it, ExportModal passes `caseComparison` to the summary too). verify-pdf-export 32/32.
- **PDF navigation aids (2026-07-07):** reviewer navigation added to the FULL report (`generateProjectPdf` only; `generateSummaryPdf` untouched), purely additive (no content/figures/module-tab numbering change; only physical footer page numbers renumber to absorb the nav pages). pdf-lib has no high-level ToC/outline/link API, so all built from the low-level object model. (1) **Front Table of Contents** inserted right after the cover (`insertTocPages` via `doc.insertPage` + `paintToc`): Executive Summary -> modules -> sub-tabs, each row a GoTo link with a resolved page number. (2) **Section-break page per module** (`renderSectionBreak` + `paintBreakPages`): module title + "Sections in this module" (links to its tabs) + "All modules" cross-nav (links to every module). (3) **Internal hyperlinks** = `/Annot /Subtype /Link` with `/A /S /GoTo` and an explicit page-ref `/Dest` (position-independent, survive ToC insertion). (4) **`/Outlines` bookmark tree** (`buildOutline`, Exec + modules -> sub-tabs) + `PageMode /UseOutlines`. Anchors captured during render (`PDFPage.ref` + top-y); invisible page-dict markers `REFMNav` (`toc`/`break:<key>`) + `REFMTab` (`<key>::<tab>`) make it verifiable without decoding glyph-encoded page text. `drawFooters` now iterates `doc.getPages()` (true order) so numbering stays sequential after insertion; nav gated on `selectedModuleKeys.length>0` so empty-selection + summary output is unchanged. verify-pdf-export 44/44 (12 new object-model assertions: ToC presence/position, one break page per module, outline hierarchy + correct dests, ToC/break links reach every target, zero dangling links, empty-selection + summary carry no nav).
- **Excel MODEL export, FULL LIVE REBUILD 2026-06-11 (commit `8983760`):** `lib/excel/{styles,buildModelWorkbook,liveModel}.ts` (ExcelJS). A SIMPLE, fully formula-linked 14-sheet three-statement + returns model, so editing any input flows to the IRR (ExportModal "Excel" -> `generateModelWorkbookBuffer`). KEY: every calc cell = `fcell(formula, result)` where `result` comes from `liveModel.ts`, a pure TS twin computing the SAME arithmetic the Excel formulas emit (opens correct, recalculates on edit, self-consistent). Kept the 5 already-live tabs (Cover/Assumptions/Timeline/Land & Area/Capex); REBUILT live: Revenue (`base × per-period profile%`, the Capex allocation pattern), Cost of Sales (`revenue × dev-cost/GDV`), Opex (`revenue × ratio` + HQ), **Financing = the engine** (depreciation, interest = `rate × opening debt`, tax, deficit-funded debt/equity drawdown + surplus cash-sweep, a clean FORWARD recurrence, NO circularity), P&L + Cash Flow (link-and-present), Balance Sheet (rolls forward, **balances by construction**, verified diff=0), Returns (NOI + terminal value + FCFF/FCFE streams + live `IRR()`/`NPV()`/MOIC, IRR seeded with the twin's value as the convergence guess), Checks. SIMPLIFIED vs the platform engine (escrow / milestone-AR / IDC-capitalisation / multi-method funding); magnitudes follow the same drivers, financing/IRR use the simplified recurrence. Verifiers: `scripts/verify-excel-export.ts` (67, structure + liveness + reconciliation) + `scripts/verify-excel-recalc.ts` (7, **HyperFormula evaluates the formula graph** to prove streams recompute cell-for-cell, NPV matches the twin, an input edit moves the output, catching wrong references that cached values hide). Shared fixture `scripts/excelSampleState.ts`. Known limits: exit-year COLUMN fixed at build (editing the offset re-prices terminal value, doesn't re-window the IRR range); not a byte-for-byte engine mirror. See memory `project_excel_full_rebuild_2026-06-11`.

**Inputs-at-top audit:** most tabs already lead with inputs (M4 BS = Working Capital + Equity Inputs; M5 = AssumptionsPanel; M1 inputs in the Inputs sub-tab; M2/M3 dedicated Inputs tabs). Only the Cash Sweep tab needed the fix (above). Redundancy: the Cash Waterfall closing == M4 CF closing == BS cash etc. are intentional cross-views / tie-outs, NOT removed (per the user's "only if it won't affect our work" gate).

**Outstanding ops (carry forward until done):**
- Migrations **152** (version change_log) + **153** (version_label / task_name / comment) are NOT yet applied to production Supabase. Apply via the dashboard SQL editor. Code is schema-tolerant so the platform runs without them, but the change_log + named-version features stay inert until applied.
- M4 follow-ups still open: financing/IDC CF residual, capex-past-handover BS gap, opening-cash seed offset (all printed by `verify-m4-reconciliation-broad.ts`), per-asset non-uniform capex spread, PIT-handover Unearned negative window.

### Project-switch state-leak (commit `ca5c152`)

A project switch could write the new project's snapshot to the previous project's `refm_project_versions` row (data corruption), leak the old store snapshot into the new id for 200-500ms (visual), and clobber `lastSavedJson` cross-project. Fix: **detach-before-hydrate everywhere** (the non-negotiable rule codified in the versioning conventions above), `await attach` before flipping `activeProjectId` (+ `isSwitchingProject` overlay), and a cross-project guard in `runAutoSave`. **Recovery convention**: `refm_project_versions` is append-only, so pre-pollution snapshots reload via VersionModal "Load"; the migration-152 change log localizes which save corrupted the data. Full root-cause detail in memory `[[project_session_2026-05-31]]`.

### Session-based versioning (commit `d25a20b`, migration 152)

Replaces the M1.6 "every keystroke creates a new auto-save version" model with a session-based one driven by user intent:

1. Project opens read-only against the most recent version (`sessionBaseVersionId`).
2. User's first edit fires `fmp:refm-session-needs-name` window event → `NameVersionModal` opens.
3. Naming the session POSTs ONE new version row pinned via `base_version_id`, and every subsequent edit PATCHes the SAME row in place (`patchVersion` endpoint at `/api/refm/projects/[id]/versions/[versionId]`).
4. Server pre-computes `change_log = diffSnapshots(base.snapshot, this.snapshot)` on every PATCH (or POST), so the history UI renders "what changed" from one column.
5. `VersionModal` history list gains a "View log" toggle per version, expanding into typed add / remove / update entries with before → after value chips.

**State machine** (`module1-sync.ts`):
```
VIEWING ──first edit──▶ WAITING_FOR_NAME
WAITING_FOR_NAME ──startEditSession──▶ EDITING
WAITING_FOR_NAME ──revertEditSession──▶ VIEWING
EDITING ──store mutation──▶ EDITING (PATCH in place)
* ──detach──▶ (detached)
```

**Architectural conventions locked**:
- **Detach-before-hydrate is non-negotiable.** Any code that mutates the Zustand store on behalf of a different project MUST call `detachSync()` first. Otherwise the prior project's autosave subscriber catches the hydrate event.
- **Session = version.** One named version row per editing session. PATCHes in place. The pre-fix "auto-save creates N versions per session" pattern is retired.
- **Auto-start on first edit, no blocking modal** (refined 2026-05-31 evening). The sync subscriber detects the first snapshot-changing edit and calls `startEditSession(defaultLabel)` automatically. UI shows a non-blocking banner with Rename + Dismiss buttons. Never lose an edit to a discarded modal.
- **Edit-entry is a user choice (2026-06-17).** Three session entry points: `startEditSession` POSTs a new version (create-new), `startEditInPlace()` PATCHes the loaded version (no new row), `saveAsNewVersion()` branches the current state into a new row and continues there. The `EditChoiceModal` picks among them on Edit. "Session = version" still holds within a session; what changed is that opening one no longer forces a NEW row.
- **Diff is server-computed against the row's base.** PATCH endpoint re-loads `existing.base_version_id` and recomputes `change_log`; it never trusts a client-supplied diff (defense against polluted history).
- **`change_log` is on the row**, not derived. Survives base-version deletion (`ON DELETE SET NULL`). Versions stay readable even if their base is gone; the diff just records the pre-deletion comparison.
- **`refm_project_versions` stays append-only.** Migration 152 adds columns but never deletes / mutates existing rows; recovery from data corruption uses the version history via VersionModal "Load".
- **All new server-side reads/writes that depend on a new column MUST ship with a schema-tolerant fallback.** Pattern: try-FULL-select-first, catch PostgreSQL error code `42703` / PostgREST `PGRST204`, retry with BASE select and synthesize the new fields. Cache the probe result module-scope (`m152Applied: boolean | undefined`) to avoid double-probing per request. Reason: project convention is "apply migrations manually via Supabase dashboard" (see CLAUDE-DB.md migrations 142-148 entries), so production schema lags repo schema for hours-to-days.
- **Reads from any potentially-large table MUST paginate** via explicit `.range(from, to)` in pages of 1000. PostgREST's default `max-rows=1000` silently truncates and the dropped rows are typically the OLDEST (DESC order) — exactly the slice users need for historical recovery. See `listVersionsPaginated` in `lib/persistence/server.ts` for the pattern. **Never count by pulling rows and measuring the array** (`select('col').in(...)` then `.length`): the cap then silently BECOMES the answer, which is exactly how `listProjects` under-reported a 1,397-version project as ~1,000 until 2026-08-01. Count with `select('id', { count: 'exact', head: true })`: head returns no rows, so no cap can apply. Locked by `verify-refm-version-reads`.

**Files + hotfixes**: migration 152 + `snapshot-diff.ts` / `module1-sync.ts` / `server.ts` / `client.ts` / the two versions API routes + `NameVersionModal` / `VersionModal` + `verify-versioning.ts` (40/40). Post-`d25a20b` hotfixes `e2a7ba9` / `988dde5` (schema-tolerant `isMissingColumnError` via PG `42703` / PostgREST `PGRST204`) / `ff96aad` (paginated `listVersionsPaginated` + History date filter) / `7d76bf4` (auto-start on first edit + non-blocking banner). Per-file detail in CLAUDE-ROUTES.md "2026-05-31 session" + memory `[[project_session_2026-05-31]]`.


## 2026-05-25 session (M4 statements correctness + UI polish)

Two root-cause fixes closed the user-reported BS imbalance + Direct/Indirect mismatch, plus UI work. All render-layer + composer; engine schedules (the "ready" source of truth) untouched.

- **Escrow = restricted cash, not a liability** (commits `b2ddf48` + `e544267`). Escrow was a current liability while the held cash was removed from operating cash with no offsetting asset, AND the two CF methods used opposite escrow signs. Now escrow is a restricted-cash ASSET (`ProjectBS.escrowRestrictedCashPerPeriod`), Indirect CFO uses `−escrowChange` (matching Direct's `netRevAdj`). BS UI: "Restricted Cash (Escrow)" under Current Assets. BS Schedules: moved L3→A4. Zero-escrow fixtures always balanced, which hid it.
- **Residential P&L revenue uses RECOGNISED timing** (commit `f8dbfd4`). Per-asset Sell P&L summed `presalesRevenuePerPeriod` (sale-value timing) instead of `presalesRecognitionPerPeriod` (the recognition profile — the SAME field Module 2 Revenue Output renders as "Pre-Sales Recognised", `Module2RevenueOutput.tsx:1310`). PAT disagreed with the Unearned schedule, so cash methods diverged + Unearned could go negative. Fix at `financials-resolvers.ts` revRow build: `presalesRecognitionPerPeriod + postSalesRevenuePerPeriod`. M4 P&L now matches Module 2 per-asset recognition exactly (over-time / handover / custom). Verifier S pins it.
- **Both CF methods sum their OWN line items to their OWN closing cash** (commits `890ba33` + `ea3c402`). Direct closing = running sum of Direct net CF (also the BS Cash source); Indirect closing = running sum of Indirect net CF. They agree only because both are correct (not copied). Indirect now mirrors Direct's sweep + dividend adjustments + exposes Opening/Closing cash.
- **Per-line BS Reconciliation Bridge** (commit `2213615`): `snap.bsReconciliation` decomposes Δ(BS diff) = NetCF − Δ(Liab+Equity) + Δ(non-cash Assets), exact identity (`unexplained` must be ~0). Rendered on the BS tab to localize any future leak by line.
- **Universal prior-year column** (commit `8fe9ceb`): every results table leads with a prior-year column (= projectStartYear − 1); M2/M3 local `PeriodTable` + shared `VintageMatrix` auto-derive it to match M1/M4. Same commit: `VintageMatrix` header "Cohort Total" → "Total"; removed "Cum % Sold" from the Revenue 1c table.
- **AP DPO inputs moved to Opex Inputs tab** (commit `b9ea22a`): Project Default DPO + Days basis + per-asset override now on Module 3 Opex Inputs (card "💳 Accounts Payable (DPO)"); Output tab keeps only the AP roll-forward.

Verifier sections P5/P6/R/S pin: Direct==Indirect (with dividends), bridge exact identity, escrow balances, recognition == Module 2.

## Pass 2O → 2Z (2026-05-24 session) — condensed

Full per-pass detail lives in memory `[[project_m4_pass2o_through_2z]]` + git history. Summary: IDC policy split (`Project.idcConfig`, accounting/funding decoupled via `interestForAssetBasis` on `FacilityResult`) → BS Equity split (Cash / In-Kind / Existing) + RE roll-forward → IDC integrated into Fixed Assets roll-forward → Funding Gap sub-tab (`computeFundingGap`, Methods 2 + 3) → Cash Sweep + Dividend consolidated master waterfall (`computeCashWaterfall` → `CashSweepSnapshot` + `DividendSnapshot`, per-phase EBITDA cap, priority by phase.status) → Phase 1 i=0 lump rescue + per-parcel in-kind stamping (the 2026-05-24 BS-imbalance fixes; superseded as the full balance fix by the 2026-05-25 escrow + recognition root causes above). The durable conventions from that session:

**Architectural conventions (apply to all future modules):**
- **Projection rule**: phase-local `i=0` (the "Y0 lump") lands at `projIdx = Math.max(0, offset - 1)` on the project axis. Phase 1 (offset=0) goes to axis Y0; Phase 2+ goes to `offset-1` (the year before phase starts). Mirror this rule on EVERY side that consumes phase-local arrays (capex / equity / debt / inventory) so BS stays balanced.
- **Prior-year column convention** (made UNIVERSAL 2026-05-25, commit `8fe9ceb`): EVERY results table on the platform renders a prior column (= projectStartYear − 1) between Total and the first axis year, so the year axis aligns column-for-column across all modules. Module 1 (`buildResultsPeriodAxis`) + Module 4 (`m4Table.tsx` `priorYearLabel`) already did this; Module 2 (Revenue Output / CoS / Schedules / Escrow) + Module 3 (Opex Output) now match via auto-derive (`resolvedPriorYear = priorYearLabel ?? yearLabels[0] - 1`) inside each local `PeriodTable` + the shared `VintageMatrix`. Pre-axis events (existing equity, existing debt opening, pre-capex, opening NBV / Land) appear in the prior column; M2/M3 prior cells are blank (no pre-axis activity). Axis values shift right by one cell. Inline `<tr>` rendering MUST include the empty prior cell to align with the helper headers. Same commit: `VintageMatrix` row-total header default "Cohort Total" → "Total"; removed the "Cum % Sold" trailing column from the Revenue 1c table. See `[[project_universal_prior_year_column]]`.
- **In-kind equity**: stamped per parcel at the owning phase's projected i=0 axis index (Pass 2Z). Never lump at axis[0] regardless of phase.
- **IDC policy split**: accounting (capitalize) and funding (sweep mode) are independent decisions. Engine carries both via separate fields on `FacilityResult`.
- **Cash sweep + dividend waterfall**: single consolidated master view (Pass 2V). Per-phase EBITDA cap enforced on dividends (Pass 2T-Fix). Priority assigned by phase.status (Pass 2U-Fix). Cash availability above min reserve checked at each step.

**Current state (2026-05-21):**

- **Module 4 (Financial Statements)** is composing live. `computeFinancialsSnapshot` in `src/hubs/modeling/platforms/refm/lib/financials-resolvers.ts` pulls every upstream engine (revenue / opex / AP / escrow / fixed assets / financing) and produces P&L + Direct CF + Indirect CF + BS + IDC allocation. Module 4 sidebar has 4 tabs: **Schedules** (parent shell with Fixed Assets & D&A + BS Schedules sub-tabs) / **P&L** / **Cash Flow** / **Balance Sheet**. Phase filter buttons on all 4 surfaces.
- **Pass 2N-Fix series** closed every off-by-one slice + BS imbalance + AR settlement bug raised in the user audit:
  - **Debt outstanding off-by-one** (commit `cc301ed`): composer + BS Feeders + BS surface all read `fac.outstanding[t]` (length-N, year-t closing). Previous `.slice(1, 1+N)` was dropping year-0 closing and zeroing the last year. New `FacilityResult.openingBalance` exposes pre-axis balance for the BS prior column.
  - **Financing slice off-by-one across 11 sites** (commit `89fb5db`): IDC capitalised, interest expensed, capex, equity / debt draws, debt repays, interest paid — all changed to `.slice(0, N)`. Stale "+1" docstring in `financing/types.ts` updated.
  - **BS Share Capital seeded with priorEquity** (commits `89fb5db`, `035cfb3`): includes pre-axis equity opening (`priorEquity + cumulative new draws`). Equity engine's `existingEquityPerPeriod` lump removed from composer's `equityDraws` to avoid double-count through `cashFromFin → netCf → closingCash`.
  - **A1 AR + L2 Unearned literal per-asset mirrors** (commit `035cfb3`): drops SDO-inclusive billed/collected rows; each surface now shows opening + pre-sales sale value − pre-sales cash collected = closing PLUS a per-asset closing breakdown beneath. Project total = literal sum of per-asset rows.
  - **CF financing consolidated by origin** (commit `da83c06`): Existing / New buckets instead of per-tranche detail. Phase tag dropped. Prior column seeded with existing equity / debt opening / pre-capex.
  - **Cohort tail-catchup** (commit `47ab734`): cash payment profile positions past axis end (e.g. 5-year cohort with cash to year 7) used to be silently dropped, leaving AR stuck at the un-collected residual forever. Now any percentage at `position >= N` settles at the last axis year (N-1), so every cohort row sums to 100% of cohortValue and AR closes to 0. Applies to both `absolute_with_catchup` and `relative_to_sale` modes.
  - **M2 CoS Capex by stage now includes IDC** (commit `89fb5db`): per-asset IDC pulled from `finSnap.idc.byAsset`, added to `capexPerPeriod` so CoS unwinds IDC alongside base capex.
  - **CF UI polish** (commits `dece8f9`, `47ab734`): P&L + Direct CF strategy buckets show subtotal inline on dropdown header (no separate Total row). Total column +25% width. Pre-Capex row above Total Capex when historical pre-capex > 0. Finance Cost (Capitalised via IDC drawdown) memo per origin so the user sees finance cost being "paid" during construction via debt drawdown.
- **M2 Pass 9L** (commit `0e7e1d3`): Sell+Manage companion (Operate side) moves from Residential to Hospitality bucket on Revenue Inputs. Each strategy bucket now carries only inputs for that strategy; companion gets "linked to {parent}" chip.
- **M2 Pass 9L-Fix** (commit `dafa40a`): every dual-write setter (velocity / cash / recognition / occupancy) rebuilds byPhase from the full updated legacy axis instead of `paddedArray(existingByPhase, phaseLen)` + single-index assign. The previous pattern produced an empty byPhase covering the full phase window that SHADOWED legacy values entered in prior sessions, wiping historical entries the user hadn't re-typed. Hydration migration `migrateM2Pass9LBackfillByPhase` auto-repairs existing snapshots on load.
- **M2 Pass 9M + M3 Pass 5d** (commits `eea1fa6`, `18caaa0`, `7893eb8`, `b8d042e`): sticky asset quick-nav strip on every per-asset M2 / M3 / M4 surface. `AssetQuickNav` lives in `_shared/`. Click a pill → expand the parent section + smooth-scroll to the asset card with an outline pulse. Selective expand (only the target asset's section, not all).
- **Excel-style trace-to-source scaffolding** (commit `47ab734`): `M4Row.trace` field + ⤴ icon dispatches `fmp:trace-to`. `RealEstatePlatform` listens, flips active module + tab, scrolls. Foundation for wiring P&L Finance Cost → BS L4 Debt, etc.

**Open follow-ups (in priority order):**

1. **M1 Funding Methods 2 + 3** (cash-deficit driven), sized against the real CF deficit from M4. Method 1 (Total Capex) is live; 2 + 3 stay stubbed pending user testing of M4 statements first.
2. **Per-asset capex non-uniform spread** within construction windows. Project totals stay exact via the financing engine; per-asset CF / Inventory approximation is acceptable today but flagged.
3. **PIT-handover recognition economics**: point-in-time handover recognises ALL pre-sales at the handover year, including cohorts sold AFTER handover, which can make Unearned briefly negative. The statements still balance + tie (M4 mirrors Module 2 exactly); deferring later cohorts is an optional Module 2 recognition-engine tweak, not a balance issue.

> BS imbalance + Direct/Indirect mismatch: **RESOLVED 2026-05-25** (escrow restricted-cash + residential recognised-revenue root causes). BS balances by construction and both CF methods tie; the per-line Reconciliation Bridge on the BS tab localizes any future leak. See the 2026-05-25 session block at the top.

**M2 lock conventions** (apply to M3 + M4 unless overridden):
- The reference Excel at repo root is the verification benchmark, not a behavioural spec. Every reference-specific behaviour stays configurable; never hard-code currency, locale, escrow, or DSO defaults into engine paths.
- Engine storage today is HYBRID per [[project_m4_pass2h_period_data_fix]]: asset-scoped per-period fields are PHASE-LOCAL (`ByPhase: number[]`, `arr[0]` = first year of owning phase); HQ + financing year-keyed (`ByYear: Record<string, number>`). Legacy axis-indexed `number[]` fields remain on schema but marked `@deprecated` and zeroed by hydration migrations.
- Vintage matrices (cohort year × cash year, cohort year × recognition year, capex year × CoS year) are the canonical mechanic for both recognition and cash distribution.
- PIT recognition handover = LAST construction year (`phaseStart + cp − 1 − projectStart`), NOT first operations year. Verifier A2-1..A2-5 pin this.
- INPUTS group by strategy bucket; OUTPUTS group by phase. Inputs flat by asset list, phase shown as a small tag per card.
- Every UI setter writing to a phase-local array MUST prune out-of-window indices on save. Pattern: pass `validPhaseIndices: Set<number>` into the write helper.
- See [[project_audit_2026-05-20]] for the full current state.

<!-- LOCKED-module pass narrative collapsed 2026-06-15. The escrow + fixed-assets/
     depreciation engine mechanics below are preserved in full in CLAUDE-ROUTES.md
     (escrow.ts / depreciation/ / fixed-assets-resolvers lib entries) + the cited
     memory files; this is the one-line index. -->

**M2 Escrow engine (Pass 9h/9h-1..3, LOCKED, commits `87f0075` → `7ab3019`):** per-project/per-asset configurable `computeEscrow` (`held[t]=preSalesCash×heldPct`, release lump on the release year, balance rolls to zero; heldPct=0 disables). Schema `Project.escrow?` + `Asset.revenue.sell.escrow?` (override > default > handover+1). `Module2Escrow.tsx` 5th sub-tab; verify-escrow 46/46. Detail: `[[project_m2_pass9h_escrow]]` / `[[project_m2_pass9h1_escrow_simplified]]` + ROUTES escrow.ts.

**M4 Fixed Assets + Depreciation engine (Pass 1/1c/1d, LOCKED, commits `1b5e9b9` → `26c221b`):** `src/core/calculations/depreciation/` — SL + Reducing Balance allocators (RB default rate `2/life`), per-addition vintage roll-forward from `max(t,startIdx)`, residual stays as NBV at exit. Resolver `computeAllFixedAssetResults` walks Operate/Lease/companion assets only (Sell capex flows through M2 CoS); Land split out as non-depreciable (life=0); capex projection mirrors `aggregateProjectCapex`. Asset schema `depreciationMethod`/`depreciationRate` (optional). verify-fixed-assets 82/82. Reference-benchmark methodology (10-component v7.0 block) in `[[reference_v7_depreciation]]`; decisions in `[[project_m4_pass1_decisions]]`.

**Module 3 status (Opex, LOCKED Pass 5d):** `src/core/calculations/opex/` — per-asset `computeAssetOpex` + project-wide `computeHQOpex`, two-pass (Pass A non-GOP lines, Pass B GOP = Revenue − Direct − Indirect → `pct_of_gop`). **Durable rule: inflation applies ONLY to fixed-cost modes** (`fixed_baseline` / `per_room_year` / `per_sqm_year`); %-of-revenue + `pct_of_gop` lines auto-escalate via the stream (engine zeroes their indexation). Asset/HQ `defaultIndexation` + per-line `useAssetDefault` override; `rateMode: 'single' | 'yoy'` (+ `yoyRates[]`) bypasses inflation. Applies to Hospitality/Operate (incl. companions) + Retail/Lease only (Sell parents + pure Sell have no opex). verify-opex 38/38. Full line-mode / category / seed / UI detail in CLAUDE-ROUTES.md opex/ entry + memory `[[project_m3_pass3_decisions]]` / `[[project_m3_pass5b_decisions]]`.

**Phase startDate cascade** (commit `50a4c89`): NOTE this cascade was later DISABLED (2026-05-20, commit `b209afe`, memory `[[project_phase_date_cascade_disabled]]`) — `updatePhase` no longer shifts asset arrays, user inputs are preserved verbatim, and storage is now the HYBRID phase-local / year-keyed model (see the M2 lock conventions above). The original sliding-array cascade is retained in git history only.

**Module 2 final state (LOCKED Pass 9N):** Revenue engines Residential Sell / Hospitality (Operate + Sell+Manage companions) / Retail-Lease; PIT recognition handover / sale_year / custom; CoS `costOfSalesV2` joint-cumulative (Drivers → Vintage Matrix → Summary → Inventory roll-forward); shared `VintageMatrix` (Cash + Recognition + CoS); rental-pool enrollment toggle (auto-link vs Day-1); Schedules tab = raw line-item FS feed (zero rows hidden). Retired Phase Overlap → per-asset `operationsStartYearOverride`; "3. Costs" → "3. Capex". Snap-to-zero |x|<1000. Pass detail in memory `[[project_m2_pass7_decisions]]` / `[[project_m2_pass9i_9j_9k]]` / `[[project_m2_pass9L_byphase_shadow]]`.

**Engine conventions (carry into M3):**
- Pre-sales: revenue lumps at sale year; cash via payment profile; recognition via recognition profile (or PIT anchor).
- Post-sales (SDO): revenue = cash = recognition in same period.
- Hospitality + Lease: revenue = cash = recognition same period; AR delay via DSO engine.
- Project axis: `arr[0]` = first active project year.
- YoY rounding (units or sqm) before revenue computation.
- Sale value drives AR + UR (gross credit); cash drains AR; recognition drains UR.
- AR/UR signatures: `buildAccountsReceivable(saleValue, cash, N)`, `buildUnearnedRevenue(recognition, saleValue, N)`.

---

<!-- Per-pass narrative archived (2026-05-20). M1 M2.0 phase history
     (Passes 11-58 inc. 2026-05-12 diagnostic blocks + M2.0 Pass 56-58
     lock-in), M2 Pass 1-7 build narrative, and M2.0 Passes 11-55 themes
     are in CLAUDE-FEATURES.md "Module 1 (REFM) M2.0 Phase History".
     See git history for raw per-pass commit detail. -->

> **See also:**
> - [CLAUDE.md](CLAUDE.md), Root project brief, session rules, stack, auth, envs
> - [CLAUDE-MODELING-HUB.md](CLAUDE-MODELING-HUB.md), Modeling Hub platform catalog + P-Sync conventions
> - [CLAUDE-FEATURES.md](CLAUDE-FEATURES.md), Archived Module 1 phase narratives (M1.R → M1.13d pre-M2.0, M2.0 → M2.0i post-rebuild)
> - [CLAUDE-DB.md](CLAUDE-DB.md), Database tables, migrations log
> - [CLAUDE-ROUTES.md](CLAUDE-ROUTES.md), Routes + components + lib structure

---

## REFM Design System

REFM (Module 1 tabs + shell + modals + Area Program tab) uses **FAST input blue** instead of `.input-assumption`:
- Input bg: `var(--color-navy-pale)` + text: `var(--color-navy)` via the local `inputStyle` constant in each component
- Calculated outputs use `calcOutputStyle`: `var(--color-grey-pale)` bg + `var(--color-heading)` text
- Established Phases 4.6 → 4.15 (2026-04-30), extended into M1.7 Area Program tab (2026-05-02)
- The `.input-assumption` class is reserved for actual financial-model assumption cells (rates, ratios, escalators) and continues to apply outside REFM

---

## REFM Verifier Scripts

Active engine + composer coverage (run all on every meaningful change). **25 scripts, full suite green 2026-06-02.** Per-script counts grow as sections are added, so run a script for its live total rather than trusting a hard-coded number here. Run the whole suite with `for f in scripts/verify-*.ts; do npx tsx "$f"; done` (PowerShell: `Get-ChildItem scripts/verify-*.ts | % { npx tsx $_.FullName }`).

```bash
# M2 / M3 / M4 / M5 + financing (current daily loop)
npx tsx scripts/verify-report-arithmetic.ts        # PDF review pass 1: the identities a reader would try (78)
npx tsx scripts/verify-report-readability.ts       # PDF review pass 2: no truncation / signed zeros, ALL scales (55)
npx tsx scripts/verify-report-consistency.ts       # PDF review pass 3: metric basis + missing sections (59)
npx tsx scripts/verify-report-presentation.ts      # export review pass 4: flow-vs-balance, Total-vs-Closing, structural zeros (92)
npx tsx scripts/verify-revenue-rebuild.ts         # M2 Sell + Hospitality + Lease + AR + UR
npx tsx scripts/verify-escrow.ts                  # M2 Pass 9h escrow + cleanup
npx tsx scripts/verify-cost-of-sales-v2.ts        # M2 Pass 9e + 9N cohort tail-catchup
npx tsx scripts/verify-opex.ts                    # M3 asset + HQ opex engine
npx tsx scripts/verify-opex-ap.ts                 # M4 Pass 2a Accounts Payable
npx tsx scripts/verify-fixed-assets.ts            # M4 Pass 1 depreciation
npx tsx scripts/verify-idc-depreciation.ts        # M4 IDC -> D&A + annuity amortization (§K)
npx tsx scripts/verify-asset-cost-allocation.ts   # M1 computeAssetCost allocation
npx tsx scripts/verify-m4-bs-reconciliation.ts    # M4 BS identities + debt/share-cap
npx tsx scripts/verify-m4-reconciliation-broad.ts # M4 Direct==Indirect + BS bridge (broad guard)
npx tsx scripts/verify-bs-hq-ap-link.ts           # BS HQ-AP link (cf6200d drift guard)
npx tsx scripts/verify-funding-methods.ts         # Methods 2/3 gap-sizing + conditional IDC + sweep + fixed-point [CONV]
npx tsx scripts/verify-returns-engine.ts          # M5 pure IRR/MOIC/payback/TV/RE-metrics/analytics + Pass 2 partners/sensitivity (101)
npx tsx scripts/verify-returns-snapshot.ts        # M5 stream build-up + sponsor-IRR + sources/uses + Pass 2 partners/exit-year/per-asset/sensitivity (81)
npx tsx scripts/verify-phase-date-preservation.ts # M4 Pass 2h hybrid storage
npx tsx scripts/verify-phase-date-scenarios.ts    # M2/M3 setter dual-write + 9L-Fix
npx tsx scripts/verify-versioning.ts              # session versioning + change_log
npx tsx scripts/verify-version-naming.ts          # auto version names + rollover
npx tsx scripts/verify-refm-version-reads.ts      # no persistence read can be silently truncated by the PostgREST 1000-row cap: structural (every query site bounded) + behavioural (in-memory fake that truncates at 1000) (46)
npx tsx scripts/verify-ic-narrative-e2e.ts        # M7 AI Unit 9: the END-TO-END check across all nine units, for the SEAMS between them. Toggle gates generation server-side, cap hard-stops with zero ai calls, only real engine figures reach the prompt, an invented figure is caught, a FLAGGED draft is shown with a warning and stays editable/appliable (the settled policy), the draft is never auto-saved, quota mirrors the server and never renders unknown as zero, the tab survives with the feature on and off, a deployment failure (no key) costs no credit, and every failed generation REFUNDS its credit while a successful or merely flagged one does not (199)
npx tsx scripts/verify-ic-narrative-ui.ts         # M7 AI Unit 8: Generate buttons + quota UI. Target resolution over a deck seeded from the REAL templates with a REAL engine model (every narrative field must resolve to a writable block, and resolution must REFUSE rather than guess when a block is deleted, duplicated, or its caption renamed), patch shaping per block kind, placeholder-vs-authored reporting, and the UI contract (panel never patches, review is the only path to a slide, quota comes from the server, palette on DECK_THEME) (126)
npx tsx scripts/verify-ic-narrative-generation.ts # M7 AI Unit 7: IC narrative generation. Field catalog + availability gating + output shaping (em-dash strip, risk-row parsing) + the SPEND GATE counted on injected fakes (an unavailable field, a capped user, and a disabled feature must each make ZERO ai calls) + prompt assembly over a REAL engine-built model + audit teeth. No key, no DB (149)
npx tsx scripts/verify-pdf-export.ts              # full-project PDF export smoke test (pdf-lib)
npx tsx scripts/verify-cases.ts                   # scenario/case merge engine (applyOverrides/buildOverrides/seed) 19/19
npx tsx scripts/verify-module6-field-census.ts    # EMPIRICAL per-field audit on the LIVE project fixture (gitignored; skips-with-notice when absent, refresh via scripts/fetch-fmp-re-hub.ts). Asserts every picker field is a proven KPI mover OR gated/excluded with a reason; 17 checks

# Module 1 historical (LOCKED; run only when touching M1 code)
npx tsx scripts/verify-tab3-regression-2.ts       # Tab 3 regressions
npx tsx scripts/verify-tab2-pass3.ts              # Tab 2 quick fixes
npx tsx scripts/verify-m20costsCleanup-pass10.ts  # M2.0 Pass 10 Costs cleanup
npx tsx scripts/verify-m20M.ts                    # M2.0M Financing rewrite
# Older scripts/verify-m20*.ts remain runnable; not part of the daily loop.

# Playwright e2e
npx playwright test tests/e2e/m20L-costs-financing.spec.ts  # 10 specs + dark-mode
```

### Per-phase verification workflow (M1.7+)
Standing preference (2026-05-02): every REFM phase ships a `scripts/verify-[phaseId].ts` covering 5 sections:
1. **Database / persistence**: Supabase JSONB roundtrip via service-role
2. **Route smoke tests**: 401-without-auth gates; skips when `localhost:3000` is down
3. **Calculation correctness**: snapshot diffs + targeted assertions on fixture inputs
4. **State integrity**: load fixture into store, mutate via store actions, assert cascade
5. **UI rendering**: Playwright headless light + dark screenshots saved to `tests/screenshots/[phase]/{light,dark}-*.png`; skips when dev server is down or Playwright not installed

Test-user fixture id `00000000-0000-0000-0000-000000000000` with `ON DELETE CASCADE` cleans downstream rows on teardown. M1.7 reference: 25 pass / 0 fail / 2 skip without dev server.

**Dev dependencies**: `@playwright/test ^1.59.1` + chromium browser (`npx playwright install chromium`).

---

## Module 1 Conventions (v8 + M2.0L contract, applies to all downstream modules)

> Single source of truth for Module 1 patterns and downstream-module obligations. Replaces the per-phase "pattern decisions" sections that ran M2.0 → M2.0L. Archived per-phase narrative lives in CLAUDE-FEATURES.md under "Module 1 (REFM) M2.0 Phase History".

**Schema + migrations**
- **Hard-cut on every schema bump.** Pre-vN snapshots flag with explicit error rather than silent coercion. v3/v4 → v5, v5 → v6, v6 → v7, v7 → v8 all follow this policy. Non-version-bumping additive fields (M2.0f, M2.0h, M2.0i, M2.0j, M2.0L) default off/undefined for legacy snapshots.
- **Phase-scoped cost line ids (M2.0L).** Standard catalog ids compose as `${baseId}__${phaseId}` to keep them globally unique across multi-phase projects. Use `composeLineId` / `deriveLineBaseId` / `isStandardCostLineBaseId` from `module1-types.ts`. Calc engine helpers that key by line id (e.g., `deriveCostStage`, `selectedLineIds` resolution) strip the suffix before lookup. Custom user lines (`custom-${timestamp}`) are already unique. `migrateM20lDedupeCostLineIds` retrofits legacy duplicate-id snapshots on hydrate.
- **Migration banner pattern.** `CheckedHydration.migrationNotice` → `AttachResult.migrationNotice` → dismissable banner once per project open. Migration helper kicks an immediate save so banner doesn't reappear.
- **Snapshot baseline: ONE file per major schema version** at `scripts/baselines/module1-v5.json` (now v8 content, name retained).

**Cost allocation**
- **A SHARE BASIS MUST NEVER RETURN 0 FOR EVERY ASSET.** `total > 0 ? mine / total : 0` looks safe and is a **silent money-loss bug**: with a zero denominator every asset scores 0, the shares sum to 0 instead of 1, and a project-level lump sum on that basis is allocated to nobody and vanishes from the model with no warning. It cost a measured 1,000,000 on `land_share` with no land in the phase (fixed 2026-08-15). Every share path in `resolveAllocationFactor` / `resolveDriverFactor` now falls back to the shared `equalPhaseShare`, matching what `computeAssetLandSqm` and the `category` basis already did. **Any new basis or driver must do the same, and its factors must be proven to sum to 1.** See the root [CLAUDE.md](CLAUDE.md) bullet and 2026-08-15b above.
- **Only the `fixed` method consults an allocation basis** (`FIXED_METHODS_NEEDING_ALLOC`). Every `rate_per_X` / `percent_of_X` method already returns the asset's own contribution because its metrics are asset-scoped, so applying a basis on top double-allocates.

**Timing**
- **Phase.startDate is authoritative.** Tab 1 + Tab 2 read `computePhaseTimeline(phase, project)`. M5 Statements + M3 Cashflow consume same helper for column dates.
- **`constructionPeriods === 0` is canonical for operational phases.** computePhaseTimeline returns `operationsStart === phase.startDate` when cp=0. Asset.status='operational' on cp=0 phases gets historical baseline treatment.
- **End-of-period dates everywhere.** Use `periodEndDate`; never display "Jan 1 of next year" as period end.
- **`ProjectTimeline.endYear` is inclusive.** No +1 offset in display layers.
- **Period dates align to PHASE start.** Cost / revenue / opex / financing schedules all measure from `phase.startDate`. Project-wide rollup tables offset by `(phaseStartYear - projectStartYear)` to place phase Y1 in project Y2 / Y3 correctly.

**Status + lifecycle**
- **Phase + Asset status drives lifecycle treatment.** `'planning' / 'construction' / 'operational'`. Operational reveals `historicalBaseline` (sunk capex / equity / debt / accumulated dep / trailing revenue + opex). M5 reads `computePhaseHistorical(phase)` for opening balances + `computeOperationalRunRate(baseline, period)` for rollforward.
- **Status pill colors:** planned = grey, construction = warm amber, operational = green-success. M5 + M3 reuse same scale.
- **Asset.type is optional.** Treat `''` as unspecified. Useful Life falls back to category default (`DEFAULT_USEFUL_LIFE_YEARS`: residential 30 / hospitality 20 / retail 25 / default 25).

**Inputs + outputs**
- **Inputs are annual; outputs flex.** No user-visible "model granularity" toggle. Every Module 1 input is annual. Display uses `distributeAnnualToPeriods(annualValues, granularity, phasing)` with sum-integrity guarantee. M2.1 / M3 / M5 adopt same convention.
- **`project.outputGranularity` is the project-wide view setting.** Tab 3 / Tab 4 / future M5 all read it.
- **Phasing is Even + Manual % only.** Read-side accepts legacy `frontloaded` / `backloaded` / `sCurve` / `phase_aligned` via `migrateM20jPhasing`. Manual % UX: per-period inputs + sum indicator + auto-normalize button.

**Display + formatting**
- **Project-scoped formatting.** `project.displayScale` (full/thousands/millions) + `project.displayDecimals` (0..3). Use `formatScaled(num, scale, decimals)` or `makeProjectFormatter(project)`. Cells render pure numbers (no currency suffix).
- **Currency lives in the per-tab header line** via `currencyHeaderLine(currency, scale)` → "All figures in SAR" / "...SAR '000" / "...SAR M".
- **Percentages always 2 decimals** via `formatPercent` default. **Areas (sqm) use `formatArea`** (no scale conversion). **Integer counts bypass scale via `formatInteger`**.
- **MANDATORY platform-wide input primitives** (applies to every Module - 1, 2, 3, 4, 5+):
  - **Every money / currency / area / count input MUST use `AccountingNumberInput`.** Raw `<input type="number">` on focus + accounting-formatted text on blur (commas, parens for negatives, `-` for zero with `blankWhenZero` option, currency suffix never). Parser handles commas + wrapping parens.
  - **Every percent input MUST use `PercentageInput`.** Default 2 decimals + `%` suffix + parens for negatives + `0.00%` for zero (percentages never collapse to a dash). Value prop is in percent units (0-100), onChange returns the same. Parser strips commas + trailing `%` + wrapping parens. Storage convention: when the underlying engine stores 0-1, multiply by 100 for `value` and the setter divides back.
  - **Never use bare `<input type="number">` for money/percent in new code.** Bare inputs lose the formatting + parsing contract and break the platform's visual consistency. Audit existing surfaces during feature work and migrate.
  - Examples: M1 Tab 4 financing inputs (Pass 42), M2 Pass 7 velocity grids + cash/recognition profile strips migrated 2026-05-16.

**Area hierarchy**
- **Three-tier hierarchy: NSA ⊂ BUA ⊂ GFA.** NSA = revenue sub-units (Sellable + Operable + Leasable); BUA = NSA + Support (sub-unit + asset-level); GFA = BUA + Parking (asset-level). Consume `computeAssetAreaHierarchy(asset, subUnits)`; never re-derive from `Asset.buaSqm` directly.
- **Sub-unit BUA is source of truth.** `computeAssetBua` / `computeAssetSellableBua` fall back to `asset.buaSqm` only when sub-units are empty.
- **SubUnitMetric is `'units' | 'area'`.** Legacy `'count'` accepted on read. Use `switchMetric()` to preserve area sqm on toggle.
- **Parking is sqm-only at the cost-engine level.** No parkingBays input. Parking-bay-driven revenue (fee/bay/year) models as a Leasable sub-unit.

**Land**
- **Multi-parcel landAllocation.** Asset gains `landAllocation: { parcelId?, sqm?, pct?, multiParcelSplits?, customRate? }`. Sentinels: `PARCEL_WEIGHTED_AVG`, `PARCEL_CUSTOM_RATE`. M2.1 reads `Asset.landAllocation` for per-parcel disposition.
- **Parcel NDA is parcel-level.** Each parcel carries `hasNdaDeduction` + `roadsPct` + `parksPct`. Land allocation references NDA (not gross area); full parcel cost flows to assets at inflated effective NDA rate.
- **Reconciliation is compact-by-default.** Collapsed summary line with status icon (✓/✗/⚠) + expand affordance + auto-expand on mismatch + localStorage persistence. Pattern applies to land reconciliation, asset area reconciliation, and future revenue/debt/capex reconciliations.

**Cost engine**
- **Direct vs Allocated category (M2.0L Pass 5).** Every `CostLine` carries `costCategory?: 'direct' | 'allocated'` (default `direct`). Direct = asset-specific (current Pass 3+ math: `rate × asset.metric`, `allocFactor = 1` except for method='fixed'). Allocated = project-wide pool, split per asset via `costDriver` (`bua_share` / `land_share` / `value_share`). Calc engine computes Allocated lines against `aggregatePhaseMetrics(phaseAssets, metricsByAsset)` to get the pool, then `resolveDriverFactor(driver, asset, ...)` distributes per asset. `value_share` currently falls back to `bua_share` (deferred until M2.1 Revenue ships projected per-asset value). Auto-derived `CostType` (`hard` / `soft` / `land_cash` / `land_in_kind` / `operating`) via `deriveCostType(line)` is internal-only, not user-visible.
- **Parent/child inheritance is the canonical Costs UX (M2.0L Pass 4).** One editable master cost line table per phase (`CostLine[]` with `targetAssetId === undefined`) + per-asset resolved replicas below. Each replica row carries a Source pill (Inherited/Override) + an Override toggle button. **NOTE Pass 7 (2026-05-12) deprecated this surface**: Tab 3 Inputs is now per-asset only; CostOverride[] flattened on hydrate.
- **CostOverride resolution:** `override.overridden === false` reverts to master entirely. `override.overridden !== false` (true OR legacy undefined treated as true) uses override fields with master fallback per field. Same rule for the migration banner: legacy CostOverride entries stamp `overridden=true` on hydrate via `migrateM20Pass4Inheritance`.
- **`Project.costInputMode` is deprecated.** Stripped on hydrate. The Same vs Individual mode UX is gone; the inheritance surface always renders both views.
- **Capex Excl Land In-Kind is the cash-impact schedule** that feeds the Financing module's drawdown curve for debt sizing + equity funding requirement. Results Table 3 in Tab 3. Land In-Kind is non-cash equity (Tab 4 In-Kind Equity tile, never on Cash Flow Statement); Total Capex Incl Land Value (Results Table 2) is the basis for Fixed Assets / Inventory book value in M5.
- **Capex capitalisation rule.** Every cost line capitalises into asset basis. `classifyAssetCapex(asset, capexBasis, landTotal)` routes to `{ COGS, FixedAssets, Depreciation }` per strategy. Land never depreciates.
- **Land in-kind treatment.** `computeCashFlowImpact(capexBasis, landInKindPortion)` returns `{ cashOutflow, equityInKind }`. M3 Cashflow consumes directly.
- **`CostLine` is open-ended `id: string`.** Custom + seed lines coexist; `isLocked` protects seed rows. `STANDARD_COST_LINE_IDS` exports the 9-line standard catalog. `deriveCostStage(line)` returns stage by stable id; custom lines fall back to `line.stage`.
- **Per-sub-unit custom rates** is the pattern for granular cost differentiation. `CostMethod = 'per_sub_unit_custom_rates'` + `CostLine.perSubUnitRates` keyed on sub-unit id with reserved keys `'__support__'` / `'__parking__'`. M2.1 can mirror with `RevenueLine.perSubUnitRates`.
- **Cost line caption pattern.** Inline caption under value cell showing `rate × metric = total`. M2.1 follows with `revenueLineCaption`.

**Strategy + revenue obligations (for M2.1)**
- **Sell / Operate / Lease / Sell + Manage.** Short labels with `STRATEGY_TOOLTIPS` map for longform hover.
- **Sub-unit category + metric drive Rate Unit** (Sellable+units = per unit, Operable+units = per room/night, Leasable+area = per sqm/year, etc.). M2.1 wires each combination to its revenue stream.
- **Sell + Manage** drives recurring management fee revenue via `Asset.managementAgreement` (managementFeePct × operating revenue over agreementDurationPeriods, starting at agreementStartPeriod or handover).
- **Asset.status gates revenue:** planned = no revenue, construction = pre-sale only (Sell + Sell+Manage), operational = full revenue per strategy.

**Layout + UX**
- **Sticky sidebar.** Outer wrapper `height: 100vh; overflow: hidden`; scrollable `<main>`. Standard for any module shell.
- **Sub-tab Inputs / Results pattern** is canonical for editable + read-only views. M2.1 + M3 follow.
- **Per-asset selector + 3 summary cards** is canonical Inputs layout. Asset selector bar + per-asset section + 3 summary cards.
- **Hide zero rows in Results.** Filter `total=0` rows from display; keep in Inputs.
- **Granularity toggle remounts via key.** `key={`summary-${granularity}`}` to avoid stale state.
- **Summary table column convention:** [Description] [Total] [Period/Stage/Type cols...] so totals visible without scrolling right.

**Catalogs**
- **Project type catalog is additive.** 14 project types with `ASSET_TYPES_BY_PROJECT_TYPE` filter for Tab 2 Type dropdown. `SUGGESTED_CATEGORIES_BY_PROJECT_TYPE` provides empty-state nudges; never auto-creates assets.

---

## Module 1 archived phase history (M2.0 → M2.0j)

Full closure narrative for each phase below lives in **CLAUDE-FEATURES.md** under "Module 1 (REFM) M2.0 Phase History (M2.0 → M2.0j, archived 2026-05-11)". One-line index here for quick recall:

- **M2.0j** (2026-05-07), 16 audit + display + structural fixes (cp=0, Asset.type optional, Land Parcel rate header, Display Scale export comment, Display Scale + Decimals on Land Parcel, sub-unit area/units bidirectional sync, accounting format on blur, cost line caption per method, phasing simplified to Even+Manual, period dates align to phase start, Capex by Period audit + granularity remount, hide zero rows, drop stage labels, drop 3 summary tables, asset selector + 3 summary cards). `verify-m20j.ts` + `m20j-costs-audit.spec.ts`. Superseded by M2.0L which fixed the cost line duplication bug it introduced.
- **M2.0i** (2026-05-07), final polish (10 fixes): Display Settings panel, drop Model Granularity input + Parking Bays, sub-unit Units/Area, Strategy short labels, compact reconciliation, Operational phase Historical Baseline. `verify-m20i.ts` + `m20i-final-polish.spec.ts`.
- **M2.0h** (2026-05-07), area hierarchy + cost granularity (6 fixes + v7→v8 migration banner): NSA/BUA/GFA tiers, parcel NDA toggle, per-sub-unit custom rates, runtime granularity toggle, currency header line. `verify-m20h.ts` + `m20h-area-hierarchy-cost-granularity.spec.ts`.
- **M2.0g** (2026-05-06), display + reconciliation + Costs restructure (v7→v8 schema bump): annual-only inputs, displayScale, end-of-period dates, asset Support/Parking, land reconciliation, sub-tabs Inputs/Results, 4 summary tables, Manual % phasing restore. `verify-m20g.ts` + `m20g-display-recon-costs.spec.ts`.
- **M2.0f** (2026-05-06), structural fixes (6 fixes): 14 project types, Phase Start Date column, multi-parcel landAllocation, sub-unit BUA source of truth, Parking sub-unit. `verify-m20f.ts` + `m20f-structural-fixes.spec.ts`.
- **M2.0e** (2026-05-06), wizard simplification + Tab 2 canonical entry: per-phase asset sections, Sell+Manage / UsefulLife sub-forms, Status pill, computePhaseTimeline. `verify-m20e.ts` + `m20e-wizard-tab2.spec.ts`.
- **M2.0d** (2026-05-06), Costs polish + v7 schema: Sell+Manage rename, per-asset cost segregation, classifyAssetCapex, computeCashFlowImpact, 3 summary tables, Tab 4 In-Kind Equity tile. `verify-m20d.ts` + `m20d-costs-polish.spec.ts`.
- **M2.0c** (2026-05-06), Dev Costs + Financing restore on v6: 13 cost methods, 5×5 financing matrix, IDC capitalization, per-tranche schedules. `verify-m20c.ts` + `m20c-costs-financing.spec.ts` (skipped, frozen).
- **M2.0b** (2026-05-06), brand-styled shell on v5: Topbar + Sidebar + Dashboard + Modals restored, dark-mode toggle, playwright.config.ts baseURL. `verify-m20b.ts` + `m20b-shell.spec.ts`.
- **M2.0** (2026-05-06), v5 hard-cut rebuild: flat Project → Phase → Asset → SubUnit hierarchy, 4 tabs, 9 fixed cost lines, 5×3 financing matrix, 30.8 KB v5 baseline. `verify-m20.ts` + `m20-full-flow.spec.ts`.

---

## Module 1 phase history (M1.R through M1.13d, pre-M2.0)

M2.0 (2026-05-06) hard-cut Module 1 to the v5 schema, replacing the v3 / v4 hierarchy (Master Holding / Sub-Project / Plot / Zone / FAR / Cascade / Parking Allocator). The pre-M2.0 narrative (M1.R → M1.5 → M1.5b → M1.6 → M1.7 → M1.8 → M1.9 → M1.9b → M1.10 → M1.10b → M1.11 → M1.12 → M1.13 → M1.13b → M1.13c → M1.13d) plus M1.8 wizard hotfix series and the legacy 3-baseline snapshot diff pattern lives in **CLAUDE-FEATURES.md** under "Module 1 (REFM) Phase History (frozen pre-M2.0)". Read it only if you are excavating an old commit or a deferred-from-M1 issue resurfaces; current work happens against M2.0 Pass 9 on v8.

---

## Module 7: IC Presentation Builder, full build narrative

Moved out of the root CLAUDE.md on 2026-08-01: it had grown to 12.9KB in a file
that loads on every session, including sessions that never open Module 7. The
root keeps a summary and points here.

REBUILT 2026-07-16 into a PowerPoint-style slide editor (Phase 1 of 4 shipped), REPLACING the old form-plus-fixed-report UI (`Module7Reports.tsx` DELETED; the old `icReport.ts` assembler + `buildReportPptx` + `refm_report_inputs` form are KEPT and reused). Three-panel shell (`Module7Deck.tsx`): slide navigator (left, live thumbnails) / scaled 16:9 canvas (centre) / properties (right). **The whole point is auto-sync by construction**: a slide holds OBJECTS, and every data object stores a BINDING KEY (e.g. `headline.projectIrr`), never a copied number, resolved at render time against the live `ICReportModel`, so when Modules 1-6 change the deck follows with no sync step. A binding with no data renders a visible amber UNLINKED state, not a stale figure ("no broken links" = live or loudly absent). New pure layer under `lib/reports/deck/`: `types.ts` (Deck/Slide/DeckObject on a 1280x720 = pptxgenjs LAYOUT_WIDE grid), `theme.ts` (palette from the "REFM Report Color Coding" reference slide: ramp #B9C9DC/#6E9BCB/#4E7CB0/#1B4F8A, green #2E7D52 totals, red #B23A3A flags), `bindings.ts` (typed metric/chart/table/text registry over `ICReportModel` + `makeDeckFmt` money scaling so axes read SAR m not raw currency), `layout.ts` (composition primitives), `templates.ts` (18 institutional slides, each `available(model)`-gated so a pure-Sell / single-case / no-debt model auto-omits the right slides, section-numbered AFTER omission, seeded from `refm_report_inputs` narrative non-destructively). Renderers `components/modules/deck/{SlideObjectView,SlideCanvas}.tsx` (DOM so text stays editable + Recharts native; KPI tiles / model-driven Gantt / colour-graded sensitivity heatmap / risk matrix). Persistence: **migration 199** `refm_report_decks` (one jsonb deck per project, schema-tolerant, `coerceDeck` re-validates untrusted jsonb + clamps geometry + drops unknown types), `deck-server.ts`, `GET/PUT/DELETE /api/refm/projects/[id]/report-deck` (write-gated on read-only grace). An additive `operating` block (NOI/EBITDA) was added to `icReport.ts`. Verifier `verify-report-deck` 82+52 preserved (icReport, pptx exporter untouched), **new `verify-report-deck` 47**. **Phase 2-4 TODO: direct manipulation (drag/resize/snap/inline text) + Insert menu + property editors + image upload + PPTX/PDF/PNG export from the deck model.** **Migration 199 APPLIED 2026-07-16** (verified live: table + FK + jsonb round-trip). **Phase 2 shipped 2026-07-16 (direct manipulation):** `EditLayer.tsx` is an interaction overlay ON TOP of the read-only `SlideCanvas` (visual renderer stays pure): pointer drag-move + 8-handle resize in device px converted to logical via one scale factor, so what you drag is what PowerPoint receives; **snap-to-grid + live red alignment guides** (`lib/reports/deck/snapping.ts`, pure: snaps object edges/centres to other objects, content margins, slide centre, else the 8px grid); inline contentEditable text editing (double-click text/bullets); multi-select (shift-click). All structural edits go through pure `lib/reports/deck/mutations.ts` (update/add/remove/duplicate with collision-safe `freshId`, z-order = array order, keyboard nudge = EXACT delta not grid-snapped, slide add/duplicate/move/remove that never empties the deck), so **undo/redo is a stack of whole-deck snapshots**. Shell (`Module7Deck.tsx`) gained: history + undo/redo, an edit toolbar (undo/redo/duplicate/delete/z-order + Preview toggle), keyboard shortcuts (Del, arrows nudge, shift+arrow=8px, Ctrl+D/Z/Y, Ctrl+[/]), an editable **properties panel** (geometry X/Y/W/H, lock/hide, text formatting, KPI variant/label, chart/table title+options) + **Brand controls** (header/footer/company/fonts/primary colour/slide-numbers/white-label) when nothing selected, and slide ops in the navigator. Data objects stay model-linked: editing style never touches the number. Verifiers **verify-report-deck 47 + verify-report-deck-edit 41** (pure mutations + snapping). **Phase 3 EXPORT shipped 2026-07-17 (PPTX + PDF from the deck model):** one pure shared contract `lib/reports/deck/exportModel.ts` (`resolveDeckExport` resolves every bound object to a concrete paint through the SAME registry the canvas uses, so a file figure can never drift from the on-screen deck; a binding with no data becomes a visible amber `unlinked` paint, never a fabricated number; hidden slides/objects dropped, visible page numbers gapless; geometry stays logical px, converted once per exporter). `deckPptx.ts` = pptxgenjs, px/96 -> LAYOUT_WIDE inches, so what you dragged is what PowerPoint receives: NATIVE + editable (addText/addShape/addTable/addChart, native Office charts, per-slide notes, chrome band + page numbers). `deckPdf.ts` = pdf-lib, one landscape 960x540pt page per slide (px*0.75, y-flip), StandardFonts (no font file to embed: brand family maps to a serif Times / sans Helvetica face), drawn tables / bar+line+doughnut charts / KPI tiles / Gantt / colour-graded heatmap / risk matrix; a `sane()` pass folds unencodable glyphs so a project name can never crash the render. Both SERVER-SIDE via `POST /api/refm/projects/[id]/report-deck/export` (Node runtime; owner-checked + `assertExportAllowed` grace/lapse gate, same as the PDF/Excel routes): the client POSTs the deck + the already-assembled `ICReportModel` + scale/currency (NO recompute), the route rebuilds `makeDeckFmt` and streams the .pptx/.pdf. Client wrapper `exportReportDeck` returns a Blob; `Module7Deck` toolbar gained an **Export** dropdown (PowerPoint / PDF). Verifier **new `verify-report-deck-export` 25** (resolved-contract numbers match the model, unlinked stays unlinked, px->inch/pt geometry, real PPTX PK-zip + reloadable 960x540 PDF, on the full AND a reduced no-debt/single-case model). Live E2E proof: 18-slide deck -> 847KB PPTX with 5 native embedded charts + 33KB PDF. **Phase E (2026-07-28): PDF bookmarks + a dedicated PDF verifier.** Diagnosis established the IC PDF was ALREADY a same-model pdf-lib renderer (charts already vector-drawn, no rasterisation anywhere; money scale / Management-case pin / header-footer / show-hide + reorder / picker blocks already carried; Export dropdown already had PDF), so NO second renderer was built, which would have drifted from the deck. The one real gap was navigation depth: `deckPdf.ts` had GoTo link annotations but no bookmark tree. Ported the `buildOutline` mechanism from `generateProjectPdf.ts` (commit ea58dac8) as `buildDeckOutline`: a hand-built `/Outlines` dictionary chain (Parent/Prev/Next/First/Last/Count, pdf-lib has no outline API), ONE flat entry per VISIBLE slide titled from the slide and anchored `[pageRef, XYZ, null, PAGE_H, null]` at the page top, plus `PageMode: UseOutlines` so a reader opens with the bookmark panel showing. Flat by design (the deck is a flat slide list; the project PDF nests module->tab because its content nests). Built AFTER the link pass so every page ref exists. Because `resolveDeckExport` has already dropped hidden slides, bookmarks and the gapless page numbering cannot disagree. New verifier **`verify-report-pdf-export` 26** (pages/landscape dims, outline present + Count + titles + per-page destinations, PageMode, GoTo annots in range, Contents entries land on the named slide, no placeholder text, hidden-slide drop, reduced no-debt model, money-scale toggle); proven to have teeth: 9 of its 26 checks fail against the pre-Phase-E renderer, and every check RUNS unconditionally so a missing tree can never pass as "no failures". Engine untouched (verify-returns-snapshot 99). **FULL year-by-year schedules (2026-07-27, DECK_SCHEMA_VERSION 2 -> 3):** the 2026-07-26 statements were a two-column CONDENSED summary; the deck now also carries the complete period detail. New `schedules` block on `ICReportModel` (`ICScheduleBlock`/`ICScheduleRow`, raw numbers, no recompute): income statement / cash flow / balance sheet across EVERY model year (16/18/19 lines read straight off `snap.pl` / `snap.directCF` / `snap.bs`) + FCFF / FCFE / DDM build-ups across every stream period INCLUDING inception (`rs.streamYearLabels` + `rs.buildup`, the exact rows Module 5 shows on screen, plus a cumulative memo). A flow row's `total` is its own sum; a STOCK row (balance sheet, cash balances, cumulative) carries `total: null` so no renderer can invent one. **Pagination is a first-class binding concept**: `TableDef` gained `paginated`/`pageCount`, `resolveTable(key, m, f, {page})`, `TableObject.page`, and `SCHEDULE_YEARS_PER_PAGE = 10`, so a long horizon runs onto MORE SLIDES rather than shrinking out of legibility; the project-life Total column appears only on the LAST page (a Total beside a partial window would not tie), and a page past the end of the current model renders the amber unlinked state, never a blank grid. `SlideTemplate` gained `pages(m)` + `build(..., page)` and `seedDeck`/`buildSlidesFromTemplate` expand a template into a slide FAMILY (titled "(2 of 3)"); six new templates `{is,cf,bs,fcff,fcfe,ddm}_schedule` in a new "Full schedules" group. `TableData` gained `colWidths` (label column gets its own share) + `TableCell.indent`, honoured by ALL THREE renderers (canvas colgroup, PPTX native `colW` in inches + cell margins, PDF width fractions), and an opt-in `showUnitNote` prints the scale / year span / page under the table. `deckUpgrade` is now PER-VERSION (`TEMPLATES_BY_VERSION`), so a v2 deck gains only the v3 slides and a slide deleted in an earlier round is never resurrected; each schedule inserts after the summary it expands. Because a deck can now run to ~40 slides, the live ToC flows into up to three columns (`tocLayout` in `exportModel.ts`, shared by canvas + PPTX + PDF) instead of clipping. No migration (jsonb, `coerceDeck` passes the payload through). Verifiers **new `verify-deck-schedules` 113** + `verify-deck-toc` 20 -> 30; report-deck 47 / edit 41 / export 42 / financials 72 / ic-report 82 unchanged. **UX / LAYOUT PASS + DRAG-TO-REORDER (2026-08-02, presentation only):** the builder wasted a large band of the workspace, rendered its slide small inside a scrollbar, and showed slide names as vertical ladders. Three structural causes, all fixed. (1) **Dead space** was a `vh`-inside-`zoom` mismatch: the shell runs at `zoom: 0.8` with `height: calc(100vh / 0.8)`, and CSS `zoom` does NOT rescale `vh`, so `Module7Deck`'s `height: calc(100vh - 190px)` claimed ~100vh of a container holding ~125vh, leaving roughly 345 CSS px of nothing below the deck and worsening on taller screens. The deck now takes `height: 100%` and `RealEstatePlatform` makes `main` a flex column (`overflow: hidden`, `module-content` `flex: 1`) for module 7 ONLY via `isDeckModule`, so Modules 1-6 keep their block-flow scrolling untouched. (2) **The canvas was fit-to-WIDTH only** (`canvasW = column - 48`, height whatever fell out, with ~120px of toolbar chrome never subtracted), so the slide overflowed into the centre column's own scrollbar on wide screens while the page below sat blank, and left spare column height unused on narrow ones. The centre column is now a fixed control strip over a MEASURED stage and the slide scales to fit BOTH axes (`min((w-56)/1280, (h-56)/720)`); if a banner appears the strip grows and the slide scales down rather than overflowing. `EditLayer` keeps its exact `scale`/`width`/`height` contract, so drag/resize maths is unchanged. (3) **Vertical slide names** were width starvation, not a text transform: the navigator row was a horizontal flex (index 14 + thumb 120 + gaps) inside a 214px aside, leaving the title ~27px, which at `fontSize: 10` with `wordBreak: break-word` splits a name into a ladder of two and three character fragments. New `components/modules/deck/SlideNavigator.tsx` gives the thumbnail the full row width with the slide number as a corner badge and puts the title on its own line (2-line clamp, `wordBreak: normal`), with a clear selected state (3px navy rail + paleWash + navy ring), a Hidden chip, and row actions moved below the title. **Responsive** breakpoints (nav 248 / props 300 at >=1600, 216 / 280 at >=1100, properties overlay below 1100, horizontal filmstrip below 820) are driven by a ResizeObserver on the root, NOT media queries, because viewport queries also lie inside the zoomed shell (a 1536px viewport is 1920 CSS px in-shell). **Drag-to-reorder** is a pointer-capture gesture on the row deliberately modelled on `EditLayer` (capture on pointerdown, track on pointermove, release on pointerup, no window listeners and no cleanup effect, the failure mode that left the canvas gestures dead for ~10 days): a 4px threshold keeps click-to-select intact, the dragged row dims and a navy insertion line marks the drop, the rail auto-scrolls at its edges, and the drop index is bounded by the slide count so a stale ref after a delete cannot index past the deck. Touch does not start a drag (the rail keeps scrolling with a finger); Up / Down remain the touch reorder path. The gesture only reports `(from, to)` and the shell applies the EXISTING `moveSlide` through `commit`, so a reorder is ONE undo step that marks the deck dirty and persists on the same Save: no deck schema change, no `DECK_SCHEMA_VERSION` bump, no migration. No deck data, generation, AI or export logic touched. Engine untouched (`verify-returns-snapshot` 99/99); report-deck 47 / edit 41 / export 42 / deck-financials 72 / schedules 113 / toc 30 all unchanged, type-check + build clean, no new lint problems. **Shipped WITHOUT live browser verification** (the Chrome extension was not connected), which for a pointer-gesture change in this component is a real gap: the drag, the fit-to-box scaling and the rail have been proven by types, verifiers and build only. **TEXT BOX + UNDO SCOPE + NAMED VERSIONS (2026-08-02):** three gaps closed. (1) **Free text box.** Every insert path produced a model-BOUND object, so a user could not put their own words on a slide: arbitrary text existed only where a template had placed it, and "Add blank slide" produced an `objects: []` slide that could never be given a title. `addTextBox` reuses the `text()` primitive, so the result is an ordinary UNBOUND TextObject that the canvas, inline editor, `coerceDeck`, PPTX and PDF already handle (no schema change, no `DECK_SCHEMA_VERSION` bump); a `+ Text box` control sits beside Insert data and the box lands staggered, selected and in edit mode. (2) **Undo/redo scope fix, NOT a rebuild.** The history already existed and already met the spec (60 snapshots, Ctrl+Z / Ctrl+Y / Ctrl+Shift+Z, redo cleared on a new edit, suppressed inside active text editing, covering add/delete/move, text, slide reorder and applied AI drafts). The real defect was REACH: the handler hung off the canvas column`s `onKeyDown` with `tabIndex={0}`, so Ctrl+Z did nothing unless that column held focus. It is now bound once at the document and bails inside input/textarea/select/contentEditable so the browser`s native text undo still wins. Moving it there made two modal regressions reachable (Delete over the versions or AI review modal would have removed objects on the slide BEHIND it, Escape would have cleared that selection), so both modals now hold the keyboard. Keyboard listeners only: the EditLayer and SlideNavigator drag gestures stay on pointer capture. (3) **Named presentation versions (migration 207 APPLIED).** One deck per project was enforced in the schema (`refm_report_decks.project_id` is the PRIMARY KEY) and every Save overwrote it, with the deck absent from project versioning entirely. New `refm_report_deck_versions` mirrors the `refm_project_versions` pattern: append-only rows, monotonic per-project `version_number` assigned MAX+1 server-side behind a unique index, and a nullable `current_version_id` on the working row (`ON DELETE SET NULL`). `refm_report_decks` stays the LIVE working deck, so nothing migrates and a project that never uses versions is unaffected. The server store tolerates 207 being absent (degrades to single-deck rather than breaking the tab); two routes carry the same ownership + read-only-grace gating as the deck route; `DeckVersionsModal` does save-as-named / list / load / delete. Saving a version also saves the working deck in the same call so the two cannot disagree, and loading routes through `commit`, so it is ONE undo step that still needs Save: looking at an old version can never destroy the deck on screen. Because slides hold BINDING KEYS, a saved version records layout and narrative, not figures, so an old version reloads showing TODAY`s numbers. New verifier **`verify-deck-textbox-versions` 41**; report-deck 47 / edit 41 / export 42 / financials 72 / schedules 113 / toc 30 / ic-narrative-ui 139 unchanged; engine untouched (`verify-returns-snapshot` 99/99). Live end-to-end proof against the database: monotonic numbering, list order, current pointer, two versions as INDEPENDENT documents, binding keys preserved on load, cross-project id isolation, delete, and the real project`s working deck restored byte-identical. **Shipped WITHOUT live browser verification** (the Chrome extension was unavailable), which for a keyboard-handling change across the whole editor is a real gap. **SAVE CHOICE ON EVERY SAVE (2026-08-02):** the deck save used to overwrite the working document silently and never touched the named versions, so saving a version then carrying on editing left `current_version_id` claiming a match that no longer held. Every save now goes through `DeckSaveChoiceModal`, the deck`s equivalent of the platform`s `EditChoiceModal`, asking on EVERY save (the deck has no edit-session gate to hang the question off and never auto-saves) and offering two options rather than three (loading a different version belongs to the versions modal). The linked version is resolved live and NAMED in the option so an overwrite is never blind; unlinked it degrades to "Update this presentation". **Update in place** overwrites the linked version`s jsonb and upserts the working deck in ONE call, preserving `version_number` and `created_at` (the same version revised, not a new one) and renaming only when a label is supplied. **This is the ONE deliberate exception to migration 207`s append-only wording**, taken knowingly and mirroring `refm_project_versions`, whose migration 149 carries the same "never UPDATEd" comment while `PATCH /versions/[versionId]` has always overwritten a snapshot: a version a user is actively working in has to be correctable, or every fix spawns a version and the names stop meaning anything. **Save as new version** reuses the 207 path + the versions modal for the name and leaves the user on the working deck (no reload, no slide change, no selection change). The **topbar Save** now drives the deck while the Presentation tab is open (`onRegisterSave` / `onDirtyChange` register the module`s save with the shell on mount and clear it on unmount, so the topbar hands itself back to the model on leaving), and the topbar unsaved indicator follows the deck. Verifier **`verify-deck-textbox-versions` 41 -> 63** (the patch payload never carries `version_number` or `created_at`, project-scoped update, working deck saved in the same call, PATCH behind the grace gate, Save routes through the chooser, chooser in the keyboard guard, topbar prefers the deck handler, save-as-new does not switch slide). Live end-to-end against the database, 15 checks: number/date/label/comment preserved, the target version holds the revision, a SECOND version untouched, no extra version created, pointer moved, working deck saved in the same call, cross-project update rejected, probes removed and the real working deck restored byte-identical. Engine untouched (`verify-returns-snapshot` 99/99). **Still not browser-verified.** **AUTO-NAMED REPORT VERSIONS + OPEN THE LAST ONE (2026-08-03):** two gaps closed, both additive, no migration (207 already applied), engine untouched. (1) **Auto-naming.** A presentation version required TYPING a name: the POST route 400d without one and the versions modal disabled its Save on an empty field, while the platform`s MODEL versions have auto-generated theirs since 2026-06-01. New pure `lib/reports/deck/versionNaming.ts` produces `{ProjectName}_Presentation_v{Major}.{Minor}_{MMDDYYYY}` (e.g. `FMP RE HUB_Presentation_v1.3_08032026`) by IMPORTING `getNextVersionNumber` / `formatVersionDate` / `sanitizeForFilename` from the model-version helper, so both schemes roll over identically (1.0 to 1.9 then 2.0, advancing from the LATEST by `created_at` so a delete leaves its number spent) and a hand-typed name whose label does not parse is IGNORED rather than resetting the sequence. Naming happens SERVER-SIDE in `saveDeckVersion` (which reads the existing labels + `refm_projects.name`), so no client path can write an unnamed row; a supplied name still wins, and the versions modal keeps an OPTIONAL name field. The save choice no longer hands off to the versions modal for a name: "save as a new version" saves immediately, and an in-place save with NOTHING linked creates the first version rather than an orphan working row, so a deck`s history is never empty however often it was saved. (2) **The last saved version opens with the project.** `getDeck` read only the working row and nothing on screen said which version it was. It now reads the working row plus the newest `refm_report_deck_versions` row and returns `{ deck, canSave, openedVersion, versionsAvailable }`. The decision is the exported PURE `shouldOpenVersion({hasWorkingDeck, workingUpdatedAt, versionCreatedAt})`: the version wins by DEFAULT, including when a timestamp is unreadable, and the working row wins ONLY when strictly newer, which covers a deck saved before this change and an in-place version update (whose row keeps its `created_at` while the working row is rewritten), so nothing saved later is ever discarded. `versionsAvailable:false` (207 absent) degrades to the single working deck exactly as before. The toolbar shows a `v{n} {name}` chip for the open version, the versions modal became the BROWSING surface (OPEN badge, Load renamed Open and disabled on the open row, `openedVersionId` from the editor beating the stored pointer), and opening another version re-points what an in-place Save updates. The save chooser is now TOLD which version is open instead of resolving `current_version_id` itself: that pointer only moves on a SAVE, so after opening an older version it still named the previously saved one and confirming "update" would have named one version while overwriting another. **Report versioning stays FULLY separate from model/project versioning**, asserted structurally by the verifier: separate table, routes, client wrappers and UI; the deck persistence and routes never name `refm_project_versions`, the project persistence and its version route never name the deck tables, and the ONE shared thing is a pure function module with no client, no table and no fetch. New verifier **`verify-deck-version-autoname` 52**; report-deck 47 / edit 41 / textbox-versions 63 / export 42 / financials 72 / schedules 113 / toc 30 unchanged; type-check + build clean, no new lint problems. **Still not browser-verified.** **Phase 3-4 REMAINING: Insert menu (add new bound objects/images/shapes), image upload, group/ungroup/align-distribute, PNG (per-slide, satori+sharp).** Live browser DOM verification of the editor still PENDING. Prior (superseded) IC-report detail follows: LIVE (2026-07-09). Three report types via an in-tab **Report Builder** selector (IC / Lender Package / Investor One-Pager), snapshot READ-ONLY (no engine change, no recompute): financials auto from the computed snapshot, narrative + editable header/footer + font from a per-project **report-inputs** form (`refm_report_inputs`, migs 191+192), parties from M1 by role, per-report show/hide + reorder. Pure assemblers `lib/reports/{icReport,lenderReport,onePagerReport}.ts`; Lender covenant heatmap reuses `lib/covenants.ts` (same verdicts as RE Metrics). **PPT export (Phase 3a)** via pptxgenjs, generated SERVER-SIDE (`POST /api/refm/projects/[id]/report-pptx`, Node runtime, because pptxgenjs `node:` imports cannot browser-bundle; `next.config` is off-limits): 16:9 editable master, navy header band, ToC with clickable internal slide links, per-section divider slides, KPI tiles, chosen fonts (named faces). **Phase 3b (PDF) TODO = same-model pdf-lib renderer** (literal PPT->PDF NOT feasible on Vercel: no LibreOffice/soffice). Nav enablement needed `module7` in `ModuleKey` + `MODULE_VISIBILITY` (all roles) beyond modules-config/MODULE_TABS. **IC REBUILT to a 21-section model-driven structure + narrative (Phase A+B, mig 193; Lender/One-Pager PARKED).** **Phase C+D (2026-07-13):** native brand-palette charts on the IC structure, SAME data in preview (Recharts) and PPT (native editable Office `addChart`), auto-omit-gated: asset-mix doughnut, cost-stack bar, revenue-recognition columns, senior-debt-balance columns, scenario IRR/NPV bars, exit-MOIC line; plus the development-programme **Gantt** (phase construction/operations swimlanes + debt-repaid/exit markers; preview grid, PPT positioned shapes). Chart series + palette (`IC_CHART_PALETTE`) + programme lanes are pure additions to `lib/reports/icReport.ts`; engine untouched, no migration. **IC renderer REBUILT to a composed IC-grade layout (2026-07-13):** no standalone number-divider slides (section number is a header chip; deck ~halved), finding-as-subtitle (shared `icFindingLine`, killed the "Figures in All figures" bug), KPI tiles for headline numbers, every chart paired with a captioned block, two-col Sources&Uses, sensitivity heatmap, cover transaction-summary KPI wall; reusable primitives (sectionHeader/kpiTile/captionBlock/calloutBox/phaseCard/riskCard). **Two IC deck settings (mig 197 APPLIED, schema-tolerant):** `icDeckCase` pins the deck to the Management/base case by DEFAULT regardless of the active case (switchable to active), and `icMoneyScale` is user-selectable millions (default) / thousands driving tables + chart axes (fixes raw-currency axes). Verifiers verify-ic-report 82, verify-report-models-phase2 26, verify-report-pptx-export 52.
