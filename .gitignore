node_modules

# Environment secrets — never commit
.env.local
.env.production
.env.*.local

# Vercel
.vercel

# Next.js build output
.next/
out/

# TypeScript build info
tsconfig.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Claude Code agent worktrees
.claude/worktrees/

# One-shot operational drafts. Deleted after the operator copies them.
TEMP_*.md

# Excel exporter fixture output (regenerated by `npx tsx scripts/excel-export-fixture.ts`)
scripts/excel-export-fixture.out.xlsx

# PDF exporter fixture output (regenerated by `npx tsx scripts/pdf-export-fixture.ts`)
scripts/pdf-export-fixture.out.pdf

# Local-only xlsx extraction temp artifacts (created when inspecting reference workbooks)
*.pwest-tmp.zip
*.xlsx-tmp.zip
.pwest-extract/
.xlsx-extract/

# Playwright test artifacts (regenerated on every run)
test-results/
playwright-report/
tests/screenshots/
# Private reference workbooks (kept locally only, never committed). Add
# a glob here for any private financial model the user keeps at the
# repo root for verification. The reference client's actual filename
# MUST NOT appear in this file — use generic patterns.
*Cashflow*.xlsx
# Excel binary workbooks (xlsb / xlsm), kept locally only for reference
# / inspection. Macro-enabled .xlsm variants explicitly excluded so an
# accidental `git add -A` cannot snag them.
*.xlsb
*.xlsm
.m20b-legacy/
